# Override VITE_API_URL for test runs so apiFetch uses relative paths.
# MSW handlers are registered with relative paths, which resolve against
# the JSDOM base URL (http://localhost:3000) — not localhost:8000.
VITE_API_URL=
