---
gsd_state_version: 1.0
milestone: v1.0.1
milestone_name: — Content Preview and Rendered Images
status: v1.0.1 complete
stopped_at: Completed quick task 260424-l0d (Phase B — approve→post-to-X). User-initiated per-item atomic FastAPI endpoint POST /items/{id}/post-to-x posts via tweepy v2 OAuth 1.0a User Context. Narrow MVP ships text-only {breaking_news, thread} content_types. New `approval_state` column on draft_items (orthogonal to existing `status`) with enum {pending, posted, failed, discarded, posted_partial} via CHECK constraint. SELECT FOR UPDATE inside async with db.begin() holds row lock through tweepy round-trip for idempotency. Client-side reply-chain thread orchestration with posted_partial state on mid-thread failure (no auto-rollback of already-posted tweets, no auto-retry). Frontend "Post to X" button in ContentDetailModal footer + base-ui Dialog confirm modal with verbatim D10 copy ("This will immediately post to your X account. You can't undo this."). Simulate mode (X_POSTING_ENABLED=false default) writes posted_tweet_id=sim-{uuid4} without calling tweepy. CLAUDE.md narrowly rewrites "Auto-posting" row per D12: user-initiated approve→post is now in scope; unattended/scheduled/cron/batch posting remains permanently out of scope. Dual-model parity (backend + scheduler draft_item.py byte-identical). Alembic 0009 migration. tweepy[async]>=4.14 added to backend/pyproject.toml. New env vars X_ACCESS_TOKEN + X_ACCESS_TOKEN_SECRET + X_POSTING_ENABLED + X_POSTING_SIM_PREFIX. Verifier: status=human_needed (12/12 must-have truths verified; 2 external-resource items routed to user — live Postgres alembic up-down-up + real-mode posting with X Developer Portal credentials). 5 atomic commits (T1 alembic 50c43d4 / T2 service 7d6a9ff / T3 route add1f1e / T4 frontend 39dc9ee / T5 docs 7abb171) + merge 96d6177. Test totals: backend 92 passed (+23 new — 11 x_poster + 12 post_to_x), scheduler 180 passed (+5 parity guard), frontend 59 passed (+4 PostToXConfirmModal). Preservation gate empty (zero diff on scheduler/agents/, worker.py, whatsapp.py, senior_agent.py, content_agent.py). Ran via `/gsd:quick --discuss --research --full` — full pipeline (CONTEXT 15 locked decisions D1-D15 → RESEARCH HIGH confidence with route-name + text-source + thread-field-key corrections to CONTEXT → PLAN 5 tasks → plan-checker PASSED iteration 1 → executor 5 atomic commits → verifier 12/12 PASS). Deferred to Phase B.5 (trivial registry add): quotes + gold_history. Deferred to Phase B+ (needs media_upload): gold_media + infographics. **Pre-deploy runbook (CLAUDE.md anchor):** generate X Developer Portal access tokens AFTER toggling app permissions to Read+Write, then set X_ACCESS_TOKEN + X_ACCESS_TOKEN_SECRET + X_POSTING_ENABLED=true in Railway prod env.
last_updated: "2026-04-25T04:30:00.000Z"
last_activity: "2026-04-22 — Completed quick-260422-no4 (surface zero-item agent runs in per-agent queue UI with "No content found"). **Origin:** user ask immediately after shipping the gmb debug fix — verbatim: *"Even if some of the agents runs dont find anything, for examples the quotes or gold media one, I still want it to say 'no content found' so I know it is working tomorrow"*. The 12:00 PT cron verification tomorrow needs every tick visible in the UI regardless of whether items were produced. **Reconnaissance:** `frontend/src/pages/PerAgentQueuePage.tsx` L137-138 rendered bare `<EmptyState />` when items=0, and `groupByRun` L19-54 silently dropped runs with 0 matched items — so a sequence like "5, 0, 2 items across 3 runs" rendered as 2 groups, and all-zero-item days showed the generic empty state indistinguishable from "agent never ran". Data contract was already sufficient (runs fetched via `getAgentRuns()` returning `items_found` / `items_queued` / `notes` fields). Ran via `/gsd:quick --discuss` — orchestrator pre-surfaced 6 gray-area questions (Q1 wording, Q2 telemetry placement, Q3 render location, Q4 truly-empty fallback, Q5 page subtitle, Q6 per-agent gating) with Recommended answers; user's "yeah push it" locked all 6 decisions (D-01 through D-06) to Recommendations. **Changes (surgical, frontend-only):** (a) `groupByRun` emits a group for every recent run including runs with 0 matched items via `groups.get(key) ?? { run, items: [] }`; (b) render branch guard now `items.length === 0 && runs.length === 0` before falling through to `<EmptyState />` so items=0+runs>0 renders timeline; (c) new `parseRunNotes` module-scope helper parses gmb's structured JSON into a muted inline subtitle with graceful degradation (null→skip, malformed→null, partial→show-present-only); (d) zero-item groups render "No content found" row in RunHeader-consistent `text-xs text-muted-foreground` style. Ink's specific-timestamp fallback preserved; mfg's zero-grep hygiene preserved (zero `video_clip` refs). **Tests (+4, 52 → 56):** zero-item-renders-no-content-found, chronological-interleaving, notes-subtitle-present-vs-null-vs-malformed-vs-partial, items=0+runs>0-falls-through, items=0+runs=0-still-empty-state. **Gates all 5 green:** lint + tsc + vitest 56/56 + build + zero-grep. Single atomic commit `8080b4c` on main. **Third deliverable of tonight's session** after gmb debug fix (85fccfc refactor + 3efe3b9 docs). All 5 zid follow-ups now shipped + gmb debug fix + no4 UX gap close — the 12:00 PT verification tomorrow is maximally instrumented and visible. **Known limitation:** other 6 sub-agents don't have gmb-style structured `notes` telemetry yet — they show "No content found" without the counter subtitle. Extending telemetry to breaking_news/threads/long_form/quotes/infographics/gold_history is a separate follow-up. **Deploy:** Vercel auto-deploys on push; user sees this in UI on next page load after push → origin."
progress:
  total_phases: 8
  completed_phases: 8
  total_plans: 45
  completed_plans: 45
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-03-30)

**Core value:** Every piece of content the system drafts must be genuinely valuable to the gold conversation it enters — a data point, an insight, a connection no one else made.
**Current focus:** v1.0.1 shipped — awaiting next milestone definition

## Current Position

Phase: Not started (v1.0.2 TBD)
Plan: Not started

## Performance Metrics

**Velocity:**

- Total plans completed: 0
- Average duration: -
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| - | - | - | - |

**Recent Trend:**

- Last 5 plans: none yet
- Trend: -

*Updated after each plan completion*
| Phase 01 P02 | 279 | 3 tasks | 13 files |
| Phase 01 P04 | 5 | 3 tasks | 8 files |
| Phase 01 P05 | 2 | 2 tasks | 6 files |
| Phase 01 P06 | 3 | 2 tasks | 6 files |
| Phase 02-fastapi-backend P04 | 163 | 2 tasks | 3 files |
| Phase 03 P03 | 191 | 2 tasks | 11 files |
| Phase 03 P04 | ~20 | 2 tasks | 7 files |
| Phase 04-twitter-agent P01 | 5 | 2 tasks | 11 files |
| Phase 04-twitter-agent P02 | 426 | 2 tasks | 12 files |
| Phase 04-twitter-agent P03 | 1320 | 1 tasks | 1 files |
| Phase 04-twitter-agent P04 | 124 | 2 tasks | 3 files |
| Phase 04-twitter-agent P05 | 15 | 1 tasks | 4 files |
| Phase 04-twitter-agent P05 | 15 | 2 tasks | 4 files |
| Phase 05-senior-agent-core P01 | ~8 | 2 tasks | 9 files |
| Phase 05-senior-agent-core P02 | 440m | 1 tasks | 2 files |
| Phase 05-senior-agent-core P03 | 15 min | 1 tasks | 2 files |
| Phase 05-senior-agent-core P04 | 20 | 1 tasks | 2 files |
| Phase 05 P05 | 2 minutes | 1 tasks | 2 files |
| Phase 06-instagram-agent P01 | 5 min | 2 tasks | 3 files |
| Phase 06-instagram-agent P02 | 10 | 1 tasks | 2 files |
| Phase 06 P03 | 129 | 1 tasks | 2 files |
| Phase 06 P04 | 207 | 2 tasks | 2 files |
| Phase 06 P05 | 8m | 1 tasks | 3 files |
| Phase 07-content-agent P01 | 2 | 2 tasks | 6 files |
| Phase 07-content-agent P02 | - | 2 tasks | 3 files |
| Phase 07-content-agent P03 | - | 1 tasks | 1 files |
| Phase 07-content-agent P04 | - | 1 tasks | 1 files |
| Phase 07-content-agent P05 | 5 | 2 tasks | 3 files |
| Phase 08-dashboard-views-and-digest P01 | 8 | 2 tasks | 10 files |
| Phase 08-dashboard-views-and-digest P03 | 15 | 2 tasks | 7 files |
| Phase 08 P02 | 32 | 1 tasks | 6 files |
| Phase 08-dashboard-views-and-digest P04 | 612 | 2 tasks | 9 files |
| Phase 08-dashboard-views-and-digest P06 | checkpoint | 1 tasks | 0 files |
| Phase 09-agent-execution-polish P01 | 8 | 2 tasks | 4 files |
| Phase 09-agent-execution-polish P02 | 2 | 2 tasks | 4 files |
| Phase 07-content-agent P07 | 10 | 2 tasks | 1 files |
| Phase 07-content-agent P08 | 5 | 2 tasks | 1 files |
| Phase 07 P09 | 2 | 2 tasks | 1 files |
| Phase 07 P10 | 3 | 2 tasks | 3 files |
| Phase 10-senior-agent-whatsapp-notifications P01 | 2 | 2 tasks | 2 files |
| Phase 10 P03 | 525589 | 2 tasks | 7 files |
| Phase 11 P01 | 45 min | 5 tasks | 22 files |
| Phase 11 P02 | 11 min | 2 tasks | 3 files |
| Phase 11 P03 | 3 min | 2 tasks | 3 files |
| Phase 11 P04 | 3 min | 1 tasks | 2 files |
| Phase 11 P05 | 5 min | 2 tasks | 4 files |
| Phase 11 P06 | 15 min | 2 tasks | 9 files |
| Phase 11 P07 | checkpoint | 3 tasks | 3 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [Pre-build]: Separate scheduler worker from API server — crash isolation, prevents duplicate execution
- [Pre-build]: Full schema from day one including JSONB alternatives array — no schema migration debt later
- [Pre-build]: APScheduler 3.11.2 only — v4.x alpha has breaking API changes
- [Pre-build]: shadcn/ui tailwind-v4 branch only — main branch targets Tailwind v3
- [Phase 01]: pytest config embedded in pyproject.toml [tool.pytest.ini_options] — no separate pytest.ini
- [Phase 01]: asyncio_mode=auto in both projects — eliminates need for @pytest.mark.asyncio decorator on individual tests
- [Phase 01]: asyncpg SSL: strip sslmode=require from URL, use connect_args ssl=True — asyncpg rejects sslmode as URL param
- [Phase 01]: FastAPI lifespan used for engine.dispose() on shutdown — cleaner than deprecated on_event pattern
- [Phase 01]: test_schema.py skip condition checks for real neon.tech URL to prevent env var leakage from test_health.py fake DATABASE_URL
- [Phase 01]: APScheduler 3.11.2 AsyncIOScheduler with advisory lock — numReplicas=1 is primary prevention, pg_try_advisory_lock is defense-in-depth
- [Phase 02-02]: Queue router uses no prefix — /queue and /items/{id}/* are separate top-level paths matching spec, not nested under /queue prefix
- [Phase 02-02]: VALID_TRANSITIONS dict is single source of truth for state machine — only DraftStatus.pending has allowed targets
- [Phase 02-fastapi-backend]: asyncio.to_thread wraps synchronous Twilio SDK to avoid blocking the FastAPI event loop in WhatsApp service
- [Phase 02-fastapi-backend]: Retry-once on TwilioRestException with logging (D-16): warning on attempt 1, error on attempt 2, re-raise
- [Phase 03-03]: QueuePage runs all 3 platform queries in parallel to populate badge counts — queries are cheap and avoids tab-switch loading delays
- [Phase 03-03]: PlatformTabBar is prop-driven (no internal state) — QueuePage owns active tab state for testability
- [Phase 03-03]: NavLink end={true} only for root '/' route — prevents matching /settings as Queue-active state
- [Phase 03-04]: RelatedCardBadge uses callback prop (onSwitchPlatform) not Zustand — active platform is local QueuePage state
- [Phase 03-04]: ContentSummaryCard stopPropagation on buttons — card body click opens modal, buttons don't trigger modal
- [Phase 03-04]: Seed script builds own async engine from DATABASE_URL — avoids importing Settings which requires all env vars
- [Phase 04-twitter-agent]: tweepy[async] extra required — tweepy.asynchronous needs aiohttp + async-lru installed via [async] optional dep
- [Phase 04-twitter-agent]: Test stubs use per-function lazy imports so all 20 tests are collectable before agent module exists (Wave 0 correct RED state)
- [Phase 04-twitter-agent]: Pure scoring functions are module-level (not class methods) for direct testability in TDD
- [Phase 04-twitter-agent]: tweepy[async] extra required for AsyncClient — base tweepy missing aiohttp dep
- [Phase 04-twitter-agent]: scheduler/models/ mirrors backend/app/models/ — scheduler has no access to backend package
- [Phase 04-twitter-agent]: Two-model LLM pattern: Sonnet for drafting quality, Haiku for compliance speed/cost
- [Phase 04-twitter-agent]: Fail-safe compliance: ambiguous LLM response = block (not pass) for Seva Mining/financial advice check
- [Phase 04-twitter-agent]: Module-level wrapper functions (draft_for_post, filter_compliant_alternatives, build_draft_item) use __new__ to bypass __init__ for test injection
- [Phase 04-twitter-agent]: TwitterAgent instantiated inside job closure (not at build time) to avoid import side effects at test collection
- [Phase 04-twitter-agent]: All 25 watchlist accounts seeded at relationship_value=5 per user request (maximum priority for initial seed)
- [Phase 04-twitter-agent]: Backend models must be kept in sync with scheduler models manually — no shared package, each service owns its own copy of the model
- [Phase 05-senior-agent-core]: Wave 0 test stubs put pytest.skip() BEFORE the lazy import — ensures 19 tests show as 'skipped' not 'error' when senior_agent module doesn't exist yet
- [Phase 05-senior-agent-core]: migration 0004 adds both engagement_alert_level (engagement alert dedup) and alerted_expiry_at (expiry alert dedup) to draft_items in a single migration
- [Phase 05-senior-agent-core]: Jaccard similarity >= 0.40 threshold for story dedup, configurable via senior_dedup_threshold config key (default 0.40); lookback 24h via senior_dedup_lookback_hours
- [Phase 05-senior-agent-core]: cashtag-aware regex r'$[a-z0-9]+|\b\w+\b' preserves dollar-prefixed tokens like $gld and $2400 as atomic fingerprint units
- [Phase 05-senior-agent-core]: ORDER BY score ASC, expires_at ASC tiebreaking — soonest-expiring tied-score item is displaced; no special-case branch needed
- [Phase 05-senior-agent-core]: process_new_items module-level function uses lazy SeniorAgent instantiation to avoid circular imports from TwitterAgent call site
- [Phase 05-senior-agent-core]: run_expiry_sweep does NOT call _check_breaking_news_alert (that fires at item intake via process_new_item); engagement dedup uses one-way state machine null->watchlist->viral via engagement_alert_level column
- [Phase 05]: _headline_from_rationale splits on period-space to avoid splitting decimal numbers; queue_snapshot total computed in Python; yesterday_approved includes empty items list; var_2 truncated at last semicolon-space boundary within 200 chars
- [Phase 06-instagram-agent]: Wave 0 test stubs use pytest.skip() BEFORE lazy import — ensures 15 tests show as 'skipped' not 'error' when instagram_agent module doesn't exist yet; uv sync --all-extras required to preserve dev dependencies when adding prod deps
- [Phase 06-instagram-agent]: passes_engagement_gate accepts datetime parameter (not post_age_hours float) to match existing test stub signatures from Plan 01
- [Phase 06-instagram-agent]: select_top_posts named without instagram_ prefix to match Wave 0 test stubs
- [Phase 06]: Module-level functions (draft_for_post, check_compliance, build_draft_item_expiry) in instagram_agent.py for direct test injection without class instantiation
- [Phase 06]: Pre-screen '#' and 'seva mining' locally before calling Claude Haiku — avoids LLM cost for obvious compliance blocks
- [Phase 06]: Fail-safe compliance: only explicit 'pass' substring returns True; everything else blocks draft
- [Phase 06]: asyncio.sleep(2**attempt) gives 1s/2s backoff; 3 total attempts; empty list returned after exhaustion
- [Phase 06]: _check_critical_failure returns count only; alert at exactly consecutive_zeros==2 in _run_pipeline (dedup)
- [Phase 06]: Seed script uses 15 Instagram accounts (best-effort from 25 Twitter entities); 10 skipped with no active IG presence
- [Phase 07-content-agent]: Wave 0 test stubs use pytest.skip() BEFORE lazy import in content agent tests — ensures 16 tests show SKIPPED not ERROR during Wave 0 when module doesn't exist
- [Phase 07-content-agent]: ContentBundle scheduler mirror uses datetime.now(timezone.utc) lambda instead of deprecated datetime.utcnow — timezone-aware default
- [Phase 07-content-agent]: Content agent seed script only seeds config keys (no watchlists/keywords) — agent uses RSS + SerpAPI not platform watchlists
- [Phase 07-content-agent]: RSS_FEEDS and SERPAPI_KEYWORDS are module-level constants — accessible as ca.RSS_FEEDS and ca.SERPAPI_KEYWORDS in tests without class instantiation
- [Phase 07-content-agent]: run() creates and commits AgentRun immediately before pipeline starts — status visible even if pipeline crashes mid-flight
- [Phase 07-content-agent]: _extract_check_text is module-level (not class method) — consistent with established pattern for testable pure functions
- [Phase 08-dashboard-views-and-digest]: MSW /config/quota handler listed before /config/:key to prevent generic param matching quota path
- [Phase 08-dashboard-views-and-digest]: KeywordCreate.term field (not keyword) throughout frontend types — matches backend schema exactly
- [Phase 08-dashboard-views-and-digest]: ContentPage tests use vi.mock for API modules instead of MSW server.use() — relative fetch URLs cannot be intercepted in jsdom/Node.js MSW environment
- [Phase 08]: localStorage mock via vi.stubGlobal required for apiFetch tests — jsdom does not provide localStorage by default; any component calling apiFetch on render needs this mock in beforeEach
- [Phase 08]: useEffect syncs TanStack Query data to local state in DigestPage — direct render-time setState causes silent test failures
- [Phase 08-dashboard-views-and-digest]: JSDOM URL must be set to http://localhost:3000 in vitest config so MSW relative path handlers resolve to matching fetch URLs
- [Phase 08-dashboard-views-and-digest]: Human verification checkpoint approved — all three dashboard pages confirmed correct by operator
- [Phase 09-agent-execution-polish]: Twitter _get_config returns Optional[Config], so threshold reads use int(_cfg.value) if _cfg else default pattern — consistent with existing quota reads
- [Phase 09-agent-execution-polish]: Default parameter values in passes_engagement_gate match previous hardcoded values — backward compatibility preserved for existing tests without changes
- [Phase 09-agent-execution-polish]: expiry_sweep and morning_digest scheduler config keys placed in seed_content_data.py — scheduler-level concerns with no agent home; content seed is closest match
- [Phase 09-agent-execution-polish]: build_scheduler changed to async def to support await _read_schedule_config(engine) at startup; APScheduler API unaffected since main() already async
- [Phase 07-content-agent]: RSS_FEEDS expanded to 8 (reuters, bloomberg, goldseek, investing); SERPAPI_KEYWORDS to 10 (macro/inflation terms)
- [Phase 07-content-agent]: breaking_news format added as 4th Sonnet option with urgency-preference and senior analyst voice in system prompt
- [Phase 07-content-agent]: Multi-story pipeline with per-story error isolation replaces single-story; _is_already_covered_today() for cross-run dedup
- [Phase 07-content-agent]: VIDEO_ACCOUNTS capped at first 5 for API query length limits; fixed score 7.5 for Twitter-sourced content (video_clip, quote); _run_twitter_content_search() extracted as separate method for clean 07-07 merge; quote format added as choosable from article content in Sonnet prompt
- [Phase 07]: Sonnet prompt rewritten as central format decision engine for all 7 content types with Instagram design system (#F0ECE4/#0C1B32/#D4AF37) and historical pattern SerpAPI verification fallback
- [Phase 07]: GoldHistoryAgent uses fixed baseline score 8.0 for curated history stories (not RSS-scored)
- [Phase 07]: story slug tracked in Config before DraftItem creation so partial failures never cause story re-selection
- [Phase 10-senior-agent-whatsapp-notifications]: Phase 10: Switched whatsapp.py from content_sid (Meta-approved template SIDs) to body (free-form text) — Twilio sandbox accepts free-form without template approval; TEMPLATE_SIDS and send_whatsapp_template() removed
- [Phase 10]: Phase 10-03: expiry_sweep removed from scheduler; morning_digest at 15:00 UTC; WhatsApp failure non-fatal in run_morning_digest
- [Phase 11]: Used _get_render_bundle_job() helper for patchable ImportError-safe lazy scheduler import — Allows monkeypatching in tests without needing to patch a local import; scheduler agents/__init__.py causes tweepy ImportError in backend process which is caught gracefully
- [Phase 11-02]: genai_types.GenerateImagesConfig (plural) is the correct class name in google-genai 1.73.1 — RESEARCH.md had typo GenerateImageConfig (singular)
- [Phase 11-02]: Per-role retry (not per-bundle): each role gets 3 independent attempts; partial renders (e.g. 3/4) written as partial list; empty list on all-roles-failed
- [Phase 11-02]: Role names use twitter_visual/instagram_slide_1/2/3 convention to match RenderedImage schema; quote=2 roles not 1 per PLAN.md test behaviors
- [Phase 11-03]: Backend rerender endpoint uses asyncio.create_task in the backend's own event loop — cross-process scheduler.add_job is not possible (backend and scheduler are separate Railway services)
- [Phase 11-05]: TanStack Query v5 refetchInterval callback receives a query object (not (data, query) like v4); tests exercise the callback directly by extracting the query from the cache to avoid vi.useFakeTimers deadlocks
- [Phase 11-06]: RenderedImagesGallery mounted only for infographic + quote formats (D-04); other formats never render image skeletons
- [Phase 11-07]: importlib.util.spec_from_file_location bypasses scheduler/agents/__init__.py tweepy import in the backend process; scheduler/config.py uses extra="ignore" to tolerate backend-only env vars in the shared .env
- [2026-04-19 / quick 260419-lvy]: Instagram Agent (Phase 6) fully deprecated and purged from codebase — Apify scraping not viable, $50/mo spend cut; Seva Mining is now a three-agent system (Twitter + Senior + Content). Frontend IG UI removed, scheduler IG job removed, Apify deps removed from scheduler/backend. Content agent still emits instagram_post/instagram_caption/instagram_brief/carousel fields and image_render still emits IG slide roles, but nothing downstream consumes them (no migration written — safe no-op data).
- [2026-04-19 / quick 260419-lvy]: Content queue page now groups by agent_run matching Twitter pattern (showRunGroups = platform === 'twitter' || 'content'); content branch renders ContentSummaryCard inside grouped layout.
- [2026-04-20 / quick 260420-sn9]: Twitter Agent (Phase 4) and the broader Senior Agent (Phase 5, Phase 10) fully deprecated and purged. X API Basic tier ($100/mo) spend not justified when Content Agent's news pipeline produces higher-signal drafts; Senior Agent had narrowed to morning_digest-only in practice. Scheduler trimmed from 5 jobs → 3 jobs (content_agent + morning_digest + gold_history_agent). `scheduler/agents/twitter_agent.py` + tests deleted; `scheduler/agents/senior_agent.py` trimmed to morning-digest-only. Frontend Platform type narrowed from `'twitter' | 'content'` → `'content'`; 7 Twitter-only files deleted (ApprovalCard, PlatformTabBar, QueuePage, QuotaBar, WatchlistTab + 2 tests); Twitter nav/tab/scoring/keyword/agent-option purged. Backend `/config/quota` endpoint removed; test fixtures migrated to content-only. Prod DB: 177 `draft_items WHERE platform='twitter'` rows deleted; 40 watchlists rows deleted (25 twitter + 15 instagram orphans from lvy); 8 `config` rows with `twitter_*` keys deleted. **Preserved intentionally:** `tweepy[async]>=4.14` + `x_api_bearer_token/x_api_key/x_api_secret` env wiring + `VIDEO_ACCOUNTS` + `_search_video_clips` + `_draft_video_caption` + `_run_twitter_content_search` in content_agent.py — the video_clip pipeline still needs X API read access. Seva Mining is now a single-agent system (Content only).
- [2026-04-20 / quick 260420-sn9]: quick task `260420-s3b` (Twitter agent autonomous auto-posting spec) **cancelled before a PLAN.md was written**. Context+research docs surveyed the X API for write-scope, but operator's "never auto-post" rule (hard constraint in CLAUDE.md) superseded the exploration — and sn9 purged the Twitter Agent entirely, making auto-posting moot. s3b CONTEXT.md + RESEARCH.md retained for historical record only.
- [2026-04-20 / quick 260420-sn9]: One Rule-3 auto-fix in prod DB purge transaction — a single `instagram` row (orphan from `260419-lvy` purge) had `related_id` pointing at a `twitter` row, blocking the DELETE via FK violation. UPDATE-NULL'd that one `related_id` in the same transaction; instagram row itself left in place (scope-bounded).
- [2026-04-21 / quick 260421-eoe]: Content Agent architecture pivoted from single-agent monolith to **7-sub-agent system with cron-less review service**. Mid-workflow Phase C pivot — operator rejected the original orchestrator-cron design in favor of `content_agent.py` as a pure library module exposing `fetch_stories()` + `review()` for sub-agents to call into. Sub-agents are self-contained per-tick (no cross-tick `draft_content IS NULL` handoff), eliminating a proposed `research_context` JSONB migration and a 30-min gate-latency concern. Scheduler grew from 3 jobs → 8 jobs (morning_digest + 7 sub-agents), each sub-agent on its own 2h `IntervalTrigger` staggered across `[0,17,34,51,68,85,102]` min offsets within the 2h window. Lock IDs 1010–1016 allocated. Frontend combined `/content` route deleted → replaced by single dynamic `/agents/:slug` route driven by `CONTENT_AGENT_TABS` config. The per-format drafting logic was preserved verbatim (functional parity — no new prompts, no new content types, no schema changes); operator plans to iterate per-sub-agent later. System description updated: "single-agent system" → "7 sub-agent system + Content Agent review service."
- [2026-04-21 / quick 260421-eoe]: During T02 extraction, discovered pre-split `content_agent.py` used one combined `_research_and_draft` function rather than 5 separate `_draft_<type>` helpers the plan assumed. Resolved by creating shared `scheduler/agents/content/__init__.py::run_text_story_cycle(agent_name, content_type, draft_fn)` helper that the 5 text sub-agents (breaking_news, threads, long_form, quotes, infographics) delegate through; extracted 5 format-specialized Sonnet prompts preserving functional parity. `video_clip` + `gold_history` retain bespoke flows (video_clip uses X API read, gold_history uses curated source + `gold_history_used_topics` Config dedup); both still call `content_agent.review()` inline. Net: every persistence path goes through `review()` as required.
- [2026-04-21 / quick 260421-eoe]: Post-verifier follow-up commit removed defunct `content_agent_interval_hours` + `gold_history_hour` keys from `scheduler/seed_content_data.py` `CONFIG_DEFAULTS` list + added DELETE clause to the script's DB-migration docstring (marked retired by this task). Dead-code follow-ups flagged but not removed in this task: `content_agent.py::breaking_window_hours` parameter (no caller), orphan `ContentPage.tsx` + test (after `/content-review` route removal), `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` config keys (pending post-deploy confirmation of zero reads).
- [2026-04-21 / quick 260421-k9z / prod DB migration]: Executed the canonical DELETE from `scheduler/seed_content_data.py` docstring against prod Neon via local `.env` DATABASE_URL + asyncpg (same pattern as sn9). Transaction-wrapped with pre/post counts and auto-rollback on post-count non-zero. **Pre-counts:** `content_agent_interval_hours`=1, `gold_history_hour`=1, `content_agent_max_stories_per_run`=1, `content_agent_breaking_window_hours`=1 (4 rows present). `content_agent_schedule_hour` + `content_agent_midday_hour` were already absent (eoe follow-up commit `047ef1d`'s DELETE clause had covered them or they never existed in prod). **Post-counts:** all 6 keys at 0. **Net:** 4 rows deleted in a single transaction. No reinsert path remains — `worker.py::upsert_agent_config` no longer includes the k9z-removed overrides, and `seed_content_data.py::CONFIG_DEFAULTS` no longer lists any of the 6 keys. Scheduler is already running commit `8ce8210` (post-k9z), so next startup's upsert_agent_config will NOT re-add the keys.
- [2026-04-21 / quick 260421-k9z]: Dead-code follow-up to eoe — removed the three items flagged as open follow-ups. **Scope expanded on closer grep:** `breaking_window_hours` parameter lives inside an entire orphan-island — `select_qualifying_stories`, `select_top_story`, and `_is_within_window` are all defined in `content_agent.py` but zero callers exist in live scheduler code or tests (old references only in `.claude/worktrees/` scratch dirs). All three functions deleted as a unit (−81 lines). `classify_format_lightweight` (L247-288, live) preserved between the two orphan blocks via two-block surgical deletion; `from datetime import datetime, timezone` import retained (~18 other live call sites). **Deviation #6 in eoe SUMMARY was stale** — it claimed `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` were still read by `fetch_stories()`, but post-split `fetch_stories()` takes no parameters and consults no config. Both keys were zombie writes: `seed_content_data.py::CONFIG_DEFAULTS` (L42-43) + `worker.py::upsert_agent_config` overrides (L295-296) wrote on every startup but no code read. Both write sites removed; DELETE migration docstring in `seed_content_data.py` extended from 4 → 6 retired keys (operator-run one-off against prod). Frontend: `ContentPage.tsx` + `ContentPage.test.tsx` deleted (only self-import was the test itself); stale 3-line orphan comment in `App.tsx:24-26` replaced with single-line "Other pages." comment. **Validation:** scheduler ruff clean + pytest 74/74 (eoe baseline); backend ruff clean + pytest 69 passed / 5 skipped (unchanged); frontend eslint clean + vitest 52/52 across 8 files (eoe baseline 61/9; delta of −9 tests / −1 file exactly = deleted ContentPage.test.tsx suite); tsc + vite build green (541 kB → 163 kB gzip unchanged).

- [2026-04-23 / quick 260423-k8n]: `sub_long_form` fully deprecated and purged — topology reduces from 7 to 6 sub-agents (quick-260423-k8n). Rationale: "makes things simpler" — longer-form content can ride under threads sub-agent when warranted. Historical DB rows preserved (no migration). ThreadPreview.tsx `long_form_post` compat field retained (thread-format feature, not long_form reference). Scheduler now 7 jobs (6 subs + morning_digest); stagger offsets `[0,17,34]` → `[0,17]`; lock ID 1012 retired. Mirrors `260419-lvy` (Instagram) and `260420-sn9` (Twitter+Senior) purge precedents.

### Pending Todos

None yet.

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260427-k5h | **Fix gold_history Instagram Carousel slide-content renderer.** Pure-backend assembler bug at `scheduler/agents/content_agent.py:635-653` (the `gold_history` branch of `build_draft_item`): code read `slide.get('text', '')` but the actual schema produced by `scheduler/agents/content/gold_history.py:198-208` is `{slide, headline, body, visual_note}` — there is no `text` field. Net effect: dashboard detail modal showed `Slide 1:`, `Slide 2:`, …, `Slide 5:` with nothing after each colon, even though the underlying `content_bundles.draft_content.instagram_carousel` JSONB was fully populated correctly (verified live via Neon SQL on the AJ Bell's Mould bundle). Fix renders each slide as `Slide N: HEADLINE\n  BODY\n  Visual: VISUAL_NOTE` (multi-line with each present field on its own indented line). Each field omitted from output if absent; legacy string-slide compat preserved. `_extract_check_text` at line 714 already read `slide.headline + slide.body` correctly (line 736-737), so compliance-review path was unaffected — only the persisted draft_text blob was broken. **Locked discuss-phase decisions** (Q1 multi-line slide format / Q2 include `visual_note` with `Visual:` prefix / Q3 forward-fix only — no backfill of <20 existing rows / Q4 audit-other-renderers — confirmed only `gold_history` had drift; the dead `charts[0].title` fallback in the infographic branch is harmless and left alone). **Audit (Q4) result:** every other format renderer matches its drafter prompt — `breaking_news` (`tweet`+`infographic_brief.{headline,data_points}`), `thread` (`tweets[]`), `infographic` (`twitter_caption`), `gold_media` (`twitter_caption`/`instagram_caption`/`source_account`), `quote` (`twitter_post`/`instagram_post`/`quote_text`+`speaker`). **Tests (+4):** `test_build_draft_item_gold_history_renders_carousel_slides` (full 5-slide payload — asserts every headline + body + visual_note appears in output, asserts section markers preserved, asserts no broken `Slide 1:\n\nSlide 2:` regression pattern), `test_build_draft_item_gold_history_handles_partial_slide` (missing one of headline/body/visual_note still renders present fields), `test_build_draft_item_gold_history_legacy_string_slide_compat` (bare-string slides still render as `Slide N: <str>`), `test_build_draft_item_gold_history_empty_carousel_falls_back` (empty tweets+carousel falls back to `Gold History: {story_title}`). 180 → 184 scheduler tests pass; ruff clean. **Manual REPL** confirmed output matches locked Q1/Q2 spec exactly. Zero-diff preservation on `scheduler/agents/content/gold_history.py` (drafter), the other 5 sub-agent modules, `_extract_check_text`, frontend, backend, alembic, models. **Why this slipped through:** the existing `test_build_draft_item_stores_bundle_id_in_engagement_snapshot` only exercised the `breaking_news` branch — no gold_history coverage. Workflow: `/gsd:quick --discuss --full` (no research subagent — fix was mechanical and audit was inline); single atomic commit. **Out of scope (deferred):** backfill of existing `draft_items.alternatives` blobs (Q3 — too low-volume to justify a script; ages out naturally). | 2026-04-27 | TBD | Verified (local) — pending Railway deploy | [260427-k5h](./quick/260427-k5h-fix-gold-history-instagram-carousel-slid/) |
| 260424-l0d | **Phase B — approve→post-to-X** (user-initiated per-item, atomic, OAuth 1.0a). New FastAPI route `POST /items/{item_id}/post-to-x` at `backend/app/routers/post_to_x.py` orchestrates a single-transaction `SELECT ... FOR UPDATE` row lock, bundle-text lookup via `draft_items.engagement_snapshot["content_bundle_id"]` → `content_bundles.draft_content`, tweepy v2 `AsyncClient.create_tweet` (or simulate-mode `sim-{uuid4}` write when `X_POSTING_ENABLED=false`), and atomic write of post-state columns. **Narrow MVP** ships text-only `breaking_news` (single `draft_content["tweet"]`) + `thread` (client-side reply-chain over `draft_content["tweets"]` list with `in_reply_to_tweet_id`); out-of-scope content_types return 400 listing supported types. **New `approval_state` column** on draft_items (orthogonal to the existing `status` column from `PATCH /items/{id}/approve`) with enum `{pending, posted, failed, discarded, posted_partial}` enforced via CHECK constraint (D6 — chose CHECK over PG enum for migration ergonomics). 4 sibling columns: `posted_tweet_id` (TEXT), `posted_tweet_ids` (JSONB array for threads), `posted_at` (TIMESTAMPTZ), `post_error` (TEXT — `"{code}:{message}"` per D11). **Dual-model parity** required by RESEARCH Pitfall 5: identical column shapes on both `backend/app/models/draft_item.py` AND `scheduler/models/draft_item.py` — guarded by `scheduler/tests/test_draft_item_model.py` parity test. Alembic 0009 migration at `backend/alembic/versions/0009_add_x_post_state_to_draft_items.py` is hand-written (not autogenerated) so the CHECK constraint expression is explicit; reverse migration drops index + columns + constraint cleanly. **Idempotency** via SELECT FOR UPDATE inside `async with db.begin():` — second concurrent POST blocks on the lock, reads `approval_state='posted'` after first commits, returns 200 with `already_posted=true` + same `posted_tweet_id`. Tested via SQL-compile regex assertion (SQLite test env silently no-ops live row locks per RESEARCH). **Thread partial-failure** (D7): `post_thread()` returns `(ids, PostError | None)` tuple — first-tweet-fail returns `([], err)`; mid-thread fail returns `(ids_so_far, err)` with `approval_state='posted_partial'`, `posted_tweet_ids=[<ids posted so far>]`, `post_error="thread posted N/M: {code}:{message}"`. No automatic rollback (we do NOT auto-delete the successfully-posted tweets), no automatic retry (single attempt; user manually re-triggers via the same button). **Backend service `backend/app/services/x_poster.py`** wraps `tweepy.AsyncClient` with OAuth 1.0a User Context (4-key construction: consumer_key/secret + access_token/secret); exposes `post_single_tweet(text) -> str` and `post_thread(tweets: list[str]) -> tuple[list[str], PostError | None]`. Exception mapping enforces catch order TooManyRequests/Unauthorized/Forbidden/BadRequest BEFORE HTTPException BEFORE TweepyException (RESEARCH Pitfall 7), each mapped to `PostError(code, message)` with codes `429/401/403/400/500`. **Simulate mode** (D2): when `settings.x_posting_enabled is False` (default in dev/CI per `backend/tests/conftest.py`), the route writes `posted_tweet_id=f"{x_posting_sim_prefix}{uuid4()}"` (configurable prefix `sim-`) WITHOUT calling tweepy — frontend flows exercise end-to-end without touching the live X account. **Frontend** ships in `ContentDetailModal.tsx` footer (modal-only per D10; deliberately NOT on the summary card face): "Post to X" button gated on `content_type ∈ {breaking_news, thread} AND approval_state ∉ {posted, posted_partial}`; click opens `frontend/src/components/approval/PostToXConfirmModal.tsx` (wraps the existing base-ui `dialog.tsx`) with full tweet preview (numbered list for threads, monospace pre-wrap for single) and verbatim D10 copy: title "Post to X?", body "This will immediately post to your X account. You can't undo this.", buttons "Cancel" / "Post to X". `frontend/src/hooks/usePostToX.ts` mirrors the existing `useApprove.ts` TanStack Query mutation pattern, invalidates the queue query on success. `ContentSummaryCard.tsx` gains state badges for `posted` (green + tweet link) / `posted_partial` (amber + tooltip with first-tweet link + post_error) / `failed` (red + tooltip with post_error); `pending`/`discarded` show no new badge. Types layer: `ApprovalState` union + `PostToXResponse` interface in `api/types.ts`; `postToX(itemId)` API call in `api/queue.ts`. **CLAUDE.md narrow override** per locked decision D12 — the "Auto-posting to any platform / Never implement" row in the "What NOT to Use" table becomes "Unattended auto-posting to any platform" with body explaining the risk model is unattended autonomous publishing (NOT user-initiated approve→post). Use Instead column points to `POST /items/{id}/post-to-x` for in-scope content_types and clipboard copy for everything else. Forbids cron-driven posting + bulk "post all queued" actions explicitly. Additional CLAUDE.md additive section "X Developer Portal — Approve→Post-to-X Runbook" documents the 5-step OAuth1 setup (Read+Write app permission BEFORE token regeneration; otherwise tokens carry only Read scope and create_tweet returns 403 — RESEARCH OAuth1 gotcha). **New env vars** registered in BOTH `backend/app/config.py` AND `scheduler/config.py` Settings (parity): `x_access_token`, `x_access_token_secret`, `x_posting_enabled` (default `False`), `x_posting_sim_prefix` (default `"sim-"`). `.env.example` gains the 4 entries with the Read+Write hint. `backend/pyproject.toml` adds `tweepy[async]>=4.14` (regenerated `backend/uv.lock`) — tweepy was previously scheduler-only. **Validation gates all green:** backend `uv run pytest -x` 92 passed (+23 net new — 11 in `test_x_poster.py` covering happy/thread/partial/exception-mapping/SELECT-FOR-UPDATE-compile-assertion + 12 in `test_post_to_x.py` covering simulate-single/simulate-thread/idempotency/404/400-missing-bundle-id/400-out-of-scope-content_type/real-happy-single/real-happy-thread/thread-partial-failure/PostError-failed/auth-401 + 8-line defensive fixture extension to `test_queue.py` for the new schema columns); backend ruff clean; scheduler `uv run pytest -x` 180 passed (+5 parity-guard tests in new `test_draft_item_model.py`); scheduler ruff clean; frontend `npm run lint && npm run typecheck && npm test` all green (59 tests passed +4 new in `PostToXConfirmModal.test.tsx`); frontend `npm run build` succeeds. **Preservation gate empty (HARD):** `git diff main -- scheduler/agents/ scheduler/worker.py scheduler/services/whatsapp.py scheduler/agents/senior_agent.py scheduler/agents/content_agent.py` returns 0 bytes — Phase B does NOT touch the drafting pipeline / scheduler cron / whatsapp service / senior digest / content agent service. **Workflow:** ran via `/gsd:quick --discuss --research --full` (full pipeline). Discussion phase locked 15 decisions (D1 narrow MVP scope=BN+thread / D2 simulate-mode default / D3 SELECT FOR UPDATE / D4 atomic trigger / D5 5-state machine / D6 column placement on draft_items via CHECK / D7 thread reply-chain + posted_partial / D8 OAuth 1.0a 4-key / D9 rely-on-tweepy-429 / D10 modal verbatim copy / D11 `{code}:{message}` taxonomy / D12 narrow CLAUDE.md rewrite / D13 byte-for-byte fidelity / D14 all-mocked tests / D15 single-account scope) captured in CONTEXT.md. RESEARCH.md (HIGH confidence) corrected three CONTEXT assumptions: route name `post-to-x` not `approve-and-post` (collision-avoidance with existing `PATCH /approve`), text source on `content_bundles.draft_content` not `draft_items.draft_content` (the latter doesn't exist — lookup via engagement_snapshot.content_bundle_id), thread field key `tweets` not `thread_tweets`. Plan-checker PASSED iteration 1 (no revisions needed). Executor shipped 5 atomic commits in worktree `agent-a98a47fd` (T1 alembic+models+config 50c43d4 → T2 service 7d6a9ff → T3 route add1f1e → T4 frontend 39dc9ee → T5 docs 7abb171), merged into main via `--no-ff` merge commit 96d6177. Verifier: status=`human_needed` — 12/12 must-have truths verified, all 7 key-link paths real, all 13 artifacts present with substance, all 11 test gates green; 2 items routed to user (NOT gaps): (a) live alembic up-down-up cycle against Neon dev Postgres before prod deploy (SQLite test suite passes; live-PG cycle deferred per SUMMARY's external-resource note), (b) end-to-end real-mode posting requires X Developer Portal credentials. **Deferred:** Phase B.5 (trivial registry add for `quotes` + `gold_history` content_types — they're text-only and use the existing `post_single_tweet`); Phase B+ (`gold_media` + `infographics` — require `tweepy.media_upload` + X API media endpoints, fundamentally different auth/upload pattern). **Pre-deploy runbook:** at the X Developer Portal — log in → open the Basic-tier app → User authentication settings → set App permissions to "Read and write" (NOT "Read and write and Direct message" — DMs out of scope) → save → Keys and tokens tab → Access Token and Secret → Regenerate → copy new tokens to Railway env `X_ACCESS_TOKEN` + `X_ACCESS_TOKEN_SECRET` → set `X_POSTING_ENABLED=true` in Railway prod env. To roll back, set `X_POSTING_ENABLED=false` (or unset) — route reverts to simulate-mode without redeploy. | 2026-04-24 | 50c43d4 / 7d6a9ff / add1f1e / 39dc9ee / 7abb171 / 96d6177 | Needs Review (live alembic + X Portal real-mode pending) | [260424-l0d](./quick/260424-l0d-implement-phase-b-approve-then-auto-post/) |
| 260424-kqa | Restore `sub_breaking_news` cadence 2h → 1h. Flipped `CONTENT_INTERVAL_AGENTS[0]` tuple `interval_hours` from `2` to `1` at `scheduler/worker.py:123`. Reverts vxg's 2h bump (260422-vxg cited duplicate-story churn concerns with m9k's 1h). User directive driving this: *"Breaking news agent is doing a run every 30 minutes, change that to an hour"* — cadence code was 2h not 30min; likely Railway zero-downtime redeploys were firing `sub_breaking_news` (offset=0, fires ~10s after each restart) more often than the 2h interval suggested. j5i's quality-gate shipped earlier today (`BREAKING_NEWS_MIN_SCORE=6.5` + `max_count=3` + composite-score sort + `floored_by_min_score` telemetry) + `fetch_stories()`'s 30-min TTL cache bound duplicate-story churn even at 1h cadence — addresses vxg's original concern. Narrative comments updated: worker.py module docstring (added kqa provenance line), L9 + L108-113 CONTENT_INTERVAL_AGENTS comment, L329 build_scheduler log-line. Test at `scheduler/tests/test_worker.py:242-248 (test_interval_agents_cadences)` assertion flipped `"sub_breaking_news": 2` → `1` with updated docstring; stale "locked invariant" comment at L533-534 inside the j5i-era `test_sub_threads_interval_is_three_hours` test removed (the 2h invariant it asserted was broken by this commit). `scheduler/agents/content/breaking_news.py:2-11` module header rewritten to record the m9k→vxg→kqa cadence history plus the j5i-post-gate churn-bound argument. Zero-diff on all 6 other sub-agents, content_agent.py, whatsapp.py, senior_agent.py, backend/, frontend/, alembic/, models/. 175 scheduler tests pass, ruff clean. Single atomic commit. **Post-deploy watch:** if user still sees <1h cadence after Railway picks up this commit, the root cause is Railway restart frequency (not the interval setting), which is a separate investigation — flagged in SUMMARY.md/worker.py module docstring but not fixed here. | 2026-04-24 | ac90ee3 | Verified (local) — pending Railway deploy | [260424-kqa](./quick/260424-kqa-breaking-news-cadence-2h-to-1h/) |
| 260424-j5i | Tighten sub_breaking_news + sub_threads story selection to "top-of-top." Added composite-score sort + top-N cap + `min_score=6.5` floor to both sub-agents. BN now passes `max_count=3, sort_by="score", min_score=BREAKING_NEWS_MIN_SCORE`; Threads passes `max_count=2, sort_by="score", min_score=THREADS_MIN_SCORE`. `run_text_story_cycle` signature extended with trailing `min_score: float \| None = None` kwarg (default disables = zero impact for other 4 sub-agents). Floor check placed AFTER Haiku gate + compliance review (D6) — ContentBundle PERSISTED for floored items, DraftItem SKIPPED. Recency curve at `content_agent.py:111-133` grew a `<48h=0.3` bucket between `<24h=0.4` and `≥48h=0.2` (D3 — strictly monotonic, benefits all 6 sub-agents). Rich-telemetry branch at `content/__init__.py` broadened from `content_type == "infographic"` to `content_type in ("infographic", "breaking_news", "thread")`, adding `floored_by_min_score` counter to the payload (D7). Two folded-in user directives: (D8) removed `"rare earth restrictions"` from SERPAPI_KEYWORDS at `content_agent.py:79` — htu-era expansion comment updated "8 → 7 additions"; SERPAPI_KEYWORDS length drops 18 → 17; Haiku gate prompt at `L443-500` with hyphenated `rare-earth` examples at `L455/L502` BYTE-FOR-BYTE preserved per D4. (D9) `sub_threads` cadence flipped 4h → 3h at `worker.py:118` tuple (`("sub_threads", threads.run_draft_cycle, "Threads", 1011, 17, 3)`); narrative "4h" comments at L10/L109/L314/L328 updated to "3h"; `sub_breaking_news` stays at 2h. Per-agent tunable constants `BREAKING_NEWS_MIN_SCORE: float = 6.5` (`content/breaking_news.py:31`) and `THREADS_MIN_SCORE: float = 6.5` (`content/threads.py:26`) enable one-line tuning post-deploy. **Post-launch observability signal:** Research Q5 showed live DB retention at the 6.5 floor over the last 7d was **41% for sub_breaking_news** (n=251 compliance-passed) and **50% for sub_threads** (n=52) — `breaking_news` sits BELOW CONTEXT.md's 50-80% sanity band. Not a blocker: `BREAKING_NEWS_MIN_SCORE` is a one-line tunable; empty runs are silent per i8b firehose contract; `agent_run.notes.floored_by_min_score` is now observable per run. Operator should monitor first 72h — if >70% of compliance-passed stories floor across a 24h window, drop to 6.0 (Q5 projection → ~62% retention). 4 atomic commits (T1 RED / T2 GREEN / T3 TEST-ADJUST / T4 DOCS). Test suite 167 → 175 (+9 new tests across test_content_agent.py, test_content_wrapper.py, test_breaking_news.py, test_threads.py, test_worker.py; 5 pre-existing assertions reconciled in T3 including 1 not pre-flagged by plan — `test_infographics.py::test_run_draft_cycle_writes_structured_notes` gained `"floored_by_min_score": 0` in expected telemetry dict). Zero-diff verified across 10 out-of-scope paths: 4 other sub-agents (quotes, infographics, gold_media, gold_history), scheduler/services/whatsapp.py, scheduler/agents/senior_agent.py, backend/, frontend/, alembic/, scheduler/models/. scheduler `uv run pytest -x` 175 passed; `uv run ruff check .` clean. No DB migrations, no new Config keys, no new API integrations, no frontend changes, no draft-prompt edits (D5), no Haiku-gate prompt edits (D4). | 2026-04-24 | 62205bb / e45fb74 / 2120598 / 223cfe1 | Verified (local) — pending Railway deploy + 72h retention monitoring | [260424-j5i](./quick/260424-j5i-tighten-sub-breaking-news-and-sub-thread/) |
| 260424-i8b | Split WhatsApp notifications into two streams: (1) per-run firehose for sub_breaking_news + sub_threads — fires every run with ≥1 approved item (empty runs silent); (2) midday digest retimed 07:00→12:30 PT, scope-filtered to infographics + quotes + gold_media + gold_history via _assemble_digest NOT EXISTS subquery on ContentBundle.content_type. New helpers: build_chunks() (1500-char greedy pack, [short X/N] multi-chunk prefix), send_agent_run_notification() dispatcher. JOB_LOCK_IDS key renamed morning_digest→midday_digest (lock ID 1005 unchanged). DIGEST_EXCLUDED_CONTENT_TYPES = frozenset({"breaking_news", "thread"}). Narrow-diff: senior_agent.py gets excluded_content_types kwarg + 4 query filters only. Zero-diff on all 6 sub-agent modules, content_agent.py, backend/, frontend/, alembic/, models/. 166 tests GREEN, ruff clean. T1+T2 combined per plan allowance. | 2026-04-24 | 9c783f0 | Verified (local) | [260424-i8b](.planning/quick/260424-i8b-split-whatsapp-notifications-per-run-fir/) |
| 260424-htu | Expanded SERPAPI_KEYWORDS 10→18 (added: critical minerals, rare earth restrictions, strategic metals, sovereign wealth fund gold, treasury gold sale, gold mining M&A, US China metals, mineral supply chain). Swapped dead Reuters RSS for BNN Bloomberg. Directly addresses EU/US critical minerals deal miss + Azerbaijan $3B sovereign gold drawdown miss. 4 new coverage tests + updated len assertion. 147 scheduler tests green, ruff clean. T1+T2 collapsed per CONTEXT Q9. | 2026-04-24 | 7530663 | Verified (local) | [260424-htu](.planning/quick/260424-htu-expand-serpapi-rss-ingestion-coverage-cr/) |
| 260424-e37 | Replaced sub_gold_history's curated 12-JSON fact-sheet pool with an online SerpAPI fetch that surfaces fresh historical gold-sector stories each run, using the fetched article body as the sole fact source. Partially unwinds **260422-lbb** (curated whitelist + Claude-from-training-data + SerpAPI snippet-verify model). Reuses the day-seeded-shuffle + fetch-helper pattern from **260423-lvp** (sub_infographics analytical-historical fallback). Dedup migrated from slug-based `Config.gold_history_used_topics` to all-time URL-based `Config.gold_history_used_urls` (prod's legacy slug key left dormant — no Alembic). Drafter input shape changed from `fact_sheet: dict` to `(article_text, headline, url, source_name)` with surgically edited system prompt (verified_facts/sources refs removed; article_text named as sole fact source). Cadence (every other day at 12:00 PT), advisory lock 1016, items-per-run cap (1), and drama-first drafter voice are preserved. `gold_history_stories/` directory (12 JSONs + __init__.py) deleted entirely. content_agent.fetch_analytical_historical_stories gets a docstring-only update noting dual usage. AgentRun.notes now carries the 8 structured keys from CONTEXT Q9. Zero-diff preservation verified on worker.py, infographics.py, frontend/, alembic/, models/, and content/__init__.py. 7 atomic commits. | 2026-04-24 | 6ade453 / fd7b324 / 3f40577 / 8183a6c / a4fb1e0 / (T6) | Verified (local) — pending Railway deploy | [260424-e37](.planning/quick/260424-e37-replace-sub-gold-history-s-curated-14-js/) |
| 260407-n9t | Content agent schedule fix (6am/12pm PST) + enhanced WhatsApp notification with titles | 2026-04-07 | 9bc17e1 | | [260407-n9t](./quick/260407-n9t-content-agent-schedule-fix-and-enhanced-/) |
| 260407-neq | Switch content agent from two cron jobs to single interval trigger every 2 hours | 2026-04-07 | 8b1e22e | | [260407-neq](./quick/260407-neq-switch-content-agent-to-interval-every-2/) |
| 260409-qe8 | Fix Twitter engagement gate (None impression_count skips views check) + content agent non-ISO date fallback | 2026-04-09 | aecc81b | | [260409-qe8](./quick/260409-qe8-fix-twitter-engagement-gate-and-content-/) |
| 260419-ko3 | Fix 8 pre-existing test failures (backend + scheduler + frontend) | 2026-04-19 | 0cd0432 | | [260419-ko3](./quick/260419-ko3-fix-8-pre-existing-test-failures-across-/) |
| 260419-l5t | Post-v1.0.1 health-check cleanup: env.example hygiene, ruff+eslint lint-zero, JWT_SECRET length assertion | 2026-04-19 | 8bfdfea | Verified | [260419-l5t](./quick/260419-l5t-post-v1-0-1-health-check-cleanup-env-exa/) |
| 260419-lvy | Full purge Instagram agent + content queue run-grouping (deprecated Phase 6) | 2026-04-19 | b2ff329 / accf735 / 2eb9125 / 4ab94e7 | Verified | [260419-lvy](./quick/260419-lvy-full-purge-instagram-agent-content-queue/) |
| 260419-n4f | Content agent relevance cleanup — Bloomberg commodities swap, drop Investing.com, sharpen prompt, two-bucket gold gate, restore 7.0 threshold | 2026-04-19 | 502ae3e / 010926d / 2cfc9cb | Verified | [260419-n4f](./quick/260419-n4f-content-agent-relevance-cleanup-swap-blo/) |
| 260419-r0r | Long_form 400-char minimum floor + sharpen thread vs long_form prompt (thread=fact-rich, long_form=article-style) | 2026-04-20 | f30dd44 / 14caedc | Verified | [260419-r0r](./quick/260419-r0r-enforce-400-char-minimum-floor-on-long-f/) |
| 260419-rqx | Content agent tuning — 3h cadence, 0.40 recency weight, top-5 Haiku format-first pipeline, listicle rejection in gold gate | 2026-04-20 | 663d6d8 / 7fb5517 / af37d3c | Verified | [260419-rqx](./quick/260419-rqx-content-agent-tuning-pass-cadence-top-5-/) |
| 260419-si2 | Display Gemini-rendered images inline on content queue cards with role label, Dialog enlarge, and fetch+blob Download | 2026-04-20 | 1ff7f07 / 8a2d991 | Verified | [260419-si2](./quick/260419-si2-display-rendered-images-on-dashboard-app/) |
| 260419-t78 | Replace Gemini Imagen infographic rendering with Claude chart_spec + Recharts 2.15.4 (a16z-style) — Pydantic schema, Node renderer (resvg-js), Python ChartRendererClient, infographic branch migrated. End-to-end smoke test on 2026-04-20 uncovered 5 renderer bugs (download-fonts zip paths, SVG xmlns missing, greedy regex swallowed Legend div, Recharts Legend rendered outside chart svg, ComparisonTable 2-col overlap) + Dockerfile sha256 pinned + dead _build_prompt branches removed. All 8 chart types render valid PNGs locally. **2026-04-20 22:00 UTC day-after check:** 9 infographic bundles produced in the last 24h had old-schema draft_content (no `charts` field) and 0 rendered images — root cause: Railway scheduler service's Root Directory is `scheduler/`, but the t78 Dockerfile used `scheduler/...` prefixed COPY paths that resolved to `scheduler/scheduler/...` → every t78 deploy failed at `[7/17] COPY scheduler/chart_renderer/` since the first push. Active Railway deploy was stuck 18h on `quick-si2`. Fix pushed as `bbc6e6a` — stripped `scheduler/` prefix from 7 COPY instructions. **2026-04-20 ~22:30 UTC a16z style pass (`3937f34`):** extracted shared `theme.js` (cream BG, deep-navy/teal palette, 110px editorial header), rewrote all 7 components for uniform navy bars + value labels at tips + horizontal-only gridlines + source-left/wordmark-right attribution. Font pivot: Playfair Display (variable-wght TTF, resvg-js 2.6.x fails to resolve wght axis) → DM Serif Display (static TTF, heavier display face, better a16z match). Font loader pivot: `fontBuffers` (Uint8Array — silently drops family name on multi-font loads) → `fontFiles` (path-based, fontdb parses `name`-table family correctly). All 9 chart types verified locally with clean serif titles. | 2026-04-20 | 898909b / 8d0ea80 / beba088 / de31622 / be1944c / 74a6e67 / 58c1d02 / bbc6e6a / 3937f34 | Verified (local) — Railway rebuild in flight with combined Dockerfile + style fixes | [260419-t78](./quick/260419-t78-replace-gemini-imagen-infographic-render/) |
| 260420-lw4 | Tighten Twitter agent filters: watchlist-only ingestion, literal GOLD_KEYWORDS match, 50-like floor (both paths), Gold Telegraph (@GoldTelegraph_) universal bypass of topic filter and engagement gate | 2026-04-20 | 18c0701 / b6cee40 | Pending Railway deploy + DB spot-check (Task 3) | [260420-lw4](./quick/260420-lw4-tighten-twitter-agent-filters-watchlist-/) |
| 260420-mfy | Pivot infographic+quote formats off on-platform rendering → claude.ai artifact prompts. Ripped `scheduler/chart_renderer/` (Node.js, fonts, Dockerfile layers), `ChartRendererClient`, Gemini/Imagen quote renderer, `ChartSpec`/`BundleCharts`/`render_bundle_job`/`_enqueue_render_job`, `RenderedImage`/`rendered_images`/`RerenderResponse` Pydantic + API + frontend types, and all image-display UI (Dialog enlarge + fetch+blob download from si2). Added `scheduler/agents/brand_preamble.py` with 1267-char `BRAND_PREAMBLE` baking in Seva look (cream #F0ECE4, navy #0C1B32/#1E3A5F/#4A7FA5, gold #D4AF37, DM Serif Display titles, Inter body, SEVA MINING wordmark, 1200x675 HTML artifact). Content agent infographic+quote branches now emit `{suggested_headline, data_facts, image_prompt}` (prompt = BRAND_PREAMBLE + type-specific direction). Frontend `InfographicPreview.tsx` + `QuotePreview.tsx` rewritten to three copy-buttoned text blocks; legacy (pre-mfy) bundles show placeholder. Dockerfile slimmed to 28-line pure-Python. 106 scheduler tests + 66 backend tests + 73 frontend tests pass, ruff + eslint clean, tsc+vite build green. | 2026-04-20 | 5514db0 / be2f7a7 / d589c68 / 8a4fcb1 / 76f3769 / df274c3 | Verified | [260420-mfy](./quick/260420-mfy-pivot-infographic-quote-post-formats-off/) |
| 260420-nnh | Content agent no-specific-miner filter — bolt company-specific rejection onto existing gold gate (single Haiku call, structured JSON return). is_gold_relevant_or_systemic_shock() now returns dict {keep, reject_reason, company}. Allowlists financial-institution citations (Goldman, WGC, JPM, Fed) as sources not subjects. Logs "ContentAgent: rejected story — primary subject is specific miner (Company)." Inverts two rqx-era tests (Newmont earnings + Barrick M&A → now reject). Adds 12 new fixture tests. 62 scheduler tests pass, ruff clean. | 2026-04-20 | 15df673 | Pending Railway verification | [260420-nnh](./quick/260420-nnh-content-agent-reject-stories-primarily-a/) |
| 260420-oa1 | Real-time market snapshot injection into content agent drafter — kills gold-price hallucinations. New `scheduler/services/market_snapshot.py` with `fetch_market_snapshot()` (httpx-direct, parallel `asyncio.gather`) pulling FRED macro (DGS10/DFII10/DFF/CPIAUCSL with YoY + observation date) and metalpriceapi.com Basic ($8.99/mo, gold XAU + silver XAG with reciprocal-rate inversion). DXY dropped (DTWEXBGS ≠ ICE DXY; yfinance unreliable). New `MarketSnapshot` ORM model (mirror in backend/) + Alembic migration 0007 with `market_snapshots` table (status `ok|partial|failed`, JSONB `data`, `fetched_at` index). Fetch runs once per `ContentAgent.run()` before `_run_pipeline`, snapshot stored on instance and injected at TOP of system_prompt (before "You are a senior gold market analyst") in all 3 Sonnet drafters: `_research_and_draft`, `_draft_video_caption`, `_draft_quote_post`. Hard instruction forbids BOTH current AND historical invented numbers verbatim — operator's "$3,500/oz in 2025" bug was a historical fabrication, not current-spot. Fail-open at every layer: missing API key → WARNING + skip; source 500/429/parse → per-source `[UNAVAILABLE]`; DB write failure → ERROR + continue in-memory; pipeline exception → fallback snapshot. 12 unit tests (reciprocal rate `1/0.000426≈$2,347.42`, FRED `.` sentinel, CPI YoY from 12-month spread, populated + fallback render blocks) + 8 integration tests (call_count=1, snapshot in all 3 drafters, DB failure non-fatal, nnh gold-gate dict contract regression guard). 138/138 scheduler tests pass, ruff clean. Adds FRED_API_KEY + METALPRICEAPI_API_KEY to .env.example + pydantic-settings. **Verification passed (10/10 must-haves).** New monthly cost: +$8.99 (metalpriceapi Basic). | 2026-04-21 | b28780b / a3738e3 / 0dfdc28 | Verified | [260420-oa1](./quick/260420-oa1-real-time-market-snapshot-injection-to-s/) |
| 260420-p8h | Extend `scheduler/worker.py::_validate_env()` to print startup status for `FRED_API_KEY` + `METALPRICEAPI_API_KEY`. New `market_data` category (distinct from existing `critical` and `optional`) logs INFO `SET ✓` when present, WARNING "MISSING — drafter will fall back to [UNAVAILABLE] for this source" when absent. Rationale: missing market keys silently degrade draft accuracy via fail-open fallback; surfacing at WARNING in startup logs lets operator spot Railway env typos immediately instead of waiting up to 3h for next content_agent cron cycle. No test changes (pure I/O logging), ruff clean. | 2026-04-21 | 352b2d4 / b510a92 | Verified (local) — Railway rebuild will exercise | [260420-p8h](./quick/260420-p8h-add-fred-metalpriceapi-env-key-status-li/) |
| 260420-pwh | Fix 2 prod bugs surfaced during oa1/p8h verification — both silently degraded drafter output via fail-open. **Bug 1 (oa1 regression):** `MetalpriceAPIFetcher.fetch()` applied `1.0 / xau` to `rates.USDXAU`, writing raw reciprocal (~$0.000208/oz) to `market_snapshots.data.gold_usd_per_oz` — oa1 plan's "reciprocal rate trap" docstring was inverted from the actual API shape. Live probe confirmed metalpriceapi with `base=USD` returns `USDXAU=4816.39` as **direct USD per oz** (the reciprocal `XAU=0.000208` is the alternate shape). Fix: removed `1.0 /` inversions at `scheduler/services/market_snapshot.py:210-214`, rewrote class docstring (lines 192-193), flipped `METALS_GOOD` fixture and test assertions (`4816.39`/`79.58` from `0.000426`/`0.035842`), renamed test to `test_metalpriceapi_direct_price_read`. **Bug 2:** `claude-3-5-haiku-latest` deprecated, returns `NotFoundError` on every call. Silently disabled (a) gold gate → specific-miner rejection (nnh) never fired, (b) format classifier → every story defaulted to `thread`. Live probe verified `claude-haiku-4-5` alias resolves to `claude-haiku-4-5-20251001`. Swapped 4 locations in `content_agent.py` (lines 276/287/503/1535), 1 in `seed_content_data.py:46`, 22 test-mock replacements. Prod `config` table has no `content_gold_gate_model` row → code default (now swapped) is authoritative; no DB UPDATE required. **Bug 3 (in-flight discovery):** Haiku 4.5 wraps JSON in ```` ```json ... ``` ```` code fences even when prompt asks for bare object. `json.loads()` failed on every call → gate silently fail-opened → specific-miner rejection still didn't fire post-Bug-2-fix. Added fence-stripping preprocess at `content_agent.py:558-566` (mirrors pattern already used in 3 other locations in same file) + 3 new tests. **Live verification:** fresh `content_agent` run shows zero NotFoundError/JSONDecodeError, 80+ Anthropic 200 OKs, gate rejected `Kalgoorlie Gold Mining` as specific-miner + one generic as not-gold-relevant. DB `market_snapshots` latest row: `gold_usd_per_oz: 4808.86` + `silver_usd_per_oz: 79.34` (matches live metalpriceapi $4,816.39/$79.58 within normal intraday spread). 85 scheduler tests pass, ruff clean. gsd-verifier verdict: PASSED. | 2026-04-21 | 87878e1 / 50720f1 / b2bce6f / 3525c47 | Verified | [260420-pwh](./quick/260420-pwh-fix-metalpriceapi-reciprocal-inversion-u/) |
| 260420-r18 | Two content-pipeline cleanups triggered by pwh post-verification dashboard inspection. **Task A (frontend field-name fix):** Long-form dashboard bundles showed "No draft content available" despite having 1500+ chars of real prose in DB. Root cause: `LongFormPreview.tsx` read `d.post_text` but backend drafter at `content_agent.py:1261` wrote the field as `post` (validator at 1321 also reads `post`). Fixed frontend-side rather than backend (existing DB rows already have `post` key — frontend fix is retroactive with zero migration; backend fix would only help future rows). Changed `LongFormPreview.tsx:6` from `post_text?: string` to `post?: string` + line 11 from `d.post_text` to `d.post`. Updated 1 test fixture at `ContentDetailModal.test.tsx:159`. Net change: +3/-3 across 2 files. **Task B (remove X as quote-post source):** Per user directive "for quotes, I don't want it to pull from X. Quotes should only be sourced from reputable articles." Deleted `QUOTE_ACCOUNTS` constant (lines 84-90, PeterSchiff/jimrickards/etc.), `_search_quote_tweets()` method (X API `>=10 like` filter, lines 944-1022), and the quote-tweet call + for-loop in `_run_pipeline` (lines 1864-1921). Added format-selection prompt enhancement at line 1152: "Prefer 'quote' format when the article contains a verbatim, attributed statement from a credible named figure (bank analyst, central bank official, WGC, IMF, etc.)." Preserved: `_draft_quote_post()` method (still used when Claude picks quote format from article path), `_draft_video_caption`, `VIDEO_ACCOUNTS`, `_search_video_clips`. Net change: +3/-151 in content_agent.py (file now 1845 lines). **DB cleanup (user-authorized one-off DELETE):** 4 stale pre-fix bundles — 2× "Gold prices news" long_form + 2× Kalgoorlie multi-miner thread — removed via `railway run` DELETE by UUID. **Live verification via `railway run`:** fresh content_agent completed with 0 quote-tweet log lines, 0 NotFoundError/JSONDecodeError, Kalgoorlie rejected by specific-miner gate, 2 new bundles both from reputable article sources (Market Index + Investing.com Nigeria), 0 new quote bundles with `source_name` matching old QUOTE_ACCOUNTS. 141 scheduler tests pass, 73 frontend tests pass, ruff + eslint clean, vite build success (224ms). gsd-verifier verdict: PASSED (6/6 truths, 10/10 checks). | 2026-04-20 | fb53254 / aa7b061 / e90d1c2 | Verified | [260420-r18](./quick/260420-r18-long-form-dashboard-field-name-fix-remov/) |
| 260420-s3b | Twitter agent autonomous auto-posting exploration (CONTEXT + RESEARCH only — no PLAN.md written). Surveyed X API v2 `POST /2/tweets` write scope requirements + OAuth 2.0 PKCE for bot-style posting. **Superseded and cancelled** by `260420-s3b` being logically incompatible with operator's hard "never auto-post" rule in CLAUDE.md, and then rendered moot entirely by `260420-sn9` which purged the Twitter Agent. Retained docs: `260420-s3b-CONTEXT.md` + `260420-s3b-RESEARCH.md` as historical record of the rejected direction. | 2026-04-20 | — | Cancelled (superseded by 260420-sn9) | [260420-s3b](./quick/260420-s3b-twitter-agent-autonomous-auto-posting-wi/) |
| 260421-eoe | Split monolithic Content Agent (~2000 lines) into 7 per-type sub-agents under `scheduler/agents/content/` + cron-less Content Agent review service. **Phase C architecture** (operator pivoted mid-workflow from original "Content Agent as orchestrator cron" design): `content_agent.py` loses its cron entirely, shrinks to 1040 lines exposing only `fetch_stories()` (shared SerpAPI+RSS ingestion with 30-min in-memory cache), `review(draft)` (Haiku compliance gate), and `classify_format_lightweight` helper. Sub-agents are self-contained within a single tick: fetch via shared cache → filter to own content_type → draft via Sonnet → call `content_agent.review()` inline → INSERT `content_bundles` row with `compliance_passed` set. **Scheduler 8-job topology:** `morning_digest` (existing) + 7 sub-agents on 2h `IntervalTrigger` staggered `[0, 17, 34, 51, 68, 85, 102]` min offsets, advisory lock IDs `1010`–`1016` (breaking_news/threads/long_form/quotes/infographics/video_clip/gold_history in order); old `content_agent` (lock 1003) + standalone `gold_history_agent` (lock 1005) crons retired. `scheduler/agents/gold_history_agent.py` deleted — folded into `scheduler/agents/content/gold_history.py`. Pool bumps: `scheduler/database.py` 3→15, `backend/app/database.py` 5→15. **Backend:** `/queue?content_type=<type>` filter added via JSONB subquery on `draft_items.engagement_snapshot->>'content_bundle_id'` (no FK — existing link pattern). **Frontend:** combined `/content` route + `PlatformQueuePage.tsx` deleted → replaced by single dynamic `<Route path="/agents/:slug">` driven by `CONTENT_AGENT_TABS` config array (7 entries in operator-priority order: Breaking News → Threads → Long-form → Quotes → Infographics → Gold Media → Gold History); Sidebar + `AgentRunsTab.AGENT_OPTIONS` both rewired config-driven; default redirect `/` → `/agents/breaking-news`; `useQueueCounts` now per-content-type via `useQueries`. **Docs:** CLAUDE.md opening paragraph + 2026-04-20 historical note's scheduler-job sentence + Stack constraint bullet updated to reflect 8-job + cron-less Content Agent model; 2026-04-19 lvy note and rest of 2026-04-20 sn9 note preserved verbatim. **No DB migrations** (all 7 content_type enum values already existed; no schema touches). **Shared helper:** `scheduler/agents/content/__init__.py::run_text_story_cycle(agent_name, content_type, draft_fn)` factors the 5-text-sub-agent common path (breaking_news, threads, long_form, quotes, infographics); video_clip + gold_history have bespoke flows (video_clip skips fetch_stories, uses X API read via existing `_search_video_clips` tweepy path; gold_history skips fetch_stories, uses curated historical source + `gold_history_used_topics` Config dedup gate). **Validation:** scheduler ruff clean + pytest 74 passed; backend ruff clean + pytest 69 passed / 5 skipped (pre-existing); frontend eslint clean + vitest 61 passed + `npm run build` green (541 kB → 163 kB gzip). gsd-verifier verdict: **PASS** (22/22 goal-backward invariants). Post-verifier follow-up commit: removed defunct `content_agent_interval_hours` + `gold_history_hour` keys from `scheduler/seed_content_data.py` + added DELETE clause to its DB-migration docstring. **Open follow-ups (dead code, non-blocking):** `content_agent.py::breaking_window_hours` parameter no longer passed by any caller; `ContentPage.tsx` + test orphaned after `/content-review` route removal; `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` config keys if confirmed unread post-deploy. | 2026-04-21 | 2fe94bf / a247cd1 / 762b08b / 979b69c / 2e43fd2 / f25faec / d20f515 / d7613b4 / 45dcbc6 / a8d512e / 047ef1d | Verified | [260421-eoe](./quick/260421-eoe-split-content-agent-into-7-sub-agents-or/) |
| 260421-k9z | Dead-code cleanup follow-up to 260421-eoe — removed three items flagged in eoe's "Dead code flagged" section. **Scheduler:** deleted orphan-island `select_qualifying_stories` + `select_top_story` + `_is_within_window` from `content_agent.py` as a unit (`breaking_window_hours` param dies with the island; `classify_format_lightweight` preserved via two-block surgical deletion); removed zombie config keys `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` from BOTH `seed_content_data.py::CONFIG_DEFAULTS` AND `worker.py::upsert_agent_config` overrides (write-only post-eoe, zero readers anywhere); extended seed-script DELETE migration docstring from 4 → 6 keys for operator one-off. **Frontend:** deleted orphan `ContentPage.tsx` (342 lines) + `ContentPage.test.tsx` (205 lines); replaced stale 3-line comment in `App.tsx:24-26` with single-line "Other pages." comment. **Net:** −550 LOC across scheduler (−81 content_agent.py, ±8 seed_content_data.py, ±7 worker.py) + frontend (−547 ContentPage + test + ±4 App.tsx). scheduler ruff clean + pytest 74/74 (eoe baseline); backend ruff clean + pytest 69/5 (unchanged); frontend eslint clean + vitest 52/52 (eoe baseline 61/9, delta exactly matches deleted ContentPage suite); tsc + vite build green. No behavior change. **Prod DB migration executed:** transaction-wrapped DELETE against Neon deleted 4 retired rows (`content_agent_interval_hours`, `gold_history_hour`, `content_agent_max_stories_per_run`, `content_agent_breaking_window_hours` → 0 each); 2 other keys in the canonical list (`content_agent_schedule_hour` + `content_agent_midday_hour`) were already absent from earlier cleanups. Post-count verification confirmed all 6 keys at 0. No reinsert path remains in live code. | 2026-04-21 | b8aec6e / 704d7b3 | Verified + prod migrated | [260421-k9z](./quick/260421-k9z-clean-up-3-dead-code-items-flagged-in-eo/) |
| 260421-mos | Quotes sub-agent daily 12pm Pacific + reputable-source whitelist + top-2 cap + drafter quality gate. **Scheduler topology:** `scheduler/worker.py::CONTENT_SUB_AGENTS` (7 entries on IntervalTrigger) split into `CONTENT_INTERVAL_AGENTS` (6 entries, same tuple shape from m9k: `(job_id, run_fn, name, lock_id, offset, interval_hours)`) + `CONTENT_CRON_AGENTS` (1 entry — `sub_quotes`; shape: `(job_id, run_fn, name, lock_id, hour, minute, timezone)`). Added `from apscheduler.triggers.cron import CronTrigger` and a second registration loop in `build_scheduler()` for cron agents. Startup log line reports both interval + cron counts. `JOB_LOCK_IDS` unchanged (8 keys, sub_quotes still 1013) — only the schedule shape moved. **Shared pipeline:** `scheduler/agents/content/__init__.py::run_text_story_cycle` gains two optional kwargs `max_count: int \| None = None, source_whitelist: frozenset[str] \| None = None`. Filter logic inserted AFTER the format filter + BEFORE the candidate loop — whitelist first (case-insensitive substring match on `story["source_name"]`), then max_count cap (sort by `story["published_at"]` desc, take top N). Both filters log stage counts. Defaults=None → zero behavior change for the 4 other sub-agents using this helper (breaking_news, threads, long_form, infographics). **Quotes wiring:** `scheduler/agents/content/quotes.py` adds module-level `REPUTABLE_SOURCES: frozenset[str]` with 27 patterns across tier-1 financial (reuters, bloomberg, wsj, ft, barron, marketwatch, cnbc, economist, financial post), gold-specialist (kitco, mining.com, mining journal, northern miner, gold hub, mining weekly), and institutional (WGC, IMF, BIS, Fed, ECB, BoE, BoJ, PBoC, S&P, Moody's). `run_draft_cycle` passes `max_count=2, source_whitelist=REPUTABLE_SOURCES`. `_draft` prompt extended with 4-criterion quality bar (speaker credibility — named senior role; substance — specific number/timeframe/catalyst/contrarian view; freshness — current article; clarity — self-contained claim) + reject-path JSON option `{"reject": true, "rationale": "..."}`. Return logic handles reject BEFORE parsing draft_content — returns None, which triggers the existing stub-bundle branch in run_text_story_cycle (ContentBundle with `compliance_passed=False`, no draft_content, no draft_item) — same shape as a JSON parse failure, treated identically downstream. **Tests (+8 total, 74→82):** `test_worker.py` — `CONTENT_SUB_AGENTS→CONTENT_INTERVAL_AGENTS` import rename; `test_sub_agents_total_seven` (renamed, asserts `len(INTERVAL) + len(CRON) == 7`); offsets list trimmed to `[0,17,34,68,85,102]` (51 gone with sub_quotes); `test_sub_agent_lock_ids` iterates both lists; new `test_quotes_is_daily_cron` asserts 1013/12/0/"America/Los_Angeles". `test_quotes.py` — +3: `test_reputable_sources_set_populated`, `test_draft_returns_none_on_reject`, `test_run_draft_cycle_passes_filters`. New file `scheduler/tests/test_content_init.py` — 4 tests for run_text_story_cycle filter logic (whitelist filters stories, max_count caps candidates, both no-op paths preserved). **Rule 3 deviations:** (1) worktree spawned from k9z HEAD `aaac399` — executor rebased onto m9k HEAD `3fa7212` before editing since plan treated m9k's 6-tuple schema + 10s buffer as prerequisite; (2) `America/Los_Angeles` appears 7× in worker.py (1 tuple entry + 1 log format + 5 doc/comment references — all inert; plan validation accepted ≥1); (3) removed unused MagicMock import from new test_content_init.py per ruff F401. **Gates:** scheduler `uv run ruff check .` clean; `uv run pytest -x` 82 passed (baseline 74 on m9k). All 9 validation greps pass. **No DB migrations, no frontend changes, no changes to the other 6 sub-agents.** Single atomic implementation commit `4dbeab3` on branch `quick/260421-mos-quotes-daily-noon-reputable` (sits on top of m9k commits `c5d32f3`+`3fa7212`). **Post-deploy expectation (operator confirms on Railway):** startup log reports both counts; next 12:00 America/Los_Angeles fires sub_quotes (DST-aware — handles PST/PDT automatically); first-fire logs show reputable filter + max_count cap funnel counts + 0-2 quote drafts OR per-story reject rationale; /agents/quotes page shows "Pulled from agent run at 12:00 PM · {date}" within seconds OR stays empty 24h if no quote met the bar. Existing pending Quote drafts in prod DB left untouched per user directive. | 2026-04-21 | 4dbeab3 | Verified (local) — pending Railway deploy | [260421-mos](./quick/260421-mos-quotes-sub-agent-daily-12pm-pacific-repu/) |
| 260422-ep9 | Market snapshot gains temporal anchors (gold 24h high/low + prior close + 52w high/low + at_52w_high + tracking_since) to kill "hitting highs" drafter hallucinations. **Origin bug:** 2026-04-22 breaking_news draft claimed `"GOLD whipsaws $100+ intraday"` + `"gold and equities hitting highs SIMULTANEOUSLY today"` — gold actually gave back Trump Taco rally gains that day. The existing `market_snapshot` from oa1 injected current spot only, with no temporal anchor to catch directional claims. **Changes (`scheduler/services/market_snapshot.py` only):** (1) Extended `MarketSnapshot` TypedDict with 7 new Optional fields — `gold_24h_high`, `gold_24h_low`, `gold_prior_close`, `gold_52w_high`, `gold_52w_low`, `at_52w_high: bool \| None` (strict equality: `current_spot >= gold_52w_high`), `tracking_since: date \| None`. (2) New private helper `_compute_gold_anchors(session)` runs a single SQL window query (MAX/MIN over 24h + 52w + prior_close as `ORDER BY fetched_at DESC LIMIT 1 OFFSET 1` + `MIN(fetched_at)` for `tracking_since`) against `market_snapshots` table filtered to `status IN ('ok', 'partial')`. Uses `cast(MarketSnapshotORM.data["gold_usd_per_oz"].astext, Float)` because the price is stored inside JSONB `data`, not a top-level column. (3) `fetch_market_snapshot(session)` now calls `_compute_gold_anchors(session)` **BEFORE** `session.add(snapshot_row)` to prevent the in-flight row from contaminating `prior_close`. (4) `render_snapshot_block` gains 2 new lines: `"- Gold 24h range: $X - $Y/oz (prior close: $Z/oz)"` + `"- Gold 52w range: $X - $Y/oz (at_52w_high: true)"` with graceful `[UNAVAILABLE — tracking since YYYY-MM-DD, N day(s) of history]` fallback labels. (5) `_HARD_INSTRUCTION` extended with second paragraph forbidding `"at highs / at a new high / at an all-time high / breaking out / at a record / hitting a new record"` unless `at_52w_high: true`, AND forbidding specific intraday move claims (`"$X+ intraday / whipsaws $X / X% move today"`) unless the 24h range supports the magnitude. **Locked decisions honored (from CONTEXT.md, 5/5):** (D-01) No backfill — graceful `[UNAVAILABLE]` during the ~1-year data-gap period; (D-02) Gold only, 5 anchors (silver deferred); (D-03) On-every-fetch SQL window query, no new columns, no migration, no new job; (D-04) Strict equality for `at_52w_high` (no fuzzy tolerance); (D-05) Extended `_HARD_INSTRUCTION` with explicit forbidden-phrase list. **Zero changes outside market_snapshot.py** — all 7 sub-agent drafters (breaking_news, threads, long_form, quotes, infographics, video_clip, gold_history) receive the enriched snapshot block verbatim via the existing `render_snapshot_block(market_snapshot)` call. **Backward-compat:** new TypedDict fields are all Optional with default None, so 6 downstream tests that patch `fetch_market_snapshot` with None/minimal dicts continue to pass. **Tests (+8 net, 90→98):** `_MockAnchorSession` helper + `_build_fred_metals_side_effect` helper + 8 new tests — compute-happy-path (seeded rows spanning 24h + 52w windows), compute-empty-history-returns-Nones, compute-partial-history-24h-ok-52w-unavailable, render-block-populated-shape (all 2 anchor lines present), render-block-fallback-labels (includes `tracking since` with plural/singular day wording), render-block-empty-first-snapshot, hard-instruction-regex (new forbidden phrases all present), strict-equality-at-52w-high (spot==52w_high→true, spot==52w_high*0.999→false). **Gates:** `uv run ruff check .` clean, `uv run pytest -x` 98 passed (baseline 90 + 8 new). All 7 validation greps from CONTEXT.md pass. **No DB schema changes, no Alembic migration, no new Config keys, no new API integrations, no frontend changes, no changes to sub-agent drafter code.** Single atomic implementation commit `55b1eb0` on branch `quick/260422-ep9-market-snapshot-temporal-anchors` (parent = `66225f7`, the vxg docs commit). **Post-deploy expectation:** next `fetch_market_snapshot` call on Railway logs show the new anchor values in the rendered snapshot block; drafters no longer produce claims like "hitting highs SIMULTANEOUSLY today" unless `at_52w_high: true` is emitted. During first ~24h post-deploy, 52w_high line will show `[UNAVAILABLE — tracking since 2026-04-21]` and drafters must hedge; 24h_range becomes real after ~24h of hourly-ish snapshots accumulate. | 2026-04-22 | 55b1eb0 | Verified (local) — pending Railway deploy | [260422-ep9](./quick/260422-ep9-enrich-market-snapshot-with-temporal-anc/) |
| 260422-vxg | 7-sub-agent cadence rebalance. **Topology:** 3 interval (BN=2h, Threads=4h, Long-form=4h, offsets `[0,17,34]`) + 4 cron at 12:00 America/Los_Angeles (sub_quotes/sub_infographics/sub_video_clip daily; sub_gold_history every-other-day via `day="*/2"`). Reverts m9k's BN=1h experiment to BN=2h; slows Threads + Long-form to 4h; moves Infographics/Video Clip/Gold History from 2h interval to daily cron; Quotes stays daily. **Schema upgrade:** `CONTENT_CRON_AGENTS` tuple shape changed from 7-field fixed (`hour, minute, timezone`) to 5-field dict-of-kwargs (`cron_kwargs: dict`), so heterogeneous cron patterns coexist cleanly. Registration loop calls `CronTrigger(**cron_kwargs)`. **Quotes tightening:** `max_count=2 → 1` in `scheduler/agents/content/quotes.py::run_draft_cycle` — surface only the single highest-signal reputable quote per day. **Gold Media (video_clip) analyst quality gate:** VIDEO_ACCOUNTS reordered + trimmed to `["Kitco", "CNBC", "Bloomberg", "BloombergTV", "ReutersBiz", "FT", "MarketWatch"]` (dropped PR-sourced BarrickGold/WorldGoldCouncil/Mining/Newaborngold); `_draft_video_caption` user_prompt extended with 3-criterion analyst quality bar (speaker credibility — named analyst/economist with tier-1 affiliation; substance — specific view or forecast; freshness — current commentary) + reject-path JSON option `{"reject": true, "rationale": "..."}` returning None; `run_draft_cycle` sorts clips by `created_at` desc (tz-aware fallback `datetime.min.replace(tzinfo=timezone.utc)` to avoid naive-vs-aware comparison crash), caps to `MAX_DRAFT_ATTEMPTS = 5`, breaks after first successful persist — goal is 1 analyst clip/day or none if nothing passes the bar. **Docstrings:** `breaking_news.py::L3` reverted "every 1 hour" (m9k) → "every 2 hours"; `gold_history.py` cadence line replaced with `day='*/2'` every-other-day noon PT reference; worker.py module docstring + startup log line updated for 3-interval/4-cron topology. Other sub-agent docstrings untouched — grep-first confirmed no cadence mentions. **Tests (+8 net, 82→90):** `test_worker.py` rescoped `test_sub_agents_total_seven` (3+4), `test_sub_agent_staggering` (`[0,17,34]`), `test_quotes_is_daily_cron` (dict-shape); added `test_interval_agents_cadences`, `test_cron_agents_count_four`, `test_cron_agents_use_dict_shape`, `test_gold_history_is_every_other_day`, `test_infographics_is_daily_cron`, `test_video_clip_is_daily_cron`. `test_quotes.py::test_run_draft_cycle_passes_filters` asserts `max_count==1`. `test_video_clip.py` added `test_video_accounts_reordered_analyst_first` + `test_draft_video_caption_returns_none_on_reject`. **Scope deferred per operator's "Option A" directive:** (1) Infographics historical-fallback drafter (Claude picker + `infographics_used_topics` Config key + drafter + integration, modeled on gold_history.py architecture); (2) Gold Media source switch to YouTube Data API / podcast RSS (current X API video source kept, tightened with analyst bar). **Gates:** scheduler `uv run ruff check .` clean; `uv run pytest -x` 90 passed (baseline 82 + 8 new tests, comfortably above the ≥87 gate). All 8 validation greps pass. **No DB migrations, no new Config keys, no frontend changes, no changes to breaking_news/threads/long_form/infographics drafter logic beyond docstrings.** Single atomic implementation commit `68b21d1` on branch `quick/260422-vxg-cadence-rebalance-7-sub-agents` (parent = `ddb5721`). | 2026-04-22 | 68b21d1 | Verified (local) — pending Railway deploy | [260422-vxg](./quick/260422-vxg-multi-agent-cadence-rebalance/) |
| 260421-m9k | Breaking News sub-agent hourly cadence + APScheduler offset=0 first-fire fix. **Change 1 (scheduling cadence):** `scheduler/worker.py::CONTENT_SUB_AGENTS` tuple shape extended from 5 → 6 fields by appending `interval_hours: int`. `sub_breaking_news` row set to `interval_hours=1`; other 6 sub-agents remain at `2`. Loop at L266-274 unpacks 6 fields and passes `IntervalTrigger(hours=interval_hours, ...)` with dynamic `name=f"{name} — every {interval_hours}h (offset +{offset}m)"`. Type annotation widened. **Change 2 (offset=0 first-fire fix):** `start_date = now + timedelta(minutes=offset) + timedelta(seconds=10)` — 10-second buffer guarantees `start_date > now` at `scheduler.start()` wall clock even when `offset=0`. Prevents APScheduler `IntervalTrigger.get_next_fire_time` from treating `start_date == now` as a past/missed schedule and pushing the first fire to `start_date + interval` (which had silently forced a 2h dark period on every Railway redeploy for Breaking News — no agent_run row for `sub_breaking_news` until 2h after boot). Explanatory 2-line comment added above the start_date line. **Docstring refreshes:** 5 worker.py docstring/comment references to "2h"/"every 2h" updated to note mixed cadence (BN hourly, other 6 on 2h); `breaking_news.py::L3` module docstring "every 2 hours" → "every hour". **Rule 3 deviation:** `scheduler/tests/test_worker.py::test_sub_agent_lock_ids` directly unpacked `CONTENT_SUB_AGENTS` as 5-tuple; extended unpacking pattern to 6 fields (no semantic change, still validates identical lock-ID mapping). 3 files modified (worker.py, breaking_news.py, test_worker.py) instead of 2 as plan predicted. Scheduler ruff clean + pytest 74/74 (matches pre-change baseline). Frontend + backend untouched. **Post-deploy expectation (operator confirms on Railway):** all 7 sub-agents fire within first offset window on next redeploy (no 2h dark for offset=0); `sub_breaking_news` logs agent_run rows hourly thereafter; "Pulled from agent run at {time}" header appears on /agents/breaking-news ~10s after scheduler boot. | 2026-04-21 | c5d32f3 | Verified (local) — pending Railway deploy | [260421-m9k](./quick/260421-m9k-breaking-news-sub-agent-hourly-cadence-o/) |
| 260420-sn9 | Full purge Twitter agent + trim Senior Agent to morning-digest-only. 5 atomic commits (one per layer), single-agent system end-state (Content only). **Task 1 — scheduler:** deleted `scheduler/agents/twitter_agent.py` + tests; trimmed `scheduler/agents/senior_agent.py` to `run_morning_digest()` only (removed dedup/expiry/queue-cap methods); `worker.py` `_make_job` no longer branches twitter/senior; scheduler jobs now content_agent + morning_digest + gold_history_agent; `JOB_LOCK_IDS` entries for twitter_agent/senior_agent removed; `tweepy[async]>=4.14` + `x_api_*` env vars + `_validate_env` critical `X_API_BEARER_TOKEN` check **preserved** (Content Agent video_clip pipeline is downstream consumer). **Task 2:** senior_agent trim commit — confined to that one file with tests. **Task 3 — backend:** `/config/quota` router endpoint removed; test fixtures migrated (`test_crud_endpoints.py` twitter watchlist fixtures → content; `test_queue.py::make_draft_item` default platform twitter → content; `test_content_bundles.py` rendered_images role twitter_visual → content_visual); `seed_mock_data.py` rewritten to content-only (removed 5 twitter + 5 instagram mock items, 2 content items kept, no cross-platform related_id wiring); model/schema comments updated to document Twitter purge; long comments reflowed to ≤100 chars for ruff E501. **Task 4 — frontend:** Platform type narrowed `'twitter' \| 'content'` → `'content'`; `QuotaResponse` + `getQuota()` removed; deleted ApprovalCard + PlatformTabBar + QueuePage + QuotaBar + WatchlistTab + associated tests; Sidebar/App.tsx/AgentRunsTab/KeywordsTab/ScoringTab/PlatformBadge/RelatedCardBadge narrowed to content; MSW handlers rewritten content-only; DigestPage stats grid `grid-cols-3` → `grid-cols-2` (Twitter tile removed); SettingsPage tabs 6 → 5 (Watchlists removed, Keywords now default); PlatformQueuePage both branches render ContentSummaryCard. **Task 5 — prod DB + docs:** Neon DELETEs via `railway run` inside single transaction — **177** draft_items `platform='twitter'` → **0**, **40** watchlists (all) → **0**, **8** config `twitter_%` keys → **0**; one Rule-3 auto-fix (NULL'd related_id on an orphan instagram row pointing at a twitter row from lvy-era data); Content row count preserved (955 before + 955 after). CLAUDE.md historical note appended for 2026-04-20, Project blurb updated to "single-agent." ROADMAP.md deprecation notice added, Phase 4/5 struck through, Phase 10 partial-deprecation banner, progress table updated. REQUIREMENTS.md: TWIT-01..14 + SENR-01..05 + SENR-08..09 struck through; SENR-06/07 retained (shipped via Phase 10). STATE.md: 2 new decision entries + this row. Pre/post DB counts recorded in SUMMARY.md. Verification: scheduler pytest 100/100, backend pytest 70 collected (66 passed / 5 skipped), frontend vitest 56/56, ruff + eslint clean, tsc + vite build green. | 2026-04-20 | 62ffda4 / be9cd8b / aeaf412 / c30e288 / 55100c9 | Verified | [260420-sn9](./quick/260420-sn9-full-purge-twitter-agent-trim-senior-to-/) |
| 260422-zid | Remove predicted_format routing gate from run_text_story_cycle + add max_count=2 caps for threads/long_form. Root cause: Haiku classifier assigned ~80-90% of gold-sector stories to "breaking_news", starving threads/long_form/quotes/infographics sub-agents (all returned items_queued=0). Fix: replaced `candidates = [s for s in stories if s.get("predicted_format") == content_type]` with `candidates = list(stories)` in `scheduler/agents/content/__init__.py` — each sub-agent's own drafter already specializes format; the pre-filter was redundant and harmful. Added max_count=2 to threads + long_form to cap Claude API spend now that they see the full story pool (quotes already had max_count=1). Updated 5 agent test files (renamed test_run_draft_cycle_no_candidates_exits_cleanly → test_run_draft_cycle_completes_with_stories) + test_content_init.py docstrings. predicted_format label retained as analytics-only metadata, not used for routing. Phase B source topology audit conducted: video_clip kept on X API (unique analyst signal), gold_history design retained (Claude picks + SerpAPI verifies). 5 follow-up issues documented in debug file: (a) video_clip consistent 0 items — X API quota; (b) sub_gold_media/sub_video_clip frontend label confusion; (c) orphan running rows in agent_runs; (d) gold_history hallucination risk; (e) APScheduler misfire_grace_time=300 too short for Railway deploys. | 2026-04-22 | 3819972 | Verified (local) | [260422-zid](./debug/resolved/260422-zid-non-bn-agents-produce-zero-items.md) |
| 260422-ink | Per-agent queue RunHeader shows specific timestamp instead of bare "(earlier)" fallback. **Origin bug:** Threads tab rendered `Pulled from agent run (earlier)` while Breaking News rendered `Pulled from agent run at 12:52 PM · Apr 22`. Root cause was in `frontend/src/pages/PerAgentQueuePage.tsx`: (i) `getAgentRuns(tab.agentName, 7)` only pulled the last 7 DAYS of `agent_runs` (second param is `days`, not row count — confirmed via `frontend/src/api/settings.ts:53-58`); (ii) `groupByRun` bucketed each item by `started_at <= created_at` and fell through to `NO_RUN_KEY = '__no_run__'` when nothing matched; (iii) `RunHeader` rendered the bare string `(earlier)` on the no-run branch. Threads had pre-eoe-split drafts (pre-2026-04-21, when thread content came from the monolithic `content_agent`, not `sub_threads`) whose `created_at` predated every `sub_threads` row in the 7-day window. **Fix — two changes in a single file:** (1) Widened the fetch window to 30 days (`getAgentRuns(tab.agentName, 30)`). Sub-agents produce at most a few dozen runs/month so the extra query cost is trivial. (2) Refactored `RunHeader` via **Approach A** (caller-resolves): signature changed from `{ run: AgentRunResponse \| null }` to `{ displayTime: string; itemsQueued?: number \| null }`. Ternary and `(earlier)` literal gone. Mapping loop now computes `sourceDate = group.run ? new Date(group.run.started_at) : new Date(group.items[0].created_at)` (items within each bucket are already sorted DESC by `created_at`, so `items[0]` is newest), formats `${h:mm a} · ${MMM d}` once, and passes the resolved string in. Component is now purely presentational — zero conditional logic. **Scope kept tight per plan:** frontend-only, no backend/API/DB changes, no test additions (plan marked coverage as recommended not required; existing 5 `PerAgentQueuePage.test.tsx` tests all hit `showRunGroups === false` branch which doesn't exercise RunHeader, so no regression risk). **Net diff:** +25 / -13 in `frontend/src/pages/PerAgentQueuePage.tsx` (the only file modified). **Gates (all 7 green):** `grep -c "getAgentRuns(tab.agentName, 30)"` = 1; `grep "(earlier)"` = 0 matches; `grep -c "Pulled from agent run at"` = 1; `pnpm -C frontend lint` clean; `pnpm -C frontend exec tsc --noEmit` clean; `pnpm -C frontend test --run` 8 files / 52 tests passed; `pnpm -C frontend build` 2322 modules in 199ms. Single atomic fix commit `b629916` on branch `quick/260422-ink-run-header-fallback-time` (parent = `f3f15d6`, the ep9 docs commit). | 2026-04-22 | b629916 | Verified (local) — pending Railway deploy | [260422-ink](./quick/260422-ink-show-specific-timestamp-in-peragentqueue/) |
| 260422-lbb | Harden `scheduler/agents/content/gold_history.py` so it only drafts 100% true historical stories — no hallucinated names, dates, or dollar figures. **Origin:** follow-up (d) from the zid debug session, fourth of five. User directive verbatim: "For gold history, we can leave as is. It needs to actually be a 100% true story." **Architecture swap (curated whitelist, decision D-01):** retired the Claude-picks-story-from-memory + SerpAPI-runtime-verification flow (risked hallucinations by construction — Claude's training data has invented specifics; SerpAPI snippet-keyword overlap ≠ semantic corroboration). Replaced with **12 pre-researched JSON fact sheets** committed to `scheduler/agents/content/gold_history_stories/` (new directory). Each fact sheet: `{story_slug, story_title, summary, tags, recommended_arc, verified_facts: [{claim, source_url, published_date}...], sources: [{ref, url, publisher, accessed_date}...]}` with ≥6 facts per story and every claim backed by a distinct `source_url`. **Seed set (12):** bre-x-busang-fraud-1997, california-gold-rush-1848, germany-gold-repatriation-2013, hunt-brothers-silver-corner-1980, klondike-gold-rush-1896, lbma-gold-fix-scandal-2014, munk-barrick-gold-founding-1983, nixon-closes-gold-window-1971, spdr-gld-etf-launch-2004, venezuela-gold-repatriation-2011, witwatersrand-discovery-1886, zimbabwe-zig-gold-backed-currency-2024. Totals: 77 verified_facts + 28 distinct source URLs across Wikipedia, Deutsche Bundesbank, Canadian Encyclopedia, Parks Canada, LBMA, Reuters, World Gold Council, History.com. 6 additional stories from the plan's target list deferred (Giustra/GoldCorp, Newmont/Carlin, USSR/China/Russia reserves, Swiss referendum) — optional future follow-up. **Code changes (`gold_history.py` rewrite, 347 lines):** `_pick_story` + `_verify_facts` FULLY REMOVED (not commented); `_pick_fresh_slug(whitelist, used_topics)` filters unused slugs and random-picks one; `_load_fact_sheet(slug)` reads the JSON; `_draft_gold_history(fact_sheet, client)` takes full fact sheet and locks drafter via explicit **FACT FIDELITY clause** embedded byte-for-byte in system_prompt, docstring, and user_prompt (decision D-03): drafter may only use names/dates/dollar figures/place names/percentages that appear EXPLICITLY in verified_facts; narrative connective tissue allowed, new specifics NOT allowed. `serpapi` import removed. Drafter output gains top-level `sources` field (decision D-04) — clean copy in tweets/slides, audit trail in JSON for approval dashboard. `deep_research` shape simplified to `{story_slug, sources}` — verified_facts live on disk, not in DB row. **Loader helper:** `scheduler/agents/content/gold_history_stories/__init__.py::load_all_stories() -> list[dict]` iterates *.json files and validates required keys + non-empty source_urls. **Tests (+8 net, 101→109):** retired 3 `_pick_story` tests; added 11 new tests — `test_whitelist_loads_all_stories`, `test_pick_fresh_slug_{excludes_used,returns_none_when_all_used,returns_slug_when_none_used,empty_whitelist}`, `test_load_fact_sheet_{returns_dict_for_known_slug,returns_none_for_unknown_slug}`, `test_draft_gold_history_{includes_sources_field,prompt_contains_fact_fidelity_clause,returns_none_on_missing_sources_key,returns_none_on_bad_json}`. **Gates (all 9 green):** scheduler `uv run ruff check .` clean; `uv run pytest -x` 109/109 (baseline 101 + 8 net); JSON count=12 (≥10 required); all `verified_facts[*].source_url` non-empty; `FACT FIDELITY` 3 hits in gold_history.py; `_pick_story`+`_verify_facts` zero hits (fully removed); `sources` field handled 13 times; `import serpapi` removed; exactly 1 impl commit on branch from a1ea13c. **Leakage paths closed (all 4):** (1) Claude-invents-stories → `_pick_fresh_slug` only picks committed slugs; (2) drafter-introduces-specifics → FACT FIDELITY clause explicitly forbids it; (3) SerpAPI-snippet-keyword-overlap-≠-corroboration → `_verify_facts` + `serpapi` removed entirely; (4) unverified-claims-proceed-to-drafter → drafter locked to verified_facts list; no runtime SerpAPI path exists. **Preserved:** `gold_history_used_topics` Config key dedup (still reads via `_get_used_topics` via `asyncio.to_thread`), `content_agent.review()` compliance gate downstream, `ContentBundle`/`DraftItem` persistence shape. **Merge path:** worktree `worktree-agent-a034f265` FF-merged cleanly into main (1 impl commit `cbe4197`, SUMMARY in main-side planning dir only). Zero deviations from plan/CONTEXT.md. **Four of five zid follow-ups now addressed:** zid itself + krz + l40 + lbb. Remaining: (a) sub_video_clip 0-items + (b) gold_media/video_clip frontend label confusion. **No DB migrations, no backend changes, no frontend changes, no changes to the 6 other sub-agents** (run_text_story_cycle, video_clip.py, content_agent.py all untouched). | 2026-04-22 | cbe4197 | Verified (local) — pending Railway deploy | [260422-lbb](./quick/260422-lbb-harden-gold-history-py-so-it-only-produc/) |
| 260422-mfg | Full rename `sub_video_clip` → `sub_gold_media` across scheduler + backend + frontend + DB — fifth of the zid debug follow-ups (follow-up (b)), paired with the still-queued (a) 0-items investigation. **Origin:** `scheduler/agents/content/video_clip.py` hard-coded `CONTENT_TYPE="video_clip"` and `AGENT_NAME="sub_video_clip"` while `frontend/src/config/agentTabs.ts` L32 already set `label: 'Gold Media'` and CLAUDE.md enumerated "gold media" as the canonical sub-agent name. The split wasted cycles in the zid debug session (debugger looked for a `gold_media.py` file that didn't exist). **Workflow:** `/gsd:quick --discuss` — user responded "All clear" to 3 gray-area AskUserQuestion batch, locking 8 decisions (D-01 through D-08) to Recommended paths: D-01 full rename (not code-only, file-only, or no-op); D-02 Alembic migration (auditable, vs one-off `railway run` UPDATE — the pattern from sn9/k9z); D-03 test file `test_video_clip.py` → `test_gold_media.py`; D-04 frontend `VideoClipPreview.tsx` → `GoldMediaPreview.tsx`; D-05 worker.py display "Gold Media" already correct, unchanged; D-06 `JOB_LOCK_IDS` key rekeyed but lock value 1015 preserved; D-07 `VIDEO_ACCOUNTS` → `GOLD_MEDIA_ACCOUNTS`; D-08 no backward-compat aliases introduced, no dual-read logic. **Files renamed via `git mv` (history preserved):** `scheduler/agents/content/video_clip.py` → `gold_media.py` (88% similarity — `git log --follow` traces back through vxg `68b21d1` + eoe `762b08b`), `scheduler/tests/test_video_clip.py` → `test_gold_media.py` (61% — content rewritten to match new symbols), `frontend/src/components/content/VideoClipPreview.tsx` → `GoldMediaPreview.tsx` (90%). **Planner extended rename scope** (beyond the 8 locked decisions) for zero-grep hygiene consistency: internal helpers `_search_video_clips` → `_search_gold_media_clips` and `_draft_video_caption` → `_draft_gold_media_caption`, plus constant `VIDEO_CLIP_SCORE` → `GOLD_MEDIA_SCORE` (numeric value 7.5 unchanged; only the symbol name moves). **Alembic 0008 migration** (`backend/alembic/versions/0008_rename_video_clip_to_gold_media.py`, `revision="0008"`, `down_revision="0007"`, single-headed chain confirmed via `alembic heads` = `0008 (head)`): upgrade `UPDATE content_bundles SET content_type = 'gold_media' WHERE content_type = 'video_clip'` then `UPDATE agent_runs SET agent_name = 'sub_gold_media' WHERE agent_name = 'sub_video_clip'`; downgrade is the symmetric reverse (agent_runs first then content_bundles — readable ordering, no FK dependency). This migration is THE single permitted location for `video_clip` / `sub_video_clip` strings in the codebase (`WHERE` clauses + downgrade `SET` clauses by design). **Gates (all 9 green):** scheduler `uv run ruff check .` clean; scheduler `uv run pytest -x` 109/109 (baseline from lbb, rename churn not a test-count change); backend `uv run ruff check .` clean (fixed E501 line-too-long in migration docstring); backend `uv run pytest -x` 69 passed + 5 skipped; alembic heads = 0008 (head); frontend `pnpm lint` clean; frontend `tsc --noEmit` clean; frontend `pnpm test --run` vitest 52/52; frontend `pnpm build` green (541 kB bundle, pre-existing chunk-size warning). **Zero-grep hygiene:** 0 matches for `video_clip`, `sub_video_clip`, `VideoClipPreview`, `VIDEO_ACCOUNTS`, `VIDEO_CLIP_SCORE`, `_search_video_clips`, `_draft_video_caption` across `scheduler/` + `backend/app/` + `frontend/src/` (excluding the 0008 migration). **19 files touched:** 3 git-renamed (above), 16 modified — scheduler (`agents/content/__init__.py`, `agents/content_agent.py` L568+L652 format branches, `models/content_bundle.py` docstring, `worker.py` import + `JOB_LOCK_IDS` key + `CONTENT_CRON_AGENTS` tuple, `tests/test_worker.py`, `tests/test_gold_media.py` content rewrite), backend (`app/models/content_bundle.py` docstring, `app/routers/queue.py` docstring, NEW `alembic/versions/0008_rename_video_clip_to_gold_media.py`), frontend (`config/agentTabs.ts` slug/contentType/agentName, `components/approval/ContentDetailModal.tsx` + its test, `components/settings/AgentRunsTab.tsx` option label, `pages/PerAgentQueuePage.test.tsx`). **Five of five zid follow-ups now addressed at the code level:** zid + krz + l40 + lbb + mfg. **Follow-up (a) still queued:** "Why does sub_gold_media produce 0 items" — separate `/gsd:debug` investigation path (X API quota / `GOLD_MEDIA_ACCOUNTS` availability / analyst-quality-gate rejection rate). The rename was purely cosmetic/structural; 0-items behavior is a distinct investigation. **No changes to the 6 other sub-agents** (breaking_news, threads, long_form, quotes, infographics, gold_history). | 2026-04-22 | e835b61 / 69efac1 | Verified (local) — pending Railway deploy + `alembic upgrade head` | [260422-mfg](./quick/260422-mfg-rename-sub-video-clip-to-sub-gold-media-/) |
| 260422-no4 | Surface zero-item agent runs in per-agent queue UI with "No content found" so cron ticks are visibly distinguishable from broken/not-fired agents. **Origin:** user ask immediately after shipping the gmb fix — *"Even if some of the agents runs dont find anything, for examples the quotes or gold media one, I still want it to say 'no content found' so I know it is working tomorrow"*. Tomorrow = the 12:00 PT cron verification after gmb + mfg + Railway deploy; the user wants every cron tick visible in the UI. **Workflow:** `/gsd:quick --discuss` — orchestrator pre-surfaced 6 gray-area questions with Recommended answers; user's "yeah push it" interpreted as "All clear / proceed with Recommendations", locking 6 decisions (D-01 wording "No content found"; D-02 inline muted telemetry subtitle under RunHeader with graceful degradation; D-03 chronological interleaving of zero and non-zero runs; D-04 EmptyState preserved only for items=0+runs=0 genuine-never-ran case; D-05 "N pending" page subtitle unchanged; D-06 uniform across all 7 sub-agents). **Reconnaissance before workflow:** confirmed `PerAgentQueuePage.tsx` L137-138 rendered `<EmptyState />` unconditionally when items=0 and `groupByRun` L19-54 silently dropped runs with 0 matched items — so zero-item runs were literally invisible in the UI. Data contract was already sufficient (runs fetched via `getAgentRuns()` with `items_found` / `items_queued` / `notes` fields populated). **Changes (surgical, frontend-only):** (a) `groupByRun` now emits a group for every recent run in `sorted`, including runs with 0 matched items (change: `groups.get(key) ?? { run, items: [] }`); (b) render-branch guard now checks `items.length === 0 && runs.length === 0` before falling through to `<EmptyState />` — so when items=0 but runs>0, the timeline renders instead; (c) new `parseRunNotes` module-scope helper parses gmb's structured `{x_candidates, llm_accepted, compliance_blocked, queued}` JSON into a muted inline subtitle under the RunHeader, with graceful degradation: null notes → skip silently; malformed JSON → treat as null; partial fields → show only present ones joined with `·`; (d) zero-item group render shows "No content found" row styled consistently with RunHeader's `text-xs text-muted-foreground` Tailwind classes. RunHeader props API untouched — ink's specific-timestamp fallback preserved; mfg's rename zero-grep hygiene preserved (uses `sub_gold_media` / `gold-media` / `GoldMediaPreview`, zero `video_clip` references). **Tests (+4, 52 → 56):** zero-item run renders "No content found"; timeline interleaves zero and non-zero runs chronologically; notes telemetry subtitle renders when present AND is omitted when null/malformed/partial; items=0 AND runs>0 falls through to timeline (not EmptyState); items=0 AND runs=0 still shows EmptyState. **Gates (all 5 green):** `pnpm -C frontend lint` clean; `pnpm -C frontend exec tsc --noEmit` clean; `pnpm -C frontend test --run` 56/56; `pnpm -C frontend build` green; grep gate: 0 new `video_clip` / `VideoClipPreview` references. **Scope discipline:** frontend-only, no backend/API/DB changes, no new deps, no `<EmptyState />` modifications. **Deploy path:** Vercel auto-deploys from main on push. **Known limitation:** other 6 sub-agents don't have gmb-style structured `notes` telemetry yet — they render "No content found" without the counter subtitle. If we want per-stage telemetry parity, that's a separate follow-up (extend `run_draft_cycle` in each agent module to write structured notes JSON). **Third deliverable of tonight's session:** after gmb debug fix (85fccfc + 3efe3b9). All 5 zid follow-ups now shipped at code level (zid + krz + l40 + lbb + mfg) + gmb debug fix + no4 UX gap close — the 12:00 PT verification tomorrow is maximally instrumented. | 2026-04-22 | 8080b4c | Verified (local) — pending Vercel auto-deploy | [260422-no4](./quick/260422-no4-surface-zero-item-agent-runs-in-per-agen/) |
| 260422-l40 | Boot-time reconciler for orphan `agent_runs` rows left behind by hard process kills. **Problem:** all three sub-agent pipelines (`run_text_story_cycle`, `gold_history.run_draft_cycle`, `video_clip.run_draft_cycle`) already have correct `try/except/finally` for in-process exceptions. But `finally` never runs when the Python process is hard-killed (Railway SIGKILL during deploy, OOM, container crash) → row stays `status='running'` + `ended_at=NULL` forever, accumulating over time. This is follow-up (c) from zid debug. Debugger's "try/finally hardening" framing was off-target — the existing blocks are correct; the orphans come from process kills they can't reach. **Fix:** new `reconcile_stale_runs(threshold_minutes: int = 30) -> int` in `scheduler/worker.py`. Awaited exactly once in `main()` between `upsert_agent_config()` and `build_scheduler(engine)` — runs at scheduler startup BEFORE any sub-agent jobs are registered. Bulk `update(AgentRun).where(status=='running').where(started_at < cutoff)` sets `status='failed'`, `errors=["scheduler restart — run abandoned (process killed before finally block)"]` (JSONB array, matching column type — planner catch), `ended_at=now`. Returns rowcount. Logs at INFO only when count > 0. **Threshold = 30 min** mirrors the new `misfire_grace_time=1800s` from krz — any run still "running" past 30 min is definitely dead. **Imports added to worker.py:** `update` from sqlalchemy, `AsyncSessionLocal` from database, `AgentRun` from models.agent_run. No changes to backend/frontend/schema/migrations/config keys, no changes to the 3 sub-agent pipelines (byte-identical to main@bd8e26e, verified via grep gate). **Tests (+3 net, 98→101):** `test_reconcile_stale_runs_updates_orphans` (rowcount=2 path, verifies UPDATE targets `agent_runs`, `status='failed'`, errors is a list with "scheduler restart" marker, commit called), `test_reconcile_stale_runs_skips_fresh_rows` (rowcount=0 path, execute/commit still called but no log), `test_reconcile_stale_runs_custom_threshold` (threshold_minutes=60 path, WHERE-clause cutoff within ±2s of now−60min). **Gates (all 5 green):** `grep -c "reconcile_stale_runs" worker.py` = 3 (≥2 required); `grep -c "scheduler restart" worker.py` = 1; `uv run ruff check .` clean; `uv run pytest -x` 101/101; `git diff --stat main..HEAD` exactly 2 files (worker.py + test_worker.py). **Merge path:** clean FF merge of worktree branch `worktree-agent-a66eb6b9` (impl commit `2ad4aab` + SUMMARY commit `80747a8`) into main — no divergence this time (krz's cherry-pick gymnastics was a one-off caused by the SUMMARY committing to main while the worktree also had the fix). **Post-deploy expectation:** first scheduler boot after this lands will log `reconcile_stale_runs: marked N orphan 'running' agent_runs as 'failed' (older than 30 min)` if any orphans exist in the DB today; subsequent boots show no log line until the next Railway redeploy leaves an orphan. | 2026-04-22 | 2ad4aab | Verified (local) — pending Railway deploy | [260422-l40](./quick/260422-l40-add-boot-time-reconciler-for-orphan-agen/) |
| 260423-d30 | Make `sub_quotes` run independently of other sub-agents — one-kwarg addition of `dedup_scope="same_type"` to `run_text_story_cycle` call, closing the last topology-spec delta. **Origin:** user read back the confirmed 7-sub-agent spec ("Breaking News every 2h, Threads + Long-form every 4h in unity with BN, Quotes + Infographics + Gold Media daily 12pm PT independent, Gold History every-other-day 12pm PT independent") and asked what needed to change to match — scorecard showed 6/7 matched, only Quotes still inherited default `cross_agent` dedup. **Change:** `scheduler/agents/content/quotes.py::run_draft_cycle` gains `dedup_scope="same_type"` on its `run_text_story_cycle` call, mirroring of3's infographics wiring byte-for-byte. Docstring expanded with d30/vxg/mos decision-ID bullets matching the infographics.py documentation pattern. No other lines in quotes.py touched — `_draft`, `REPUTABLE_SOURCES` (27 patterns), `CONTENT_TYPE`, `AGENT_NAME` unchanged. **Test (+1 net, 117→118):** new sibling `test_run_draft_cycle_passes_dedup_scope` added after existing `test_run_draft_cycle_passes_filters` using the `side_effect`-capture pattern from `test_infographics.py::test_run_draft_cycle_passes_new_kwargs` (codebase migration toward explicit-failure-message style) — asserts agent_name="sub_quotes", content_type="quote", draft_fn callable, max_count=1 (vxg guard), source_whitelist is REPUTABLE_SOURCES (mos guard), dedup_scope="same_type" (d30 gate — primary assertion). Existing filter test kept as-is for transition documentation per plan instruction. **Preservation confirmations (7/7 green):** `source_whitelist=REPUTABLE_SOURCES` in run_draft_cycle still present; `max_count=1` still present; `REPUTABLE_SOURCES` frozenset unchanged; `_draft` reject-path unchanged; of3 infographics wiring untouched; `run_text_story_cycle` signature in `__init__.py` zero diff; breaking_news/threads/long_form zero `dedup_scope` references. **Gates (all 6 green):** scheduler `uv run ruff check .` clean (0 errors); `uv run pytest -x` 118/118 (baseline 117 + 1 new); `grep -rn 'dedup_scope="same_type",$' scheduler/agents/content/*.py` exactly 2 hits (infographics.py:155 + quotes.py:212); `grep -n 'dedup_scope' breaking_news.py threads.py long_form.py` zero matches; `git diff scheduler/agents/content/__init__.py` empty; workflow ran cleanly without `--discuss`/`--full`/`--research` flags (design was locked from of3 pattern). **No DB migrations, no schema changes, no frontend changes, no new Config keys, no changes to the 6 other sub-agents or the shared pipeline** — surgical one-kwarg backend-only change. **7-of-7 topology now matches user's spec:** BN (2h) + Threads (4h) + Long-form (4h) respect cross-agent dedup → Quotes + Infographics + Gold Media (daily 12pm PT) all independent → Gold History (every-other-day 12pm PT) independent. **Post-deploy expectation:** next 12:00 PT cron tick where `sub_breaking_news` drafted a strong reputable-source story at 08:00 PT or 10:00 PT containing a verbatim named-analyst quote — `sub_quotes` at noon can now lift that quote as a standalone pull-quote post instead of being silently blocked by the cross-agent dedup gate. Single atomic impl commit on main. | 2026-04-23 | 4281f40 | Verified (local) — pending Railway deploy | [260423-d30](./quick/260423-d30-make-sub-quotes-independent-add-dedup-sc/) |
| 260423-hq7 | Fix `max_count` semantics in `scheduler/agents/content/__init__.py::run_text_story_cycle` from "trim candidates to top N before dedup loop" → "break out of for-loop after N successful compliance-passing persists". **Origin:** user dashboard screenshots showed sub_threads + sub_long_form producing 0 items for 20+ consecutive runs going back to 2026-04-22, and sub_quotes at 12:00 PT producing 0 items. Not a slow-news day — sub_breaking_news drafted 8 items in today's screenshots alone (6 @ 9:25 PDT + 2 @ 11:34 PDT). **Root cause:** the pre-loop `candidates = candidates[:max_count]` slice at L~175-193 happened BEFORE the `_is_already_covered_today` dedup check. BN every 2h + fetch_stories 30-min cache meant threads/long_form (staggered 17 + 34 min after BN) always saw the same top-2 by recency, both already drafted by BN → `continue` × 2 → items_queued=0. Same pattern compounded for quotes with source_whitelist + _draft quality-reject stacking. **Fix:** removed the slice (sort still runs when max_count is set — condition changed from `len(candidates) > max_count` to `len(candidates) > 1`); added `if max_count is not None and items_queued >= max_count: break` inside the `if compliance_ok:` block after `items_queued += 1`. Compliance-failed drafts persist as stubs but do NOT count toward the cap (matches pre-existing items_queued semantics). **Workflow:** `/gsd:quick` plain (no --full/--discuss/--research — bug was fully diagnosed upfront). **Tests (+3 net, 118→121):** `test_run_text_story_cycle_max_count_iterates_past_dedup_hits` (top 3 stories dedup-blocked; loop must continue to stories 4+5 → items_queued=2, FAILED before fix — the regression pin), `test_max_count_exhausts_if_insufficient_successes` (10 stories, 9 return None from draft_fn → all 10 reached, items_queued=1, no infinite loop), `test_max_count_none_still_iterates_all` (breaking_news regression guard — all 10 drafted). 4 existing sort/cap tests rewritten via new `_run_with_stories_succeeding()` helper that mocks review()→compliance_passed=True. **Deviation (Rule 1 auto-fix):** `scheduler/tests/test_infographics.py::test_run_draft_cycle_writes_structured_notes` was hardcoded to old trim semantics (candidates:2, drafted:2, queued:1, only 2 review mocks) — raised `RuntimeError: coroutine raised StopIteration` post-fix. Extended to 3 review mocks (True, False, True) with expected notes `candidates:3, drafted:3, compliance_blocked:1, queued:2` (Top A pass → queued=1; Top B fail → blocked; Trimmed pass → queued=2 → break). Included in same atomic commit per plan's single-commit instruction. **Preservation (zero diff on all 5 callers):** breaking_news.py (max_count=None) + threads.py (max_count=2) + long_form.py (max_count=2) + quotes.py (max_count=1, source_whitelist, dedup_scope=same_type) + infographics.py (max_count=2, sort_by=score, dedup_scope=same_type) all unchanged — API widens, doesn't narrow. `_is_already_covered_today` 85% fuzzy match unchanged. of3 best-by-score guarantee now actually holds under dedup collision. **Validation gates (all green):** `grep "candidates\[:max_count\]" __init__.py` → 0 matches; `grep "items_queued >= max_count" __init__.py` → 1 match at L310; `git diff` on 5 caller files → empty; `uv run pytest scheduler/ -x` → 121/121; `uv run ruff check scheduler/` → clean. **Deploy:** Railway auto-deploys on push to main. Empirical test at 14:17 PT (next threads cron) + 14:34 PT (next long_form cron) — if 0 items persist, fix didn't land. Noon PT cron fires for quotes/infographics already passed today; tomorrow's noon is the empirical test for those two. Single atomic impl commit on main; PLAN.md backfilled in follow-up commit (executor omitted it from doc commit). | 2026-04-23 | 4b0ee67 | Verified | [260423-hq7](./quick/260423-hq7-fix-max-count-semantics-break-after-n-su/) |
| 260423-k8n | Remove `sub_long_form` sub-agent entirely (topology 7→6 sub-agents; historical `content_type='long_form'` rows preserved; no DB migration; mirrors lvy/sn9). Deletes `long_form.py` + `test_long_form.py` + `LongFormPreview.tsx`. Removes from scheduler worker (JOB_LOCK_ID 1012, cron offset 34), content_agent.py classifier + builders, agentTabs.ts nav entry, ContentDetailModal switch, AgentRunsTab dropdown. CLAUDE.md updated to 6-sub-agent topology + new 2026-04-23 historical note. ThreadPreview.tsx `long_form_post` compat field retained (thread-format feature). Tests: scheduler 131→127 (−4), backend unchanged, frontend 56→54 (−2). | 2026-04-23 | 337fdf5 | Verified | [260423-k8n](./quick/260423-k8n-remove-sub-long-form-sub-agent-entirely-/) |
| 260423-lvp | Add analytical-historical fallback tier to `sub_infographics` so it produces exactly 2 infographics per day. **Origin:** user verbatim *"I want it to create 2 infographics every single day. It should prioritize stuff that is relevant news. If no relevant news to make infographics from, it should make historical infographics"* + clarifiers *"Sub gold history should be separate from sub infographics. They should both do their own agent pulls from online. Infographic stuff and historic stories will be different information. Historical infographics should be more of an analytical perspective. For example, 'what gold does during wars' or something like that"* and *"You dont need to make the infographics. Keep the prompts to what we already have and what the infographic agent already does."* **Workflow:** `/gsd:quick --discuss` — pre-surfaced 8 gray-area questions; round 1 recommendations were redirected by the two clarifiers (architecture: sub_gold_history sibling path → two fully-independent agents with analytical-pattern content living under sub_infographics fallback; pool: new curated JSON files → SerpAPI online fetching with analytical query strings; prompts: zero changes to existing `_draft` + `BRAND_PREAMBLE` + `image_prompt`). **Two-phase pipeline (`scheduler/agents/content/infographics.py::run_draft_cycle`):** (Phase 1) news-based `run_text_story_cycle` call with `max_count=2, sort_by="score", dedup_scope="same_type"` (of3+hq7 behavior, unchanged). If `news_items_queued < 2`, (Phase 2) `_run_analytical_fallback(shortfall = 2 - news_items_queued)` fires. **Fallback internals (`_run_analytical_fallback`, infographics.py):** day-seeded deterministic shuffle (`random.Random(date.today().toordinal())`) picks top `shortfall + 2` queries from module-level `ANALYTICAL_HISTORICAL_QUERIES` (10 entries: "gold price performance during major wars historical", "gold bull markets historical analysis 1970-2025", "central bank gold purchases trends 2020-2025", "gold inflation correlation 50 years historical", "gold during recessions analysis 1973 2008 2020", "gold vs dollar weakness historical pattern", "safe haven asset performance gold historical", "gold ETF flows historical trends 2010-2025", "gold mining output decline historical analysis", "gold crisis performance historical comparison"), fetches via new `content_agent.fetch_analytical_historical_stories(queries)` (side-by-side with `fetch_stories()`, NOT cached — rare invocation, query-specific), runs gold gate → existing `_draft` → compliance `review()` → persists `ContentBundle` + `DraftItem` with `content_type="infographic"` (same as news phase). **Separate `AgentRun` row per phase** with `notes={"phase":"analytical_fallback","shortfall":N,"queued":M}` for clean telemetry. Respects `_is_already_covered_today(content_type="infographic")` dedup (of3 same_type scope preserved). **Pipeline signature change (`scheduler/agents/content/__init__.py`):** `run_text_story_cycle` return type `None → int` (returns `items_queued`). All 4 callers (breaking_news, threads, quotes, infographics) continue to work — first 3 discard the return, infographics captures it as the phase-1 trigger signal. **Tests (+9 net, 127→136):** `test_content_agent.py` +2 (fetch helper shape + no-client guard); `test_infographics.py` +7 (query rotation determinism across days, fallback creates shortfall items, empty-fetch resilience, gate-rejects-all resilience, 0-news→2-analytical wiring, 1-news→1-analytical wiring, 2-news→0-analytical wiring with zero-SerpAPI-call assertion). 3 existing fixture tests patched to mock `_run_analytical_fallback(return_value=0)` per plan T5 anticipation — explicitly called out in PLAN.md. **Preservation (zero diff across 5 surfaces):** `scheduler/agents/content/gold_history.py` + `gold_history_stories/` (fully independent; every-other-day cadence unchanged per Q5/Q6) + `scheduler/worker.py` (no cron changes) + `frontend/` (fallback rows surface as normal `content_type='infographic'`) + `alembic/` (no migration) all empty diff verified via `git diff HEAD~6 --`. `infographics.py::_draft` body + `BRAND_PREAMBLE` + `image_prompt` unchanged per Q7 lock. `content_agent.py::SERPAPI_KEYWORDS` + `fetch_stories()` unchanged (new `ANALYTICAL_HISTORICAL_QUERIES` list is SEPARATE module-level constant in infographics.py; new fetch helper is SIDE-BY-SIDE in content_agent.py). **Validation gates (all green):** `uv run pytest scheduler/ -x` → 136/136 in 0.97s; `uv run ruff check scheduler/` → clean; `grep ANALYTICAL_HISTORICAL_QUERIES scheduler/agents/content/infographics.py` → 5 matches (definition + 4 usages); `grep -rn "gold_history_stories\|sub_gold_history" scheduler/agents/content/infographics.py` → 0 matches (no coupling). 5 atomic impl commits on worktree branch (T1 `d8b88ed` fetch helper, T2 `738a811` return-type refactor, T3 `0f400c2` query constant + rotation, T4 `bd344f6` fallback helper, T5 `27c4138` two-phase wiring) + 1 docs commit (`d643425` SUMMARY); worktree-isolated executor; fast-forward merge to main. **Post-deploy expectation (tomorrow's 12:00 PT cron):** on sparse news days, sub_infographics `agent_runs` shows TWO rows — phase-1 news row + phase-2 `analytical_fallback` row — totaling 2 `items_queued`. On rich news days, only phase-1 fires and zero SerpAPI credit is consumed by analytical queries. Builds on of3 (composite-score sort + same-type dedup), hq7 (break-after-N-successes), j7x (bearish-toward-gold filter applies to analytical content as well — analytical patterns framed bullish/neutral will pass). | 2026-04-24 | d8b88ed / 738a811 / 0f400c2 / bd344f6 / 27c4138 | Verified (local) — pending Railway deploy | [260423-lvp](./quick/260423-lvp-add-analytical-historical-fallback-to-su/) |
| 260423-j7x | Add bullish-only content policy filter across all 7 sub-agents — reject stories negative toward gold or its price. Three categories blocked: (1) price-bearish predictions ("Morgan Stanley cuts gold price forecast"), (2) anti-gold narrative ("bitcoin replaces gold"), (3) factual-negative price movement ("gold fell 1.2% today on stronger dollar"). **Origin:** user sent screenshot of sub_infographics queue showing a drafted "Morgan Stanley cuts gold price forecast by almost 10%" (mining.com, score 8.5/10) — verbatim *"Across everything, I don't want any content pulled that is negative towards gold and its price. For example, the infographics screenshot" → "So all 3 should be implemented"*. Note: of3+hq7 pipeline worked correctly — infographics picked top-score story, produced 1 draft as designed. Bug was content policy, not pipeline. **Workflow:** `/gsd:quick --full` — plan-checker caught a blocker on iteration 1: existing gate prompt at content_agent.py L417-420 named Morgan Stanley as a "safe source whose presence does NOT trigger rejection", directly contradicting the new REJECT example which WAS a Morgan Stanley forecast cut. Revision iteration 1/2 addressed the blocker (verbatim preamble scope-clarify) + 2 warnings (flat/mixed neutral framing, test #9 caplog comment). Re-check passed 15/15 must-haves. TDD executed in 3 atomic commits: RED (1a6bea1) → GREEN gate (0e758be) → GREEN gold_media (b9b8dad). **Gate 1 (Haiku relevance gate, content_agent.py):** extended `is_gold_relevant_or_systemic_shock` system prompt with bearish_toward_gold REJECT criterion covering all 3 categories with examples; added `sentiment: "bullish" | "neutral" | "bearish"` field to JSON contract; widened return to `{keep, reject_reason, company, sentiment}` (additive — callers only read `keep`); verbatim KEEP-source preamble amendment scopes "their presence does NOT trigger rejection" to `is_gold_relevant=false` specifically, clarifying that forecast direction still determines sentiment (bearish MS forecast → sentiment=bearish → rejected); verbatim flat/mixed framing sentence classifies "gold holds steady/flat/mixed" as sentiment=neutral and KEPT; new `content_bearish_filter_enabled` Config flag (default "true", independent from `content_gold_gate_enabled`) gates Gate 1 bearish-reject logic only; fail-open preserved on JSON parse/API error. **Gate 2 (Sonnet drafter, gold_media.py):** added criterion #4 "Bullish or neutral stance" to `_draft_gold_media_caption` quality-bar (existing 3-criteria reject path reused, zero new logic). **Gate 3 (gold_history.py):** confirmed safe by planner read — curated `GOLD_HISTORY_STORIES` whitelist + drama-first historian voice precludes bearish framing. Zero diff. **Tests (+10 net, 121→131):** 8 gate tests (rejects price-bearish forecast, anti-gold narrative, factual-negative price decline; keeps bullish central-bank buying, bullish Goldman $4K forecast, neutral record-high, flat "holds steady"; fail-open on parse error; flag-disabled bypasses bearish check) + 2 gold_media tests (rejects bearish analyst clip, accepts bullish analyst clip). Verbatim caplog comment in test #9 documents assertion verifies log path, NOT production rationale wording. **Preservation (zero diff on all 5 text-story callers + gold_history):** breaking_news + threads + long_form + quotes + infographics + gold_history all untouched — API widens additively. nnh specific-miner rejection precedence preserved (nnh check runs BEFORE bearish check). `content_gold_gate_enabled` flag unchanged. `content_agent.review()` compliance layer unchanged (topical policy vs legal/brand compliance — separate layers). **Validation gates (all 15 must-haves green):** `grep "bearish_toward_gold" content_agent.py` → 3 matches (prompt + return + INFO log); `grep "Bullish or neutral" gold_media.py` → 1 match; `grep "content_bearish_filter_enabled"` → present in content_agent.py + content/__init__.py + seed_content_data.py; verbatim preamble amendment at content_agent.py L428; verbatim flat/mixed sentence at L456; verbatim caplog comment at test_gold_media.py L153; `uv run pytest scheduler/ -q` → 131 passed; `uv run ruff check scheduler/` → clean. **Deploy:** Railway auto-deploys on push. Empirical test: next sub_infographics 12:00 PT cron (tomorrow) — if another Morgan-Stanley-type story surfaces, must NOT appear in queue. Rollback: toggle `content_bearish_filter_enabled=false` in DB Config. | 2026-04-23 | 0e758be / b9b8dad | Verified | [260423-j7x](./quick/260423-j7x-add-bullish-only-filter-reject-bearish-t/) |
| 260422-of3 | Redesign `sub_infographics` to produce 2 best infographics per day by quality/score (not recency) and allow it to reuse stories already drafted by `sub_breaking_news` earlier the same day. User's verbatim ask: *"I want it to claim the same stories the breaking news agent does, so it can have the best infographics of recent news. If breaking news has a really good piece of news in the morning that the infographic agent can create something about, it should still use it. Infographic agent should be creating the 2 best infographics it possibly can everyday"*. **Workflow:** `/gsd:quick --discuss` — pre-surfaced 6 gray-area questions with Recommended answers; user's "All clear — push it" locked D-01 through D-06 (composite `(score, published_at)` desc sort; narrow dedup to same content_type via new `dedup_scope` kwarg; two orthogonal kwargs `sort_by` + `dedup_scope` with backward-compat defaults; `max_count=2` cap; gmb-style structured notes telemetry gated on `content_type=='infographic'`; no historical fallback — honest zero-infographic day surfaced via no4 UI). **Shared pipeline changes (`scheduler/agents/content/__init__.py`):** added `sort_by: Literal["published_at","score"] = "published_at"` and `dedup_scope: Literal["cross_agent","same_type"] = "cross_agent"` kwargs on `run_text_story_cycle` with backward-compat defaults → byte-for-byte identical behavior for the 4 other text-story sub-agents that don't pass them; `_is_already_covered_today` gained optional keyword-only `content_type: str \| None = None` scoping the SELECT to `ContentBundle.content_type == :content_type` when set; composite `(score, published_at)` desc tuple sort with `reverse=True` when `sort_by=="score"` (recency tiebreaker); new `drafted_count` + `compliance_blocked_count` counters inside draft loop; structured notes JSON `{candidates, top_by_score, drafted, compliance_blocked, queued}` written to `AgentRun.notes` ONLY when `content_type=="infographic"` (other callers retain minimal `{"candidates": N}` payload per D-05); empty-candidates early-return also writes zero-filled notes for infographics so no4's subtitle has something to render on 0-content days per D-06. **Infographics wiring (`scheduler/agents/content/infographics.py`):** `run_draft_cycle()` now passes `max_count=2, sort_by="score", dedup_scope="same_type"` to `run_text_story_cycle` — docstring references D-01/D-02/D-04 for traceability. **Tests (+8 net, 109→117):** `test_content_init.py` +5 (test_sort_by_score_picks_top_by_score, test_sort_by_score_breaks_ties_by_recency, test_sort_by_default_is_published_at, test_dedup_scope_same_type_passes_content_type, test_is_already_covered_today_scopes_by_content_type — WHERE-clause inspected via regex `\bWHERE\b(.*)` since SQLAlchemy emits `\nWHERE`); `test_infographics.py` +3 (test_run_draft_cycle_passes_new_kwargs, test_run_draft_cycle_writes_structured_notes with 3 stories score=[9,8,3] → notes={candidates:2, top_by_score:2, drafted:2, compliance_blocked:1, queued:1}, test_run_draft_cycle_writes_notes_on_empty_candidates with zero-filled payload). **Preservation checks (all 4 green):** zid's predicted_format-gate removal preserved (only comment lines remain); lbb's source_whitelist isolation untouched; gmb/no4 notes schema family aligned (no4's `parseRunNotes` tolerates any integer-valued fields + silent no-op on absent); grep confirms the 4 other text-story sub-agents (breaking_news, threads, long_form, quotes) pass neither new kwarg. **Validation gates (all 6 green):** `uv run ruff check .` clean; `uv run pytest -x` 117/117 in 0.83s; `grep 'sort_by="score"'` only in infographics.py; `grep 'dedup_scope="same_type"'` only in infographics.py; `grep 'predicted_format'` comment-only in __init__.py; other 4 sub-agents unchanged via `grep -A 4 'run_text_story_cycle('`. **No DB migrations, no schema changes, no frontend changes, no new Config keys** — backend-only change, ships to Railway auto-deploy. **Post-deploy expectation (tomorrow's 12:00 PT cron):** `agent_runs` row for `sub_infographics` has `items_queued <= 2` + `notes` containing the 5-field JSON; no4's per-agent queue subtitle renders `"2 candidates · 2 drafted · 1 blocked · 1 queued"` style inline; if `sub_breaking_news` drafted a strong gold story at 08:00 PT, that same story can now be drafted as an infographic at 12:00 PT (reuse path proven); zero-gold-news day surfaces as "No content found" with zero-filled telemetry subtitle. Single atomic impl commit on main. **Five of five zid follow-ups now closed at the behavior level:** (a) sub_gold_media 0-items via gmb, (b) gold_media/video_clip label rename via mfg, (c) infographics 0-items likely-cause via of3, (d) zero-item run visibility via no4, (e) gold_history hallucination hardening via lbb. Original zid issue resolved. | 2026-04-23 | 23ce6fb | Verified (local) — pending Railway deploy | [260422-of3](./quick/260422-of3-redesign-sub-infographics-to-produce-2-b/) |
| 260422-krz | Bump APScheduler `misfire_grace_time` 300s → 1800s in `scheduler/worker.py::build_scheduler` `job_defaults` (single-line change at L282). **Origin incident:** 2026-04-22 noon-PT cron fires for quotes/infographics/video_clip were silently coalesced and dropped because a Railway auto-redeploy (triggered by the zid push at ~13:30 PT and/or ep9 push at ~10:48 PT) interrupted the scheduler during the 5-minute misfire grace window. With `coalesce: True` + `misfire_grace_time: 300`, any cron job unable to execute within 300s of its scheduled time is dropped rather than deferred. Railway deploy windows typically run 3-7 minutes (build + container startup), which is larger than the 300s grace → routine redeploys near cron boundaries silently swallow fires. **Fix:** single integer value 300 → 1800 (30 min). This means cron jobs can still fire up to 30 minutes after their scheduled time if the scheduler was unavailable — still timely enough to be useful for daily-at-noon content drafts, comfortably larger than any observed Railway redeploy window. No behavior change for jobs that fire on time. **Scope:** 1 file, 1 line, 0 tests (job_defaults is static config; not tested directly). `scheduler/agents/content/*` drafters, triggers, and test surfaces all untouched. **Gates:** `grep -c "misfire_grace_time.*1800"` = 1; `grep -c "misfire_grace_time.*300"` = 0; `uv run ruff check .` clean; `uv run pytest -x` 98/98 (zid baseline). **Merge path:** worktree branch `worktree-agent-XXX` had fix commit `9d03381`; main had executor's SUMMARY commit `26b650e`; branches diverged so FF-merge failed → cherry-picked `9d03381` onto main as `8dab545` (clean cherry-pick, same tree). Final main contents: 3 commits since 16096eb — `26b650e` (SUMMARY), `8dab545` (fix), and this STATE update. **Post-deploy expectation:** next Railway redeploy near a cron boundary no longer drops fires; agent_runs rows appear for all scheduled sub-agents within 30 min of their scheduled time, even if deploy overlapped. | 2026-04-22 | 8dab545 | Verified (local) — pending Railway deploy | [260422-krz](./quick/260422-krz-bump-apscheduler-misfire-grace-time-from/) |

### Blockers/Concerns

- [Phase 1]: Twilio WhatsApp templates must be submitted to Meta during Phase 1 — approval takes 1-7 business days. Confirm whether a WhatsApp Business account already exists for Seva Mining before starting Phase 1 planning.
- [Phase 1]: Verify Neon free tier connection limit before setting pool_size — may need adjustment from default 5/10.
- [Phase 7]: SerpAPI plan selection needed before Content Agent — 100 searches/mo on basic plan may be insufficient for daily multi-topic runs.
- [quick-mfy]: Railway to pick up infographic+quote prompt pivot on next scheduler rebuild. Post-deploy watch-list: (a) confirm scheduler build is pure-Python (no Node layers) — image size should be noticeably smaller than the t78 build, (b) next content_agent cron (every 3h) should produce infographic/quote bundles with `suggested_headline` + `data_facts` + `image_prompt` in draft_content (NOT the old `charts` array or Imagen prompt), (c) dashboard cards render three copy blocks with no broken image refs; legacy pre-mfy bundles show "Legacy format — regenerate" placeholder. Pre-mfy queue rows are stranded by design — operator dismisses via dashboard.
- [quick-nnh]: Railway spot-check on next content_agent cron (every 3h) — confirm (a) Haiku gate prompt returns structured JSON with `keep`/`reject_reason`/`company` keys, (b) company-specific stories (B2Gold, McLaren, Barrick, Newmont, Seva Mining patterns) emit `ContentAgent: rejected story <title> — primary subject is specific miner (<company>).` log line and do NOT reach the drafter, (c) macro/sector stories (Goldman/WGC/central-bank/CPI/ETF) still reach drafter and produce bundles.
- [quick-oa1]: **Operator action required before Railway deploy picks this up:** provision both API keys and add them to Railway scheduler service env vars. (a) `FRED_API_KEY` — register at https://fred.stlouisfed.org/docs/api/api_key.html (free, instant, email verification). (b) `METALPRICEAPI_API_KEY` — sign up at https://metalpriceapi.com/pricing and subscribe to Basic plan ($8.99/mo, 10k req/mo). Without these keys, drafter runs with `[UNAVAILABLE]` fallback (fail-open) — drafts still produce but fall back to qualitative language. Also verify on first post-deploy content_agent cron: (i) log line `ContentAgent: market snapshot fetched (status=ok)` with populated values, (ii) `market_snapshots` DB row inserted with non-null `data` JSONB, (iii) draft output references real current figures matching goldprice.org + FRED values at time of draft, (iv) any hallucinated historical claim like "gold hit $X in 2023" no longer appears.

## Session Continuity

Last session: 2026-04-23T20:45:02.375Z
Last activity: 2026-04-23 — Completed quick-260423-j7x (add bullish-only content policy filter across all 7 sub-agents, rejecting stories negative toward gold or its price). **Origin:** user saw a drafted infographic titled "Morgan Stanley cuts gold price forecast by almost 10%" (mining.com, score 8.5/10) in the approval queue; verbatim request *"Across everything, I don't want any content pulled that is negative towards gold and its price. For example, the infographics screenshot" → "So all 3 should be implemented"* — all three bearish categories blocked (price-bearish predictions, anti-gold narrative, factual-negative price movement). **Workflow:** `/gsd:quick --full` — plan-checker caught a blocker on iteration 1 (existing gate prompt at content_agent.py L417-420 named Morgan Stanley as a safe source "whose presence does NOT trigger rejection", directly contradicting the new REJECT example which WAS a Morgan Stanley forecast cut). Revision iteration 1/2 added verbatim preamble scope-clarify + flat/mixed neutral-framing sentence + test #9 caplog comment. Re-check passed 15/15 must-haves. TDD in 3 atomic commits: RED (1a6bea1) → GREEN Gate 1 (0e758be) → GREEN Gate 2 (b9b8dad). **Gate 1 (content_agent.py Haiku relevance gate):** extended system prompt with bearish_toward_gold REJECT criterion covering 3 categories with examples; added `sentiment: "bullish" | "neutral" | "bearish"` to JSON contract; widened return to `{keep, reject_reason, company, sentiment}` (additive — callers only read `keep`); preamble amendment scopes "presence does NOT trigger rejection" to `is_gold_relevant=false` specifically, clarifying that forecast direction still determines sentiment (bearish MS forecast → sentiment=bearish → rejected); "gold holds steady/flat/mixed" explicitly classified sentiment=neutral and KEPT; new `content_bearish_filter_enabled` Config flag (default "true", independent from `content_gold_gate_enabled`) gates Gate 1 bearish-reject logic only; fail-open preserved on parse/API error. **Gate 2 (gold_media.py Sonnet drafter):** added criterion #4 "Bullish or neutral stance" to `_draft_gold_media_caption` quality-bar (existing 3-criteria reject path reused, zero new logic). **Gate 3 (gold_history.py):** confirmed safe by planner read-through — curated whitelist + drama-first historian voice precludes bearish framing. Zero diff. **Tests (+10 net, 121→131):** 8 gate tests (rejects price-bearish forecast / anti-gold narrative / factual-negative price decline; keeps bullish central-bank buying / Goldman $4K / neutral record-high / flat "holds steady"; fail-open on parse error; flag-disabled bypass) + 2 gold_media tests (rejects bearish analyst clip, accepts bullish analyst clip). Verbatim caplog comment in test #9 documents it verifies log-path fires, NOT production rationale wording (variable by model). **Preservation (zero diff across 5 text-story callers + gold_history):** breaking_news + threads + long_form + quotes + infographics + gold_history untouched — API widens additively. nnh specific-miner rejection precedence preserved (nnh runs BEFORE bearish check). `content_gold_gate_enabled` flag unchanged. `content_agent.review()` compliance layer unchanged (topical policy vs legal/brand compliance — separate). **Validation gates (15/15 must-haves verified):** `grep "bearish_toward_gold" content_agent.py` → 3 matches (prompt + return + INFO log); `grep "Bullish or neutral" gold_media.py` → 1 match; `content_bearish_filter_enabled` present in 3 files; verbatim preamble amendment at content_agent.py L428; verbatim flat/mixed sentence at L456; verbatim caplog comment at test_gold_media.py L153; `uv run pytest scheduler/ -q` → 131 passed; `uv run ruff check scheduler/` → clean. **Deploy:** Railway auto-deploys on push. Empirical test: next sub_infographics 12:00 PT cron (tomorrow) — if another Morgan-Stanley-type story surfaces, must NOT appear in queue. Rollback: toggle `content_bearish_filter_enabled=false` in DB Config. **Preceded by quick-260423-hq7 (same session):** fixed max_count semantics in `run_text_story_cycle` from "trim candidates to top N before dedup loop" → "break out of for-loop after N successful persists", unblocking threads/long_form/quotes that had been producing 0 items for 20+ consecutive runs. of3 best-by-score guarantee now holds under dedup collision. Tests 118→121 (+3 regression pins). 121→131 after j7x.

Prior activity: 2026-04-22 — Completed quick-260422-mfg (full rename `sub_video_clip` → `sub_gold_media` across scheduler + backend + frontend + DB). **Origin:** follow-up (b) from the zid debug session — `scheduler/agents/content/video_clip.py` hard-coded `CONTENT_TYPE="video_clip"` and `AGENT_NAME="sub_video_clip"` while `frontend/src/config/agentTabs.ts` L32 already showed `label: 'Gold Media'` and CLAUDE.md enumerated "gold media" as a canonical content type; the split wasted cycles in the zid debug session (debugger hunted for a `gold_media.py` file that didn't exist). Ran via `/gsd:quick --discuss` workflow; user responded "All clear" to the 3 gray-area AskUserQuestion batch, locking 8 decisions (D-01 through D-08) to Recommended paths: D-01 full rename, D-02 Alembic migration (auditable, vs one-off railway-run UPDATE), D-03 test file renamed, D-04 frontend component file renamed, D-05 worker.py "Gold Media" display unchanged, D-06 lock value 1015 preserved, D-07 `VIDEO_ACCOUNTS` → `GOLD_MEDIA_ACCOUNTS`, D-08 no backward-compat aliases. **Changes:** 3 git-renamed files (history preserved — `scheduler/agents/content/gold_media.py` 88% similarity `git log --follow` reaches vxg 68b21d1 + eoe 762b08b, `scheduler/tests/test_gold_media.py` 61%, `frontend/src/components/content/GoldMediaPreview.tsx` 90%) + 16 modified files across all 4 layers + 1 new Alembic migration `0008_rename_video_clip_to_gold_media.py` (single-headed chain confirmed via `alembic heads` = `0008 (head)`; upgrade updates `content_bundles.content_type` then `agent_runs.agent_name`; downgrade symmetric reverse). Planner extended rename scope for zero-grep hygiene consistency: internal helpers `_search_video_clips` → `_search_gold_media_clips`, `_draft_video_caption` → `_draft_gold_media_caption`, constant `VIDEO_CLIP_SCORE` → `GOLD_MEDIA_SCORE` (value 7.5 unchanged; only symbol moves). **Gates all 9 green:** scheduler 109/109 pytest + ruff clean; backend 69 passed + 5 skipped + ruff clean + alembic heads = 0008; frontend 52/52 vitest + tsc + lint + build all clean. **Zero-grep:** 0 matches for `video_clip`, `sub_video_clip`, `VideoClipPreview`, `VIDEO_ACCOUNTS`, `VIDEO_CLIP_SCORE`, `_search_video_clips`, `_draft_video_caption` across `scheduler/` + `backend/app/` + `frontend/src/` (0008 migration is sole permitted location for old strings by design). 2 commits on main: `e835b61` (refactor) + `69efac1` (SUMMARY). **Five of five zid follow-ups now addressed at the code level:** zid + krz + l40 + lbb + mfg. **Remaining:** (a) sub_gold_media (post-rename) consistent 0-items behavior — deferred as separate `/gsd:debug` investigation path (probe X API quota, `GOLD_MEDIA_ACCOUNTS` availability, analyst-quality-gate rejection rate from vxg). This rename was purely cosmetic/structural; 0-items is a distinct investigation.

Prior activity: 2026-04-22 — Completed quick-260422-lbb (gold_history hallucination hardening via curated fact-sheet whitelist). **Origin:** fourth of five zid debug follow-ups — #(d) gold_history hallucination risk. User directive verbatim: "For gold history, we can leave as is. It needs to actually be a 100% true story." Ran via `/gsd:quick --discuss` workflow; user responded "All clear — you decide" to the 5 gray-area questions, locking all 5 decisions (D-01 through D-05) to Recommended paths (curated whitelist approach; seeded canonical stories via multi-source SerpAPI corroboration; FACT FIDELITY clause in drafter prompt; separate `sources` output field; no runtime SerpAPI verification). **Architecture swap:** retired Claude-picks-story-from-memory + weak-SerpAPI-verification flow (both hallucination leaks by construction) in favor of `scheduler/agents/content/gold_history_stories/` directory with 12 committed JSON fact sheets (bre-x, california, germany-repatriation, hunt-silver, klondike, lbma-fix, munk-barrick, nixon-gold-window, spdr-gld, venezuela-repatriation, witwatersrand, zimbabwe-zig). Each fact sheet: ≥6 verified_facts with distinct source_urls, deduplicated sources list. Totals: 77 facts + 28 distinct URLs across Wikipedia, Bundesbank, Canadian Encyclopedia, Parks Canada, LBMA, Reuters, WGC, History.com. 6 target stories deferred (Giustra/GoldCorp, Newmont/Carlin, USSR/China/Russia reserves, Swiss referendum) for optional future expansion. **Code:** `gold_history.py` rewritten (347 lines); `_pick_story` + `_verify_facts` fully removed (not commented); new `_pick_fresh_slug` + `_load_fact_sheet` + rewritten `_draft_gold_history(fact_sheet, client)` with FACT FIDELITY clause in 3 places (system_prompt, docstring, user_prompt). `serpapi` import removed. Drafter output gains `sources` top-level field; `deep_research` shape simplified to `{story_slug, sources}`. New `gold_history_stories/__init__.py::load_all_stories()` validates schema at load. **Tests (+8 net, 101→109):** retired 3 `_pick_story` tests; added 11 new (whitelist load, fresh-slug selection 4 variants, fact-sheet load 2 variants, sources field, FACT FIDELITY clause, drafter failure modes 2 variants). **Gates all 9 green.** Worktree `worktree-agent-a034f265` FF-merged cleanly into main (1 impl commit `cbe4197`). Zero deviations from plan/CONTEXT.md. All 4 leakage paths closed by construction: (1) Claude-invents → whitelist only; (2) drafter-adds-specifics → FACT FIDELITY forbids; (3) SerpAPI-verification-weak → removed; (4) unverified-proceeds → drafter locked to verified_facts. **Four of five zid follow-ups now addressed:** zid + krz + l40 + lbb. **Remaining:** (a) sub_video_clip 0-items (X API quota / curated-account availability probe — needs investigation) + (b) gold_media/video_clip frontend label confusion (no `gold_media.py` file; scheduler uses video_clip.py but dashboard labels as "Gold Media" — small frontend rename task). Both deferred pending operator go-ahead.

Prior activity: 2026-04-22 — Completed quick-260422-l40 (boot-time reconciler for orphan `agent_runs` running rows). **Origin:** third of the five zid debug follow-ups — #(c) orphan `running` rows in agent_runs. My initial read corrected the debugger's "try/finally hardening" framing: all 3 sub-agent pipelines (`run_text_story_cycle`, `gold_history.run_draft_cycle`, `video_clip.run_draft_cycle`) already have correct try/except/finally structure. The orphans come from hard process kills (Railway SIGKILL, OOM, container crash) where `finally` never executes. Fix is a boot-time sweep, not try/finally modification. **Implementation:** new `async def reconcile_stale_runs(threshold_minutes: int = 30) -> int` in `scheduler/worker.py` called once from `main()` between `upsert_agent_config()` and `build_scheduler(engine)` (guarantees "before any sub-agent job is registered" without asyncio.run gymnastics — `main()` is already async). Bulk `update(AgentRun).where(status=='running').where(started_at < cutoff)` sets status=failed, errors=["scheduler restart — run abandoned (process killed before finally block)"] (JSONB array — planner caught that errors column is JSONB not Text), ended_at=now. 30-min threshold mirrors misfire_grace_time=1800 from krz. Logs at INFO only when rowcount > 0 (keeps steady-state boots quiet). **Imports added:** `update` from sqlalchemy, `AsyncSessionLocal` from database, `AgentRun` from models.agent_run. **Tests (+3, 98→101):** stale-rows-updated (rowcount=2, UPDATE targets agent_runs, errors is a list with "scheduler restart" marker), fresh-rows-skipped (rowcount=0, no log), custom-threshold (60min WHERE-clause cutoff). **Gates all 5 green.** Worktree `worktree-agent-a66eb6b9` FF-merged cleanly into main (no divergence like krz's cherry-pick dance). Commits on main: `2ad4aab` (feat) + `80747a8` (SUMMARY) + this STATE update. **Three of five zid follow-ups now addressed:** zid itself (predicted_format filter) + krz (misfire_grace_time) + l40 (orphan reconciler). Remaining: (a) sub_video_clip 0-items (X API quota) + (b) gold_media/video_clip frontend label confusion + (d) gold_history hallucination risk. Next task queued: gold_history hallucination hardening (d) via `/gsd:quick --discuss` — has real design decisions (verification-gate tightening vs curated whitelist + pre-researched fact sheets).

Prior activity: 2026-04-22 — Completed quick-260422-krz (bump APScheduler `misfire_grace_time` 300s → 1800s in `scheduler/worker.py::build_scheduler`). **Origin:** second of the two follow-up issues flagged by zid's debug session — #(e) `misfire_grace_time=300` too short for Railway deploys. Railway redeploy windows typically run 3-7 min; cron fires that land inside a deploy window were silently coalesced + dropped because grace was <<= deploy time. 2026-04-22 noon-PT fires for quotes/infographics/video_clip were confirmed lost via agent_runs query — rows show fires at 19:00:01 UTC before the 20:37:26 UTC scheduler boot, but the missing coverage came from grace-window expiries, not the pre-boot fires themselves. **Fix (single-line):** `scheduler/worker.py:282` `"misfire_grace_time": 300,` → `"misfire_grace_time": 1800,`. 1800s (30 min) comfortably exceeds observed Railway redeploy windows; still short enough that noon-scheduled content drafts arrive "timely" even after recovery. No test changes (job_defaults is static config). **Gates:** 2 greps pass, scheduler ruff clean, pytest 98/98 (zid baseline). **Merge path had a twist:** worktree branch had fix commit `9d03381`; main already had executor's SUMMARY commit `26b650e`; branches diverged so FF-merge failed → cherry-picked `9d03381` onto main as `8dab545` (clean). Main now has `26b650e` (SUMMARY) + `8dab545` (fix) + this STATE update on top of `16096eb`. **Combined with zid, both root cause (predicted_format filter) + hardening (misfire grace) are now on main, pending push.** Once pushed, Railway redeploys and the cron + interval sub-agents should both produce reliable agent_runs rows — ink fallback timestamp + the run-header UX already shipped catches any still-blank panels. Next tick-boundary: 2026-04-23 12:00 PT sub_quotes/sub_infographics/sub_video_clip cron (sub_gold_history every-other-day; 2026-04-22 was an ON day so 2026-04-23 is OFF, then 2026-04-24 ON).

Prior activity: 2026-04-22 — Completed quick-260422-zid (remove predicted_format routing gate from run_text_story_cycle + add max_count=2 caps for threads/long_form). Root cause of zero-items bug: `scheduler/agents/content/__init__.py` line 119 filtered the story pool to only stories where `predicted_format == content_type`. Haiku classifier assigns ~80-90% of gold-sector stories to "breaking_news", so threads/long_form/quotes/infographics each received 0 candidates → items_queued=0. Fix: `candidates = list(stories)` — each sub-agent's own drafter specializes format; pre-filter was redundant. max_count=2 added to threads + long_form to cap spend. 9 files changed, 98/98 tests pass. Commit 3819972 on branch quick/260422-zid-remove-predicted-format-filter (merged into main as 3819972 + docs closure 16096eb, pushed to origin). 5 follow-up issues flagged in .planning/debug/resolved/260422-zid-non-bn-agents-produce-zero-items.md: video_clip 0-items, gold_media/video_clip label confusion, orphan running rows in agent_runs, gold_history hallucination risk, APScheduler misfire_grace_time too short for Railway deploys (→ addressed by krz above).

Prior activity: 2026-04-22 — Completed quick-260422-ink (per-agent queue RunHeader fallback shows specific timestamp instead of bare "(earlier)" + widened agent_runs fetch window 7→30 days) via `/gsd:quick` workflow. Single atomic fix commit `b629916` on branch `quick/260422-ink-run-header-fallback-time` (parent = `f3f15d6`, ep9 docs commit). **Origin bug:** operator screenshot showed Threads tab rendering `Pulled from agent run (earlier)` while Breaking News rendered `Pulled from agent run at 12:52 PM · Apr 22`. Root cause in `frontend/src/pages/PerAgentQueuePage.tsx`: (i) `getAgentRuns(tab.agentName, 7)` only pulls 7 DAYS of `agent_runs` (second param is `days` not row count — confirmed `frontend/src/api/settings.ts:53-58`); (ii) `groupByRun` buckets items via `started_at <= created_at` with `NO_RUN_KEY = '__no_run__'` catch-all; (iii) `RunHeader` rendered bare `(earlier)` on the null-run branch. Threads had pre-eoe-split drafts (pre-2026-04-21, when thread content came from monolithic `content_agent` not `sub_threads`) whose `created_at` predated every `sub_threads` row in the 7-day window → they all fell into the no-run bucket. **Fix (frontend-only, 1 file, +25/-13):** (1) widened fetch window to 30 days — sub-agents produce at most a few dozen runs/month so trivial extra query cost; (2) refactored `RunHeader` via Approach A (caller-resolves): signature went from `{ run: AgentRunResponse \| null }` → `{ displayTime: string; itemsQueued?: number \| null }`. Ternary and `(earlier)` literal both gone. Mapping loop computes `sourceDate = group.run ? new Date(group.run.started_at) : new Date(group.items[0].created_at)` (items pre-sorted DESC by `created_at`, so `items[0]` is newest), formats `${h:mm a} · ${MMM d}` once, passes resolved string in. Component now purely presentational — zero conditional logic. No backend/API/DB changes, no test additions (existing 5 `PerAgentQueuePage.test.tsx` tests exercise `showRunGroups === false` branch → RunHeader not exercised → no regression risk). **Gates (7/7 green):** 3 greps pass (window widened, `(earlier)` gone, `Pulled from agent run at` emitted unconditionally); `pnpm -C frontend lint` clean; `pnpm -C frontend exec tsc --noEmit` clean; `pnpm -C frontend test --run` 52/52 across 8 files; `pnpm -C frontend build` 2322 modules in 199ms. Branch pending FF merge into main + push to origin. **Post-deploy expectation:** Threads tab + any other sub-agent with drafts older than the 30-day window now shows specific timestamp derived from the draft's own `created_at` — no more ambiguous "(earlier)" label. No risk to matched-run rendering (that path is unchanged — caller passes `group.run.started_at`-derived string).

Prior activity: 2026-04-22 — Completed quick-260422-ep9 (market_snapshot temporal anchors + extended hard instruction) via `/gsd:quick --discuss` workflow. Single atomic implementation commit `55b1eb0` on branch `quick/260422-ep9-market-snapshot-temporal-anchors` (parent = `66225f7`, vxg docs commit). **Origin bug:** 2026-04-22 breaking_news draft hallucinated "GOLD whipsaws $100+ intraday" and "gold and equities hitting highs SIMULTANEOUSLY today" — gold actually gave back Trump Taco rally gains that day. Root cause: existing oa1 market_snapshot injected current spot only, no temporal anchor to catch directional claims; `_HARD_INSTRUCTION` permitted "near recent highs" hedge which drafter stretched to "hitting highs SIMULTANEOUSLY today". **Fix:** extended `MarketSnapshot` TypedDict with 7 new Optional fields (gold_24h_high/low, gold_prior_close, gold_52w_high/low, at_52w_high: bool|None via strict equality, tracking_since: date|None). New `_compute_gold_anchors(session)` helper runs a single SQL window query against `market_snapshots` table (MAX/MIN over 24h + 52w windows, prior_close via `ORDER BY fetched_at DESC LIMIT 1 OFFSET 1`, MIN(fetched_at) for tracking_since label) — uses `cast(MarketSnapshotORM.data["gold_usd_per_oz"].astext, Float)` because price lives inside JSONB `data`, not a top-level column. Query runs BEFORE `session.add(snapshot_row)` so prior_close reflects last-written snapshot, not the in-flight one. `render_snapshot_block` gains 2 new lines ("Gold 24h range: $X - $Y/oz (prior close: $Z/oz)" + "Gold 52w range: $X - $Y/oz (at_52w_high: true)") with graceful `[UNAVAILABLE — tracking since YYYY-MM-DD, N day(s) of history]` fallback during the ~1-year data-gap period since oa1 deployed 2026-04-21. `_HARD_INSTRUCTION` extended with explicit forbidden-phrase list: "at highs / at a new high / at an all-time high / breaking out / at a record / hitting a new record" unless `at_52w_high: true`, AND specific intraday move claims ("$X+ intraday / whipsaws $X / X% move today") unless the 24h range supports the magnitude. **Discussion phase** (`/gsd:quick --discuss`) surfaced + locked 5 decisions via AskUserQuestion — no backfill (graceful [UNAVAILABLE]), gold only (5 anchors, silver deferred), on-every-fetch SQL window query (no new columns/migration/job), strict equality for at_52w_high (no fuzzy tolerance), extended _HARD_INSTRUCTION with explicit forbidden-phrase list. Captured in CONTEXT.md with verbatim target snapshot-block shapes for populated/partial/empty cases. Planner caught critical ORM quirk (gold_usd_per_oz in JSONB `data` not top-level column) and embedded the correct SQLAlchemy cast pattern in the plan. Executor honored all 5 decisions + the 6 planner guidance points, wrote 8 new tests via `_MockAnchorSession` + `_build_fred_metals_side_effect` helpers. **Gates:** scheduler ruff clean, pytest 98/98 (baseline 90 + 8 new). All 7 validation greps from CONTEXT.md pass. Backward-compat preserved — 6 downstream tests that patch `fetch_market_snapshot` with None/minimal dicts still pass because new TypedDict fields are all Optional with default None. **Zero changes outside market_snapshot.py** — all 7 sub-agent drafters receive the enriched block verbatim. Branch pending FF merge into main + push to origin. **Post-deploy expectation:** next `fetch_market_snapshot` call logs show new anchor lines; drafters no longer produce "hitting highs SIMULTANEOUSLY today" hallucinations because `at_52w_high: false` AND explicit forbidden-phrase list AND qualitative-hedge fallback instruction all apply. First ~24h post-deploy: 52w range shows `[UNAVAILABLE — tracking since 2026-04-21]` and drafters must hedge; 24h range becomes real after ~24h of snapshots accumulate.

Prior activity: 2026-04-22 — Completed quick-260422-vxg (7-sub-agent cadence rebalance + quotes max=1 + Gold Media analyst quality bar). Single atomic implementation commit `68b21d1` on branch `quick/260422-vxg-cadence-rebalance-7-sub-agents` (parent = `ddb5721`, the mos docs commit). Topology went from 6 interval + 1 cron → 3 interval + 4 cron. Interval agents: BN=2h (reverts m9k 1h experiment — too much churn), Threads=4h, Long-form=4h, offsets trimmed to `[0, 17, 34]`. Cron agents (all 12:00 America/Los_Angeles): sub_quotes daily (unchanged from mos), sub_infographics daily (new), sub_video_clip/Gold Media daily (new), sub_gold_history every-other-day via `day="*/2"` (new). `CONTENT_CRON_AGENTS` tuple schema upgraded from 7-field fixed (hour/minute/timezone) → 5-field dict-of-kwargs (`cron_kwargs: dict`) so heterogeneous cron patterns coexist — registration loop calls `CronTrigger(**cron_kwargs)`. Quotes `max_count` 2→1 (surface only the single highest-signal reputable quote/day). Gold Media analyst quality gate: `VIDEO_ACCOUNTS` reordered/trimmed to 7 analyst-heavy handles (Kitco/CNBC/Bloomberg/BloombergTV/ReutersBiz/FT/MarketWatch), `_draft_video_caption` extended with 3-criterion quality bar (speaker credibility, substance, freshness) + reject-path JSON `{"reject": true, "rationale": "..."}` returning None, `run_draft_cycle` sorts clips desc by `created_at` (tz-aware fallback) and caps attempts at `MAX_DRAFT_ATTEMPTS=5` with break-after-first-persist. Docstring touches: `breaking_news.py::L3` reverted "every 1 hour" → "every 2 hours"; `gold_history.py` cadence line replaced with `day='*/2'` reference; `worker.py` module docstring + startup log line updated. +8 tests net (82→90): test_worker.py +6 (interval cadences, cron count=4, dict-shape, gold_history every-other-day, infographics+video_clip daily cron shape), test_quotes.py updated assertion (max_count=1), test_video_clip.py +2 (accounts reorder + reject path). Scheduler ruff clean, pytest 90/90. All 8 validation greps pass. **Explicitly deferred per user's "Option A" directive:** (1) Infographics historical-fallback drafter (Claude picker + `infographics_used_topics` Config key + drafter, modeled on gold_history architecture); (2) Gold Media source switch to YouTube Data API / podcast RSS (current X API video source kept + tightened with analyst bar only). Branch pending merge into main + push + Railway redeploy. **Post-deploy expectation:** startup log shows 3 interval + 4 cron agents scheduled; next 12:00 PT tick fires 3 or 4 cron agents (4 on even-calendar days when gold_history runs); BN first fire within 2h of boot thanks to m9k's +10s buffer; Quotes + Gold Media daily fires may return 0 drafts when nothing passes the quality bar.

Prior activity: 2026-04-21 — Completed quick-260421-mos (Quotes sub-agent daily 12pm Pacific + reputable-source whitelist + top-2 cap + drafter quality gate). Single atomic implementation commit `4dbeab3` on branch `quick/260421-mos-quotes-daily-noon-reputable` (rebased on top of m9k). Split `CONTENT_SUB_AGENTS` into `CONTENT_INTERVAL_AGENTS` (6) + `CONTENT_CRON_AGENTS` (1, sub_quotes daily 12:00 America/Los_Angeles), added `CronTrigger` import + second registration loop. Added `max_count` + `source_whitelist` optional kwargs to shared `run_text_story_cycle` (None defaults = zero behavior change for the 4 other sub-agents using it). `quotes.py` got 27-pattern `REPUTABLE_SOURCES` frozenset (tier-1 financial + gold-specialist + institutional), `max_count=2` + whitelist wired in `run_draft_cycle`, and a 4-criterion quality bar + reject path in `_draft` (returns None → existing stub-bundle branch). +8 tests across `test_worker.py` (+1, lock-ids iterate both lists + new daily-cron shape test), `test_quotes.py` (+3, whitelist sanity + reject path + filter-kwargs passthrough), and new `test_content_init.py` (+4, whitelist filter + max_count cap + both no-op paths). Scheduler pytest 82/82, ruff clean. All 9 validation greps pass. Branch NOT pushed — awaiting operator merge into main + Railway redeploy. Post-deploy expectation: next 12:00 America/Los_Angeles fires sub_quotes DST-aware; first-fire logs show reputable filter + max_count cap funnel counts; /agents/quotes dashboard shows "Pulled from agent run at 12:00 PM · {date}" header within seconds OR stays empty 24h if no quote met the quality bar.

Prior activity: 2026-04-21 — Completed quick-260421-m9k (sub_breaking_news hourly + APScheduler offset=0 first-fire fix). Single atomic commit `c5d32f3` on branch `quick/260421-m9k-breaking-news-hourly`. Extended `CONTENT_SUB_AGENTS` tuple to carry per-agent `interval_hours` (BN=1, others=2), added `+10s` buffer to `start_date` so offset=0 jobs don't skip first fire, refreshed 6 worker.py docstring sites + 1 breaking_news.py docstring + 1 test_worker.py unpack pattern (Rule 3 deviation — test broke on the 5→6 tuple shape change; fixed in same commit). Scheduler ruff clean, pytest 74/74 (baseline match). Pending operator merge/push + Railway redeploy; post-deploy expectation is that the "Pulled from agent run at {time}" header appears on /agents/breaking-news ~10s after boot (instead of the prior 2h dark period that prevented the header from rendering until the first interval had fully elapsed).

Prior activity: 2026-04-21 — Completed quick-260421-k9z (dead-code cleanup follow-up to eoe). Removed orphan-island `select_qualifying_stories`/`select_top_story`/`_is_within_window` from `scheduler/agents/content_agent.py` (−81 lines; `breaking_window_hours` param dies with the island); deleted orphan `frontend/src/pages/ContentPage.tsx` + test (−547 lines) and stripped stale App.tsx comment; removed zombie config keys `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` from both `seed_content_data.py::CONFIG_DEFAULTS` AND `worker.py::upsert_agent_config` (write-only post-eoe, zero readers). DELETE migration docstring in seed script extended from 4 → 6 retired keys. 2 atomic commits on `quick/260421-k9z-dead-code-cleanup`: `b8aec6e` (scheduler) / `704d7b3` (frontend). All gates green (scheduler 74/74, backend 69/5, frontend 52/52, ruff/eslint/tsc/build all clean). Deviation #6 in eoe SUMMARY was documented as stale — both config keys were write-only post-split, not read by `fetch_stories()` as previously claimed.

Prior activity: 2026-04-21 — quick-260420-sn9 fully purged the Twitter Agent and trimmed the Senior Agent to morning-digest-only, across 5 atomic commits. System is now **single-agent** (Content only, with `morning_digest` + `gold_history_agent` as supporting crons). **Task 1 (scheduler, `62ffda4`):** deleted `scheduler/agents/twitter_agent.py` + `scheduler/tests/test_twitter_agent.py`; `worker.py` `_make_job` simplified (no twitter/senior branches), `JOB_LOCK_IDS` twitter/senior entries removed; 3 scheduler jobs remain (content_agent, morning_digest, gold_history_agent); `tweepy[async]>=4.14`, `x_api_bearer_token`/`x_api_key`/`x_api_secret`, and `_validate_env` `X_API_BEARER_TOKEN` critical gate **preserved** — Content Agent video_clip pipeline still reads X API. **Task 2 (senior trim, `be9cd8b`):** senior_agent.py reduced to `run_morning_digest()` only (dedup/expiry/queue-cap methods removed); paired tests updated. **Task 3 (backend, `aeaf412`):** `/config/quota` router endpoint removed; model/schema comments updated to document Twitter purge (with ruff E501 reflow); `seed_mock_data.py` rewritten content-only (removed 5 twitter + 5 instagram mock items); test fixtures migrated (watchlist/draft_item/keyword/rendered_image platforms → content); backend pytest 66 passed / 5 skipped, ruff clean. One deferred item logged: pre-existing ruff I001 in `backend/alembic/versions/0007_add_market_snapshots.py` (from `b28780b` / oa1 task, out-of-sn9-scope). **Task 4 (frontend, `c30e288`):** Platform type narrowed `'twitter' \| 'content'` → `'content'`; 7 Twitter-only files deleted (ApprovalCard + test, PlatformTabBar + test, QueuePage, QuotaBar, WatchlistTab); 13 files modified to remove Twitter UI (Sidebar nav, App.tsx `/twitter` route, AgentRunsTab filter option, KeywordsTab platform dropdown, ScoringTab "Twitter Agent Scoring" section, PlatformBadge/RelatedCardBadge config maps, useQueueCounts, handlers.ts mocks, DigestPage stats grid 3-col → 2-col + Twitter tile removed, SettingsPage tabs 6 → 5 (Keywords default), PlatformQueuePage both branches → ContentSummaryCard); 3 test assertions updated (DigestPage + SettingsPage + PlatformQueuePage); `pnpm tsc --noEmit` clean, `pnpm lint` clean, `pnpm test` 56/56, `pnpm build` 544KB green. **Task 5 (prod DB + docs, `55100c9`):** Prod Neon DB purge via `npx @railway/cli run` + asyncpg inside single transaction — **pre/post counts: 177 → 0 `draft_items WHERE platform='twitter'`**, **40 → 0 `watchlists` (25 twitter + 15 instagram orphans)**, **8 → 0 `config WHERE key LIKE 'twitter_%'`**; Content rows preserved at 955 both sides. One Rule-3 auto-fix inside the same txn: one `instagram` orphan row (from lvy-era data) had `related_id` pointing at a twitter row, triggering FK constraint; UPDATE-NULL'd that one `related_id` before DELETE could proceed. CLAUDE.md Project blurb updated "three-agent AI system" → "single-agent AI system," new historical note appended for 2026-04-20 mirroring the 2026-04-19 lvy note, Stack constraint line updated to list the 3 surviving scheduled jobs. `.planning/ROADMAP.md` deprecation notice added for Phase 4 + Phase 5, Phase 10 partial-deprecation banner, progress table updated. `.planning/REQUIREMENTS.md` TWIT-01..14 and SENR-01,02,03,04,05,08,09 struck through with `**(DEPRECATED 2026-04-20)**`; SENR-06 + SENR-07 retained (shipped via Phase 10-03 as `morning_digest` at 15:00 UTC). `.planning/STATE.md` (this file) updated with 2 new sn9 decision entries + 1 s3b cancellation entry + 2 new rows in Quick Tasks table. 5 commit SHAs on main: `62ffda4` / `be9cd8b` / `aeaf412` / `c30e288` / `55100c9`. **No push to origin/main** per operator instruction — commits remain local until operator reviews.
Stopped at: Completed quick task 260423-lvp (add analytical-historical fallback to sub_infographics; guarantees 2 infographics/day via SerpAPI query fallback when news phase produces fewer than 2)
Resume file: None
