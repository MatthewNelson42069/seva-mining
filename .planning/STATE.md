---
gsd_state_version: 1.0
milestone: v1.0.1
milestone_name: — Content Preview and Rendered Images
status: v1.0.1 complete
stopped_at: Completed quick-260422-ep9 — market_snapshot gains temporal anchors (gold 24h high/low + prior_close + 52w high/low + at_52w_high binary + tracking_since). Anchors computed via SQL window query against market_snapshots table on every fetch_market_snapshot() call (no new APIs, no migrations). `_HARD_INSTRUCTION` extended with second paragraph forbidding "at highs / at a new high / breaking out / at a record / hitting a new record" unless `at_52w_high: true`, and forbidding specific intraday move claims ("$100+ intraday / whipsaws $X / X% move today") unless 24h range supports the magnitude. Graceful `[UNAVAILABLE — tracking since YYYY-MM-DD, N days of history]` fallback for the ~1-year data-gap period (oa1 shipped 2026-04-21). Ships the durability fix for the 2026-04-22 breaking_news hallucination ("GOLD whipsaws $100+ intraday / hitting highs SIMULTANEOUSLY today") — drafter now sees real temporal reference data + explicit forbidden-phrase list. Single atomic commit `55b1eb0` on branch `quick/260422-ep9-market-snapshot-temporal-anchors`. Scheduler pytest 98/98 (baseline 90 + 8 net new tests), ruff clean. All 7 validation greps pass. Backward-compat: all downstream tests patching fetch_market_snapshot with None/minimal dicts still pass (new TypedDict fields all Optional, default None).
last_updated: "2026-04-22T17:55:00.000Z"
progress:
  total_phases: 11
  completed_phases: 9
  total_plans: 68
  completed_plans: 56
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-03-30)

**Core value:** Every piece of content the system drafts must be genuinely valuable to the gold conversation it enters — a data point, an insight, a connection no one else made.
**Current focus:** v1.0.1 shipped — awaiting next milestone definition

## Current Position

Phase: Not started (v1.0.2 TBD)
Plan: Not started

## Performance Metrics

**Velocity:**

- Total plans completed: 0
- Average duration: -
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| - | - | - | - |

**Recent Trend:**

- Last 5 plans: none yet
- Trend: -

*Updated after each plan completion*
| Phase 01 P02 | 279 | 3 tasks | 13 files |
| Phase 01 P04 | 5 | 3 tasks | 8 files |
| Phase 01 P05 | 2 | 2 tasks | 6 files |
| Phase 01 P06 | 3 | 2 tasks | 6 files |
| Phase 02-fastapi-backend P04 | 163 | 2 tasks | 3 files |
| Phase 03 P03 | 191 | 2 tasks | 11 files |
| Phase 03 P04 | ~20 | 2 tasks | 7 files |
| Phase 04-twitter-agent P01 | 5 | 2 tasks | 11 files |
| Phase 04-twitter-agent P02 | 426 | 2 tasks | 12 files |
| Phase 04-twitter-agent P03 | 1320 | 1 tasks | 1 files |
| Phase 04-twitter-agent P04 | 124 | 2 tasks | 3 files |
| Phase 04-twitter-agent P05 | 15 | 1 tasks | 4 files |
| Phase 04-twitter-agent P05 | 15 | 2 tasks | 4 files |
| Phase 05-senior-agent-core P01 | ~8 | 2 tasks | 9 files |
| Phase 05-senior-agent-core P02 | 440m | 1 tasks | 2 files |
| Phase 05-senior-agent-core P03 | 15 min | 1 tasks | 2 files |
| Phase 05-senior-agent-core P04 | 20 | 1 tasks | 2 files |
| Phase 05 P05 | 2 minutes | 1 tasks | 2 files |
| Phase 06-instagram-agent P01 | 5 min | 2 tasks | 3 files |
| Phase 06-instagram-agent P02 | 10 | 1 tasks | 2 files |
| Phase 06 P03 | 129 | 1 tasks | 2 files |
| Phase 06 P04 | 207 | 2 tasks | 2 files |
| Phase 06 P05 | 8m | 1 tasks | 3 files |
| Phase 07-content-agent P01 | 2 | 2 tasks | 6 files |
| Phase 07-content-agent P02 | - | 2 tasks | 3 files |
| Phase 07-content-agent P03 | - | 1 tasks | 1 files |
| Phase 07-content-agent P04 | - | 1 tasks | 1 files |
| Phase 07-content-agent P05 | 5 | 2 tasks | 3 files |
| Phase 08-dashboard-views-and-digest P01 | 8 | 2 tasks | 10 files |
| Phase 08-dashboard-views-and-digest P03 | 15 | 2 tasks | 7 files |
| Phase 08 P02 | 32 | 1 tasks | 6 files |
| Phase 08-dashboard-views-and-digest P04 | 612 | 2 tasks | 9 files |
| Phase 08-dashboard-views-and-digest P06 | checkpoint | 1 tasks | 0 files |
| Phase 09-agent-execution-polish P01 | 8 | 2 tasks | 4 files |
| Phase 09-agent-execution-polish P02 | 2 | 2 tasks | 4 files |
| Phase 07-content-agent P07 | 10 | 2 tasks | 1 files |
| Phase 07-content-agent P08 | 5 | 2 tasks | 1 files |
| Phase 07 P09 | 2 | 2 tasks | 1 files |
| Phase 07 P10 | 3 | 2 tasks | 3 files |
| Phase 10-senior-agent-whatsapp-notifications P01 | 2 | 2 tasks | 2 files |
| Phase 10 P03 | 525589 | 2 tasks | 7 files |
| Phase 11 P01 | 45 min | 5 tasks | 22 files |
| Phase 11 P02 | 11 min | 2 tasks | 3 files |
| Phase 11 P03 | 3 min | 2 tasks | 3 files |
| Phase 11 P04 | 3 min | 1 tasks | 2 files |
| Phase 11 P05 | 5 min | 2 tasks | 4 files |
| Phase 11 P06 | 15 min | 2 tasks | 9 files |
| Phase 11 P07 | checkpoint | 3 tasks | 3 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [Pre-build]: Separate scheduler worker from API server — crash isolation, prevents duplicate execution
- [Pre-build]: Full schema from day one including JSONB alternatives array — no schema migration debt later
- [Pre-build]: APScheduler 3.11.2 only — v4.x alpha has breaking API changes
- [Pre-build]: shadcn/ui tailwind-v4 branch only — main branch targets Tailwind v3
- [Phase 01]: pytest config embedded in pyproject.toml [tool.pytest.ini_options] — no separate pytest.ini
- [Phase 01]: asyncio_mode=auto in both projects — eliminates need for @pytest.mark.asyncio decorator on individual tests
- [Phase 01]: asyncpg SSL: strip sslmode=require from URL, use connect_args ssl=True — asyncpg rejects sslmode as URL param
- [Phase 01]: FastAPI lifespan used for engine.dispose() on shutdown — cleaner than deprecated on_event pattern
- [Phase 01]: test_schema.py skip condition checks for real neon.tech URL to prevent env var leakage from test_health.py fake DATABASE_URL
- [Phase 01]: APScheduler 3.11.2 AsyncIOScheduler with advisory lock — numReplicas=1 is primary prevention, pg_try_advisory_lock is defense-in-depth
- [Phase 02-02]: Queue router uses no prefix — /queue and /items/{id}/* are separate top-level paths matching spec, not nested under /queue prefix
- [Phase 02-02]: VALID_TRANSITIONS dict is single source of truth for state machine — only DraftStatus.pending has allowed targets
- [Phase 02-fastapi-backend]: asyncio.to_thread wraps synchronous Twilio SDK to avoid blocking the FastAPI event loop in WhatsApp service
- [Phase 02-fastapi-backend]: Retry-once on TwilioRestException with logging (D-16): warning on attempt 1, error on attempt 2, re-raise
- [Phase 03-03]: QueuePage runs all 3 platform queries in parallel to populate badge counts — queries are cheap and avoids tab-switch loading delays
- [Phase 03-03]: PlatformTabBar is prop-driven (no internal state) — QueuePage owns active tab state for testability
- [Phase 03-03]: NavLink end={true} only for root '/' route — prevents matching /settings as Queue-active state
- [Phase 03-04]: RelatedCardBadge uses callback prop (onSwitchPlatform) not Zustand — active platform is local QueuePage state
- [Phase 03-04]: ContentSummaryCard stopPropagation on buttons — card body click opens modal, buttons don't trigger modal
- [Phase 03-04]: Seed script builds own async engine from DATABASE_URL — avoids importing Settings which requires all env vars
- [Phase 04-twitter-agent]: tweepy[async] extra required — tweepy.asynchronous needs aiohttp + async-lru installed via [async] optional dep
- [Phase 04-twitter-agent]: Test stubs use per-function lazy imports so all 20 tests are collectable before agent module exists (Wave 0 correct RED state)
- [Phase 04-twitter-agent]: Pure scoring functions are module-level (not class methods) for direct testability in TDD
- [Phase 04-twitter-agent]: tweepy[async] extra required for AsyncClient — base tweepy missing aiohttp dep
- [Phase 04-twitter-agent]: scheduler/models/ mirrors backend/app/models/ — scheduler has no access to backend package
- [Phase 04-twitter-agent]: Two-model LLM pattern: Sonnet for drafting quality, Haiku for compliance speed/cost
- [Phase 04-twitter-agent]: Fail-safe compliance: ambiguous LLM response = block (not pass) for Seva Mining/financial advice check
- [Phase 04-twitter-agent]: Module-level wrapper functions (draft_for_post, filter_compliant_alternatives, build_draft_item) use __new__ to bypass __init__ for test injection
- [Phase 04-twitter-agent]: TwitterAgent instantiated inside job closure (not at build time) to avoid import side effects at test collection
- [Phase 04-twitter-agent]: All 25 watchlist accounts seeded at relationship_value=5 per user request (maximum priority for initial seed)
- [Phase 04-twitter-agent]: Backend models must be kept in sync with scheduler models manually — no shared package, each service owns its own copy of the model
- [Phase 05-senior-agent-core]: Wave 0 test stubs put pytest.skip() BEFORE the lazy import — ensures 19 tests show as 'skipped' not 'error' when senior_agent module doesn't exist yet
- [Phase 05-senior-agent-core]: migration 0004 adds both engagement_alert_level (engagement alert dedup) and alerted_expiry_at (expiry alert dedup) to draft_items in a single migration
- [Phase 05-senior-agent-core]: Jaccard similarity >= 0.40 threshold for story dedup, configurable via senior_dedup_threshold config key (default 0.40); lookback 24h via senior_dedup_lookback_hours
- [Phase 05-senior-agent-core]: cashtag-aware regex r'$[a-z0-9]+|\b\w+\b' preserves dollar-prefixed tokens like $gld and $2400 as atomic fingerprint units
- [Phase 05-senior-agent-core]: ORDER BY score ASC, expires_at ASC tiebreaking — soonest-expiring tied-score item is displaced; no special-case branch needed
- [Phase 05-senior-agent-core]: process_new_items module-level function uses lazy SeniorAgent instantiation to avoid circular imports from TwitterAgent call site
- [Phase 05-senior-agent-core]: run_expiry_sweep does NOT call _check_breaking_news_alert (that fires at item intake via process_new_item); engagement dedup uses one-way state machine null->watchlist->viral via engagement_alert_level column
- [Phase 05]: _headline_from_rationale splits on period-space to avoid splitting decimal numbers; queue_snapshot total computed in Python; yesterday_approved includes empty items list; var_2 truncated at last semicolon-space boundary within 200 chars
- [Phase 06-instagram-agent]: Wave 0 test stubs use pytest.skip() BEFORE lazy import — ensures 15 tests show as 'skipped' not 'error' when instagram_agent module doesn't exist yet; uv sync --all-extras required to preserve dev dependencies when adding prod deps
- [Phase 06-instagram-agent]: passes_engagement_gate accepts datetime parameter (not post_age_hours float) to match existing test stub signatures from Plan 01
- [Phase 06-instagram-agent]: select_top_posts named without instagram_ prefix to match Wave 0 test stubs
- [Phase 06]: Module-level functions (draft_for_post, check_compliance, build_draft_item_expiry) in instagram_agent.py for direct test injection without class instantiation
- [Phase 06]: Pre-screen '#' and 'seva mining' locally before calling Claude Haiku — avoids LLM cost for obvious compliance blocks
- [Phase 06]: Fail-safe compliance: only explicit 'pass' substring returns True; everything else blocks draft
- [Phase 06]: asyncio.sleep(2**attempt) gives 1s/2s backoff; 3 total attempts; empty list returned after exhaustion
- [Phase 06]: _check_critical_failure returns count only; alert at exactly consecutive_zeros==2 in _run_pipeline (dedup)
- [Phase 06]: Seed script uses 15 Instagram accounts (best-effort from 25 Twitter entities); 10 skipped with no active IG presence
- [Phase 07-content-agent]: Wave 0 test stubs use pytest.skip() BEFORE lazy import in content agent tests — ensures 16 tests show SKIPPED not ERROR during Wave 0 when module doesn't exist
- [Phase 07-content-agent]: ContentBundle scheduler mirror uses datetime.now(timezone.utc) lambda instead of deprecated datetime.utcnow — timezone-aware default
- [Phase 07-content-agent]: Content agent seed script only seeds config keys (no watchlists/keywords) — agent uses RSS + SerpAPI not platform watchlists
- [Phase 07-content-agent]: RSS_FEEDS and SERPAPI_KEYWORDS are module-level constants — accessible as ca.RSS_FEEDS and ca.SERPAPI_KEYWORDS in tests without class instantiation
- [Phase 07-content-agent]: run() creates and commits AgentRun immediately before pipeline starts — status visible even if pipeline crashes mid-flight
- [Phase 07-content-agent]: _extract_check_text is module-level (not class method) — consistent with established pattern for testable pure functions
- [Phase 08-dashboard-views-and-digest]: MSW /config/quota handler listed before /config/:key to prevent generic param matching quota path
- [Phase 08-dashboard-views-and-digest]: KeywordCreate.term field (not keyword) throughout frontend types — matches backend schema exactly
- [Phase 08-dashboard-views-and-digest]: ContentPage tests use vi.mock for API modules instead of MSW server.use() — relative fetch URLs cannot be intercepted in jsdom/Node.js MSW environment
- [Phase 08]: localStorage mock via vi.stubGlobal required for apiFetch tests — jsdom does not provide localStorage by default; any component calling apiFetch on render needs this mock in beforeEach
- [Phase 08]: useEffect syncs TanStack Query data to local state in DigestPage — direct render-time setState causes silent test failures
- [Phase 08-dashboard-views-and-digest]: JSDOM URL must be set to http://localhost:3000 in vitest config so MSW relative path handlers resolve to matching fetch URLs
- [Phase 08-dashboard-views-and-digest]: Human verification checkpoint approved — all three dashboard pages confirmed correct by operator
- [Phase 09-agent-execution-polish]: Twitter _get_config returns Optional[Config], so threshold reads use int(_cfg.value) if _cfg else default pattern — consistent with existing quota reads
- [Phase 09-agent-execution-polish]: Default parameter values in passes_engagement_gate match previous hardcoded values — backward compatibility preserved for existing tests without changes
- [Phase 09-agent-execution-polish]: expiry_sweep and morning_digest scheduler config keys placed in seed_content_data.py — scheduler-level concerns with no agent home; content seed is closest match
- [Phase 09-agent-execution-polish]: build_scheduler changed to async def to support await _read_schedule_config(engine) at startup; APScheduler API unaffected since main() already async
- [Phase 07-content-agent]: RSS_FEEDS expanded to 8 (reuters, bloomberg, goldseek, investing); SERPAPI_KEYWORDS to 10 (macro/inflation terms)
- [Phase 07-content-agent]: breaking_news format added as 4th Sonnet option with urgency-preference and senior analyst voice in system prompt
- [Phase 07-content-agent]: Multi-story pipeline with per-story error isolation replaces single-story; _is_already_covered_today() for cross-run dedup
- [Phase 07-content-agent]: VIDEO_ACCOUNTS capped at first 5 for API query length limits; fixed score 7.5 for Twitter-sourced content (video_clip, quote); _run_twitter_content_search() extracted as separate method for clean 07-07 merge; quote format added as choosable from article content in Sonnet prompt
- [Phase 07]: Sonnet prompt rewritten as central format decision engine for all 7 content types with Instagram design system (#F0ECE4/#0C1B32/#D4AF37) and historical pattern SerpAPI verification fallback
- [Phase 07]: GoldHistoryAgent uses fixed baseline score 8.0 for curated history stories (not RSS-scored)
- [Phase 07]: story slug tracked in Config before DraftItem creation so partial failures never cause story re-selection
- [Phase 10-senior-agent-whatsapp-notifications]: Phase 10: Switched whatsapp.py from content_sid (Meta-approved template SIDs) to body (free-form text) — Twilio sandbox accepts free-form without template approval; TEMPLATE_SIDS and send_whatsapp_template() removed
- [Phase 10]: Phase 10-03: expiry_sweep removed from scheduler; morning_digest at 15:00 UTC; WhatsApp failure non-fatal in run_morning_digest
- [Phase 11]: Used _get_render_bundle_job() helper for patchable ImportError-safe lazy scheduler import — Allows monkeypatching in tests without needing to patch a local import; scheduler agents/__init__.py causes tweepy ImportError in backend process which is caught gracefully
- [Phase 11-02]: genai_types.GenerateImagesConfig (plural) is the correct class name in google-genai 1.73.1 — RESEARCH.md had typo GenerateImageConfig (singular)
- [Phase 11-02]: Per-role retry (not per-bundle): each role gets 3 independent attempts; partial renders (e.g. 3/4) written as partial list; empty list on all-roles-failed
- [Phase 11-02]: Role names use twitter_visual/instagram_slide_1/2/3 convention to match RenderedImage schema; quote=2 roles not 1 per PLAN.md test behaviors
- [Phase 11-03]: Backend rerender endpoint uses asyncio.create_task in the backend's own event loop — cross-process scheduler.add_job is not possible (backend and scheduler are separate Railway services)
- [Phase 11-05]: TanStack Query v5 refetchInterval callback receives a query object (not (data, query) like v4); tests exercise the callback directly by extracting the query from the cache to avoid vi.useFakeTimers deadlocks
- [Phase 11-06]: RenderedImagesGallery mounted only for infographic + quote formats (D-04); other formats never render image skeletons
- [Phase 11-07]: importlib.util.spec_from_file_location bypasses scheduler/agents/__init__.py tweepy import in the backend process; scheduler/config.py uses extra="ignore" to tolerate backend-only env vars in the shared .env
- [2026-04-19 / quick 260419-lvy]: Instagram Agent (Phase 6) fully deprecated and purged from codebase — Apify scraping not viable, $50/mo spend cut; Seva Mining is now a three-agent system (Twitter + Senior + Content). Frontend IG UI removed, scheduler IG job removed, Apify deps removed from scheduler/backend. Content agent still emits instagram_post/instagram_caption/instagram_brief/carousel fields and image_render still emits IG slide roles, but nothing downstream consumes them (no migration written — safe no-op data).
- [2026-04-19 / quick 260419-lvy]: Content queue page now groups by agent_run matching Twitter pattern (showRunGroups = platform === 'twitter' || 'content'); content branch renders ContentSummaryCard inside grouped layout.
- [2026-04-20 / quick 260420-sn9]: Twitter Agent (Phase 4) and the broader Senior Agent (Phase 5, Phase 10) fully deprecated and purged. X API Basic tier ($100/mo) spend not justified when Content Agent's news pipeline produces higher-signal drafts; Senior Agent had narrowed to morning_digest-only in practice. Scheduler trimmed from 5 jobs → 3 jobs (content_agent + morning_digest + gold_history_agent). `scheduler/agents/twitter_agent.py` + tests deleted; `scheduler/agents/senior_agent.py` trimmed to morning-digest-only. Frontend Platform type narrowed from `'twitter' | 'content'` → `'content'`; 7 Twitter-only files deleted (ApprovalCard, PlatformTabBar, QueuePage, QuotaBar, WatchlistTab + 2 tests); Twitter nav/tab/scoring/keyword/agent-option purged. Backend `/config/quota` endpoint removed; test fixtures migrated to content-only. Prod DB: 177 `draft_items WHERE platform='twitter'` rows deleted; 40 watchlists rows deleted (25 twitter + 15 instagram orphans from lvy); 8 `config` rows with `twitter_*` keys deleted. **Preserved intentionally:** `tweepy[async]>=4.14` + `x_api_bearer_token/x_api_key/x_api_secret` env wiring + `VIDEO_ACCOUNTS` + `_search_video_clips` + `_draft_video_caption` + `_run_twitter_content_search` in content_agent.py — the video_clip pipeline still needs X API read access. Seva Mining is now a single-agent system (Content only).
- [2026-04-20 / quick 260420-sn9]: quick task `260420-s3b` (Twitter agent autonomous auto-posting spec) **cancelled before a PLAN.md was written**. Context+research docs surveyed the X API for write-scope, but operator's "never auto-post" rule (hard constraint in CLAUDE.md) superseded the exploration — and sn9 purged the Twitter Agent entirely, making auto-posting moot. s3b CONTEXT.md + RESEARCH.md retained for historical record only.
- [2026-04-20 / quick 260420-sn9]: One Rule-3 auto-fix in prod DB purge transaction — a single `instagram` row (orphan from `260419-lvy` purge) had `related_id` pointing at a `twitter` row, blocking the DELETE via FK violation. UPDATE-NULL'd that one `related_id` in the same transaction; instagram row itself left in place (scope-bounded).
- [2026-04-21 / quick 260421-eoe]: Content Agent architecture pivoted from single-agent monolith to **7-sub-agent system with cron-less review service**. Mid-workflow Phase C pivot — operator rejected the original orchestrator-cron design in favor of `content_agent.py` as a pure library module exposing `fetch_stories()` + `review()` for sub-agents to call into. Sub-agents are self-contained per-tick (no cross-tick `draft_content IS NULL` handoff), eliminating a proposed `research_context` JSONB migration and a 30-min gate-latency concern. Scheduler grew from 3 jobs → 8 jobs (morning_digest + 7 sub-agents), each sub-agent on its own 2h `IntervalTrigger` staggered across `[0,17,34,51,68,85,102]` min offsets within the 2h window. Lock IDs 1010–1016 allocated. Frontend combined `/content` route deleted → replaced by single dynamic `/agents/:slug` route driven by `CONTENT_AGENT_TABS` config. The per-format drafting logic was preserved verbatim (functional parity — no new prompts, no new content types, no schema changes); operator plans to iterate per-sub-agent later. System description updated: "single-agent system" → "7 sub-agent system + Content Agent review service."
- [2026-04-21 / quick 260421-eoe]: During T02 extraction, discovered pre-split `content_agent.py` used one combined `_research_and_draft` function rather than 5 separate `_draft_<type>` helpers the plan assumed. Resolved by creating shared `scheduler/agents/content/__init__.py::run_text_story_cycle(agent_name, content_type, draft_fn)` helper that the 5 text sub-agents (breaking_news, threads, long_form, quotes, infographics) delegate through; extracted 5 format-specialized Sonnet prompts preserving functional parity. `video_clip` + `gold_history` retain bespoke flows (video_clip uses X API read, gold_history uses curated source + `gold_history_used_topics` Config dedup); both still call `content_agent.review()` inline. Net: every persistence path goes through `review()` as required.
- [2026-04-21 / quick 260421-eoe]: Post-verifier follow-up commit removed defunct `content_agent_interval_hours` + `gold_history_hour` keys from `scheduler/seed_content_data.py` `CONFIG_DEFAULTS` list + added DELETE clause to the script's DB-migration docstring (marked retired by this task). Dead-code follow-ups flagged but not removed in this task: `content_agent.py::breaking_window_hours` parameter (no caller), orphan `ContentPage.tsx` + test (after `/content-review` route removal), `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` config keys (pending post-deploy confirmation of zero reads).
- [2026-04-21 / quick 260421-k9z / prod DB migration]: Executed the canonical DELETE from `scheduler/seed_content_data.py` docstring against prod Neon via local `.env` DATABASE_URL + asyncpg (same pattern as sn9). Transaction-wrapped with pre/post counts and auto-rollback on post-count non-zero. **Pre-counts:** `content_agent_interval_hours`=1, `gold_history_hour`=1, `content_agent_max_stories_per_run`=1, `content_agent_breaking_window_hours`=1 (4 rows present). `content_agent_schedule_hour` + `content_agent_midday_hour` were already absent (eoe follow-up commit `047ef1d`'s DELETE clause had covered them or they never existed in prod). **Post-counts:** all 6 keys at 0. **Net:** 4 rows deleted in a single transaction. No reinsert path remains — `worker.py::upsert_agent_config` no longer includes the k9z-removed overrides, and `seed_content_data.py::CONFIG_DEFAULTS` no longer lists any of the 6 keys. Scheduler is already running commit `8ce8210` (post-k9z), so next startup's upsert_agent_config will NOT re-add the keys.
- [2026-04-21 / quick 260421-k9z]: Dead-code follow-up to eoe — removed the three items flagged as open follow-ups. **Scope expanded on closer grep:** `breaking_window_hours` parameter lives inside an entire orphan-island — `select_qualifying_stories`, `select_top_story`, and `_is_within_window` are all defined in `content_agent.py` but zero callers exist in live scheduler code or tests (old references only in `.claude/worktrees/` scratch dirs). All three functions deleted as a unit (−81 lines). `classify_format_lightweight` (L247-288, live) preserved between the two orphan blocks via two-block surgical deletion; `from datetime import datetime, timezone` import retained (~18 other live call sites). **Deviation #6 in eoe SUMMARY was stale** — it claimed `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` were still read by `fetch_stories()`, but post-split `fetch_stories()` takes no parameters and consults no config. Both keys were zombie writes: `seed_content_data.py::CONFIG_DEFAULTS` (L42-43) + `worker.py::upsert_agent_config` overrides (L295-296) wrote on every startup but no code read. Both write sites removed; DELETE migration docstring in `seed_content_data.py` extended from 4 → 6 retired keys (operator-run one-off against prod). Frontend: `ContentPage.tsx` + `ContentPage.test.tsx` deleted (only self-import was the test itself); stale 3-line orphan comment in `App.tsx:24-26` replaced with single-line "Other pages." comment. **Validation:** scheduler ruff clean + pytest 74/74 (eoe baseline); backend ruff clean + pytest 69 passed / 5 skipped (unchanged); frontend eslint clean + vitest 52/52 across 8 files (eoe baseline 61/9; delta of −9 tests / −1 file exactly = deleted ContentPage.test.tsx suite); tsc + vite build green (541 kB → 163 kB gzip unchanged).

### Pending Todos

None yet.

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260407-n9t | Content agent schedule fix (6am/12pm PST) + enhanced WhatsApp notification with titles | 2026-04-07 | 9bc17e1 | | [260407-n9t](./quick/260407-n9t-content-agent-schedule-fix-and-enhanced-/) |
| 260407-neq | Switch content agent from two cron jobs to single interval trigger every 2 hours | 2026-04-07 | 8b1e22e | | [260407-neq](./quick/260407-neq-switch-content-agent-to-interval-every-2/) |
| 260409-qe8 | Fix Twitter engagement gate (None impression_count skips views check) + content agent non-ISO date fallback | 2026-04-09 | aecc81b | | [260409-qe8](./quick/260409-qe8-fix-twitter-engagement-gate-and-content-/) |
| 260419-ko3 | Fix 8 pre-existing test failures (backend + scheduler + frontend) | 2026-04-19 | 0cd0432 | | [260419-ko3](./quick/260419-ko3-fix-8-pre-existing-test-failures-across-/) |
| 260419-l5t | Post-v1.0.1 health-check cleanup: env.example hygiene, ruff+eslint lint-zero, JWT_SECRET length assertion | 2026-04-19 | 8bfdfea | Verified | [260419-l5t](./quick/260419-l5t-post-v1-0-1-health-check-cleanup-env-exa/) |
| 260419-lvy | Full purge Instagram agent + content queue run-grouping (deprecated Phase 6) | 2026-04-19 | b2ff329 / accf735 / 2eb9125 / 4ab94e7 | Verified | [260419-lvy](./quick/260419-lvy-full-purge-instagram-agent-content-queue/) |
| 260419-n4f | Content agent relevance cleanup — Bloomberg commodities swap, drop Investing.com, sharpen prompt, two-bucket gold gate, restore 7.0 threshold | 2026-04-19 | 502ae3e / 010926d / 2cfc9cb | Verified | [260419-n4f](./quick/260419-n4f-content-agent-relevance-cleanup-swap-blo/) |
| 260419-r0r | Long_form 400-char minimum floor + sharpen thread vs long_form prompt (thread=fact-rich, long_form=article-style) | 2026-04-20 | f30dd44 / 14caedc | Verified | [260419-r0r](./quick/260419-r0r-enforce-400-char-minimum-floor-on-long-f/) |
| 260419-rqx | Content agent tuning — 3h cadence, 0.40 recency weight, top-5 Haiku format-first pipeline, listicle rejection in gold gate | 2026-04-20 | 663d6d8 / 7fb5517 / af37d3c | Verified | [260419-rqx](./quick/260419-rqx-content-agent-tuning-pass-cadence-top-5-/) |
| 260419-si2 | Display Gemini-rendered images inline on content queue cards with role label, Dialog enlarge, and fetch+blob Download | 2026-04-20 | 1ff7f07 / 8a2d991 | Verified | [260419-si2](./quick/260419-si2-display-rendered-images-on-dashboard-app/) |
| 260419-t78 | Replace Gemini Imagen infographic rendering with Claude chart_spec + Recharts 2.15.4 (a16z-style) — Pydantic schema, Node renderer (resvg-js), Python ChartRendererClient, infographic branch migrated. End-to-end smoke test on 2026-04-20 uncovered 5 renderer bugs (download-fonts zip paths, SVG xmlns missing, greedy regex swallowed Legend div, Recharts Legend rendered outside chart svg, ComparisonTable 2-col overlap) + Dockerfile sha256 pinned + dead _build_prompt branches removed. All 8 chart types render valid PNGs locally. **2026-04-20 22:00 UTC day-after check:** 9 infographic bundles produced in the last 24h had old-schema draft_content (no `charts` field) and 0 rendered images — root cause: Railway scheduler service's Root Directory is `scheduler/`, but the t78 Dockerfile used `scheduler/...` prefixed COPY paths that resolved to `scheduler/scheduler/...` → every t78 deploy failed at `[7/17] COPY scheduler/chart_renderer/` since the first push. Active Railway deploy was stuck 18h on `quick-si2`. Fix pushed as `bbc6e6a` — stripped `scheduler/` prefix from 7 COPY instructions. **2026-04-20 ~22:30 UTC a16z style pass (`3937f34`):** extracted shared `theme.js` (cream BG, deep-navy/teal palette, 110px editorial header), rewrote all 7 components for uniform navy bars + value labels at tips + horizontal-only gridlines + source-left/wordmark-right attribution. Font pivot: Playfair Display (variable-wght TTF, resvg-js 2.6.x fails to resolve wght axis) → DM Serif Display (static TTF, heavier display face, better a16z match). Font loader pivot: `fontBuffers` (Uint8Array — silently drops family name on multi-font loads) → `fontFiles` (path-based, fontdb parses `name`-table family correctly). All 9 chart types verified locally with clean serif titles. | 2026-04-20 | 898909b / 8d0ea80 / beba088 / de31622 / be1944c / 74a6e67 / 58c1d02 / bbc6e6a / 3937f34 | Verified (local) — Railway rebuild in flight with combined Dockerfile + style fixes | [260419-t78](./quick/260419-t78-replace-gemini-imagen-infographic-render/) |
| 260420-lw4 | Tighten Twitter agent filters: watchlist-only ingestion, literal GOLD_KEYWORDS match, 50-like floor (both paths), Gold Telegraph (@GoldTelegraph_) universal bypass of topic filter and engagement gate | 2026-04-20 | 18c0701 / b6cee40 | Pending Railway deploy + DB spot-check (Task 3) | [260420-lw4](./quick/260420-lw4-tighten-twitter-agent-filters-watchlist-/) |
| 260420-mfy | Pivot infographic+quote formats off on-platform rendering → claude.ai artifact prompts. Ripped `scheduler/chart_renderer/` (Node.js, fonts, Dockerfile layers), `ChartRendererClient`, Gemini/Imagen quote renderer, `ChartSpec`/`BundleCharts`/`render_bundle_job`/`_enqueue_render_job`, `RenderedImage`/`rendered_images`/`RerenderResponse` Pydantic + API + frontend types, and all image-display UI (Dialog enlarge + fetch+blob download from si2). Added `scheduler/agents/brand_preamble.py` with 1267-char `BRAND_PREAMBLE` baking in Seva look (cream #F0ECE4, navy #0C1B32/#1E3A5F/#4A7FA5, gold #D4AF37, DM Serif Display titles, Inter body, SEVA MINING wordmark, 1200x675 HTML artifact). Content agent infographic+quote branches now emit `{suggested_headline, data_facts, image_prompt}` (prompt = BRAND_PREAMBLE + type-specific direction). Frontend `InfographicPreview.tsx` + `QuotePreview.tsx` rewritten to three copy-buttoned text blocks; legacy (pre-mfy) bundles show placeholder. Dockerfile slimmed to 28-line pure-Python. 106 scheduler tests + 66 backend tests + 73 frontend tests pass, ruff + eslint clean, tsc+vite build green. | 2026-04-20 | 5514db0 / be2f7a7 / d589c68 / 8a4fcb1 / 76f3769 / df274c3 | Verified | [260420-mfy](./quick/260420-mfy-pivot-infographic-quote-post-formats-off/) |
| 260420-nnh | Content agent no-specific-miner filter — bolt company-specific rejection onto existing gold gate (single Haiku call, structured JSON return). is_gold_relevant_or_systemic_shock() now returns dict {keep, reject_reason, company}. Allowlists financial-institution citations (Goldman, WGC, JPM, Fed) as sources not subjects. Logs "ContentAgent: rejected story — primary subject is specific miner (Company)." Inverts two rqx-era tests (Newmont earnings + Barrick M&A → now reject). Adds 12 new fixture tests. 62 scheduler tests pass, ruff clean. | 2026-04-20 | 15df673 | Pending Railway verification | [260420-nnh](./quick/260420-nnh-content-agent-reject-stories-primarily-a/) |
| 260420-oa1 | Real-time market snapshot injection into content agent drafter — kills gold-price hallucinations. New `scheduler/services/market_snapshot.py` with `fetch_market_snapshot()` (httpx-direct, parallel `asyncio.gather`) pulling FRED macro (DGS10/DFII10/DFF/CPIAUCSL with YoY + observation date) and metalpriceapi.com Basic ($8.99/mo, gold XAU + silver XAG with reciprocal-rate inversion). DXY dropped (DTWEXBGS ≠ ICE DXY; yfinance unreliable). New `MarketSnapshot` ORM model (mirror in backend/) + Alembic migration 0007 with `market_snapshots` table (status `ok|partial|failed`, JSONB `data`, `fetched_at` index). Fetch runs once per `ContentAgent.run()` before `_run_pipeline`, snapshot stored on instance and injected at TOP of system_prompt (before "You are a senior gold market analyst") in all 3 Sonnet drafters: `_research_and_draft`, `_draft_video_caption`, `_draft_quote_post`. Hard instruction forbids BOTH current AND historical invented numbers verbatim — operator's "$3,500/oz in 2025" bug was a historical fabrication, not current-spot. Fail-open at every layer: missing API key → WARNING + skip; source 500/429/parse → per-source `[UNAVAILABLE]`; DB write failure → ERROR + continue in-memory; pipeline exception → fallback snapshot. 12 unit tests (reciprocal rate `1/0.000426≈$2,347.42`, FRED `.` sentinel, CPI YoY from 12-month spread, populated + fallback render blocks) + 8 integration tests (call_count=1, snapshot in all 3 drafters, DB failure non-fatal, nnh gold-gate dict contract regression guard). 138/138 scheduler tests pass, ruff clean. Adds FRED_API_KEY + METALPRICEAPI_API_KEY to .env.example + pydantic-settings. **Verification passed (10/10 must-haves).** New monthly cost: +$8.99 (metalpriceapi Basic). | 2026-04-21 | b28780b / a3738e3 / 0dfdc28 | Verified | [260420-oa1](./quick/260420-oa1-real-time-market-snapshot-injection-to-s/) |
| 260420-p8h | Extend `scheduler/worker.py::_validate_env()` to print startup status for `FRED_API_KEY` + `METALPRICEAPI_API_KEY`. New `market_data` category (distinct from existing `critical` and `optional`) logs INFO `SET ✓` when present, WARNING "MISSING — drafter will fall back to [UNAVAILABLE] for this source" when absent. Rationale: missing market keys silently degrade draft accuracy via fail-open fallback; surfacing at WARNING in startup logs lets operator spot Railway env typos immediately instead of waiting up to 3h for next content_agent cron cycle. No test changes (pure I/O logging), ruff clean. | 2026-04-21 | 352b2d4 / b510a92 | Verified (local) — Railway rebuild will exercise | [260420-p8h](./quick/260420-p8h-add-fred-metalpriceapi-env-key-status-li/) |
| 260420-pwh | Fix 2 prod bugs surfaced during oa1/p8h verification — both silently degraded drafter output via fail-open. **Bug 1 (oa1 regression):** `MetalpriceAPIFetcher.fetch()` applied `1.0 / xau` to `rates.USDXAU`, writing raw reciprocal (~$0.000208/oz) to `market_snapshots.data.gold_usd_per_oz` — oa1 plan's "reciprocal rate trap" docstring was inverted from the actual API shape. Live probe confirmed metalpriceapi with `base=USD` returns `USDXAU=4816.39` as **direct USD per oz** (the reciprocal `XAU=0.000208` is the alternate shape). Fix: removed `1.0 /` inversions at `scheduler/services/market_snapshot.py:210-214`, rewrote class docstring (lines 192-193), flipped `METALS_GOOD` fixture and test assertions (`4816.39`/`79.58` from `0.000426`/`0.035842`), renamed test to `test_metalpriceapi_direct_price_read`. **Bug 2:** `claude-3-5-haiku-latest` deprecated, returns `NotFoundError` on every call. Silently disabled (a) gold gate → specific-miner rejection (nnh) never fired, (b) format classifier → every story defaulted to `thread`. Live probe verified `claude-haiku-4-5` alias resolves to `claude-haiku-4-5-20251001`. Swapped 4 locations in `content_agent.py` (lines 276/287/503/1535), 1 in `seed_content_data.py:46`, 22 test-mock replacements. Prod `config` table has no `content_gold_gate_model` row → code default (now swapped) is authoritative; no DB UPDATE required. **Bug 3 (in-flight discovery):** Haiku 4.5 wraps JSON in ```` ```json ... ``` ```` code fences even when prompt asks for bare object. `json.loads()` failed on every call → gate silently fail-opened → specific-miner rejection still didn't fire post-Bug-2-fix. Added fence-stripping preprocess at `content_agent.py:558-566` (mirrors pattern already used in 3 other locations in same file) + 3 new tests. **Live verification:** fresh `content_agent` run shows zero NotFoundError/JSONDecodeError, 80+ Anthropic 200 OKs, gate rejected `Kalgoorlie Gold Mining` as specific-miner + one generic as not-gold-relevant. DB `market_snapshots` latest row: `gold_usd_per_oz: 4808.86` + `silver_usd_per_oz: 79.34` (matches live metalpriceapi $4,816.39/$79.58 within normal intraday spread). 85 scheduler tests pass, ruff clean. gsd-verifier verdict: PASSED. | 2026-04-21 | 87878e1 / 50720f1 / b2bce6f / 3525c47 | Verified | [260420-pwh](./quick/260420-pwh-fix-metalpriceapi-reciprocal-inversion-u/) |
| 260420-r18 | Two content-pipeline cleanups triggered by pwh post-verification dashboard inspection. **Task A (frontend field-name fix):** Long-form dashboard bundles showed "No draft content available" despite having 1500+ chars of real prose in DB. Root cause: `LongFormPreview.tsx` read `d.post_text` but backend drafter at `content_agent.py:1261` wrote the field as `post` (validator at 1321 also reads `post`). Fixed frontend-side rather than backend (existing DB rows already have `post` key — frontend fix is retroactive with zero migration; backend fix would only help future rows). Changed `LongFormPreview.tsx:6` from `post_text?: string` to `post?: string` + line 11 from `d.post_text` to `d.post`. Updated 1 test fixture at `ContentDetailModal.test.tsx:159`. Net change: +3/-3 across 2 files. **Task B (remove X as quote-post source):** Per user directive "for quotes, I don't want it to pull from X. Quotes should only be sourced from reputable articles." Deleted `QUOTE_ACCOUNTS` constant (lines 84-90, PeterSchiff/jimrickards/etc.), `_search_quote_tweets()` method (X API `>=10 like` filter, lines 944-1022), and the quote-tweet call + for-loop in `_run_pipeline` (lines 1864-1921). Added format-selection prompt enhancement at line 1152: "Prefer 'quote' format when the article contains a verbatim, attributed statement from a credible named figure (bank analyst, central bank official, WGC, IMF, etc.)." Preserved: `_draft_quote_post()` method (still used when Claude picks quote format from article path), `_draft_video_caption`, `VIDEO_ACCOUNTS`, `_search_video_clips`. Net change: +3/-151 in content_agent.py (file now 1845 lines). **DB cleanup (user-authorized one-off DELETE):** 4 stale pre-fix bundles — 2× "Gold prices news" long_form + 2× Kalgoorlie multi-miner thread — removed via `railway run` DELETE by UUID. **Live verification via `railway run`:** fresh content_agent completed with 0 quote-tweet log lines, 0 NotFoundError/JSONDecodeError, Kalgoorlie rejected by specific-miner gate, 2 new bundles both from reputable article sources (Market Index + Investing.com Nigeria), 0 new quote bundles with `source_name` matching old QUOTE_ACCOUNTS. 141 scheduler tests pass, 73 frontend tests pass, ruff + eslint clean, vite build success (224ms). gsd-verifier verdict: PASSED (6/6 truths, 10/10 checks). | 2026-04-20 | fb53254 / aa7b061 / e90d1c2 | Verified | [260420-r18](./quick/260420-r18-long-form-dashboard-field-name-fix-remov/) |
| 260420-s3b | Twitter agent autonomous auto-posting exploration (CONTEXT + RESEARCH only — no PLAN.md written). Surveyed X API v2 `POST /2/tweets` write scope requirements + OAuth 2.0 PKCE for bot-style posting. **Superseded and cancelled** by `260420-s3b` being logically incompatible with operator's hard "never auto-post" rule in CLAUDE.md, and then rendered moot entirely by `260420-sn9` which purged the Twitter Agent. Retained docs: `260420-s3b-CONTEXT.md` + `260420-s3b-RESEARCH.md` as historical record of the rejected direction. | 2026-04-20 | — | Cancelled (superseded by 260420-sn9) | [260420-s3b](./quick/260420-s3b-twitter-agent-autonomous-auto-posting-wi/) |
| 260421-eoe | Split monolithic Content Agent (~2000 lines) into 7 per-type sub-agents under `scheduler/agents/content/` + cron-less Content Agent review service. **Phase C architecture** (operator pivoted mid-workflow from original "Content Agent as orchestrator cron" design): `content_agent.py` loses its cron entirely, shrinks to 1040 lines exposing only `fetch_stories()` (shared SerpAPI+RSS ingestion with 30-min in-memory cache), `review(draft)` (Haiku compliance gate), and `classify_format_lightweight` helper. Sub-agents are self-contained within a single tick: fetch via shared cache → filter to own content_type → draft via Sonnet → call `content_agent.review()` inline → INSERT `content_bundles` row with `compliance_passed` set. **Scheduler 8-job topology:** `morning_digest` (existing) + 7 sub-agents on 2h `IntervalTrigger` staggered `[0, 17, 34, 51, 68, 85, 102]` min offsets, advisory lock IDs `1010`–`1016` (breaking_news/threads/long_form/quotes/infographics/video_clip/gold_history in order); old `content_agent` (lock 1003) + standalone `gold_history_agent` (lock 1005) crons retired. `scheduler/agents/gold_history_agent.py` deleted — folded into `scheduler/agents/content/gold_history.py`. Pool bumps: `scheduler/database.py` 3→15, `backend/app/database.py` 5→15. **Backend:** `/queue?content_type=<type>` filter added via JSONB subquery on `draft_items.engagement_snapshot->>'content_bundle_id'` (no FK — existing link pattern). **Frontend:** combined `/content` route + `PlatformQueuePage.tsx` deleted → replaced by single dynamic `<Route path="/agents/:slug">` driven by `CONTENT_AGENT_TABS` config array (7 entries in operator-priority order: Breaking News → Threads → Long-form → Quotes → Infographics → Gold Media → Gold History); Sidebar + `AgentRunsTab.AGENT_OPTIONS` both rewired config-driven; default redirect `/` → `/agents/breaking-news`; `useQueueCounts` now per-content-type via `useQueries`. **Docs:** CLAUDE.md opening paragraph + 2026-04-20 historical note's scheduler-job sentence + Stack constraint bullet updated to reflect 8-job + cron-less Content Agent model; 2026-04-19 lvy note and rest of 2026-04-20 sn9 note preserved verbatim. **No DB migrations** (all 7 content_type enum values already existed; no schema touches). **Shared helper:** `scheduler/agents/content/__init__.py::run_text_story_cycle(agent_name, content_type, draft_fn)` factors the 5-text-sub-agent common path (breaking_news, threads, long_form, quotes, infographics); video_clip + gold_history have bespoke flows (video_clip skips fetch_stories, uses X API read via existing `_search_video_clips` tweepy path; gold_history skips fetch_stories, uses curated historical source + `gold_history_used_topics` Config dedup gate). **Validation:** scheduler ruff clean + pytest 74 passed; backend ruff clean + pytest 69 passed / 5 skipped (pre-existing); frontend eslint clean + vitest 61 passed + `npm run build` green (541 kB → 163 kB gzip). gsd-verifier verdict: **PASS** (22/22 goal-backward invariants). Post-verifier follow-up commit: removed defunct `content_agent_interval_hours` + `gold_history_hour` keys from `scheduler/seed_content_data.py` + added DELETE clause to its DB-migration docstring. **Open follow-ups (dead code, non-blocking):** `content_agent.py::breaking_window_hours` parameter no longer passed by any caller; `ContentPage.tsx` + test orphaned after `/content-review` route removal; `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` config keys if confirmed unread post-deploy. | 2026-04-21 | 2fe94bf / a247cd1 / 762b08b / 979b69c / 2e43fd2 / f25faec / d20f515 / d7613b4 / 45dcbc6 / a8d512e / 047ef1d | Verified | [260421-eoe](./quick/260421-eoe-split-content-agent-into-7-sub-agents-or/) |
| 260421-k9z | Dead-code cleanup follow-up to 260421-eoe — removed three items flagged in eoe's "Dead code flagged" section. **Scheduler:** deleted orphan-island `select_qualifying_stories` + `select_top_story` + `_is_within_window` from `content_agent.py` as a unit (`breaking_window_hours` param dies with the island; `classify_format_lightweight` preserved via two-block surgical deletion); removed zombie config keys `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` from BOTH `seed_content_data.py::CONFIG_DEFAULTS` AND `worker.py::upsert_agent_config` overrides (write-only post-eoe, zero readers anywhere); extended seed-script DELETE migration docstring from 4 → 6 keys for operator one-off. **Frontend:** deleted orphan `ContentPage.tsx` (342 lines) + `ContentPage.test.tsx` (205 lines); replaced stale 3-line comment in `App.tsx:24-26` with single-line "Other pages." comment. **Net:** −550 LOC across scheduler (−81 content_agent.py, ±8 seed_content_data.py, ±7 worker.py) + frontend (−547 ContentPage + test + ±4 App.tsx). scheduler ruff clean + pytest 74/74 (eoe baseline); backend ruff clean + pytest 69/5 (unchanged); frontend eslint clean + vitest 52/52 (eoe baseline 61/9, delta exactly matches deleted ContentPage suite); tsc + vite build green. No behavior change. **Prod DB migration executed:** transaction-wrapped DELETE against Neon deleted 4 retired rows (`content_agent_interval_hours`, `gold_history_hour`, `content_agent_max_stories_per_run`, `content_agent_breaking_window_hours` → 0 each); 2 other keys in the canonical list (`content_agent_schedule_hour` + `content_agent_midday_hour`) were already absent from earlier cleanups. Post-count verification confirmed all 6 keys at 0. No reinsert path remains in live code. | 2026-04-21 | b8aec6e / 704d7b3 | Verified + prod migrated | [260421-k9z](./quick/260421-k9z-clean-up-3-dead-code-items-flagged-in-eo/) |
| 260421-mos | Quotes sub-agent daily 12pm Pacific + reputable-source whitelist + top-2 cap + drafter quality gate. **Scheduler topology:** `scheduler/worker.py::CONTENT_SUB_AGENTS` (7 entries on IntervalTrigger) split into `CONTENT_INTERVAL_AGENTS` (6 entries, same tuple shape from m9k: `(job_id, run_fn, name, lock_id, offset, interval_hours)`) + `CONTENT_CRON_AGENTS` (1 entry — `sub_quotes`; shape: `(job_id, run_fn, name, lock_id, hour, minute, timezone)`). Added `from apscheduler.triggers.cron import CronTrigger` and a second registration loop in `build_scheduler()` for cron agents. Startup log line reports both interval + cron counts. `JOB_LOCK_IDS` unchanged (8 keys, sub_quotes still 1013) — only the schedule shape moved. **Shared pipeline:** `scheduler/agents/content/__init__.py::run_text_story_cycle` gains two optional kwargs `max_count: int \| None = None, source_whitelist: frozenset[str] \| None = None`. Filter logic inserted AFTER the format filter + BEFORE the candidate loop — whitelist first (case-insensitive substring match on `story["source_name"]`), then max_count cap (sort by `story["published_at"]` desc, take top N). Both filters log stage counts. Defaults=None → zero behavior change for the 4 other sub-agents using this helper (breaking_news, threads, long_form, infographics). **Quotes wiring:** `scheduler/agents/content/quotes.py` adds module-level `REPUTABLE_SOURCES: frozenset[str]` with 27 patterns across tier-1 financial (reuters, bloomberg, wsj, ft, barron, marketwatch, cnbc, economist, financial post), gold-specialist (kitco, mining.com, mining journal, northern miner, gold hub, mining weekly), and institutional (WGC, IMF, BIS, Fed, ECB, BoE, BoJ, PBoC, S&P, Moody's). `run_draft_cycle` passes `max_count=2, source_whitelist=REPUTABLE_SOURCES`. `_draft` prompt extended with 4-criterion quality bar (speaker credibility — named senior role; substance — specific number/timeframe/catalyst/contrarian view; freshness — current article; clarity — self-contained claim) + reject-path JSON option `{"reject": true, "rationale": "..."}`. Return logic handles reject BEFORE parsing draft_content — returns None, which triggers the existing stub-bundle branch in run_text_story_cycle (ContentBundle with `compliance_passed=False`, no draft_content, no draft_item) — same shape as a JSON parse failure, treated identically downstream. **Tests (+8 total, 74→82):** `test_worker.py` — `CONTENT_SUB_AGENTS→CONTENT_INTERVAL_AGENTS` import rename; `test_sub_agents_total_seven` (renamed, asserts `len(INTERVAL) + len(CRON) == 7`); offsets list trimmed to `[0,17,34,68,85,102]` (51 gone with sub_quotes); `test_sub_agent_lock_ids` iterates both lists; new `test_quotes_is_daily_cron` asserts 1013/12/0/"America/Los_Angeles". `test_quotes.py` — +3: `test_reputable_sources_set_populated`, `test_draft_returns_none_on_reject`, `test_run_draft_cycle_passes_filters`. New file `scheduler/tests/test_content_init.py` — 4 tests for run_text_story_cycle filter logic (whitelist filters stories, max_count caps candidates, both no-op paths preserved). **Rule 3 deviations:** (1) worktree spawned from k9z HEAD `aaac399` — executor rebased onto m9k HEAD `3fa7212` before editing since plan treated m9k's 6-tuple schema + 10s buffer as prerequisite; (2) `America/Los_Angeles` appears 7× in worker.py (1 tuple entry + 1 log format + 5 doc/comment references — all inert; plan validation accepted ≥1); (3) removed unused MagicMock import from new test_content_init.py per ruff F401. **Gates:** scheduler `uv run ruff check .` clean; `uv run pytest -x` 82 passed (baseline 74 on m9k). All 9 validation greps pass. **No DB migrations, no frontend changes, no changes to the other 6 sub-agents.** Single atomic implementation commit `4dbeab3` on branch `quick/260421-mos-quotes-daily-noon-reputable` (sits on top of m9k commits `c5d32f3`+`3fa7212`). **Post-deploy expectation (operator confirms on Railway):** startup log reports both counts; next 12:00 America/Los_Angeles fires sub_quotes (DST-aware — handles PST/PDT automatically); first-fire logs show reputable filter + max_count cap funnel counts + 0-2 quote drafts OR per-story reject rationale; /agents/quotes page shows "Pulled from agent run at 12:00 PM · {date}" within seconds OR stays empty 24h if no quote met the bar. Existing pending Quote drafts in prod DB left untouched per user directive. | 2026-04-21 | 4dbeab3 | Verified (local) — pending Railway deploy | [260421-mos](./quick/260421-mos-quotes-sub-agent-daily-12pm-pacific-repu/) |
| 260422-ep9 | Market snapshot gains temporal anchors (gold 24h high/low + prior close + 52w high/low + at_52w_high + tracking_since) to kill "hitting highs" drafter hallucinations. **Origin bug:** 2026-04-22 breaking_news draft claimed `"GOLD whipsaws $100+ intraday"` + `"gold and equities hitting highs SIMULTANEOUSLY today"` — gold actually gave back Trump Taco rally gains that day. The existing `market_snapshot` from oa1 injected current spot only, with no temporal anchor to catch directional claims. **Changes (`scheduler/services/market_snapshot.py` only):** (1) Extended `MarketSnapshot` TypedDict with 7 new Optional fields — `gold_24h_high`, `gold_24h_low`, `gold_prior_close`, `gold_52w_high`, `gold_52w_low`, `at_52w_high: bool \| None` (strict equality: `current_spot >= gold_52w_high`), `tracking_since: date \| None`. (2) New private helper `_compute_gold_anchors(session)` runs a single SQL window query (MAX/MIN over 24h + 52w + prior_close as `ORDER BY fetched_at DESC LIMIT 1 OFFSET 1` + `MIN(fetched_at)` for `tracking_since`) against `market_snapshots` table filtered to `status IN ('ok', 'partial')`. Uses `cast(MarketSnapshotORM.data["gold_usd_per_oz"].astext, Float)` because the price is stored inside JSONB `data`, not a top-level column. (3) `fetch_market_snapshot(session)` now calls `_compute_gold_anchors(session)` **BEFORE** `session.add(snapshot_row)` to prevent the in-flight row from contaminating `prior_close`. (4) `render_snapshot_block` gains 2 new lines: `"- Gold 24h range: $X - $Y/oz (prior close: $Z/oz)"` + `"- Gold 52w range: $X - $Y/oz (at_52w_high: true)"` with graceful `[UNAVAILABLE — tracking since YYYY-MM-DD, N day(s) of history]` fallback labels. (5) `_HARD_INSTRUCTION` extended with second paragraph forbidding `"at highs / at a new high / at an all-time high / breaking out / at a record / hitting a new record"` unless `at_52w_high: true`, AND forbidding specific intraday move claims (`"$X+ intraday / whipsaws $X / X% move today"`) unless the 24h range supports the magnitude. **Locked decisions honored (from CONTEXT.md, 5/5):** (D-01) No backfill — graceful `[UNAVAILABLE]` during the ~1-year data-gap period; (D-02) Gold only, 5 anchors (silver deferred); (D-03) On-every-fetch SQL window query, no new columns, no migration, no new job; (D-04) Strict equality for `at_52w_high` (no fuzzy tolerance); (D-05) Extended `_HARD_INSTRUCTION` with explicit forbidden-phrase list. **Zero changes outside market_snapshot.py** — all 7 sub-agent drafters (breaking_news, threads, long_form, quotes, infographics, video_clip, gold_history) receive the enriched snapshot block verbatim via the existing `render_snapshot_block(market_snapshot)` call. **Backward-compat:** new TypedDict fields are all Optional with default None, so 6 downstream tests that patch `fetch_market_snapshot` with None/minimal dicts continue to pass. **Tests (+8 net, 90→98):** `_MockAnchorSession` helper + `_build_fred_metals_side_effect` helper + 8 new tests — compute-happy-path (seeded rows spanning 24h + 52w windows), compute-empty-history-returns-Nones, compute-partial-history-24h-ok-52w-unavailable, render-block-populated-shape (all 2 anchor lines present), render-block-fallback-labels (includes `tracking since` with plural/singular day wording), render-block-empty-first-snapshot, hard-instruction-regex (new forbidden phrases all present), strict-equality-at-52w-high (spot==52w_high→true, spot==52w_high*0.999→false). **Gates:** `uv run ruff check .` clean, `uv run pytest -x` 98 passed (baseline 90 + 8 new). All 7 validation greps from CONTEXT.md pass. **No DB schema changes, no Alembic migration, no new Config keys, no new API integrations, no frontend changes, no changes to sub-agent drafter code.** Single atomic implementation commit `55b1eb0` on branch `quick/260422-ep9-market-snapshot-temporal-anchors` (parent = `66225f7`, the vxg docs commit). **Post-deploy expectation:** next `fetch_market_snapshot` call on Railway logs show the new anchor values in the rendered snapshot block; drafters no longer produce claims like "hitting highs SIMULTANEOUSLY today" unless `at_52w_high: true` is emitted. During first ~24h post-deploy, 52w_high line will show `[UNAVAILABLE — tracking since 2026-04-21]` and drafters must hedge; 24h_range becomes real after ~24h of hourly-ish snapshots accumulate. | 2026-04-22 | 55b1eb0 | Verified (local) — pending Railway deploy | [260422-ep9](./quick/260422-ep9-enrich-market-snapshot-with-temporal-anc/) |
| 260422-vxg | 7-sub-agent cadence rebalance. **Topology:** 3 interval (BN=2h, Threads=4h, Long-form=4h, offsets `[0,17,34]`) + 4 cron at 12:00 America/Los_Angeles (sub_quotes/sub_infographics/sub_video_clip daily; sub_gold_history every-other-day via `day="*/2"`). Reverts m9k's BN=1h experiment to BN=2h; slows Threads + Long-form to 4h; moves Infographics/Video Clip/Gold History from 2h interval to daily cron; Quotes stays daily. **Schema upgrade:** `CONTENT_CRON_AGENTS` tuple shape changed from 7-field fixed (`hour, minute, timezone`) to 5-field dict-of-kwargs (`cron_kwargs: dict`), so heterogeneous cron patterns coexist cleanly. Registration loop calls `CronTrigger(**cron_kwargs)`. **Quotes tightening:** `max_count=2 → 1` in `scheduler/agents/content/quotes.py::run_draft_cycle` — surface only the single highest-signal reputable quote per day. **Gold Media (video_clip) analyst quality gate:** VIDEO_ACCOUNTS reordered + trimmed to `["Kitco", "CNBC", "Bloomberg", "BloombergTV", "ReutersBiz", "FT", "MarketWatch"]` (dropped PR-sourced BarrickGold/WorldGoldCouncil/Mining/Newaborngold); `_draft_video_caption` user_prompt extended with 3-criterion analyst quality bar (speaker credibility — named analyst/economist with tier-1 affiliation; substance — specific view or forecast; freshness — current commentary) + reject-path JSON option `{"reject": true, "rationale": "..."}` returning None; `run_draft_cycle` sorts clips by `created_at` desc (tz-aware fallback `datetime.min.replace(tzinfo=timezone.utc)` to avoid naive-vs-aware comparison crash), caps to `MAX_DRAFT_ATTEMPTS = 5`, breaks after first successful persist — goal is 1 analyst clip/day or none if nothing passes the bar. **Docstrings:** `breaking_news.py::L3` reverted "every 1 hour" (m9k) → "every 2 hours"; `gold_history.py` cadence line replaced with `day='*/2'` every-other-day noon PT reference; worker.py module docstring + startup log line updated for 3-interval/4-cron topology. Other sub-agent docstrings untouched — grep-first confirmed no cadence mentions. **Tests (+8 net, 82→90):** `test_worker.py` rescoped `test_sub_agents_total_seven` (3+4), `test_sub_agent_staggering` (`[0,17,34]`), `test_quotes_is_daily_cron` (dict-shape); added `test_interval_agents_cadences`, `test_cron_agents_count_four`, `test_cron_agents_use_dict_shape`, `test_gold_history_is_every_other_day`, `test_infographics_is_daily_cron`, `test_video_clip_is_daily_cron`. `test_quotes.py::test_run_draft_cycle_passes_filters` asserts `max_count==1`. `test_video_clip.py` added `test_video_accounts_reordered_analyst_first` + `test_draft_video_caption_returns_none_on_reject`. **Scope deferred per operator's "Option A" directive:** (1) Infographics historical-fallback drafter (Claude picker + `infographics_used_topics` Config key + drafter + integration, modeled on gold_history.py architecture); (2) Gold Media source switch to YouTube Data API / podcast RSS (current X API video source kept, tightened with analyst bar). **Gates:** scheduler `uv run ruff check .` clean; `uv run pytest -x` 90 passed (baseline 82 + 8 new tests, comfortably above the ≥87 gate). All 8 validation greps pass. **No DB migrations, no new Config keys, no frontend changes, no changes to breaking_news/threads/long_form/infographics drafter logic beyond docstrings.** Single atomic implementation commit `68b21d1` on branch `quick/260422-vxg-cadence-rebalance-7-sub-agents` (parent = `ddb5721`). | 2026-04-22 | 68b21d1 | Verified (local) — pending Railway deploy | [260422-vxg](./quick/260422-vxg-multi-agent-cadence-rebalance/) |
| 260421-m9k | Breaking News sub-agent hourly cadence + APScheduler offset=0 first-fire fix. **Change 1 (scheduling cadence):** `scheduler/worker.py::CONTENT_SUB_AGENTS` tuple shape extended from 5 → 6 fields by appending `interval_hours: int`. `sub_breaking_news` row set to `interval_hours=1`; other 6 sub-agents remain at `2`. Loop at L266-274 unpacks 6 fields and passes `IntervalTrigger(hours=interval_hours, ...)` with dynamic `name=f"{name} — every {interval_hours}h (offset +{offset}m)"`. Type annotation widened. **Change 2 (offset=0 first-fire fix):** `start_date = now + timedelta(minutes=offset) + timedelta(seconds=10)` — 10-second buffer guarantees `start_date > now` at `scheduler.start()` wall clock even when `offset=0`. Prevents APScheduler `IntervalTrigger.get_next_fire_time` from treating `start_date == now` as a past/missed schedule and pushing the first fire to `start_date + interval` (which had silently forced a 2h dark period on every Railway redeploy for Breaking News — no agent_run row for `sub_breaking_news` until 2h after boot). Explanatory 2-line comment added above the start_date line. **Docstring refreshes:** 5 worker.py docstring/comment references to "2h"/"every 2h" updated to note mixed cadence (BN hourly, other 6 on 2h); `breaking_news.py::L3` module docstring "every 2 hours" → "every hour". **Rule 3 deviation:** `scheduler/tests/test_worker.py::test_sub_agent_lock_ids` directly unpacked `CONTENT_SUB_AGENTS` as 5-tuple; extended unpacking pattern to 6 fields (no semantic change, still validates identical lock-ID mapping). 3 files modified (worker.py, breaking_news.py, test_worker.py) instead of 2 as plan predicted. Scheduler ruff clean + pytest 74/74 (matches pre-change baseline). Frontend + backend untouched. **Post-deploy expectation (operator confirms on Railway):** all 7 sub-agents fire within first offset window on next redeploy (no 2h dark for offset=0); `sub_breaking_news` logs agent_run rows hourly thereafter; "Pulled from agent run at {time}" header appears on /agents/breaking-news ~10s after scheduler boot. | 2026-04-21 | c5d32f3 | Verified (local) — pending Railway deploy | [260421-m9k](./quick/260421-m9k-breaking-news-sub-agent-hourly-cadence-o/) |
| 260420-sn9 | Full purge Twitter agent + trim Senior Agent to morning-digest-only. 5 atomic commits (one per layer), single-agent system end-state (Content only). **Task 1 — scheduler:** deleted `scheduler/agents/twitter_agent.py` + tests; trimmed `scheduler/agents/senior_agent.py` to `run_morning_digest()` only (removed dedup/expiry/queue-cap methods); `worker.py` `_make_job` no longer branches twitter/senior; scheduler jobs now content_agent + morning_digest + gold_history_agent; `JOB_LOCK_IDS` entries for twitter_agent/senior_agent removed; `tweepy[async]>=4.14` + `x_api_*` env vars + `_validate_env` critical `X_API_BEARER_TOKEN` check **preserved** (Content Agent video_clip pipeline is downstream consumer). **Task 2:** senior_agent trim commit — confined to that one file with tests. **Task 3 — backend:** `/config/quota` router endpoint removed; test fixtures migrated (`test_crud_endpoints.py` twitter watchlist fixtures → content; `test_queue.py::make_draft_item` default platform twitter → content; `test_content_bundles.py` rendered_images role twitter_visual → content_visual); `seed_mock_data.py` rewritten to content-only (removed 5 twitter + 5 instagram mock items, 2 content items kept, no cross-platform related_id wiring); model/schema comments updated to document Twitter purge; long comments reflowed to ≤100 chars for ruff E501. **Task 4 — frontend:** Platform type narrowed `'twitter' \| 'content'` → `'content'`; `QuotaResponse` + `getQuota()` removed; deleted ApprovalCard + PlatformTabBar + QueuePage + QuotaBar + WatchlistTab + associated tests; Sidebar/App.tsx/AgentRunsTab/KeywordsTab/ScoringTab/PlatformBadge/RelatedCardBadge narrowed to content; MSW handlers rewritten content-only; DigestPage stats grid `grid-cols-3` → `grid-cols-2` (Twitter tile removed); SettingsPage tabs 6 → 5 (Watchlists removed, Keywords now default); PlatformQueuePage both branches render ContentSummaryCard. **Task 5 — prod DB + docs:** Neon DELETEs via `railway run` inside single transaction — **177** draft_items `platform='twitter'` → **0**, **40** watchlists (all) → **0**, **8** config `twitter_%` keys → **0**; one Rule-3 auto-fix (NULL'd related_id on an orphan instagram row pointing at a twitter row from lvy-era data); Content row count preserved (955 before + 955 after). CLAUDE.md historical note appended for 2026-04-20, Project blurb updated to "single-agent." ROADMAP.md deprecation notice added, Phase 4/5 struck through, Phase 10 partial-deprecation banner, progress table updated. REQUIREMENTS.md: TWIT-01..14 + SENR-01..05 + SENR-08..09 struck through; SENR-06/07 retained (shipped via Phase 10). STATE.md: 2 new decision entries + this row. Pre/post DB counts recorded in SUMMARY.md. Verification: scheduler pytest 100/100, backend pytest 70 collected (66 passed / 5 skipped), frontend vitest 56/56, ruff + eslint clean, tsc + vite build green. | 2026-04-20 | 62ffda4 / be9cd8b / aeaf412 / c30e288 / 55100c9 | Verified | [260420-sn9](./quick/260420-sn9-full-purge-twitter-agent-trim-senior-to-/) |

### Blockers/Concerns

- [Phase 1]: Twilio WhatsApp templates must be submitted to Meta during Phase 1 — approval takes 1-7 business days. Confirm whether a WhatsApp Business account already exists for Seva Mining before starting Phase 1 planning.
- [Phase 1]: Verify Neon free tier connection limit before setting pool_size — may need adjustment from default 5/10.
- [Phase 7]: SerpAPI plan selection needed before Content Agent — 100 searches/mo on basic plan may be insufficient for daily multi-topic runs.
- [quick-mfy]: Railway to pick up infographic+quote prompt pivot on next scheduler rebuild. Post-deploy watch-list: (a) confirm scheduler build is pure-Python (no Node layers) — image size should be noticeably smaller than the t78 build, (b) next content_agent cron (every 3h) should produce infographic/quote bundles with `suggested_headline` + `data_facts` + `image_prompt` in draft_content (NOT the old `charts` array or Imagen prompt), (c) dashboard cards render three copy blocks with no broken image refs; legacy pre-mfy bundles show "Legacy format — regenerate" placeholder. Pre-mfy queue rows are stranded by design — operator dismisses via dashboard.
- [quick-nnh]: Railway spot-check on next content_agent cron (every 3h) — confirm (a) Haiku gate prompt returns structured JSON with `keep`/`reject_reason`/`company` keys, (b) company-specific stories (B2Gold, McLaren, Barrick, Newmont, Seva Mining patterns) emit `ContentAgent: rejected story <title> — primary subject is specific miner (<company>).` log line and do NOT reach the drafter, (c) macro/sector stories (Goldman/WGC/central-bank/CPI/ETF) still reach drafter and produce bundles.
- [quick-oa1]: **Operator action required before Railway deploy picks this up:** provision both API keys and add them to Railway scheduler service env vars. (a) `FRED_API_KEY` — register at https://fred.stlouisfed.org/docs/api/api_key.html (free, instant, email verification). (b) `METALPRICEAPI_API_KEY` — sign up at https://metalpriceapi.com/pricing and subscribe to Basic plan ($8.99/mo, 10k req/mo). Without these keys, drafter runs with `[UNAVAILABLE]` fallback (fail-open) — drafts still produce but fall back to qualitative language. Also verify on first post-deploy content_agent cron: (i) log line `ContentAgent: market snapshot fetched (status=ok)` with populated values, (ii) `market_snapshots` DB row inserted with non-null `data` JSONB, (iii) draft output references real current figures matching goldprice.org + FRED values at time of draft, (iv) any hallucinated historical claim like "gold hit $X in 2023" no longer appears.

## Session Continuity

Last session: 2026-04-22T17:55:00.000Z
Last activity: 2026-04-22 — Completed quick-260422-ep9 (market_snapshot temporal anchors + extended hard instruction) via `/gsd:quick --discuss` workflow. Single atomic implementation commit `55b1eb0` on branch `quick/260422-ep9-market-snapshot-temporal-anchors` (parent = `66225f7`, vxg docs commit). **Origin bug:** 2026-04-22 breaking_news draft hallucinated "GOLD whipsaws $100+ intraday" and "gold and equities hitting highs SIMULTANEOUSLY today" — gold actually gave back Trump Taco rally gains that day. Root cause: existing oa1 market_snapshot injected current spot only, no temporal anchor to catch directional claims; `_HARD_INSTRUCTION` permitted "near recent highs" hedge which drafter stretched to "hitting highs SIMULTANEOUSLY today". **Fix:** extended `MarketSnapshot` TypedDict with 7 new Optional fields (gold_24h_high/low, gold_prior_close, gold_52w_high/low, at_52w_high: bool|None via strict equality, tracking_since: date|None). New `_compute_gold_anchors(session)` helper runs a single SQL window query against `market_snapshots` table (MAX/MIN over 24h + 52w windows, prior_close via `ORDER BY fetched_at DESC LIMIT 1 OFFSET 1`, MIN(fetched_at) for tracking_since label) — uses `cast(MarketSnapshotORM.data["gold_usd_per_oz"].astext, Float)` because price lives inside JSONB `data`, not a top-level column. Query runs BEFORE `session.add(snapshot_row)` so prior_close reflects last-written snapshot, not the in-flight one. `render_snapshot_block` gains 2 new lines ("Gold 24h range: $X - $Y/oz (prior close: $Z/oz)" + "Gold 52w range: $X - $Y/oz (at_52w_high: true)") with graceful `[UNAVAILABLE — tracking since YYYY-MM-DD, N day(s) of history]` fallback during the ~1-year data-gap period since oa1 deployed 2026-04-21. `_HARD_INSTRUCTION` extended with explicit forbidden-phrase list: "at highs / at a new high / at an all-time high / breaking out / at a record / hitting a new record" unless `at_52w_high: true`, AND specific intraday move claims ("$X+ intraday / whipsaws $X / X% move today") unless the 24h range supports the magnitude. **Discussion phase** (`/gsd:quick --discuss`) surfaced + locked 5 decisions via AskUserQuestion — no backfill (graceful [UNAVAILABLE]), gold only (5 anchors, silver deferred), on-every-fetch SQL window query (no new columns/migration/job), strict equality for at_52w_high (no fuzzy tolerance), extended _HARD_INSTRUCTION with explicit forbidden-phrase list. Captured in CONTEXT.md with verbatim target snapshot-block shapes for populated/partial/empty cases. Planner caught critical ORM quirk (gold_usd_per_oz in JSONB `data` not top-level column) and embedded the correct SQLAlchemy cast pattern in the plan. Executor honored all 5 decisions + the 6 planner guidance points, wrote 8 new tests via `_MockAnchorSession` + `_build_fred_metals_side_effect` helpers. **Gates:** scheduler ruff clean, pytest 98/98 (baseline 90 + 8 new). All 7 validation greps from CONTEXT.md pass. Backward-compat preserved — 6 downstream tests that patch `fetch_market_snapshot` with None/minimal dicts still pass because new TypedDict fields are all Optional with default None. **Zero changes outside market_snapshot.py** — all 7 sub-agent drafters receive the enriched block verbatim. Branch pending FF merge into main + push to origin. **Post-deploy expectation:** next `fetch_market_snapshot` call logs show new anchor lines; drafters no longer produce "hitting highs SIMULTANEOUSLY today" hallucinations because `at_52w_high: false` AND explicit forbidden-phrase list AND qualitative-hedge fallback instruction all apply. First ~24h post-deploy: 52w range shows `[UNAVAILABLE — tracking since 2026-04-21]` and drafters must hedge; 24h range becomes real after ~24h of snapshots accumulate.

Prior activity: 2026-04-22 — Completed quick-260422-vxg (7-sub-agent cadence rebalance + quotes max=1 + Gold Media analyst quality bar). Single atomic implementation commit `68b21d1` on branch `quick/260422-vxg-cadence-rebalance-7-sub-agents` (parent = `ddb5721`, the mos docs commit). Topology went from 6 interval + 1 cron → 3 interval + 4 cron. Interval agents: BN=2h (reverts m9k 1h experiment — too much churn), Threads=4h, Long-form=4h, offsets trimmed to `[0, 17, 34]`. Cron agents (all 12:00 America/Los_Angeles): sub_quotes daily (unchanged from mos), sub_infographics daily (new), sub_video_clip/Gold Media daily (new), sub_gold_history every-other-day via `day="*/2"` (new). `CONTENT_CRON_AGENTS` tuple schema upgraded from 7-field fixed (hour/minute/timezone) → 5-field dict-of-kwargs (`cron_kwargs: dict`) so heterogeneous cron patterns coexist — registration loop calls `CronTrigger(**cron_kwargs)`. Quotes `max_count` 2→1 (surface only the single highest-signal reputable quote/day). Gold Media analyst quality gate: `VIDEO_ACCOUNTS` reordered/trimmed to 7 analyst-heavy handles (Kitco/CNBC/Bloomberg/BloombergTV/ReutersBiz/FT/MarketWatch), `_draft_video_caption` extended with 3-criterion quality bar (speaker credibility, substance, freshness) + reject-path JSON `{"reject": true, "rationale": "..."}` returning None, `run_draft_cycle` sorts clips desc by `created_at` (tz-aware fallback) and caps attempts at `MAX_DRAFT_ATTEMPTS=5` with break-after-first-persist. Docstring touches: `breaking_news.py::L3` reverted "every 1 hour" → "every 2 hours"; `gold_history.py` cadence line replaced with `day='*/2'` reference; `worker.py` module docstring + startup log line updated. +8 tests net (82→90): test_worker.py +6 (interval cadences, cron count=4, dict-shape, gold_history every-other-day, infographics+video_clip daily cron shape), test_quotes.py updated assertion (max_count=1), test_video_clip.py +2 (accounts reorder + reject path). Scheduler ruff clean, pytest 90/90. All 8 validation greps pass. **Explicitly deferred per user's "Option A" directive:** (1) Infographics historical-fallback drafter (Claude picker + `infographics_used_topics` Config key + drafter, modeled on gold_history architecture); (2) Gold Media source switch to YouTube Data API / podcast RSS (current X API video source kept + tightened with analyst bar only). Branch pending merge into main + push + Railway redeploy. **Post-deploy expectation:** startup log shows 3 interval + 4 cron agents scheduled; next 12:00 PT tick fires 3 or 4 cron agents (4 on even-calendar days when gold_history runs); BN first fire within 2h of boot thanks to m9k's +10s buffer; Quotes + Gold Media daily fires may return 0 drafts when nothing passes the quality bar.

Prior activity: 2026-04-21 — Completed quick-260421-mos (Quotes sub-agent daily 12pm Pacific + reputable-source whitelist + top-2 cap + drafter quality gate). Single atomic implementation commit `4dbeab3` on branch `quick/260421-mos-quotes-daily-noon-reputable` (rebased on top of m9k). Split `CONTENT_SUB_AGENTS` into `CONTENT_INTERVAL_AGENTS` (6) + `CONTENT_CRON_AGENTS` (1, sub_quotes daily 12:00 America/Los_Angeles), added `CronTrigger` import + second registration loop. Added `max_count` + `source_whitelist` optional kwargs to shared `run_text_story_cycle` (None defaults = zero behavior change for the 4 other sub-agents using it). `quotes.py` got 27-pattern `REPUTABLE_SOURCES` frozenset (tier-1 financial + gold-specialist + institutional), `max_count=2` + whitelist wired in `run_draft_cycle`, and a 4-criterion quality bar + reject path in `_draft` (returns None → existing stub-bundle branch). +8 tests across `test_worker.py` (+1, lock-ids iterate both lists + new daily-cron shape test), `test_quotes.py` (+3, whitelist sanity + reject path + filter-kwargs passthrough), and new `test_content_init.py` (+4, whitelist filter + max_count cap + both no-op paths). Scheduler pytest 82/82, ruff clean. All 9 validation greps pass. Branch NOT pushed — awaiting operator merge into main + Railway redeploy. Post-deploy expectation: next 12:00 America/Los_Angeles fires sub_quotes DST-aware; first-fire logs show reputable filter + max_count cap funnel counts; /agents/quotes dashboard shows "Pulled from agent run at 12:00 PM · {date}" header within seconds OR stays empty 24h if no quote met the quality bar.

Prior activity: 2026-04-21 — Completed quick-260421-m9k (sub_breaking_news hourly + APScheduler offset=0 first-fire fix). Single atomic commit `c5d32f3` on branch `quick/260421-m9k-breaking-news-hourly`. Extended `CONTENT_SUB_AGENTS` tuple to carry per-agent `interval_hours` (BN=1, others=2), added `+10s` buffer to `start_date` so offset=0 jobs don't skip first fire, refreshed 6 worker.py docstring sites + 1 breaking_news.py docstring + 1 test_worker.py unpack pattern (Rule 3 deviation — test broke on the 5→6 tuple shape change; fixed in same commit). Scheduler ruff clean, pytest 74/74 (baseline match). Pending operator merge/push + Railway redeploy; post-deploy expectation is that the "Pulled from agent run at {time}" header appears on /agents/breaking-news ~10s after boot (instead of the prior 2h dark period that prevented the header from rendering until the first interval had fully elapsed).

Prior activity: 2026-04-21 — Completed quick-260421-k9z (dead-code cleanup follow-up to eoe). Removed orphan-island `select_qualifying_stories`/`select_top_story`/`_is_within_window` from `scheduler/agents/content_agent.py` (−81 lines; `breaking_window_hours` param dies with the island); deleted orphan `frontend/src/pages/ContentPage.tsx` + test (−547 lines) and stripped stale App.tsx comment; removed zombie config keys `content_agent_max_stories_per_run` + `content_agent_breaking_window_hours` from both `seed_content_data.py::CONFIG_DEFAULTS` AND `worker.py::upsert_agent_config` (write-only post-eoe, zero readers). DELETE migration docstring in seed script extended from 4 → 6 retired keys. 2 atomic commits on `quick/260421-k9z-dead-code-cleanup`: `b8aec6e` (scheduler) / `704d7b3` (frontend). All gates green (scheduler 74/74, backend 69/5, frontend 52/52, ruff/eslint/tsc/build all clean). Deviation #6 in eoe SUMMARY was documented as stale — both config keys were write-only post-split, not read by `fetch_stories()` as previously claimed.

Prior activity: 2026-04-21 — quick-260420-sn9 fully purged the Twitter Agent and trimmed the Senior Agent to morning-digest-only, across 5 atomic commits. System is now **single-agent** (Content only, with `morning_digest` + `gold_history_agent` as supporting crons). **Task 1 (scheduler, `62ffda4`):** deleted `scheduler/agents/twitter_agent.py` + `scheduler/tests/test_twitter_agent.py`; `worker.py` `_make_job` simplified (no twitter/senior branches), `JOB_LOCK_IDS` twitter/senior entries removed; 3 scheduler jobs remain (content_agent, morning_digest, gold_history_agent); `tweepy[async]>=4.14`, `x_api_bearer_token`/`x_api_key`/`x_api_secret`, and `_validate_env` `X_API_BEARER_TOKEN` critical gate **preserved** — Content Agent video_clip pipeline still reads X API. **Task 2 (senior trim, `be9cd8b`):** senior_agent.py reduced to `run_morning_digest()` only (dedup/expiry/queue-cap methods removed); paired tests updated. **Task 3 (backend, `aeaf412`):** `/config/quota` router endpoint removed; model/schema comments updated to document Twitter purge (with ruff E501 reflow); `seed_mock_data.py` rewritten content-only (removed 5 twitter + 5 instagram mock items); test fixtures migrated (watchlist/draft_item/keyword/rendered_image platforms → content); backend pytest 66 passed / 5 skipped, ruff clean. One deferred item logged: pre-existing ruff I001 in `backend/alembic/versions/0007_add_market_snapshots.py` (from `b28780b` / oa1 task, out-of-sn9-scope). **Task 4 (frontend, `c30e288`):** Platform type narrowed `'twitter' \| 'content'` → `'content'`; 7 Twitter-only files deleted (ApprovalCard + test, PlatformTabBar + test, QueuePage, QuotaBar, WatchlistTab); 13 files modified to remove Twitter UI (Sidebar nav, App.tsx `/twitter` route, AgentRunsTab filter option, KeywordsTab platform dropdown, ScoringTab "Twitter Agent Scoring" section, PlatformBadge/RelatedCardBadge config maps, useQueueCounts, handlers.ts mocks, DigestPage stats grid 3-col → 2-col + Twitter tile removed, SettingsPage tabs 6 → 5 (Keywords default), PlatformQueuePage both branches → ContentSummaryCard); 3 test assertions updated (DigestPage + SettingsPage + PlatformQueuePage); `pnpm tsc --noEmit` clean, `pnpm lint` clean, `pnpm test` 56/56, `pnpm build` 544KB green. **Task 5 (prod DB + docs, `55100c9`):** Prod Neon DB purge via `npx @railway/cli run` + asyncpg inside single transaction — **pre/post counts: 177 → 0 `draft_items WHERE platform='twitter'`**, **40 → 0 `watchlists` (25 twitter + 15 instagram orphans)**, **8 → 0 `config WHERE key LIKE 'twitter_%'`**; Content rows preserved at 955 both sides. One Rule-3 auto-fix inside the same txn: one `instagram` orphan row (from lvy-era data) had `related_id` pointing at a twitter row, triggering FK constraint; UPDATE-NULL'd that one `related_id` before DELETE could proceed. CLAUDE.md Project blurb updated "three-agent AI system" → "single-agent AI system," new historical note appended for 2026-04-20 mirroring the 2026-04-19 lvy note, Stack constraint line updated to list the 3 surviving scheduled jobs. `.planning/ROADMAP.md` deprecation notice added for Phase 4 + Phase 5, Phase 10 partial-deprecation banner, progress table updated. `.planning/REQUIREMENTS.md` TWIT-01..14 and SENR-01,02,03,04,05,08,09 struck through with `**(DEPRECATED 2026-04-20)**`; SENR-06 + SENR-07 retained (shipped via Phase 10-03 as `morning_digest` at 15:00 UTC). `.planning/STATE.md` (this file) updated with 2 new sn9 decision entries + 1 s3b cancellation entry + 2 new rows in Quick Tasks table. 5 commit SHAs on main: `62ffda4` / `be9cd8b` / `aeaf412` / `c30e288` / `55100c9`. **No push to origin/main** per operator instruction — commits remain local until operator reviews.
Stopped at: sn9 complete locally; SUMMARY.md written; all 5 commits on main branch awaiting operator push decision. Outstanding items still pending from earlier work: (a) Phase 10-02 plan unchecked in ROADMAP but is now moot under single-agent system (Content new-item alert only; Twitter/Instagram sides deleted). Total monthly spend now ~$114 (was ~$214) — X API Basic tier $100/mo cut; remaining ~$114 is SerpAPI ~$50 + Claude ~$30 + metalpriceapi $9 + Twilio ~$5 + FRED free + Neon free tier + Railway + Vercel.
Resume file: None
