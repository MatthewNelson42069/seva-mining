---
quick_task: 260421-k9z
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduler/agents/content_agent.py
  - scheduler/seed_content_data.py
  - scheduler/worker.py
  - frontend/src/App.tsx
  - frontend/src/pages/ContentPage.tsx
  - frontend/src/pages/ContentPage.test.tsx
autonomous: true
requirements:
  - K9Z-01   # Remove dead orphan-island functions + defunct breaking_window_hours param from scheduler/agents/content_agent.py
  - K9Z-02   # Remove zombie config keys (max_stories_per_run + breaking_window_hours) from worker overrides + seed defaults + extend migration docstring
  - K9Z-03   # Delete orphan ContentPage.tsx + test + strip stale App.tsx comment

must_haves:
  truths:
    - "grep 'select_qualifying_stories\\|select_top_story\\|_is_within_window' scheduler scheduler/tests returns ZERO matches post-cleanup"
    - "grep 'content_agent_max_stories_per_run\\|content_agent_breaking_window_hours' scheduler backend returns ZERO matches post-cleanup"
    - "grep 'ContentPage' frontend/src returns ZERO matches post-cleanup"
    - "App.tsx contains no stale comment referencing /content-review or ContentPage"
    - "scheduler ruff clean; scheduler pytest passes (74+ tests, same count as eoe baseline minus any tests that exercised the removed orphan functions)"
    - "backend ruff clean; backend pytest passes unchanged (no backend code touched)"
    - "frontend eslint clean; frontend vitest passes (ContentPage tests removed cleanly, no import errors elsewhere); vite build succeeds"
  artifacts:
    - path: "scheduler/agents/content_agent.py"
      provides: "Content Agent review service — post-cleanup with orphan island removed"
      contains_not: "select_qualifying_stories"
    - path: "scheduler/seed_content_data.py"
      provides: "Seed script with zombie keys removed + extended DELETE migration docstring"
      contains_not: "content_agent_max_stories_per_run"
    - path: "scheduler/worker.py"
      provides: "Worker with zombie key overrides removed from upsert_agent_config"
      contains_not: "content_agent_max_stories_per_run"
    - path: "frontend/src/App.tsx"
      provides: "Router with stale 3-line comment stripped"
      contains_not: "ContentPage"
  key_links:
    - from: "scheduler/agents/content_agent.py"
      to: "datetime/timezone imports"
      via: "still used by 18+ other call sites in the file"
      pattern: "from datetime import datetime, timezone"
    - from: "seed_content_data.py migration docstring"
      to: "production DB cleanup"
      via: "DELETE clause (operator will run this manually via `railway run`)"
      pattern: "DELETE FROM config WHERE key IN"
---

<objective>
Clean up 3 dead-code items flagged in the quick-260421-eoe SUMMARY ("Dead code flagged" section + Deviation #6). The eoe split rendered these callers dead; this is the follow-up pass.

Purpose: Functional parity only — no behavior change. Eliminates confusion for future edits (no phantom "backward-compat" selectors), shrinks the Content Agent module further toward its review-service role, and removes zombie config writes that touch the DB on every startup.

Output: Three atomic commits (one scheduler, one frontend; worker.py + seed_content_data.py + content_agent.py all ship in the scheduler commit per user's constraint of "as one atomic commit"). Production DB cleanup is operator-driven via the extended migration docstring — NOT automated by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260421-eoe-split-content-agent-into-7-sub-agents-or/260421-eoe-SUMMARY.md

<!-- Ground truth verified by orchestrator with grep before planning.
     All file:line targets in tasks below are VERIFIED — do NOT re-verify.
     Proceed directly to the edits. -->

<codebase_map>
The orphan island in scheduler/agents/content_agent.py lives at lines 212-330:

  - select_top_story()           L212-221   ZERO callers (docstring refs "backward compat")
  - _is_within_window()          L224-244   ONLY called by select_qualifying_stories
  - select_qualifying_stories()  L291-330   ZERO callers (takes breaking_window_hours param)

Between L247-288 sits classify_format_lightweight() — LIVE, keep.
Between L205-211 and L331+ sit section banner comments and the next live section
("Top story selection (CONT-06, CONT-07)" header + the "Article fetch" header).

The `from datetime import datetime, timezone` import at L34 has ~18 other call sites
in the file — DO NOT remove it. (Grep count: 20 total occurrences of datetime/timezone;
only 6 of them live inside the orphan island.)
</codebase_map>

<interfaces>
<!-- Zombie config keys — write sites only, zero readers (orchestrator verified): -->

scheduler/seed_content_data.py CONFIG_DEFAULTS (L42-43):
```python
    ("content_agent_max_stories_per_run",  "5"),
    ("content_agent_breaking_window_hours", "3"),
```

scheduler/worker.py upsert_agent_config overrides dict (L295-296):
```python
    "content_agent_max_stories_per_run": "5",
    "content_agent_breaking_window_hours": "3",
```

scheduler/seed_content_data.py migration docstring (L17-22, extend in place):
```python
DELETE FROM config WHERE key IN (
    'content_agent_schedule_hour',
    'content_agent_midday_hour',
    'content_agent_interval_hours',
    'gold_history_hour'
);
```
Add two more key names to this IN (...) list: content_agent_max_stories_per_run
and content_agent_breaking_window_hours.
</interfaces>

<frontend_map>
frontend/src/App.tsx L24-26 — stale 3-line comment to strip:
```tsx
            {/* Other pages. /content-review + ContentPage were orphaned after
                quick-260421-eoe (no Sidebar link, no internal linkage); route
                removed. ContentPage.tsx retained pending a follow-up quick task. */}
```
Leave `{/* Other pages. */}` or drop the entire block — executor picks cleaner
option (no eslint preference either way).

Orphan files to delete (orchestrator-verified: only import of ContentPage is in
its own test file; no Route mounts it, no Sidebar links it):
  - frontend/src/pages/ContentPage.tsx       (11683 bytes)
  - frontend/src/pages/ContentPage.test.tsx  (7727 bytes)
</frontend_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scheduler cleanup — orphan island + zombie config keys + extended migration docstring</name>
  <files>scheduler/agents/content_agent.py, scheduler/seed_content_data.py, scheduler/worker.py</files>
  <action>
    Three edits, all in the scheduler service, committed together as one atomic commit.

    **Edit A — scheduler/agents/content_agent.py (orphan island removal, addresses K9Z-01):**

    Delete lines 208-330 inclusive as a single block. That removes:
      1. The section banner comment `# Top story selection (CONT-06, CONT-07)` at L208-210
      2. `def select_top_story(...)` L212-221 (zero callers — orchestrator grep-verified)
      3. `def _is_within_window(...)` L224-244 (only callee was select_qualifying_stories)
      4. `async def classify_format_lightweight(...)` L247-288 — **WAIT, KEEP THIS ONE**

    Revised edit: delete in TWO surgical blocks so classify_format_lightweight survives:

      Block 1: L208-244 — section banner + select_top_story + _is_within_window
      Block 2: L291-330 — select_qualifying_stories (breaking_window_hours dies with it)

    The `from datetime import datetime, timezone` import at L34 MUST stay — grep
    shows ~18 other call sites in the file (recency scoring, timestamp parsing,
    etc.). Do NOT touch L34.

    After both block deletions, run `grep -n "select_qualifying_stories\|select_top_story\|_is_within_window\|breaking_window_hours" scheduler/agents/content_agent.py`
    and confirm ZERO matches in live code (if the string survives inside an
    unrelated docstring, flag it but don't rewrite docstrings unrelated to the
    dead code).

    Also run `grep -rn "select_qualifying_stories\|select_top_story\|_is_within_window" scheduler/ scheduler/tests/`
    across the whole scheduler tree — if any tests reference these symbols,
    delete those test functions too (they'll ERROR on collection otherwise).
    Orchestrator grep found zero callers, but tests may reference them directly.
    Expected finding: zero. If non-zero, surgical-remove those test fns.

    **Edit B — scheduler/seed_content_data.py (zombie config keys + extend docstring, addresses K9Z-02 part 1):**

    1. Delete lines 42-43 from CONFIG_DEFAULTS:
       ```python
       ("content_agent_max_stories_per_run",  "5"),
       ("content_agent_breaking_window_hours", "3"),
       ```
       Leave surrounding tuples intact. CONFIG_DEFAULTS length goes from 10 → 8.

    2. Extend the DELETE clause in the module docstring (currently L17-22):
       Add two new key names to the IN (...) list:
       ```sql
       DELETE FROM config WHERE key IN (
           'content_agent_schedule_hour',
           'content_agent_midday_hour',
           'content_agent_interval_hours',
           'gold_history_hour',
           'content_agent_max_stories_per_run',
           'content_agent_breaking_window_hours'
       );
       ```
       Also add one sentence to the comment above the DELETE describing the two
       new keys — something like "(K9Z follow-up: max_stories_per_run and
       breaking_window_hours were writes-only after eoe; never read again.)"

    **Edit C — scheduler/worker.py (zombie config overrides, addresses K9Z-02 part 2):**

    In `upsert_agent_config()`, delete lines 295-296 from the overrides dict:
    ```python
    "content_agent_max_stories_per_run": "5",
    "content_agent_breaking_window_hours": "3",
    ```
    Leave the other two overrides (`content_quality_threshold` + `content_recency_weight`)
    intact — those are actively read by the live scoring pipeline.

    Also update the quick-260421-eoe comment block at L285-287 — append a
    sentence noting that the max_stories_per_run + breaking_window_hours
    overrides were removed in quick-260421-k9z because they were writes-only
    (ground truth: post-split fetch_stories() takes no params and doesn't
    consult config).

    **After all three edits:**

    Run quality gates from scheduler/ directory (via `cd scheduler && ...`):
    ```
    uv run ruff check .
    uv run pytest -x
    ```
    Both must pass. Backend is not touched, but run backend gates too for belt-and-suspenders:
    ```
    cd ../backend && uv run ruff check . && uv run pytest -x
    ```
    Backend gates should be no-op-green (same passing count as before).

    Commit as one atomic commit:
    ```
    refactor(scheduler): remove dead orphan selectors + zombie config keys (quick-260421-k9z)
    ```
  </action>
  <verify>
    <automated>cd scheduler && uv run ruff check . && uv run pytest -x 2>&1 | tail -20 && cd ../backend && uv run ruff check . && uv run pytest -x 2>&1 | tail -20 && grep -rn "select_qualifying_stories\|select_top_story\|_is_within_window" scheduler/ scheduler/tests/ || echo "EXPECTED-EMPTY-GREP-OK" && grep -rn "content_agent_max_stories_per_run\|content_agent_breaking_window_hours" scheduler/ backend/ || echo "EXPECTED-EMPTY-GREP-OK"</automated>
  </verify>
  <done>
    - scheduler/agents/content_agent.py no longer defines select_top_story, _is_within_window, or select_qualifying_stories (and thus no breaking_window_hours parameter remains)
    - scheduler/seed_content_data.py CONFIG_DEFAULTS no longer lists content_agent_max_stories_per_run or content_agent_breaking_window_hours (length 10 → 8); DELETE docstring extended to include both keys
    - scheduler/worker.py upsert_agent_config overrides dict no longer writes those two keys; eoe-era comment block extended with K9Z note
    - from datetime import datetime, timezone at L34 of content_agent.py is preserved (still used by live code)
    - scheduler ruff clean, scheduler pytest passes (74 passed in eoe; expect same count ± any tests that referenced the 3 removed symbols)
    - backend ruff clean, backend pytest passes unchanged (no backend code touched)
    - grep for the 3 orphan symbol names across scheduler returns empty
    - grep for the 2 zombie config keys across scheduler + backend returns empty
    - one atomic commit on the working branch with message starting `refactor(scheduler): ...`
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend cleanup — delete orphan ContentPage.tsx + test + strip stale App.tsx comment</name>
  <files>frontend/src/App.tsx, frontend/src/pages/ContentPage.tsx, frontend/src/pages/ContentPage.test.tsx</files>
  <action>
    Two edits + two deletions, frontend-only, one atomic commit.

    **Edit A — delete orphan files (addresses K9Z-03 part 1):**

    ```
    rm frontend/src/pages/ContentPage.tsx
    rm frontend/src/pages/ContentPage.test.tsx
    ```

    Orchestrator grep-confirmed: only import of `ContentPage` is inside
    ContentPage.test.tsx itself; no `<Route>` element mounts it; no Sidebar link
    points at it. Deletion is safe.

    **Edit B — strip stale App.tsx comment (addresses K9Z-03 part 2):**

    frontend/src/App.tsx L24-26 currently reads:
    ```tsx
                {/* Other pages. /content-review + ContentPage were orphaned after
                    quick-260421-eoe (no Sidebar link, no internal linkage); route
                    removed. ContentPage.tsx retained pending a follow-up quick task. */}
    ```

    Replace with the single-line comment:
    ```tsx
                {/* Other pages. */}
    ```
    OR delete the comment entirely if that reads cleaner (executor's choice —
    prettier/eslint will decide formatting). Do NOT touch the surrounding
    `<Route path="/digest" .../>` / `<Route path="/settings" .../>` lines.

    **After both edits:**

    Run quality gates from frontend/ directory (via `cd frontend && ...`):
    ```
    pnpm lint
    pnpm test
    pnpm tsc --noEmit
    pnpm build
    ```
    All must pass.

    - `pnpm test` test count should drop by whatever ContentPage.test.tsx
      contributed. Reference: eoe's final count was 61 passed across 9 files —
      expect fewer tests and fewer files post-deletion. Confirm no collection
      errors, no "Cannot find module './ContentPage'" errors elsewhere (there
      should be none — orchestrator verified only its own test imported it).
    - `pnpm tsc --noEmit` catches any dangling TypeScript references.
    - `pnpm build` (vite build) confirms the bundle builds without
      unresolved imports.

    Commit as one atomic commit:
    ```
    refactor(frontend): delete orphan ContentPage + strip stale App.tsx comment (quick-260421-k9z)
    ```
  </action>
  <verify>
    <automated>cd frontend && pnpm lint && pnpm test 2>&1 | tail -10 && pnpm tsc --noEmit && pnpm build 2>&1 | tail -5 && test ! -f src/pages/ContentPage.tsx && test ! -f src/pages/ContentPage.test.tsx && ! grep -rn "ContentPage" src/</automated>
  </verify>
  <done>
    - frontend/src/pages/ContentPage.tsx deleted
    - frontend/src/pages/ContentPage.test.tsx deleted
    - frontend/src/App.tsx no longer contains the 3-line stale comment referencing /content-review, quick-260421-eoe, or ContentPage
    - `grep -rn ContentPage frontend/src/` returns empty
    - frontend eslint clean, vitest green (reduced count, no errors), tsc --noEmit clean, vite build succeeds
    - one atomic commit on the working branch with message starting `refactor(frontend): ...`
  </done>
</task>

</tasks>

<verification>
Post-plan sweep — all gates from both tasks combined:

1. **Scheduler:** `cd scheduler && uv run ruff check . && uv run pytest -x` → all green, test count = eoe baseline (74) or ± tests that exercised the 3 removed orphan symbols (likely zero, orchestrator found no callers).

2. **Backend:** `cd backend && uv run ruff check . && uv run pytest -x` → unchanged from eoe (69 passed, 5 skipped pre-existing). This plan touches zero backend files; a delta here would indicate cross-service leakage and should block the commit.

3. **Frontend:** `cd frontend && pnpm lint && pnpm test && pnpm tsc --noEmit && pnpm build` → all green. Test count drops by N (= ContentPage.test.tsx's test count); no new import errors.

4. **Grep-based dead-code absence proofs (all must return empty):**
   - `grep -rn "select_qualifying_stories\|select_top_story\|_is_within_window" scheduler/`
   - `grep -rn "content_agent_max_stories_per_run\|content_agent_breaking_window_hours" scheduler/ backend/`
   - `grep -rn "ContentPage" frontend/src/`
   - `grep -n "/content-review\|ContentPage" frontend/src/App.tsx`

5. **Import integrity check:**
   - `grep -n "^from datetime\|^import datetime" scheduler/agents/content_agent.py` still returns the L34 line — imports survived the orphan-island removal.

6. **Migration docstring extension check:**
   - `grep -A 8 "DELETE FROM config WHERE key IN" scheduler/seed_content_data.py` shows 6 keys inside the IN clause (was 4), including the two new k9z additions.

7. **Commit hygiene:**
   - `git log --oneline -2` shows two new commits on working branch: scheduler refactor + frontend refactor, both with `(quick-260421-k9z)` in the subject line.
   - `git diff main..HEAD --stat` shows 6 files touched:
     * scheduler/agents/content_agent.py (deletions only)
     * scheduler/seed_content_data.py (1 + 2 line change — 2 deletes in list, 2 + 1 lines added in docstring)
     * scheduler/worker.py (2 deletes + comment extension)
     * frontend/src/App.tsx (3-line comment → 1-line comment or removed)
     * frontend/src/pages/ContentPage.tsx (deleted)
     * frontend/src/pages/ContentPage.test.tsx (deleted)
</verification>

<success_criteria>
- All 3 dead-code items from eoe SUMMARY's "Dead code flagged" section removed
- Deviation #6 of eoe SUMMARY superseded (the "max_stories_per_run + breaking_window_hours left intact" caveat is now obsolete; they're both purged)
- Zero behavior change — this is functional parity cleanup, not a feature change
- All three service-level quality-gate bundles pass (scheduler ruff+pytest; backend ruff+pytest; frontend eslint+vitest+tsc+build)
- Two atomic commits: one scheduler-only, one frontend-only, both tagged `(quick-260421-k9z)` in subject
- Production DB cleanup is NOT automated — operator runs the extended DELETE clause in seed_content_data.py's docstring manually via `railway run` as a one-off, matching the pattern used for eoe's earlier DB migration
</success_criteria>

<output>
After completion, create `.planning/quick/260421-k9z-clean-up-3-dead-code-items-flagged-in-eo/260421-k9z-SUMMARY.md` summarizing:
- Which functions + keys + files were removed (3 items × 3 sublocations)
- Gate results (scheduler pytest count before/after, backend unchanged, frontend vitest count before/after)
- Commit SHAs (2 commits)
- Operator follow-up note: the DELETE clause in seed_content_data.py needs one manual `railway run` pass against prod Neon to remove the 2 new zombie config key rows; format identical to the eoe follow-up (047ef1d) but with 2 new key names.
- Any surprises discovered mid-task (e.g., if grep turned up an unexpected caller the orchestrator missed, or if any test referenced the removed symbols).
</output>
