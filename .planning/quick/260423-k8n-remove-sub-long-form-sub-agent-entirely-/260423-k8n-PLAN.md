---
quick_task: 260423-k8n
slug: remove-sub-long-form-sub-agent-entirely
subsystem: scheduler + backend + frontend + docs
tags: [sub-agent-removal, topology, 7-to-6, long_form-purge, cleanup]
created: 2026-04-23
estimated_duration: ~60-90m (single session)
commit_strategy: single-atomic-commit
precedents:
  - .planning/quick/260419-lvy-full-purge-instagram-agent-content-queue/260419-lvy-SUMMARY.md
  - .planning/quick/260420-sn9-full-purge-twitter-agent-trim-senior-to-/260420-sn9-SUMMARY.md
context_input: .planning/quick/260423-k8n-remove-sub-long-form-sub-agent-entirely-/260423-k8n-CONTEXT.md
autonomous: true
files_deleted:
  - scheduler/agents/content/long_form.py
  - scheduler/tests/test_long_form.py
  - frontend/src/components/content/LongFormPreview.tsx
files_modified:
  # Scheduler
  - scheduler/worker.py
  - scheduler/agents/content_agent.py
  - scheduler/agents/content/__init__.py
  - scheduler/models/content_bundle.py
  - scheduler/tests/test_worker.py
  - scheduler/tests/test_content_agent.py
  - scheduler/tests/test_content_init.py
  # Backend
  - backend/app/models/content_bundle.py
  - backend/app/routers/queue.py
  # Frontend
  - frontend/src/config/agentTabs.ts
  - frontend/src/components/approval/ContentDetailModal.tsx
  - frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx
  - frontend/src/components/settings/AgentRunsTab.tsx
  - frontend/src/pages/PerAgentQueuePage.test.tsx
  # Docs
  - CLAUDE.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
files_preserved_explicit_keep:
  - frontend/src/components/content/ThreadPreview.tsx  # long_form_post field L8,L14 — thread-compat, NOT a sub_long_form ref
  - scheduler/agents/content_agent.py (L679)            # parts.append(draft_content.get("long_form_post", "")) inside thread branch — thread-compat field
test_count_deltas:
  scheduler_pytest: "131 → 127 (−4: test_long_form.py deleted)"
  backend_pytest: "unchanged (0 delta)"
  frontend_vitest: "N → N−2 (−1 ContentDetailModal Test 3, −1 PerAgentQueuePage Long-form tab test)"
must_haves:
  truths:
    - "Scheduler registers exactly 7 jobs (morning_digest + 6 sub-agents): sub_long_form removed"
    - "No scheduler/backend/frontend code imports or references sub_long_form / long_form format / LongFormPreview"
    - "ContentDetailModal no longer dispatches to LongFormPreview (falls through to default / FlatTextFallback for historical long_form rows)"
    - "Historical content_bundles.content_type='long_form' rows remain queryable (schema unchanged, no Alembic migration)"
    - "Threads sub-agent unchanged — short-thread shape preserved (Q1-A); long_form_post compat field retained in thread draft_content"
    - "CLAUDE.md active prose says 6-sub-agent / 7 scheduler jobs; new 2026-04-23 historical note documents the purge"
    - "STATE.md Quick Tasks Completed table has a k8n row; topology prose updated to 6 sub-agents"
    - "scheduler pytest: 127 passing (was 131); backend pytest unchanged; frontend vitest: N-2 passing; ruff + tsc + lint clean"
  artifacts:
    - path: "scheduler/agents/content/long_form.py"
      must_not_exist: true
    - path: "scheduler/tests/test_long_form.py"
      must_not_exist: true
    - path: "frontend/src/components/content/LongFormPreview.tsx"
      must_not_exist: true
    - path: "scheduler/worker.py"
      contains_not: "sub_long_form"
      contains: 'sub_breaking_news'
      contains: 'sub_threads'
    - path: "frontend/src/config/agentTabs.ts"
      contains_not: "long-form"
      contains_not: "sub_long_form"
    - path: "frontend/src/components/approval/ContentDetailModal.tsx"
      contains_not: "LongFormPreview"
      contains_not: "long_form: true"
    - path: "CLAUDE.md"
      contains: "Historical note (2026-04-23)"
      contains: "6-sub-agent"
  key_links:
    - from: "scheduler/worker.py::CONTENT_INTERVAL_AGENTS"
      to: "scheduler.add_job registrations"
      pattern: '\(\"sub_threads\",.*17, 4\)'
      broken_if: "sub_long_form tuple still present OR stagger offsets still [0,17,34]"
    - from: "scheduler/agents/content_agent.py::classify_format_lightweight"
      to: "valid_formats set"
      pattern: '\{"breaking_news", "thread", "infographic", "quote"\}'
      broken_if: 'valid_formats still contains "long_form"'
    - from: "frontend/src/components/approval/ContentDetailModal.tsx"
      to: "renderForFormat switch"
      pattern: "case 'long_form'"
      broken_if: "switch still has a 'long_form' case dispatching to LongFormPreview"
---

<goal>

Fully purge the `sub_long_form` sub-agent from Seva Mining across scheduler, backend, frontend, and active planning docs — reducing the topology from 7 sub-agents (`sub_breaking_news`, `sub_threads`, `sub_long_form`, `sub_quotes`, `sub_infographics`, `sub_gold_media`, `sub_gold_history`) to 6. The scheduler job set drops from 8 (7 subs + morning_digest) to 7 (6 subs + morning_digest). The interval-staggered-within-4h cohort collapses from `[0, 17, 34]` min offsets to `[0, 17]`. Historical `content_bundles.content_type='long_form'` and `agent_runs.agent_name='sub_long_form'` rows remain in the DB untouched — zero schema change, zero Alembic migration. ThreadPreview retains its `long_form_post` field (Q1-A: thread-format compat, not a sub_long_form reference). Pattern mirrors `260419-lvy` (Instagram) and `260420-sn9` (Twitter+Senior).

Success = single atomic commit landing all deletions + edits + docs in one diff, all 4 validation gates (scheduler pytest / backend pytest / frontend vitest + tsc / cross-repo grep) returning green, and CLAUDE.md reading as a clean 6-sub-agent system description with a new `> **Historical note (2026-04-23):**` paragraph appended in the lvy/sn9 blockquote pattern.

</goal>

<goal_backward_analysis>

For each surface in CONTEXT.md's MODIFY/DELETE tables, a 1-line check: "Why does removing this serve the goal?" Flagged items serve multiple goals or have collateral-damage risk.

### DELETE (whole files)

| File | Why removing serves goal | Collateral? |
|------|--------------------------|-------------|
| `scheduler/agents/content/long_form.py` | This IS the sub_long_form drafter. Its removal is the goal. | None. |
| `scheduler/tests/test_long_form.py` | Tests a deleted module; retaining would ImportError on module load. | None. |
| `frontend/src/components/content/LongFormPreview.tsx` | Sole consumer is `ContentDetailModal` case `'long_form'` — also removed. After modal change, it's dead. | **FLAG:** if any other import exists, tsc will fail — executor must `grep -r "LongFormPreview" frontend/src` before declaring done. |

### MODIFY — scheduler/

| File | Why | Multi-goal / collateral? |
|------|-----|--------------------------|
| `scheduler/worker.py` L9, L48, L86, L97, L104, L261, L275 | Removes sub_long_form from imports, JOB_LOCK_IDS, CONTENT_INTERVAL_AGENTS tuple, docstring, log format. L275 is a log format string that interpolates the same tuple — MUST match tuple length or `IndexError` at runtime. | **FLAG:** L275 log string must be re-counted after tuple shrinks 3→2. |
| `scheduler/agents/content_agent.py` L16 | Docstring reference in `classify_format_lightweight` description. | None. |
| `scheduler/agents/content_agent.py` L214, L218 | `valid_formats` set + docstring — this is the classifier's allow-list. Removing `"long_form"` here must be matched by L231 prompt update. | **KEY LINK:** valid_formats (L218) ↔ Haiku prompt text (L231) ↔ `build_draft_item` branches (L586) ↔ `_extract_check_text` branches (L680). ALL FOUR must agree or classifier will return "long_form" and downstream will ignore it silently. |
| `scheduler/agents/content_agent.py` L231 | Prompt string "breaking_news \| thread \| long_form \| infographic \| quote" — remove `long_form \|` segment. | Paired with L218. |
| `scheduler/agents/content_agent.py` L586-587 | `elif fmt == "long_form":` branch in `build_draft_item`. After removal, fall-through `draft_text` logic handles the case (unlikely to be hit post-classifier-update). | None. |
| `scheduler/agents/content_agent.py` **L679** | `parts.append(draft_content.get("long_form_post", ""))` **inside the `elif fmt == "thread":` branch** — this is a THREAD draft-content compat field, NOT a sub_long_form reference. **KEEP**. | **EXPLICIT KEEP** — Q1-A. Removing this would alter threads' compliance-check text. |
| `scheduler/agents/content_agent.py` L680-681 | `elif fmt == "long_form":` branch in `_extract_check_text`. Delete. | None. |
| `scheduler/agents/content_agent.py` L878 | Docstring reference in `fetch_stories` listing predicted_format values. | None. |
| `scheduler/agents/content/__init__.py` (3 lines per grep: L3, L92, L114) | Prose references in module docstring + 2 function docstrings enumerating the 7 sub-agents. Update count/names. | None — no code change. |
| `scheduler/models/content_bundle.py` L15 | Comment enumerating content_type values. | None. |
| `scheduler/tests/test_worker.py` L98, L128, L146, L174 | Expected JOB_LOCK_IDS, expected job id sets, expected cadences. Must stay in sync with worker.py changes. | **KEY LINK:** worker.py L86 (JOB_LOCK_IDS dict) ↔ test L98, L128. worker.py L104 (CONTENT_INTERVAL_AGENTS) ↔ test L174. |
| `scheduler/tests/test_content_agent.py` L230 | `expected = ("breaking_news", "thread", "long_form", "infographic", "quote")` — drop `"long_form"`. Must match content_agent.py L218. | Paired with scheduler content_agent L218. |
| `scheduler/tests/test_content_init.py` L29 | Comment enumerating 4 sub-agents in coverage note. Drop `long_form`. | None. |

### MODIFY — backend/

| File | Why | Collateral? |
|------|-----|-------------|
| `backend/app/models/content_bundle.py` L17 | Comment enumerating content_type values. | None. |
| `backend/app/routers/queue.py` L63 | Docstring enumerating content_type query values. No schema/behavior change. | None. |

### MODIFY — frontend/

| File | Why | Collateral? |
|------|-----|-------------|
| `frontend/src/config/agentTabs.ts` L29 | The `long-form` AgentTab entry. Router (`PerAgentQueuePage`) and Sidebar both iterate this array — deleting the entry removes the route + nav entry in one edit (Q4-A). | **KEY LINK:** After removal, any URL matching `/agents/long-form` hits the router's no-match fallback (likely redirects or 404). Acceptable per Q4-A ("bookmark-breakage acceptable"). |
| `frontend/src/components/approval/ContentDetailModal.tsx` L12, L27, L126-127 | L12 = LongFormPreview import. L27 = `long_form: true` in FORMAT_RENDERERS. L126-127 = `case 'long_form'` dispatch. Removing L27 means `showFallback` evaluates true for historical `long_form` rows → renders FlatTextFallback (graceful). | **KEY LINK:** L27 FORMAT_RENDERERS must stay in sync with L121+ switch; deleting both keeps them aligned. |
| `frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx` L136, L153-172 | L136 is `long_form_post: 'Long form version here.'` inside a THREAD fixture (Test 2) — **KEEP** per Q1-A / ThreadPreview compat. L153-172 is Test 3 `renders LongFormPreview...` — **DELETE**. | **FLAG:** L136 is explicitly KEEP — same thread-compat rationale as ThreadPreview L8,14. CONTEXT.md Line 71 says "L136 (long_form_post test fixture)" with delete intent — re-read carefully: the fixture is for Test 2 (thread), not Test 3. The fixture itself is thread-scoped. **Planner decision: KEEP L136; only delete the Test 3 block at L153-172.** This matches Q1-A and the ThreadPreview preservation. Frontend test delta is −1 (not −2 from this file). |
| `frontend/src/components/settings/AgentRunsTab.tsx` L15, L25 | L15 = comment listing 7 sub-agents. L25 = `{ value: 'sub_long_form', label: 'long_form' }` dropdown option. | None. |
| `frontend/src/pages/PerAgentQueuePage.test.tsx` L90-98 | The `renders the Long-form tab` test block. After L29 of agentTabs.ts is removed, the route `/agents/long-form` no longer resolves; test would fail. Delete. | None. |

### MODIFY — docs

| File | Why | Collateral? |
|------|-----|-------------|
| `CLAUDE.md` opening paragraph + historical notes section | Active prose: "7-sub-agent" → "6-sub-agent", "7 scheduled jobs" → "7" stays (7 subs + morning_digest was wrong before k8n-research — actually it was 7 subs + morning_digest = 8 jobs → 6 subs + morning_digest = 7 jobs). Append 2026-04-23 note. | **FLAG:** Current prose L3 says "7-sub-agent ... eight scheduled jobs" (opening + later stack line). The stack-line also says "eight scheduled jobs (7 sub-agents + morning_digest)". Need to update BOTH: 7→6 sub-agents AND 8→7 scheduled jobs. Note: the sn9 historical-note paragraph references "8 jobs" — **LEAVE THAT UNTOUCHED** (historical). |
| `.planning/STATE.md` topology section + Quick Tasks Completed table | Update topology count + add k8n row. Roll frontmatter `stopped_at` + `last_updated`. | None. |
| `.planning/REQUIREMENTS.md` | CONT-09 and CONT-10 mention `long_form` / "long-form article (600-900 words)" as content-format options; CREV-06 lists `long_form` among content-detail-modal formats. Add a `(DEPRECATED 2026-04-23 per quick 260423-k8n — long_form format retired)` note; do NOT strikethrough CONT-09/CONT-10 entirely (other clauses remain true). | None. |
| `.planning/ROADMAP.md` | Phase 7 + Phase 11 bullets list `long_form` among content formats. Add a parenthetical `(long_form retired 2026-04-23 — see quick 260423-k8n)` on those lines. | None. |

### Surfaces flagged for EXPLICIT KEEP (regression guards)

| Surface | Reason |
|---------|--------|
| `frontend/src/components/content/ThreadPreview.tsx` L5-8, L14 | `ThreadDraft.long_form_post?: string` + `const longFormPost = typeof d.long_form_post === 'string' ? d.long_form_post : ''`. This is the thread-format's "long-form companion post" (3-5 tweets PLUS a standalone X long-form post version). Q1-A locks threads shape as unchanged. |
| `scheduler/agents/content_agent.py` L679 | Same thread-compat field read in the check-text assembly. Removing it weakens thread compliance checking. |
| `frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx` L136 | `long_form_post: 'Long form version here.'` inside Test 2's THREAD fixture. Deleting would make Test 2's fixture inconsistent with ThreadDraft interface. |
| DB schema `content_bundles.content_type = String(50)` | Q3-A: no migration. Historical rows stay. |
| `scheduler/agents/content/threads.py` prompt text | Q1-A: threads prompt untouched. |

No surface on the modify list has a "multi-goal" conflict — every deletion serves the single goal of retiring sub_long_form. The only ambiguity (test fixture L136) is resolved by distinguishing the `long_form` CONTENT TYPE (deleted) from the `long_form_post` THREAD FIELD (preserved).

</goal_backward_analysis>

<tasks>

## T1 — Delete 3 long_form files

**Files:**
- `scheduler/agents/content/long_form.py` (DELETE)
- `scheduler/tests/test_long_form.py` (DELETE)
- `frontend/src/components/content/LongFormPreview.tsx` (DELETE)

**Exact changes:**
```bash
rm scheduler/agents/content/long_form.py
rm scheduler/tests/test_long_form.py
rm frontend/src/components/content/LongFormPreview.tsx
```

**Test count delta:** scheduler pytest −4 (this file defines 4 tests: `test_module_surface`, `test_draft_returns_draft_content_shape`, and 2 more — verified via `grep -cE "^(async )?def test_" scheduler/tests/test_long_form.py` → 4).

**Validation after T1:** No `pytest`/`npm test` yet (imports in remaining files will fail). Proceed to T2 directly.

---

## T2 — Scheduler worker.py: remove sub_long_form from imports / JOB_LOCK_IDS / CONTENT_INTERVAL_AGENTS / docstrings / logs

**Files:** `scheduler/worker.py`

**Exact changes:**

1. **L9** (module docstring): `sub_threads + sub_long_form every 4h` → `sub_threads every 4h`.
2. **L10** (docstring, same paragraph): offsets `[0, 17, 34]` → `[0, 17]`.
3. **L48** (imports): Remove the `long_form,` line from the `from agents.content import (...)` block (it's one line in the 7-item multi-line import).
4. **L86** (JOB_LOCK_IDS dict): Remove `"sub_long_form":     1012,`. Dict drops from 8 entries to 7.
5. **L97** (CONTENT_INTERVAL_AGENTS comment): `Only 3 news-responsive agents ... sub_threads + sub_long_form every 4h` → `Only 2 news-responsive agents ... sub_threads every 4h`. Also update the offset comment `Stagger offsets [0, 17, 34] minutes / spread the three across the first 34 minutes` → `Stagger offsets [0, 17] minutes / spread the two across the first 17 minutes`.
6. **L104** (CONTENT_INTERVAL_AGENTS tuple): Remove the entire line `("sub_long_form", long_form.run_draft_cycle, "Long-form", 1012, 34, 4),`. Tuple shrinks from 3 entries to 2.
7. **L261** (docstring elsewhere in worker.py — verify exact text pre-edit): Look for any "sub_long_form" or "Long-form" string reference in `build_scheduler` or `_make_job` docstring. Remove.
8. **L275** (log format string): The log line enumerates sub-agents or offsets. Re-verify exact text; any mention of `sub_long_form` or offset 34 must be removed. Log string's % / format args must match the (now-shorter) tuple.

**Verification command after T2 (partial — expect failures downstream):**
```bash
grep -n "sub_long_form\|long_form" scheduler/worker.py
```
Expected: 0 matches.

**Dependency:** T1 must precede T2 — deleting `long_form.py` before dropping the `long_form` import in worker.py keeps the git diff clean (no intermediate "imports from deleted module" state after T1 alone if T2 isn't paired — but since this is a single commit, that's fine; the working tree is in a broken state between T1 and T2, but git only sees the final state).

---

## T3 — Scheduler content_agent.py: remove long_form from classifier + builders + check-text

**Files:** `scheduler/agents/content_agent.py`

**Exact changes (line-specific):**

1. **L16** (module docstring): `breaking_news | thread | long_form | infographic | quote` → `breaking_news | thread | infographic | quote`.
2. **L214** (`classify_format_lightweight` docstring): Same enumeration — drop `long_form`.
3. **L218** (`valid_formats` set): `{"breaking_news", "thread", "long_form", "infographic", "quote"}` → `{"breaking_news", "thread", "infographic", "quote"}`.
4. **L231** (prompt string inside the Haiku messages): `breaking_news | thread | long_form | infographic | quote` → `breaking_news | thread | infographic | quote`.
5. **L586-587** (`build_draft_item`): Remove the two-line `elif fmt == "long_form": \n    draft_text = draft.get("post", "")` branch entirely.
6. **L679** — **DO NOT MODIFY.** This is `parts.append(draft_content.get("long_form_post", ""))` inside the `elif fmt == "thread":` branch (line 677). It's a thread-draft compat field, not a long_form content-type reference. Leave as-is.
7. **L680-681** (`_extract_check_text`): Remove the two-line `elif fmt == "long_form": \n    parts.append(draft_content.get("post", ""))` branch.
8. **L878** (`fetch_stories` docstring): `(one of breaking_news/thread/long_form/infographic/quote)` → `(one of breaking_news/thread/infographic/quote)`.

**Verification command after T3:**
```bash
grep -nE '"long_form"|long_form |long_form,|long_form\)|long_form$' scheduler/agents/content_agent.py
```
Expected: ONE match only — line 679's `"long_form_post"` string inside the thread branch. Any other match is a regression.

---

## T4 — Scheduler `content/__init__.py` + `models/content_bundle.py`: docstring/comment cleanup

**Files:** `scheduler/agents/content/__init__.py`, `scheduler/models/content_bundle.py`

**Exact changes:**

1. `scheduler/agents/content/__init__.py`:
   - **L3** (module docstring): `The 7 sub-agents (breaking_news, threads, long_form, quotes, ...)` → `The 6 sub-agents (breaking_news, threads, quotes, ...)`.
   - **L92** (function docstring): `Used by 5 of the 7 sub-agents (breaking_news, threads, long_form, quotes, ...)` → `Used by 4 of the 6 sub-agents (breaking_news, threads, quotes, infographics)`. Recount: originally "5 of 7" meant the 5 that call `run_text_story_cycle`. Post-k8n it's 4 of 6 (breaking_news, threads, quotes, infographics). Verify by inspecting which sub-agent modules still call `run_text_story_cycle`.
   - **L114** (function docstring): `behavior for breaking_news, threads, long_form, quotes` → `behavior for breaking_news, threads, quotes`.
2. `scheduler/models/content_bundle.py`:
   - **L15** (column comment): `# thread, long_form, infographic, breaking_news, gold_media, quote, gold_history` → `# thread, infographic, breaking_news, gold_media, quote, gold_history`.

**No code/behavior change — comment edits only.**

**Verification command after T4:**
```bash
grep -n "long_form" scheduler/agents/content/__init__.py scheduler/models/content_bundle.py
```
Expected: 0 matches.

---

## T5 — Scheduler tests: update test_worker.py / test_content_agent.py / test_content_init.py

**Files:** `scheduler/tests/test_worker.py`, `scheduler/tests/test_content_agent.py`, `scheduler/tests/test_content_init.py`

**Exact changes:**

1. `scheduler/tests/test_worker.py`:
   - **L98** (inside `test_sub_agent_lock_ids`): Remove `"sub_long_form":     1012,` from the expected dict. Expected dict drops from 7 entries to 6.
   - **L128** (inside `test_retired_crons_absent_from_job_lock_ids`): Set comparison — remove `"sub_long_form"`. The assertion currently checks `len(JOB_LOCK_IDS) == 8`; update to `== 7`. Same set comparison at L126-130 loses one entry.
   - **L146** (inside `test_scheduler_registers_8_jobs`): Remove `"sub_long_form"` from the expected list. Rename the test `test_scheduler_registers_8_jobs` → `test_scheduler_registers_7_jobs`; update `assert len(jobs) == 8` → `== 7`.
   - **L174** (inside `test_interval_agents_cadences`): Remove `"sub_long_form": 4,` from the cadences dict. Dict drops from 3 to 2 entries.
   - No `test_long_form.py` file-level deletion in THIS task (T1 handled it).

2. `scheduler/tests/test_content_agent.py`:
   - **L230** (inside `test_classify_format_lightweight_returns_sub_agent_filter_strings`): Tuple `("breaking_news", "thread", "long_form", "infographic", "quote")` → `("breaking_news", "thread", "infographic", "quote")`. Also update the test's docstring `5 CONTENT_TYPE strings` → `4 CONTENT_TYPE strings`.

3. `scheduler/tests/test_content_init.py`:
   - **L29** (docstring comment): `breaking_news, threads, long_form, infographics` → `breaking_news, threads, infographics`. Also L28 says "the 4 other sub-agents" — post-k8n it's 3 other sub-agents (breaking_news, threads, infographics). Update "4" → "3".

**Verification command after T5:**
```bash
cd scheduler && uv run pytest -x 2>&1 | tail -20
```
Expected: all green, 127 tests collected + passed.

**Test count delta check:** `scheduler pytest` was 131 pre-k8n. `test_long_form.py` deletion removes 4 tests. Net: 131 − 4 = 127. `test_worker.py` tests are all UPDATED in place (no count change; they're still testing the same things just with smaller expected sets).

---

## T6 — Backend: docstring/comment cleanup

**Files:** `backend/app/models/content_bundle.py`, `backend/app/routers/queue.py`

**Exact changes:**

1. `backend/app/models/content_bundle.py` **L17** (column comment):
   ```
   # thread, long_form, infographic, breaking_news, gold_media, quote, gold_history
   ```
   →
   ```
   # thread, infographic, breaking_news, gold_media, quote, gold_history
   ```

2. `backend/app/routers/queue.py` **L63** (docstring):
   ```
   - content_type (breaking_news, thread, long_form, quote, infographic,
   ```
   →
   ```
   - content_type (breaking_news, thread, quote, infographic,
   ```

**No code/behavior change — comment edits only. No Alembic migration (Q3-A).**

**Verification after T6:**
```bash
grep -rn "long_form\|LongForm" backend/ --include="*.py"
```
Expected: 0 matches.

```bash
cd backend && uv run pytest -x 2>&1 | tail -10
```
Expected: all green, unchanged count (0 delta).

---

## T7 — Frontend: agentTabs config + ContentDetailModal + AgentRunsTab dropdown

**Files:**
- `frontend/src/config/agentTabs.ts`
- `frontend/src/components/approval/ContentDetailModal.tsx`
- `frontend/src/components/settings/AgentRunsTab.tsx`

**Exact changes:**

1. `frontend/src/config/agentTabs.ts`:
   - **L29**: Delete the entire line:
     ```typescript
     { slug: 'long-form',     contentType: 'long_form',     label: 'Long-form',     priority: 3, agentName: 'sub_long_form' },
     ```
   - Array drops from 7 entries to 6. Priority numbers stay as-is (3 becomes a gap — acceptable since priority is sort-only; renumbering is cosmetic and introduces diff noise).
   - Update the header comment (currently L2): `Single source of truth for the 7 content sub-agent tabs.` → `Single source of truth for the 6 content sub-agent tabs.` Also update L5 `split into 7 sub-agents` → `split into 7 sub-agents (reduced to 6 in quick-260423-k8n)` if that preserves historical context; or just `split into sub-agents under scheduler/agents/content/`.

2. `frontend/src/components/approval/ContentDetailModal.tsx`:
   - **L12**: Delete import `import { LongFormPreview } from '@/components/content/LongFormPreview'`.
   - **L27**: Delete the line `long_form: true,` from the `FORMAT_RENDERERS` object.
   - **L126-127**: Delete the two lines:
     ```typescript
     case 'long_form':
       return <LongFormPreview draft={draft} />
     ```
   - Historical `content_type='long_form'` bundles will now hit `showFallback = true` (via L47: `!FORMAT_RENDERERS[contentType]` evaluates true) and render via `FlatTextFallback`. Acceptable per Q3-A.

3. `frontend/src/components/settings/AgentRunsTab.tsx`:
   - **L15** (comment): `7 sub-agents (\`sub_breaking_news\`, \`sub_threads\`, \`sub_long_form\`, \`sub_quotes\`, ...)` → `6 sub-agents (\`sub_breaking_news\`, \`sub_threads\`, \`sub_quotes\`, ...)`.
   - **L25**: Delete the entire line:
     ```typescript
     { value: 'sub_long_form',     label: 'long_form' },
     ```
   - AGENT_OPTIONS array drops from 9 entries (empty + 7 subs + morning_digest) to 8.

**Verification after T7:**
```bash
grep -rn "long_form\|LongForm\|long-form\|sub_long_form" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v ".test."
```
Expected matches (all INTENTIONAL KEEPS):
- `frontend/src/components/content/ThreadPreview.tsx`: 2 matches (L8, L14) — `long_form_post` thread compat field.
- 0 other matches outside `.test.` files.

```bash
cd frontend && pnpm exec tsc --noEmit
```
Expected: clean. LongFormPreview import gone, no dangling references.

---

## T8 — Frontend tests: ContentDetailModal.test.tsx + PerAgentQueuePage.test.tsx

**Files:**
- `frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx`
- `frontend/src/pages/PerAgentQueuePage.test.tsx`

**Exact changes:**

1. `frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx`:
   - **L136** — **KEEP AS-IS.** This is `long_form_post: 'Long form version here.'` inside Test 2's THREAD fixture (the `content_type: 'thread'` bundle at L131-137). It's a thread-format compat field — deleting it would inconsistently remove a valid ThreadDraft field. CONTEXT.md's reference to "L136 (long_form_post test fixture)" is accurate as a grep hit but the planner's interpretation (per Q1-A + ThreadPreview.tsx preservation) is that this fixture stays.
   - **L153-172** — DELETE. This is Test 3: `it('renders LongFormPreview when bundle.content_type === "long_form"', ...)`. Delete the entire block including the `// Test 3 — long_form` comment on L153.
   - Renumber or leave the subsequent `// Test 4 — breaking_news` comment on L175 — cosmetic only; leaving it unchanged is fine.

2. `frontend/src/pages/PerAgentQueuePage.test.tsx`:
   - **L90-98** — DELETE the entire `it('renders the Long-form tab and queries with contentType=long_form', ...)` block (9 lines including whitespace).

**Frontend test delta:** −1 from ContentDetailModal.test.tsx (Test 3) + −1 from PerAgentQueuePage.test.tsx (Long-form tab test) = **−2 test blocks total**. Matches CONTEXT.md's expected delta.

**Verification after T8:**
```bash
cd frontend && pnpm test -- --run 2>&1 | tail -20
```
Expected: all green, N−2 tests.

---

## T9 — CLAUDE.md: active prose + historical note (lvy/sn9 pattern)

**Files:** `CLAUDE.md`

**Exact changes:**

1. Opening paragraph (`## Project`): Change `A 7-sub-agent AI system` → `A 6-sub-agent AI system`. Change the content-types enumeration `(breaking news, threads, long-form, quotes, infographics, gold media, gold history)` → `(breaking news, threads, quotes, infographics, gold media, gold history)`.
2. Stack constraint line (in `### Constraints`): `APScheduler in separate worker process (eight scheduled jobs: 7 sub-agents + `morning_digest`)` → `APScheduler in separate worker process (seven scheduled jobs: 6 sub-agents + `morning_digest`)`.
3. Historical notes: **DO NOT MODIFY** the existing `> **Historical note (2026-04-19):**` (lvy) or `> **Historical note (2026-04-20):**` (sn9) blockquotes. The sn9 note mentions "8 jobs: 7 sub-agents (`sub_breaking_news`, `sub_threads`, `sub_long_form`, ...)" — this is HISTORICAL and must stay verbatim. The sn9 agent-name list includes `sub_long_form`; leaving that inside a historical-note blockquote is correct (it describes the state at 2026-04-20).
4. **Append a new blockquote below the sn9 note**, matching the pattern exactly:
   ```markdown
   >
   > **Historical note (2026-04-23):** The `sub_long_form` sub-agent was deprecated and fully purged on 2026-04-23 (quick task `260423-k8n`). Rationale: "makes things simpler" — longer-form content that warrants publication can ride under the threads sub-agent's short-thread shape; a dedicated long-form drafter added complexity without clear differentiated value. The scheduler is now a **6-sub-agent topology** (`sub_breaking_news`, `sub_threads`, `sub_quotes`, `sub_infographics`, `sub_gold_media`, `sub_gold_history`) running 7 jobs total (6 subs + `morning_digest`). The stagger-within-4h cohort collapses from offsets `[0, 17, 34]` to `[0, 17]`; the 34-minute slot is retired, not reassigned. Historical `content_bundles.content_type='long_form'` and `agent_runs.agent_name='sub_long_form'` rows are preserved in the DB — no Alembic migration, no data loss. The deprecated frontend route `/agents/long-form` is removed; bookmark-breakage is acceptable for a single-user internal tool. The `long_form_post` compat field inside `thread` draft_content (ThreadPreview.tsx, content_agent.py `_extract_check_text` thread branch) is retained — it is a thread-format feature, not a `sub_long_form` reference. This purge mirrors the `260419-lvy` (Instagram) and `260420-sn9` (Twitter + Senior) precedents; partially unwinds `260422-r0r` (long_form 400-char floor) and `260421-eoe` (the 7-sub-agent split that created `sub_long_form`).
   ```

**Verification after T9:**
```bash
grep -c "6-sub-agent" CLAUDE.md
```
Expected: ≥1 (opening paragraph).

```bash
grep -c "Historical note (2026-04-23)" CLAUDE.md
```
Expected: 1.

```bash
grep -n "7-sub-agent\|7 sub-agents" CLAUDE.md
```
Expected: 0 matches in active prose. Any surviving matches must be inside blockquote lines (`>` prefix) — those are historical notes, preserved.

---

## T10 — STATE.md: topology section + Quick Tasks Completed + frontmatter

**Files:** `.planning/STATE.md`

**Exact changes:**

1. Frontmatter `stopped_at` + `last_updated`: Roll to 2026-04-23 / Completed quick task 260423-k8n pattern. Match exact format of current `stopped_at: Completed quick task 260423-j7x (add bullish-only content filter across all 7 sub-agents)` → replace with `Completed quick task 260423-k8n (remove sub_long_form sub-agent entirely; topology 7→6 sub-agents)`. Also update the `last_updated:` field if it's a separate frontmatter key.
2. Topology / "7 sub-agents" references in body prose: `grep -n "7 sub-agents\|across all 7"` → change to `6 sub-agents` / `across all 6` where the text describes current state. **LEAVE HISTORICAL references unchanged** (e.g., descriptions of prior quick tasks that legitimately touched "all 7 sub-agents" at the time).
3. Quick Tasks Completed table: Add a new row (preserve existing table column structure):
   ```
   | 260423-k8n | Remove sub_long_form sub-agent entirely (topology 7→6 sub-agents; historical content_type=long_form rows preserved; no DB migration; mirrors lvy/sn9) | 2026-04-23 | <commit_sha> | Verified | [260423-k8n](./quick/260423-k8n-remove-sub-long-form-sub-agent-entirely-/) |
   ```
   (The commit SHA is filled in AFTER the commit lands — executor must return and patch this cell with the actual SHA, or leave as `<pending>` and update in a follow-up commit if strict single-commit discipline is maintained. Preferred: compute the commit SHA eagerly via `git rev-parse HEAD` after staging + committing, then amend if needed. For simplicity and single-commit goal, use placeholder `<pending>` in the initial commit and note this deviation in SUMMARY.md.)
4. Decisions section (if present): Optionally append an entry matching the lvy/sn9 pattern:
   ```
   [2026-04-23 / quick 260423-k8n]: sub_long_form fully deprecated and purged — topology reduces from 7 to 6 sub-agents. Rationale: simpler topology; longer-form content can ride under threads when warranted. Historical DB rows preserved (no migration). ThreadPreview.tsx long_form_post compat field retained (thread-format feature, not long_form reference).
   ```

**Verification after T10:**
```bash
grep -c "260423-k8n" .planning/STATE.md
```
Expected: ≥2 (stopped_at line + quick-tasks-table row + decision entry if added).

---

## T11 — REQUIREMENTS.md + ROADMAP.md: deprecation notes on long_form mentions

**Files:** `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`

**Exact changes:**

1. `.planning/REQUIREMENTS.md`:
   - **L74** (CONT-09): `long-form article (600-900 words) for complex/data stories` — append ` **(long_form format retired 2026-04-23 — see quick 260423-k8n; longer content now rides under threads)**` after the sentence. Don't strike-through — CONT-09 covers format decision logic broadly; the long-form clause is now deprecated but the line's other clauses stay valid.
   - **L75** (CONT-10): `Thread format drafted as both a tweet thread (3-5 tweets, each under 280 chars) AND a single long-form X post` — this refers to the thread's `long_form_post` compat field (Q1-A retained). **NO CHANGE** — this clause is still accurate. The thread sub-agent continues to emit `long_form_post`.
   - **L124** (CREV-06): `(infographic, thread, long_form, breaking_news, quote, video_clip)` → `(infographic, thread, breaking_news, quote, video_clip)`. Append ` *(long_form removed 2026-04-23 per quick 260423-k8n — historical rows render via FlatTextFallback)*`.

2. `.planning/ROADMAP.md`:
   - **L25** (Phase 7 bullet): `7 content formats (thread, long_form, breaking_news, infographic, video_clip, quote, gold_history)` → `7 content formats (thread, long_form, breaking_news, infographic, video_clip, quote, gold_history) — `long_form` format retired 2026-04-23 (quick 260423-k8n)`.
   - **L30** (Phase 11 bullet): `(infographic, thread, long_form, breaking_news, quote, video_clip)` — append `— long_form retired 2026-04-23 (quick 260423-k8n)`.
   - **L163** (body prose): `best-fit format (thread, long_form, breaking_news, infographic, quote)` → `best-fit format (thread, breaking_news, infographic, quote) — historical long_form format retired 2026-04-23 per quick 260423-k8n`.
   - **L247** (body prose): `long_form (single post)` — append ` [retired 2026-04-23 per quick 260423-k8n]`.
   - **L257** (body prose): `Thread, long_form, breaking_news, video_clip skip image rendering` — leave unchanged or append ` (long_form retired 2026-04-23)`. Cosmetic; choose whichever is cleaner in context.

**Verification after T11:**
```bash
grep -n "long_form\|long-form" .planning/REQUIREMENTS.md .planning/ROADMAP.md
```
Expected: every match is either (a) inside a deprecation note referring to quick 260423-k8n, (b) inside CONT-10 describing the thread's long_form_post compat field (KEEP), or (c) inside a historical Phase 7/11 enumeration now flagged with the retirement note.

</tasks>

<dependency_notes>

- **T1 (file deletions) must precede T2-T8 in a logical sense**, but since all tasks land in a single commit, the working-tree intermediate state is irrelevant. The only hard ordering requirement is that the executor must NOT run `pytest` between T1 and T2 (it would fail on `ModuleNotFoundError: agents.content.long_form`). First-pytest should be AFTER T5 (all scheduler edits landed).
- **T2 (worker.py) and T5 (test_worker.py) are tightly coupled** — test expectations track worker constants. Edit together; verify together.
- **T3 (content_agent.py) and T5 (test_content_agent.py L230) are tightly coupled** — classifier's valid_formats set tested directly.
- **T7 (ContentDetailModal.tsx FORMAT_RENDERERS L27 + switch L126-127) must be edited together** — they're two expressions of the same contract.
- **T9 (CLAUDE.md) must land in the same commit as T1-T8** per CONTEXT.md Self-Check §7 ("CLAUDE.md active prose + new historical note both land in the same commit").
- **T10-T11 (planning docs) can land in the same commit** OR a follow-up commit — CONTEXT.md's single-commit goal prefers everything together; no ordering constraint relative to code tasks.
- **Frontend tests (T8) depend on T7 being complete**: ContentDetailModal.tsx's deleted `long_form` switch case is what Test 3 tested. Delete the test ONLY after the case is gone (otherwise Test 3 fails first on the wrong assertion shape, masking the real regression).

**Suggested executor order:** T1 → T2 → T3 → T4 → T5 → run `uv run pytest scheduler/ -x` (expect 127 green) → run `uv run ruff check scheduler/` → T6 → run `uv run pytest backend/ -x` (expect unchanged green) + `uv run ruff check backend/` → T7 → T8 → run `pnpm -C frontend exec tsc --noEmit` + `pnpm -C frontend lint` + `pnpm -C frontend test` + `pnpm -C frontend build` → T9 → T10 → T11 → run cross-repo grep guard → stage + commit.

</dependency_notes>

<commit_strategy>

### Decision: **Single atomic commit**

### Justification

The total file count is 15 modifications + 3 deletions = 18 files. Compare to precedents:
- **lvy** used 4 commits for ~40 file touches (split by layer: scheduler/backend purge + frontend purge + frontend run-grouping refactor + planning-doc deprecation).
- **sn9** used 5 commits for ~45 file touches (split finer: scheduler + senior trim + backend + frontend + DB+docs).

k8n is **less than half the size** of either precedent. The diff fits in a single grep-reviewable unit. Splitting into two commits (scheduler+backend+docs vs. frontend+tests) would:
- Break the "new 2026-04-23 historical note + active 6-sub-agent prose land together" invariant stated in CONTEXT.md self-check §7.
- Double the SUMMARY.md bookkeeping for a diff that's already cohesive.
- Force the executor to leave the repo in a transiently-broken state between commits (first commit: scheduler expects 6 agents, frontend still has 7 tabs; tests wouldn't cleanly pass on that intermediate state without extra scaffolding).

The diff is cleanly contained, all changes serve one single goal, and there's no layer where a reviewer benefits from seeing layer-N-only in isolation.

**Fallback:** If mid-execution the executor discovers a much larger blast radius than CONTEXT projected (e.g., 40+ files from unanticipated ripple edits), split into 2 commits:
- Commit A: `scheduler/` + `backend/` + `docs/` (T1 partial + T2-T6 + T9-T11)
- Commit B: `frontend/` (T1 partial + T7-T8)

No re-ordering is needed — the frontend can be purged independently after scheduler because no scheduler test imports from frontend.

### Commit message (single atomic — proposed)

```
chore: purge sub_long_form sub-agent entirely (topology 7→6; quick 260423-k8n)

Fully removes the sub_long_form sub-agent across scheduler, backend,
frontend, and active planning docs. Historical content_bundles.content_type='long_form'
and agent_runs.agent_name='sub_long_form' rows remain in the DB untouched
(no Alembic migration). Scheduler drops from 8 jobs (7 subs + morning_digest)
to 7 (6 subs + morning_digest). Stagger offsets collapse [0,17,34] → [0,17].

Deletions:
- scheduler/agents/content/long_form.py (145 lines)
- scheduler/tests/test_long_form.py (89 lines, 4 tests)
- frontend/src/components/content/LongFormPreview.tsx (44 lines)

Key modifications:
- scheduler/worker.py: sub_long_form removed from imports + JOB_LOCK_IDS
  + CONTENT_INTERVAL_AGENTS tuple + docstring + log format string
- scheduler/agents/content_agent.py: classify_format_lightweight valid_formats
  drops "long_form"; prompt text + docstrings updated; build_draft_item
  and _extract_check_text long_form branches removed; thread-branch
  long_form_post compat field preserved (Q1-A)
- frontend/src/config/agentTabs.ts: long-form tab entry deleted
- frontend/src/components/approval/ContentDetailModal.tsx: LongFormPreview
  import + FORMAT_RENDERERS entry + switch case removed (historical rows
  render via FlatTextFallback)
- CLAUDE.md: opening paragraph 7-sub-agent → 6-sub-agent; new 2026-04-23
  historical note appended in lvy/sn9 blockquote pattern
- .planning/STATE.md, REQUIREMENTS.md, ROADMAP.md: long_form references
  flagged with "retired 2026-04-23 per quick 260423-k8n" notes

Test counts:
- scheduler pytest: 131 → 127 (−4)
- backend pytest: unchanged (0 delta)
- frontend vitest: N → N−2 (−1 ContentDetailModal Test 3 + −1 PerAgentQueuePage Long-form tab)

Preservations (regression guards):
- ThreadPreview.tsx long_form_post thread-compat field (L8, L14)
- content_agent.py L679 parts.append(draft_content.get("long_form_post")) in thread branch
- ContentDetailModal.test.tsx L136 long_form_post fixture inside thread-format Test 2

Mirrors precedent pattern: 260419-lvy (Instagram purge), 260420-sn9
(Twitter + Senior purge). Partially unwinds 260422-r0r (long_form
400-char floor) and 260421-eoe (7-sub-agent split that created
sub_long_form).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### Commit message — fallback 2-commit sequence (if diff blast radius >> projected)

```
# Commit A
chore(scheduler,backend,docs): purge sub_long_form from scheduler + backend + docs (quick 260423-k8n part 1/2)

[body covering T1 partial + T2-T6 + T9-T11]

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>

# Commit B
chore(frontend): purge sub_long_form from frontend + tests (quick 260423-k8n part 2/2)

[body covering T1 partial + T7-T8]

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

</commit_strategy>

<validation_gate_sequence>

Run these commands in order after all tasks complete, BEFORE staging for commit. Every command must return the expected result. First failure stops the sequence — fix before proceeding.

```bash
# -------- Gate 1: Scheduler python --------
cd /Users/matthewnelson/seva-mining/scheduler

# 1.1 No sub_long_form / long_form references (except line 679 thread-compat)
grep -rn "long_form\|sub_long_form\|LongForm" . --include="*.py"
# EXPECTED: exactly ONE match — content_agent.py line 679: parts.append(draft_content.get("long_form_post", ""))
# Zero matches acceptable if scheduler/agents/content_agent.py was fully quoted in the grep output and only shows line 679; any OTHER match = regression.

# 1.2 Worker.py scrubbed
grep -n "sub_long_form" worker.py
# EXPECTED: 0 matches

# 1.3 Deleted files confirmed gone
test ! -f agents/content/long_form.py
test ! -f tests/test_long_form.py

# 1.4 Tests green, count = 127
uv run pytest -x 2>&1 | tail -5
# EXPECTED: "127 passed" (was 131 pre-k8n)

# 1.5 Lint clean
uv run ruff check .
# EXPECTED: "All checks passed!" (or equivalent clean output)

# -------- Gate 2: Backend python --------
cd /Users/matthewnelson/seva-mining/backend

# 2.1 No long_form references in backend python (except content_bundle.py L17 before edit — should be 0 after edit)
grep -rn "long_form\|LongForm" . --include="*.py"
# EXPECTED: 0 matches

# 2.2 Tests green, count unchanged
uv run pytest -x 2>&1 | tail -5
# EXPECTED: same count as pre-k8n (0 delta)

# 2.3 Lint clean
uv run ruff check .
# EXPECTED: clean

# -------- Gate 3: Frontend --------
cd /Users/matthewnelson/seva-mining

# 3.1 No long_form/LongForm/long-form references OUTSIDE ThreadPreview.tsx (thread-compat keeps)
grep -rn "long_form\|LongForm\|long-form\|sub_long_form" frontend/src/
# EXPECTED: Only hits inside frontend/src/components/content/ThreadPreview.tsx (lines 8, 14) — long_form_post thread-compat field.
# Also acceptable: ContentDetailModal.test.tsx L136 (long_form_post inside thread fixture, Test 2) if planner decision to KEEP was followed.
# Zero hits in agentTabs.ts, ContentDetailModal.tsx (component, not test), AgentRunsTab.tsx, PerAgentQueuePage.test.tsx, or LongFormPreview.tsx (file must not exist).

# 3.2 TSC clean (no unused imports after LongFormPreview deletion)
pnpm -C frontend exec tsc --noEmit
# EXPECTED: 0 errors

# 3.3 Lint clean
pnpm -C frontend lint
# EXPECTED: 0 errors

# 3.4 Vitest green
pnpm -C frontend test
# EXPECTED: all green, N−2 tests (relative to pre-k8n count)

# 3.5 Build succeeds
pnpm -C frontend build
# EXPECTED: build success, no errors

# -------- Gate 4: Docs --------
cd /Users/matthewnelson/seva-mining

# 4.1 CLAUDE.md active prose uses 6-sub-agent, historical notes retain 7-sub-agent
grep -n "7-sub-agent\|7 sub-agents" CLAUDE.md
# EXPECTED: matches ONLY on lines starting with "> " (blockquote = historical note). Zero matches in active prose lines.

grep -c "6-sub-agent" CLAUDE.md
# EXPECTED: ≥1

grep -c "Historical note (2026-04-23)" CLAUDE.md
# EXPECTED: 1

# 4.2 sub_long_form references in docs only inside historical notes
grep -rn "sub_long_form" CLAUDE.md .planning/
# EXPECTED: Matches inside 260420-sn9 historical note blockquote in CLAUDE.md (pre-existing — historical), inside new 260423-k8n historical note in CLAUDE.md (this task adds it), inside .planning/STATE.md topology references to historical state, and potentially in quick-task SUMMARY files for older quick tasks (e.g., 260422-r0r, 260421-eoe). NO matches in active-state prose (scheduler/worker.py, frontend/src/, backend/app/ were Gates 1-3).

# 4.3 STATE.md has k8n row
grep -c "260423-k8n" .planning/STATE.md
# EXPECTED: ≥2 (stopped_at line + quick-tasks-table row)

# -------- Final guard: cross-repo --------
grep -rn "sub_long_form" scheduler/ backend/ frontend/src/ --include="*.py" --include="*.ts" --include="*.tsx"
# EXPECTED: 0 matches
```

**Pass criteria:** Every command above returns the specified expected output. Any deviation is a regression requiring fix before commit.

</validation_gate_sequence>

<self_check_criteria>

Reframed from CONTEXT.md's "Self-check criteria for planner" as post-execution assertions the executor MUST verify as `true` after all tasks complete:

1. [ ] **No ImportError from long_form deletion.** `uv run pytest scheduler/ -x` completes without `ModuleNotFoundError: agents.content.long_form`. (Proof: Gate 1.4 green.)

2. [ ] **Classifier ↔ builder alignment.** `scheduler/agents/content_agent.py::classify_format_lightweight::valid_formats` set equals `{"breaking_news", "thread", "infographic", "quote"}` (4 elements); `build_draft_item` (L586+) has NO `elif fmt == "long_form":` branch; `_extract_check_text` (L670+) has NO `elif fmt == "long_form":` branch. (Proof: `grep -n '"long_form"' scheduler/agents/content_agent.py` → ONLY line 679 thread-compat hit.)

3. [ ] **CONTENT_INTERVAL_AGENTS tuple structure preserved.** Tuple is a list of (job_id, run_fn, name, lock_id, offset_minutes, interval_hours) 6-tuples with exactly 2 entries (sub_breaking_news + sub_threads). Stagger offsets are `[0, 17]`. worker.py L275 log string format args match the 2-entry tuple (no IndexError at runtime).

4. [ ] **ContentDetailModal type narrowing works post-long_form removal.** `pnpm -C frontend exec tsc --noEmit` is clean. The `FORMAT_RENDERERS` record and the `renderForFormat` switch agree on allowed content_type values. Historical `content_type='long_form'` bundles trigger `showFallback = true` via L47 and render via `FlatTextFallback`.

5. [ ] **All long_form test references updated or deleted.** Gate 1.1, 1.2, 3.1 grep guards pass. `test_long_form.py` file deleted. `PerAgentQueuePage.test.tsx` Long-form tab test (L90-98) deleted. `ContentDetailModal.test.tsx` Test 3 (L153-172) deleted. `test_worker.py` / `test_content_agent.py` / `test_content_init.py` expectations updated in place.

6. [ ] **Historical DB rows queryable.** No Alembic migration written. `content_bundles.content_type` column type unchanged (`String(50)`). Schema-level: historical rows with `content_type='long_form'` remain present; querying `/queue?content_type=long_form` returns them (they just don't surface in agentTabs nav since the tab is gone). `agent_runs.agent_name='sub_long_form'` rows preserved for log review.

7. [ ] **CLAUDE.md active prose + new 2026-04-23 historical note land in the same commit.** `git show HEAD --stat` lists `CLAUDE.md` with both edits (the "6-sub-agent" active-prose change AND the new blockquote append). `grep -B1 -A1 "2026-04-23" CLAUDE.md` confirms the note follows the 2026-04-20 (sn9) note in the blockquote chain.

8. [ ] **ThreadPreview.tsx long_form_post field preserved.** `grep -n long_form_post frontend/src/components/content/ThreadPreview.tsx` returns matches on lines 8 and 14 (ThreadDraft interface + `longFormPost` const). `scheduler/agents/content_agent.py` line 679 still reads `parts.append(draft_content.get("long_form_post", ""))` inside the thread branch.

9. [ ] **content_agent.py L679 explicitly preserved.** Gate 1.1 regex confirms a lone match at content_agent.py:679 inside the thread-format compliance check. This is the ONLY acceptable `long_form*` match in scheduler python.

10. [ ] **Historical note follows lvy/sn9 pattern exactly.** New CLAUDE.md note starts with `> **Historical note (2026-04-23):**`, is a blockquote paragraph (every line prefixed `>`), mentions the quick task id (`260423-k8n`), cites the rationale ("makes things simpler"), describes the topology change (7→6), lists the retained compat field (ThreadPreview.tsx long_form_post), notes the absence of DB migration, and references the two precedent quick tasks (lvy + sn9).

11. [ ] **Commit message + Co-Authored-By footer present.** `git log -1` shows the commit with the message body describing the purge + the `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` trailer.

</self_check_criteria>

<risks_and_mitigations>

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| **TypeScript `content_type` union narrowing.** If `content_type` is narrowed to a literal union (`'thread' \| 'long_form' \| ...`) anywhere in frontend `.ts`/`.tsx`, removing `'long_form'` from that union could break downstream code that destructures on the narrowed type. | LOW | Run `grep -rn "'long_form'\|\"long_form\"" frontend/src/ --include="*.ts" --include="*.tsx"` AFTER T7 — every match must be inside (a) test fixtures (thread-compat, KEEP), (b) ThreadPreview (KEEP), or (c) an edit site already planned. If a match appears in a type definition (e.g., `frontend/src/api/types.ts`), add a T7 sub-step to narrow the union there. |
| **Log format string args mismatch (worker.py L275).** After `CONTENT_INTERVAL_AGENTS` shrinks 3→2, if the log string still interpolates 3 values from the tuple, runtime will raise `IndexError` on first scheduler tick. | MEDIUM | Executor MUST re-read worker.py L275 AFTER editing L104 tuple AND verify the `%` or `.format(...)` substitution arity matches the new tuple length. Ideally, replace positional-index access with `for entry in CONTENT_INTERVAL_AGENTS: ...` or a comprehension that doesn't depend on fixed tuple length. If L275 is just a one-shot "Scheduled sub-agents: X, Y, Z" message enumerating the tuple, a simple `", ".join(a[0] for a in CONTENT_INTERVAL_AGENTS)` is safest. |
| **Priority gap in agentTabs.ts.** Priority 3 becomes a gap (1, 2, 4, 5, 6, 7). If the rendering code does `arr[i].priority === i+1` anywhere, rendering breaks. | LOW | Sidebar + PerAgentQueuePage sort by priority but do NOT assume contiguous. Precedent from CONTEXT.md: "Priority numbers on remaining entries stay as-is (gaps are fine — priority is sort-only)". Executor should spot-check Sidebar.tsx for any index-based assertion. |
| **ContentDetailModal historical `long_form` bundles render as empty FlatTextFallback.** If FlatTextFallback expects `item.source_text` to be non-null and historical long_form rows have null `source_text`, the fallback renders blank. | MEDIUM | Spot-check `FlatTextFallback` component (likely in ContentDetailModal.tsx below L135) — verify it handles nullish `source_text`. If it doesn't, a 3-line defensive edit (display "Legacy long_form bundle — see draft_content JSON in backend" or similar) is out of scope but worth flagging. If fallback is already graceful, no action. Historical long_form row count is low (small, single-user system) — not a production blocker. |
| **Railway scheduler deploy auto-restart.** After this commit lands on main and Railway auto-deploys, the scheduler worker restarts with 7 jobs instead of 8. PostgreSQL advisory lock 1012 (sub_long_form) is never claimed by anyone again — that's fine, locks are per-connection. | LOW | No action needed — lock ID 1012 is simply retired. Executor MUST confirm Railway deploy completes without error after commit lands. |
| **CONT-10 interpretation.** REQUIREMENTS.md CONT-10 says "Thread format drafted as both a tweet thread ... AND a single long-form X post". Some readers might think this refers to sub_long_form (now deleted) — but it actually refers to the thread sub-agent's `long_form_post` companion field (Q1-A: KEEP). | LOW | Planner decision: do NOT strikethrough CONT-10. Spot-check during T11 — if CONT-10's wording is ambiguous enough that future readers think sub_long_form is still referenced, add a clarifying parenthetical: `Thread format drafted as both a tweet thread (3-5 tweets, each under 280 chars) AND a single long-form X post **(the "long_form_post" compat field inside the thread sub-agent's output — distinct from the retired sub_long_form sub-agent, purged 2026-04-23 per quick 260423-k8n)**`. |
| **test_worker.py expected-set assertions.** Tests use frozen set comparisons like `assert set(JOB_LOCK_IDS.keys()) == {...}`. A set mismatch fails clearly, but the ERROR MESSAGE might be hard to parse if the assertion only lists the "missing" side. | LOW | Use `pytest -v` to see full assertion diffs. Known test lines: L95-102 (expected dict), L126-130 (expected set), L144-148 (expected sorted list). All three must drop `sub_long_form`. |
| **Backend queue.py docstring — any consumer generating OpenAPI/Swagger docs?** FastAPI auto-generates OpenAPI schemas from docstrings. Deleting `long_form` from the enumeration could invalidate any external API consumer. | LOW | Single-user internal tool; no external API consumers. Docstring update is cosmetic. |
| **REQUIREMENTS.md / ROADMAP.md historical preservation.** Precedents (lvy, sn9) struck through deprecated REQUIREMENTS items with `[x] → [ ]` and `~~text~~`. This task is LIGHTER — CONT-09 / CREV-06 are partially still true post-k8n (other format mentions remain). Strike-through would be heavy-handed. | LOW | Planner decision: add parenthetical deprecation notes inline rather than full strikethrough. Matches CONTEXT.md guidance: "If it references long_form as a deliverable, add strikethrough-with-note or remove. (Verify during planner phase.)" — verified; inline note is appropriate. |

</risks_and_mitigations>

<output>

After all tasks complete and the commit lands, create:

`.planning/quick/260423-k8n-remove-sub-long-form-sub-agent-entirely-/260423-k8n-SUMMARY.md`

mirroring the structure of `260420-sn9-SUMMARY.md`:

- Frontmatter: `task_id`, `type: quick`, `title`, `completed: 2026-04-23`, `duration`, `commits: [<sha>]` (single SHA if single-commit strategy used), `tags: [sub-agent-removal, topology, 7-to-6, long_form-purge]`
- One-liner (1-2 sentences describing the topology change + what was preserved)
- Context (why now: user request "makes things simpler"; precedent pattern from lvy/sn9; partial unwind of r0r + eoe)
- Task 1 — Scheduler + tests purge (ref commit SHA) — files deleted + modified + verification gates output
- Task 2 — Backend docstring cleanup (ref same commit SHA if single-commit)
- Task 3 — Frontend + tests purge (ref same commit SHA) — including ThreadPreview.tsx + L679 + L136 explicit preservations
- Task 4 — Docs refresh (ref same commit SHA) — CLAUDE.md new note + STATE.md k8n row + REQUIREMENTS/ROADMAP deprecation notes
- Preserved Surfaces table (similar to sn9's — ThreadPreview long_form_post, content_agent.py L679, test fixture L136, DB rows, threads.py prompt)
- Known Stubs (expected: none; `long_form_post` thread-compat field retention is intentional, not a stub)
- Deviations from plan (record any Rule-3 auto-fixes for blocking lint errors, FK issues, or out-of-scope discoveries)
- Test count movement table (scheduler 131→127, backend 0Δ, frontend N→N−2)
- Self-Check: list the commit SHA + SUMMARY.md path + the 3 explicit-keep preservations verified

</output>
