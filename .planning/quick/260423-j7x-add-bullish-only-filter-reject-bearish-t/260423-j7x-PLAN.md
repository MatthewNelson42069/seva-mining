---
phase: quick-260423-j7x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduler/agents/content_agent.py
  - scheduler/agents/content/__init__.py
  - scheduler/agents/content/gold_media.py
  - scheduler/seed_content_data.py
  - scheduler/tests/test_content_agent.py
  - scheduler/tests/test_gold_media.py
autonomous: true
requirements:
  - J7X-01  # Reject price-bearish forecasts at gold gate (category 1)
  - J7X-02  # Reject anti-gold narrative at gold gate (category 2)
  - J7X-03  # Reject factual-negative price movement at gold gate (category 3)
  - J7X-04  # Keep bullish-or-neutral stories (no regression)
  - J7X-05  # Preserve fail-open on JSON parse / API error
  - J7X-06  # Add content_bearish_filter_enabled config flag (gate 1 toggle)
  - J7X-07  # Add bullish-or-neutral criterion to gold_media drafter (gate 2)
  - J7X-08  # Confirm gold_history is already bullish-safe or add a one-line prompt guard (gate 3)
  - J7X-09  # Preserve nnh specific-miner rejection, existing gold gate flag, and compliance layer
  - J7X-10  # Zero diff in 5 downstream sub-agent callers (breaking_news / threads / long_form / quotes / infographics)

must_haves:
  truths:
    - "Gold gate rejects 'Morgan Stanley cuts gold price forecast by ~10%' with reject_reason='bearish_toward_gold'"
    - "Gold gate rejects 'Bitcoin replaces gold as reserve asset of choice' with reject_reason='bearish_toward_gold'"
    - "Gold gate rejects 'Gold fell 1.2% today on stronger dollar' with reject_reason='bearish_toward_gold'"
    - "Gold gate keeps 'Central banks added 800t of gold in Q1' (bullish baseline still passes)"
    - "Gold gate keeps 'Goldman sees gold at $4K by year-end' (upside forecast still passes — forecast direction matters, not the word 'forecast')"
    - "Gold gate keeps 'Gold hits new record high' (neutral factual still passes)"
    - "Gold gate keeps 'Gold holds steady ahead of Fed decision' and 'Gold mixed on Tuesday' (flat/mixed framing is sentiment=neutral, not bearish)"
    - "Gold gate fail-opens (keep=True) when Haiku returns non-JSON — existing behavior preserved"
    - "Gold gate fail-opens (keep=True) when Haiku API raises — existing behavior preserved"
    - "Setting content_bearish_filter_enabled=false bypasses ONLY the bearish check; content_gold_gate_enabled toggle is independent"
    - "gold_media Sonnet drafter returns None (reject) for bearish analyst clips; returns draft dict for bullish analyst clips"
    - "gold_history drafter's system prompt contains language that precludes bearish framing (either pre-existing or newly added)"
    - "nnh specific-miner rejection still fires for 'B2Gold expects lower Q2 output' (category is primary_subject_is_specific_miner, not bearish_toward_gold)"
    - "compliance layer (content_agent.review) is untouched — Seva Mining / financial-advice gating unchanged"
    - "Zero byte diff in breaking_news.py, threads.py, long_form.py, quotes.py, infographics.py"
    - "Full suite: ~128-130 tests pass (baseline 121 + ~8 content_agent + ~2 gold_media)"
  artifacts:
    - path: "scheduler/agents/content_agent.py"
      provides: "Gold gate extended with bearish_toward_gold REJECT criterion, JSON output includes sentiment field, return dict includes 'sentiment' key, INFO log line on bearish rejection, content_bearish_filter_enabled flag honored, existing KEEP-source preamble amended to scope-clarify is_gold_relevant=false vs sentiment"
      contains: "bearish_toward_gold"
    - path: "scheduler/agents/content/gold_media.py"
      provides: "_draft_gold_media_caption adds quality-bar criterion 4 (Bullish or neutral stance) to user_prompt — REJECT path already present"
      contains: "Bullish or neutral stance"
    - path: "scheduler/agents/content/__init__.py"
      provides: "run_text_story_cycle's gate_config dict includes content_bearish_filter_enabled so the flag propagates to the gate"
      contains: "content_bearish_filter_enabled"
    - path: "scheduler/seed_content_data.py"
      provides: "CONFIG_DEFAULTS seeds content_bearish_filter_enabled='true' alongside content_gold_gate_enabled"
      contains: "content_bearish_filter_enabled"
    - path: "scheduler/tests/test_content_agent.py"
      provides: "8 new gold-gate tests — 3 bearish rejections, 3 bullish/neutral keeps, fail-open on parse error, flag-disabled bypass"
      contains: "test_gold_gate_rejects_price_bearish_forecast"
    - path: "scheduler/tests/test_gold_media.py"
      provides: "2 new _draft_gold_media_caption tests — bearish reject path, bullish accept sanity"
      contains: "test_draft_rejects_bearish_analyst_clip"
  key_links:
    - from: "scheduler/agents/content/__init__.py"
      to: "scheduler/agents/content_agent.py::is_gold_relevant_or_systemic_shock"
      via: "gate_config dict at L216-219 passes content_bearish_filter_enabled through to the gate"
      pattern: "content_bearish_filter_enabled"
    - from: "scheduler/agents/content_agent.py::is_gold_relevant_or_systemic_shock"
      to: "Haiku system prompt + JSON output contract"
      via: "New REJECT criterion block + extended JSON schema with sentiment field + amended KEEP-source preamble (scopes 'does NOT trigger rejection' to is_gold_relevant=false, not sentiment)"
      pattern: "sentiment.*bullish.*neutral.*bearish"
    - from: "scheduler/agents/content/gold_media.py::_draft_gold_media_caption"
      to: "Sonnet user_prompt quality-bar block"
      via: "Criterion 4 appended to existing 1-3 list; existing reject JSON path handles bearish output"
      pattern: "4\\. \\*\\*Bullish or neutral"
---

<objective>
Close the "bearish toward gold" content leak by tightening the three drafting gates:

  1. **Gate 1 — Haiku gold gate** (`is_gold_relevant_or_systemic_shock` in `content_agent.py`):
     extend the system prompt and JSON schema so the Haiku call now emits `sentiment: "bullish" | "neutral" | "bearish"` and the function rejects with `reject_reason="bearish_toward_gold"` when `sentiment == "bearish"`. Gated by new `content_bearish_filter_enabled` config key (default `"true"`). Also amend the existing KEEP-source preamble ("Their presence does NOT trigger rejection") to scope it to `is_gold_relevant=false` — the sentence predates the bearish filter and would otherwise contradict the new REJECT criterion when a Goldman/Morgan Stanley forecast is bearish.
  2. **Gate 2 — Sonnet gold_media drafter** (`_draft_gold_media_caption` in `gold_media.py`):
     add a fourth quality-bar criterion ("Bullish or neutral stance") alongside the existing 3. The reject-JSON path is already wired, so Sonnet's own self-reject handles bearish clips.
  3. **Gate 3 — gold_history drafter** (`_draft_gold_history` in `gold_history.py`):
     curated whitelist + drama-first historian voice already precludes bearish framing. **Planner confirmed by reading:** the prompt explicitly instructs "drama-first: lead with the most dramatic moment — the audacity, the risk, the payoff" and operates on pre-researched historical fact sheets (not live news). No bearish-narrative surface exists. **No changes needed.**

The concrete trigger: "Morgan Stanley cuts gold price forecast by almost 10%" (mining.com, score 8.5) passed the existing gold gate at 12:00 PT and was drafted by `sub_infographics`. Post-fix, it is rejected at Gate 1 at ingestion and never reaches any drafter.

The fix is a **policy** change at the gate layer — deliberately NOT a pipeline change. 260422-of3 + 260423-hq7 delivered the pipeline correctly (best-by-score selection, break-after-N-successes); we are narrowing WHAT is allowed into that pipeline.

Purpose: Every draft the system surfaces for approval must be constructive-or-neutral toward gold. Bearish forecasts, anti-gold narrative, and factual-negative price moves are out of brand.

Output:
- Extended Haiku gold gate with bearish sentiment classification + rejection + toggle flag + scope-clarified KEEP-source preamble
- Extended Sonnet gold_media drafter quality bar with criterion #4
- New config key `content_bearish_filter_enabled` seeded to `"true"`
- 8 new gold-gate tests + 2 new gold_media tests (target ~128-130 passing)
- Zero diff in the 5 downstream sub-agent callers and in compliance layer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Prior quick-task summaries (pipeline is already correct — this plan is policy-only):
@.planning/quick/260422-of3-redesign-sub-infographics-to-produce-2-b/260422-of3-SUMMARY.md
@.planning/quick/260423-hq7-fix-max-count-semantics-break-after-n-su/260423-hq7-SUMMARY.md

Primary surfaces being modified:
@scheduler/agents/content_agent.py                # Gate 1 — is_gold_relevant_or_systemic_shock L371-492
@scheduler/agents/content/gold_media.py           # Gate 2 — _draft_gold_media_caption L165-246
@scheduler/agents/content/__init__.py             # run_text_story_cycle (propagates flag at L216-219)
@scheduler/seed_content_data.py                   # CONFIG_DEFAULTS seed list L41-50

Confirmed-safe (no changes):
@scheduler/agents/content/gold_history.py         # Gate 3 — drama-first historian voice already precludes bearish framing

Downstream callers (MUST remain untouched — zero diff):
@scheduler/agents/content/breaking_news.py
@scheduler/agents/content/threads.py
@scheduler/agents/content/long_form.py
@scheduler/agents/content/quotes.py
@scheduler/agents/content/infographics.py

Test baseline (post-hq7): 121/121. Target after this plan: ~128-130 (8 new + 2 new).

<interfaces>
<!-- Current gate return shape (content_agent.py L382-388) — extending with 'sentiment' key -->

From scheduler/agents/content_agent.py::is_gold_relevant_or_systemic_shock:
```python
async def is_gold_relevant_or_systemic_shock(
    story: dict,
    config: dict,
    client: "AsyncAnthropic | None" = None,
) -> dict:
    """
    Current return shape:
        {"keep": bool, "reject_reason": str | None, "company": str | None}

    NEW return shape (backward-compatible — callers that only read "keep" / "reject_reason"
    are unaffected):
        {"keep": bool, "reject_reason": str | None, "company": str | None, "sentiment": str | None}

    Current reject_reason values:
        - None                                  (keep=True)
        - "not_gold_relevant"                   (is_gold_relevant=false)
        - "primary_subject_is_specific_miner"   (nnh rule — UNCHANGED)

    NEW reject_reason value:
        - "bearish_toward_gold"                 (sentiment="bearish" from Haiku)
    """
```

Current Haiku JSON output contract (content_agent.py L448-452):
```json
{"is_gold_relevant": true|false,
 "primary_subject_is_specific_miner": true|false,
 "company": null|"<company name>"}
```

NEW Haiku JSON output contract:
```json
{"is_gold_relevant": true|false,
 "primary_subject_is_specific_miner": true|false,
 "company": null|"<company name>",
 "sentiment": "bullish"|"neutral"|"bearish"}
```

Existing KEEP-source preamble to be AMENDED (content_agent.py L417-420, ending the
third bullet under KEEP):
```
...central banks) as a SOURCE providing a forecast, data, or analysis —
these are sources, not subjects. Their presence does NOT trigger rejection.
```
This sentence predates the bearish filter. Left as-is it contradicts the new
bearish REJECT criterion whenever the cited source's *forecast direction* is
bearish (e.g. Morgan Stanley cutting price targets). Task 2 step 4 replaces
the final clause to explicitly scope the "does NOT trigger rejection" guarantee
to `is_gold_relevant=false` only — sentiment is still evaluated independently.

Existing flag pattern (to mirror):
```python
# content_agent.py L394-396
enabled_str = config.get("content_gold_gate_enabled", "true")
if str(enabled_str).lower() in ("false", "0", "no"):
    return _KEEP
```

Caller-side gate_config dict (content/__init__.py L216-219):
```python
gate_config = {
    "content_gold_gate_enabled": "true",
    "content_gold_gate_model": "claude-haiku-4-5",
}
# Add: "content_bearish_filter_enabled": "true",
```

gold_media drafter existing quality-bar block (gold_media.py L194-207):
```
## Quality bar — draft ONLY if the video features an identifiable senior
analyst or economist with clear gold-market commentary:
1. **Speaker identifiable:** ...
2. **Substantive commentary:** ...
3. **Gold focus:** ...

If the video does NOT meet this bar, respond with:
{"reject": true, "rationale": "1-2 sentence reason"}
```
Add criterion #4 inline before the "If the video does NOT meet this bar" line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for all three gates (RED)</name>
  <files>
    scheduler/tests/test_content_agent.py
    scheduler/tests/test_gold_media.py
  </files>
  <behavior>
    ## scheduler/tests/test_content_agent.py — 8 new tests

    Each test mocks `AsyncAnthropic` via `AsyncMock` and builds a `MagicMock` response
    with `response.content = [MagicMock(text='{...json...}')]`. Follow the exact
    pattern used in `test_classify_format_lightweight_returns_valid_format` (L183-194)
    and the existing gold-gate Haiku mocking conventions (no existing gate tests —
    this is net-new coverage for is_gold_relevant_or_systemic_shock).

    Shared config fixture used by each test:
    ```python
    GATE_CONFIG = {
        "content_gold_gate_enabled": "true",
        "content_gold_gate_model": "claude-haiku-4-5",
        "content_bearish_filter_enabled": "true",
    }
    ```

    1. **test_gold_gate_rejects_price_bearish_forecast**
       Story: `{"title": "Morgan Stanley cuts gold price forecast by almost 10%", "summary": "..."}`
       Mock Haiku returns: `{"is_gold_relevant": true, "primary_subject_is_specific_miner": false, "company": null, "sentiment": "bearish"}`
       Assert: `result["keep"] is False`, `result["reject_reason"] == "bearish_toward_gold"`, `result["sentiment"] == "bearish"`.

    2. **test_gold_gate_rejects_anti_gold_narrative**
       Story: `{"title": "Bitcoin replaces gold as reserve asset of choice", ...}`
       Mock returns sentiment="bearish".
       Assert: same rejection shape as #1.

    3. **test_gold_gate_rejects_factual_price_decline**
       Story: `{"title": "Gold fell 1.2% today on stronger dollar", ...}`
       Mock returns sentiment="bearish".
       Assert: same rejection shape.

    4. **test_gold_gate_keeps_bullish_central_bank_buying**
       Story: `{"title": "Central banks added 800t of gold in Q1", ...}`
       Mock returns: `{"is_gold_relevant": true, "primary_subject_is_specific_miner": false, "company": null, "sentiment": "bullish"}`
       Assert: `result["keep"] is True`, `result["reject_reason"] is None`, `result["sentiment"] == "bullish"`.

    5. **test_gold_gate_keeps_bullish_price_forecast**
       Story: `{"title": "Goldman sees gold at $4K by year-end", ...}`
       Mock returns sentiment="bullish".
       Assert: `result["keep"] is True` — proves it's DIRECTION that matters, not the word "forecast" (prevents false positives against Goldman/UBS/etc. upside calls).

    6. **test_gold_gate_keeps_neutral_record_high**
       Story: `{"title": "Gold hits new record high", ...}`
       Mock returns sentiment="neutral".
       Assert: `result["keep"] is True`.

    7. **test_gold_gate_fail_open_on_parse_error**
       Mock Haiku returns text="not valid json at all".
       Assert: `result["keep"] is True`, `result["reject_reason"] is None`, `result["sentiment"] is None`.
       Preserves existing fail-open. (Mirrors existing L463-468 behavior.)

    8. **test_gold_gate_flag_disabled**
       Config has `content_bearish_filter_enabled="false"`.
       Mock Haiku returns sentiment="bearish" (what WOULD be rejected if flag were on).
       Assert: `result["keep"] is True` — the bearish rejection path is bypassed, but gold_relevance and specific-miner checks still fire normally (the flag is surgical — it toggles ONLY the new bearish-sentiment branch, not the whole gate).

    Additional sanity regression (NOT a new test — verify by grep that nnh test fires unchanged; we are not adding it, just confirming existing tests still pass): existing `primary_subject_is_specific_miner` behavior must still work for a mock with `is_gold_relevant=true, primary_subject_is_specific_miner=true` — this should still return `reject_reason="primary_subject_is_specific_miner"` regardless of sentiment.

    ## scheduler/tests/test_gold_media.py — 2 new tests

    Follow exact pattern of existing `test_draft_gold_media_caption_returns_none_on_reject` (L114-133) — mock Sonnet's `client.messages.create` to return a `response.content = [MagicMock(text='{"reject": true, "rationale": "..."}')]`.

    9. **test_draft_rejects_bearish_analyst_clip**
       Mock Sonnet response text: `'{"reject": true, "rationale": "Speaker is predicting near-term gold price decline — violates Bullish or neutral stance criterion"}'`
       Call `_draft_gold_media_caption` with bearish-sounding `tweet_text="Analyst: expect gold to pull back to $2100 as dollar strengthens"`.
       Assert: `draft is None`, and caplog contains "rejected" + "Bullish or neutral" or "bearish" in the rationale line.

       **Inline comment to include in the test body** (above the caplog assertion, verbatim):
       ```python
       # caplog assertion verifies the reject-log code path fires;
       # rationale wording is mocked, not tested against real model output.
       # (Real Sonnet rationales will vary — the phrase "Bullish or neutral"
       # appears here only because the mocked response string is authored
       # to contain it. This test asserts the logging wiring, not the
       # natural-language content of production rationales.)
       ```

    10. **test_draft_accepts_bullish_analyst_clip**
        Mock Sonnet response text: `'{"twitter_caption":"Central banks keep buying at these levels — structural tailwind.","instagram_caption":"Central bank gold buying remains strong..."}'`
        Call with bullish `tweet_text`.
        Assert: `draft is not None`, `draft["format"] == "gold_media"`, `draft["twitter_caption"]` is non-empty.
        (Sanity — proves criterion #4 doesn't block bullish clips.)

    All 10 tests MUST fail initially (RED) because:
      - content_agent.py does not yet emit or inspect `sentiment`
      - content_agent.py does not yet honor `content_bearish_filter_enabled`
      - gold_media.py has no criterion #4 in its prompt (though the mock-based tests for gold_media will actually PASS pre-fix because the test mocks the Sonnet response directly — this is intentional: the tests document the expected behavior contract, not the prompt text. The prompt change is verified via the grep gate in Task 3).
  </behavior>
  <action>
    Open both test files. Append the 8 new test functions to `test_content_agent.py`
    (after the existing compliance/build_draft_item block at end of file). Follow the
    module's existing conventions:
      - `@pytest.mark.asyncio` for async tests
      - `AsyncMock()` for the anthropic client
      - `MagicMock()` for response structure with `.content = [MagicMock(text=...)]`
      - Direct call: `result = await content_agent.is_gold_relevant_or_systemic_shock(story, GATE_CONFIG, client=client)`
      - Assertions on `result["keep"]`, `result["reject_reason"]`, `result["sentiment"]`

    Append the 2 new test functions to `test_gold_media.py` after the existing
    `test_draft_gold_media_caption_returns_none_on_reject` (end of file).

    **For test #9 specifically**, include the inline comment block (shown in the
    `<behavior>` section) immediately above the caplog assertion. The comment
    documents that this test is verifying the code path fires — NOT asserting
    against real Sonnet rationale wording (which varies). This closes the
    reviewer's info-level concern that future readers could mistake the caplog
    substring check for a wording contract against the live model.

    Do NOT modify any production code in this task. Run the suite — the 8 gate tests
    MUST fail (the 2 gold_media tests may pass because they mock Sonnet's response
    directly; that's fine — they document the behavior and will still be part of the
    suite going forward).

    Commit as a separate atomic RED commit:
    `test(content): add failing tests for bearish-toward-gold rejection gate (quick-260423-j7x)`
  </action>
  <verify>
    <automated>uv run pytest scheduler/tests/test_content_agent.py scheduler/tests/test_gold_media.py -q 2>&1 | tail -10</automated>
    Expected: 8 failures in test_content_agent.py (gate logic not yet implemented); test_gold_media.py passes (mocks bypass prompt).
    Also run: `uv run pytest scheduler/ -q 2>&1 | tail -5` → baseline 121 still pass + new tests show expected failures.
    Additionally: `grep -n "caplog assertion verifies the reject-log code path" scheduler/tests/test_gold_media.py` → 1 match (confirms the clarifying comment for test #9 is present verbatim).
  </verify>
  <done>
    - test_content_agent.py has 8 new tests named exactly as listed above, all @pytest.mark.asyncio except where noted
    - test_gold_media.py has 2 new tests named exactly as listed above
    - test #9 (`test_draft_rejects_bearish_analyst_clip`) includes the inline comment documenting that the caplog assertion verifies the log path fires, NOT production rationale wording
    - The 8 gate tests FAIL when run against unmodified content_agent.py (proving they'd catch regressions)
    - RED commit landed on `main`
    - `ruff check scheduler/tests/` clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bearish_toward_gold rejection in gold gate + config flag + gate_config propagation + KEEP-source preamble scope-clarify (GREEN)</name>
  <files>
    scheduler/agents/content_agent.py
    scheduler/agents/content/__init__.py
    scheduler/seed_content_data.py
  </files>
  <behavior>
    After this task all 8 gate tests from Task 1 pass, 2 gold_media tests still pass, existing nnh tests still pass, baseline tests still pass.

    Specifically:
    - Bearish Haiku verdict → keep=False, reject_reason="bearish_toward_gold"
    - Bullish / neutral Haiku verdict → keep=True (barring nnh / not_gold_relevant)
    - Flat/mixed framing ("gold holds steady", "gold mixed") → sentiment="neutral" → keep=True
    - `content_bearish_filter_enabled="false"` → bypass bearish branch only (gold_relevance + specific_miner still fire)
    - Malformed JSON → fail-open (existing behavior preserved with new sentiment key = None)
    - API error → fail-open (existing behavior preserved)
    - `primary_subject_is_specific_miner` precedence: if both specific_miner=true AND sentiment=bearish, reject_reason is still "primary_subject_is_specific_miner" (nnh wins — mining companies are rejected before sentiment is even evaluated). This matches the existing ordering at L477-484.
    - The existing KEEP-source preamble at L417-420 is amended so "Their presence does NOT trigger rejection" is scoped explicitly to `is_gold_relevant=false` — a Goldman/Morgan Stanley SOURCE citation still does not cause an `is_gold_relevant=false` rejection, but the forecast's DIRECTION is independently evaluated and a bearish forecast yields `sentiment="bearish"` and IS rejected by the new bearish filter. This eliminates the prompt-level contradiction that would otherwise have Haiku trying to satisfy two rules simultaneously.
  </behavior>
  <action>
    ### 2a. scheduler/agents/content_agent.py — extend is_gold_relevant_or_systemic_shock (L371-492)

    1. Update docstring (L376-391): mention the new bearish_toward_gold rejection category, the new sentiment key in return shape, and the new `content_bearish_filter_enabled` flag. Preserve the existing "Bypassed when config key content_gold_gate_enabled is False" line and add: "Bearish check alone bypassed when content_bearish_filter_enabled is False."

    2. Update `_KEEP` sentinel (L392) to include sentiment=None:
       ```python
       _KEEP = {"keep": True, "reject_reason": None, "company": None, "sentiment": None}
       ```

    3. Read the new flag AFTER the existing content_gold_gate_enabled check (so full-gate-off still wins):
       ```python
       # After L396 existing early-return block
       bearish_enabled_str = config.get("content_bearish_filter_enabled", "true")
       bearish_filter_on = str(bearish_enabled_str).lower() not in ("false", "0", "no")
       ```

    4. Extend the Haiku system prompt (L407-452). Two coordinated wording changes inside the system prompt string — do them in the same edit so the prompt is internally consistent at every git state:

       **4.i. AMEND the existing KEEP-source preamble sentence (currently L417-420).**

       The existing text ends the third KEEP bullet with:
       ```
       ...central banks) as a SOURCE providing a forecast, data, or analysis —
       these are sources, not subjects. Their presence does NOT trigger rejection.
       ```

       Replace that final sentence ("Their presence does NOT trigger rejection.") with the following EXACT wording — spell this into the system prompt string verbatim, preserving the leading hyphen-break and the newlines that already separate the bullet from the REJECT block:

       ```
       Their presence does NOT trigger is_gold_relevant=false rejection — but the forecast's direction still determines sentiment (a bearish forecast from Goldman/Morgan Stanley yields sentiment=bearish and is rejected by the bearish filter).
       ```

       **Why this edit is non-optional:** without it, the same prompt says both "Morgan Stanley's presence does NOT trigger rejection" (old rule, scoped to `is_gold_relevant`) and "Morgan Stanley cuts gold price forecast by ~10% → reject as bearish_toward_gold" (new rule, scoped to `sentiment`). Haiku sees a contradiction and resolves it non-deterministically. Scoping the old guarantee to `is_gold_relevant=false` makes both rules simultaneously satisfiable for every input.

       **4.ii. ADD a new REJECT section** between the existing `REJECT — primary_subject_is_specific_miner` block and the `REJECT — not_gold_relevant` block. Use this exact wording:

       ```
       REJECT — bearish_toward_gold
       (is_gold_relevant=true but sentiment="bearish") if the story's angle
       is negative toward gold or its price. Three categories:
         1. Price-bearish forecasts or predictions: analyst cuts gold price
            target, bank downgrades gold outlook, "expect pullback/correction"
            framing. Example: "Morgan Stanley cuts gold price forecast by ~10%".
         2. Anti-gold narrative: gold dismissed in favor of alternatives,
            gold losing relevance/appeal, bitcoin/crypto replacing gold.
            Example: "Bitcoin replaces gold as reserve asset of choice".
         3. Factual-negative price movement: gold fell / dropped / slumped /
            pulled back. Example: "Gold fell 1.2% today on stronger dollar".
       DIRECTION matters, not the word "forecast": upside forecasts
       ("Goldman sees gold at $4K") are sentiment="bullish" and KEPT.
       Neutral factual ("gold hits new record high") is sentiment="neutral"
       and KEPT.
       Mixed or flat movement stories ("gold holds steady", "gold flat", "gold mixed") are sentiment="neutral" and KEPT.
       ```

       The final line (mixed/flat framing) is authored verbatim as shown — it must be present in the system prompt string, not inferred. Without it, Haiku treats flat-movement wire copy as ambiguous and may default to either bucket; the explicit rule forces neutral classification (which maps to KEEP) and eliminates false-positive bearish rejections on everyday "gold holds steady ahead of Fed decision" wire stories.

    5. Update the JSON output contract line (currently L448-452) to require sentiment:
       ```
       Respond with ONLY a compact JSON object, no other text:
       {"is_gold_relevant": true|false,
        "primary_subject_is_specific_miner": true|false,
        "company": null|"<company name if primary_subject_is_specific_miner is true>",
        "sentiment": "bullish"|"neutral"|"bearish"}
       ```

    6. Parse the new field after the existing parse block (~L470-475):
       ```python
       sentiment_raw = parsed.get("sentiment")
       sentiment = str(sentiment_raw).strip().lower() if sentiment_raw else None
       if sentiment not in ("bullish", "neutral", "bearish"):
           sentiment = None  # defensive: unknown → treat as missing (fail-open on this axis)
       ```

    7. Insert the bearish rejection AFTER the existing `is_specific_miner` check (L479-484) and BEFORE the final `return _KEEP` (L485). Order: not_gold → specific_miner → bearish → keep. This preserves nnh precedence exactly.
       ```python
       if bearish_filter_on and sentiment == "bearish":
           logger.info(
               "Gold gate rejected bearish-toward-gold: %r (sentiment=bearish)",
               title[:60],
           )
           return {
               "keep": False,
               "reject_reason": "bearish_toward_gold",
               "company": None,
               "sentiment": "bearish",
           }
       ```
       (Log line mirrors the INFO pattern used by callers at content/__init__.py L244-247 — concise, title-truncated, structured.)

    8. Extend the final `return _KEEP` (L485) so sentiment propagates on the keep path:
       ```python
       return {"keep": True, "reject_reason": None, "company": None, "sentiment": sentiment}
       ```
       Also update the two intermediate reject returns (not_gold, specific_miner) to include `"sentiment": sentiment` for shape-consistency — sentiment is informational metadata on all paths. (Tests don't assert on sentiment value for the existing two reject paths, so this is a safe widening.)

    9. Fail-open branches (parse error at L463-468 and API error at L487-492) — no change to the `return _KEEP` path, but `_KEEP` now carries `sentiment=None` via step 2. These branches preserve existing fail-open semantics by construction.

    ### 2b. scheduler/agents/content/__init__.py — propagate flag at L216-219

    Extend `gate_config` so the per-cycle config dict carries the new key (the gate reads from this dict, not from DB — simplest propagation path, no refactor needed):
    ```python
    gate_config = {
        "content_gold_gate_enabled": "true",
        "content_gold_gate_model": "claude-haiku-4-5",
        "content_bearish_filter_enabled": "true",
    }
    ```
    (Hardcoded "true" mirrors the existing gold_gate_enabled pattern — runtime toggle off would require a DB read, which is out of scope for this plan. Config-backed runtime toggle is a follow-up if needed.)

    ### 2c. scheduler/seed_content_data.py — seed the new flag default

    Append to `CONFIG_DEFAULTS` (L41-50):
    ```python
    ("content_bearish_filter_enabled",       "true"),
    ```
    Placement: directly after `content_gold_gate_enabled` line for readability. Seed existing databases via the standard `seed_content_data` path — no migration needed since Config is a key-value table and the defaulted `config.get(..., "true")` in content_agent.py handles missing rows.

    ### Commit

    Single atomic GREEN commit:
    `feat(content): reject bearish-toward-gold stories at Haiku gate (quick-260423-j7x)`
    Files: `scheduler/agents/content_agent.py`, `scheduler/agents/content/__init__.py`, `scheduler/seed_content_data.py`
  </action>
  <verify>
    <automated>uv run pytest scheduler/tests/test_content_agent.py -q 2>&1 | tail -5</automated>
    Expected: all 8 new gate tests GREEN, all existing test_content_agent tests still green.

    Also:
    - `uv run pytest scheduler/ -q 2>&1 | tail -5` → target ~128 passing (121 baseline + 7 new non-gold_media; 1 of 8 may overlap with existing flag-disabled pattern → count is approximate).
    - `uv run ruff check scheduler/` → clean
    - `grep -n "bearish_toward_gold" scheduler/agents/content_agent.py` → ≥2 matches (prompt block + return dict)
    - `grep -n "content_bearish_filter_enabled" scheduler/agents/content_agent.py scheduler/agents/content/__init__.py scheduler/seed_content_data.py` → ≥3 matches (one per file)
    - `grep -n "is_gold_relevant=false rejection" scheduler/agents/content_agent.py` → 1 match (confirms the KEEP-source preamble amendment from step 4.i is present verbatim)
    - `grep -n "gold holds steady" scheduler/agents/content_agent.py` → 1 match (confirms the flat/mixed neutral-framing sentence from step 4.ii is present verbatim in the system prompt)
    - `git diff scheduler/agents/content/breaking_news.py scheduler/agents/content/threads.py scheduler/agents/content/long_form.py scheduler/agents/content/quotes.py scheduler/agents/content/infographics.py` → EMPTY (no diff in the 5 callers)
    - `git diff scheduler/agents/content/gold_history.py` → EMPTY (confirmed safe, no changes)
  </verify>
  <done>
    - All 8 gate tests from Task 1 pass
    - Existing nnh specific-miner rejection test still passes (precedence preserved)
    - Fail-open on parse error still passes (new `sentiment=None` propagates correctly)
    - content_bearish_filter_enabled flag toggles ONLY the bearish branch — gold_relevance and specific_miner unaffected
    - System prompt string now contains the exact amended KEEP-source preamble sentence: "Their presence does NOT trigger is_gold_relevant=false rejection — but the forecast's direction still determines sentiment (a bearish forecast from Goldman/Morgan Stanley yields sentiment=bearish and is rejected by the bearish filter)."
    - System prompt string now contains the exact flat/mixed neutral-framing sentence: "Mixed or flat movement stories (\"gold holds steady\", \"gold flat\", \"gold mixed\") are sentiment=\"neutral\" and KEPT."
    - Downstream callers (breaking_news / threads / long_form / quotes / infographics) have zero diff
    - gold_history.py has zero diff (Gate 3 confirmed safe)
    - GREEN commit landed on `main`
    - ruff clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add criterion #4 to gold_media Sonnet drafter quality bar (GREEN) + full validation</name>
  <files>
    scheduler/agents/content/gold_media.py
  </files>
  <behavior>
    After this task:
    - `_draft_gold_media_caption` user_prompt contains criterion #4 "Bullish or neutral stance" in the 1-3 enumerated quality bar list (becoming 1-4).
    - Sonnet is now instructed to self-reject bearish analyst commentary via the existing `{"reject": true, "rationale": "..."}` path (already wired at L233-238 — no new code).
    - The 2 gold_media tests from Task 1 continue to pass (they mock Sonnet's response directly, so they verify the consumer contract).
    - Grep gate confirms prompt text is present.
  </behavior>
  <action>
    ### 3a. scheduler/agents/content/gold_media.py — extend `_draft_gold_media_caption` user_prompt (L194-207)

    Locate the existing quality-bar block:
    ```
    ## Quality bar — draft ONLY if the video features an identifiable senior
    analyst or economist with clear gold-market commentary:
    1. **Speaker identifiable:** ...
    2. **Substantive commentary:** ...
    3. **Gold focus:** ...

    If the video does NOT meet this bar, respond with:
    {"reject": true, "rationale": "1-2 sentence reason"}
    ```

    Insert criterion #4 between criterion 3 and the "If the video does NOT meet this bar" line. Use this exact wording:

    ```
    4. **Bullish or neutral stance:** Speaker's commentary must be
       constructive or neutral on gold. REJECT if the speaker is predicting
       gold price decline, dismissing gold's role vs alternatives
       (bitcoin/crypto replacing gold, gold losing relevance), or
       emphasizing a bearish near-term outlook. Upside forecasts and
       neutral factual analysis PASS — direction matters, not the word
       "forecast".
    ```

    No code logic changes — the existing `if parsed.get("reject") is True:` path (L233-238) already returns None for Sonnet-driven self-rejections. Criterion #4 simply broadens what Sonnet rejects.

    No changes to the system_prompt (L180-188) — the user_prompt is where the quality bar lives.

    No changes to the JSON output schema in the prompt (L210-213) — reject path uses the separate {"reject": true, ...} branch.

    ### 3b. Full validation gate run

    Run the full validation suite from the task description:
    - `uv run ruff check scheduler/` → clean
    - `uv run pytest scheduler/ -x` → all pass (~128-130 target)
    - `grep -n "bearish_toward_gold" scheduler/agents/content_agent.py` → ≥1 match
    - `grep -nE "Bullish or neutral|bullish or neutral" scheduler/agents/content/gold_media.py` → ≥1 match
    - `git diff scheduler/agents/content/breaking_news.py scheduler/agents/content/threads.py scheduler/agents/content/long_form.py scheduler/agents/content/quotes.py scheduler/agents/content/infographics.py` → empty
    - `git diff scheduler/agents/content/gold_history.py` → empty
    - `git diff scheduler/agents/content_agent.py | grep -E "^\\-.*review\\(|^\\-.*check_compliance"` → empty (compliance layer untouched)

    ### Commit

    Single atomic commit:
    `feat(content/gold_media): add Bullish-or-neutral criterion #4 to drafter quality bar (quick-260423-j7x)`
    Files: `scheduler/agents/content/gold_media.py`
  </action>
  <verify>
    <automated>uv run pytest scheduler/ -q 2>&1 | tail -8 && uv run ruff check scheduler/ 2>&1 | tail -3 && grep -c "bearish_toward_gold" scheduler/agents/content_agent.py && grep -cE "Bullish or neutral|bullish or neutral" scheduler/agents/content/gold_media.py</automated>
    Expected:
      - pytest: ~128-130 passed, 0 failed
      - ruff: All checks passed!
      - bearish_toward_gold count: ≥2
      - Bullish or neutral count: ≥1

    Also manually verify zero-diff on callers:
    ```
    git diff HEAD~3 -- scheduler/agents/content/breaking_news.py scheduler/agents/content/threads.py scheduler/agents/content/long_form.py scheduler/agents/content/quotes.py scheduler/agents/content/infographics.py scheduler/agents/content/gold_history.py
    ```
    Expected: empty output (6 files, 0 lines changed across all 3 commits of this plan).
  </verify>
  <done>
    - gold_media.py user_prompt contains criterion 4 with the exact "Bullish or neutral stance" header
    - All 10 tests from Task 1 pass
    - Full suite passes (target ~128-130)
    - ruff clean
    - All 5 downstream caller files and gold_history.py have ZERO diff across all 3 commits of this plan
    - Commit landed on `main`
  </done>
</task>

</tasks>

<verification>
Full phase verification after all 3 tasks:

1. **Functional — gold gate rejects all 3 bearish categories:**
   - Morgan Stanley-style price-bearish forecast → rejected with reject_reason="bearish_toward_gold"
   - Bitcoin-replaces-gold narrative → rejected with same reason
   - "Gold fell 1.2% today" factual-negative → rejected with same reason

2. **Functional — gold gate KEEPS bullish and neutral stories:**
   - Goldman upside forecast → kept (direction matters, not the word "forecast")
   - Central bank buying → kept
   - "Gold hits record high" → kept
   - Flat/mixed framing ("gold holds steady", "gold mixed on Tuesday") → kept as sentiment=neutral

3. **Functional — gold_media Sonnet drafter:**
   - Bearish analyst clip → Sonnet self-rejects via existing {"reject": true, ...} path
   - Bullish analyst clip → draft dict returned

4. **Config flag — surgical toggle:**
   - content_bearish_filter_enabled="false" bypasses ONLY the bearish branch
   - content_gold_gate_enabled="false" still bypasses the entire gate (precedence preserved)

5. **Prompt internal consistency (new):**
   - The KEEP-source preamble ("Their presence does NOT trigger…") is scoped to `is_gold_relevant=false` — no longer a blanket guarantee that would contradict the new bearish REJECT criterion for a Goldman/Morgan Stanley bearish forecast.
   - Flat/mixed neutral framing is explicitly handled in the REJECT criterion block, eliminating the silent neutral-bucket gap.

6. **Preservation — nothing else changed:**
   - nnh specific-miner rejection: grep `primary_subject_is_specific_miner` still in place, tests pass
   - Fail-open on JSON parse error: test_gold_gate_fail_open_on_parse_error passes
   - Fail-open on API error: existing coverage (not re-tested, but untouched code path)
   - compliance layer (content_agent.review): `git diff` shows no changes to review() or check_compliance()
   - 5 downstream sub-agent callers: zero byte diff
   - gold_history.py: zero byte diff (Gate 3 confirmed safe by planner read-through)

7. **Tests:**
   - Baseline 121/121 still pass
   - 10 new tests (8 gold_gate + 2 gold_media) pass
   - Final target: 131/131 (or 129-131 depending on test_gold_media overlap — ~128-130 is the expected range in the task description)
   - `uv run ruff check scheduler/` clean
</verification>

<success_criteria>
- [ ] 3 atomic commits on `main` (RED / gate+flag GREEN / gold_media GREEN), each with message prefixed `test(content):` / `feat(content):` / `feat(content/gold_media):` and suffixed `(quick-260423-j7x)`
- [ ] `uv run pytest scheduler/ -q` reports ~128-131 tests passing, 0 failing
- [ ] `uv run ruff check scheduler/` reports 0 errors
- [ ] `grep -c "bearish_toward_gold" scheduler/agents/content_agent.py` ≥ 2
- [ ] `grep -cE "Bullish or neutral|bullish or neutral" scheduler/agents/content/gold_media.py` ≥ 1
- [ ] `grep -n "content_bearish_filter_enabled" scheduler/` returns matches in content_agent.py, content/__init__.py, and seed_content_data.py (3 files minimum)
- [ ] `grep -n "is_gold_relevant=false rejection" scheduler/agents/content_agent.py` returns 1 match (KEEP-source preamble amendment present verbatim)
- [ ] `grep -n "gold holds steady" scheduler/agents/content_agent.py` returns 1 match (flat/mixed neutral-framing sentence present verbatim)
- [ ] `grep -n "caplog assertion verifies the reject-log code path" scheduler/tests/test_gold_media.py` returns 1 match (test #9 inline comment present verbatim)
- [ ] `git diff <base>..HEAD -- scheduler/agents/content/breaking_news.py scheduler/agents/content/threads.py scheduler/agents/content/long_form.py scheduler/agents/content/quotes.py scheduler/agents/content/infographics.py scheduler/agents/content/gold_history.py` is empty (zero byte diff across 6 files)
- [ ] `git diff <base>..HEAD -- scheduler/agents/content_agent.py | grep -E "^\\-.*def (review|check_compliance)"` is empty (review/check_compliance function signatures untouched)
- [ ] Gate 1 prompt contains three bearish categories with named examples (Morgan Stanley, Bitcoin, "gold fell")
- [ ] Gate 1 prompt's KEEP-source preamble is scoped to `is_gold_relevant=false` (no contradiction with the bearish REJECT criterion when a cited source's forecast direction is bearish)
- [ ] Gate 1 prompt explicitly classifies flat/mixed framing as sentiment="neutral" and KEPT
- [ ] Gate 2 prompt contains criterion #4 with "Bullish or neutral stance" header
- [ ] Gate 3 (gold_history.py) has zero diff — planner confirmed drama-first historian voice + curated whitelist already preclude bearish framing
</success_criteria>

<output>
After completion, create `.planning/quick/260423-j7x-add-bullish-only-filter-reject-bearish-t/260423-j7x-SUMMARY.md` following the standard quick-task SUMMARY template. Include:

- One-liner: "Added bullish-only content policy across the 3 drafting gates — gold gate now rejects bearish-toward-gold stories with a new content_bearish_filter_enabled flag; gold_media Sonnet drafter adds Bullish-or-neutral criterion #4; gold_history confirmed already safe. Gate 1 prompt also scope-clarifies the existing KEEP-source preamble (is_gold_relevant vs sentiment) and explicitly classifies flat/mixed framing as neutral."
- What Changed (per-file delta with line ranges)
- Commits (3 SHAs, messages)
- Test Results (before / after)
- Preservation Checks (5 downstream callers, gold_history, compliance layer, nnh, fail-open)
- Decisions Honored (config-flag pattern mirrors content_gold_gate_enabled; nnh precedence preserved; zero-diff in callers; prompt internal consistency — KEEP-source preamble scoped to is_gold_relevant, flat/mixed framing explicitly neutral)
- Validation Gates (all green)
- Next Observable Step: Monday's 12:00 PT `sub_infographics` cron — confirm no bearish stories reach the approval queue; any Morgan-Stanley-style forecast cut logged with `reject_reason="bearish_toward_gold"` in scheduler/worker logs
</output>
