---
phase: quick-260420-r18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/content/LongFormPreview.tsx
  - frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx
  - scheduler/agents/content_agent.py
  - scheduler/tests/test_content_agent.py
autonomous: true
requirements:
  - R18-A-long-form-field-name-fix
  - R18-B-remove-x-as-quote-source

must_haves:
  truths:
    - "Dashboard long_form cards render the actual post body (prose, not the 'No draft content available.' placeholder) for existing DB rows that already wrote the `post` field"
    - "No new ContentBundle row is created with content_type='quote' and source_name in {PeterSchiff, jimrickards, JimRickards, RealJimRogers, RobertKiyosaki} after this change"
    - "Article-path quote format (Sonnet chooses 'quote' from article content) still works — _draft_quote_post() is untouched and its two existing tests still pass"
    - "Video-clip Twitter pipeline is untouched — _search_video_clips(), _draft_video_caption(), and VIDEO_ACCOUNTS still function"
    - "Scheduler `uv run ruff check .` returns clean; scheduler `uv run pytest` all tests pass"
    - "Frontend `npm run build` succeeds (tsc + vite); frontend `npm test` all tests pass"
  artifacts:
    - path: frontend/src/components/content/LongFormPreview.tsx
      provides: "Long-form preview component reading `d.post` (not `d.post_text`)"
      contains: "d.post"
    - path: frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx
      provides: "ContentDetailModal test fixture for long_form now uses `post` key"
      contains: "post: 'A detailed long-form post about gold markets.'"
    - path: scheduler/agents/content_agent.py
      provides: "Content agent with QUOTE_ACCOUNTS constant removed, _search_quote_tweets method removed, quote-tweet call site removed from _run_twitter_content_search, format-selection prompt enhanced to promote 'quote' format for article-embedded attributed quotes"
      max_lines: 1925
    - path: scheduler/tests/test_content_agent.py
      provides: "Test suite with QUOTE_ACCOUNTS / _search_quote_tweets references removed (if any), _draft_quote_post tests preserved intact"
  key_links:
    - from: "frontend/src/components/content/LongFormPreview.tsx"
      to: "DB column content_bundles.draft_content.post"
      via: "d.post destructure in the React component (matches what scheduler/agents/content_agent.py writes at the `post` key in long_form draft_content per line 1261)"
      pattern: "d\\.post"
    - from: "scheduler/agents/content_agent.py (_run_twitter_content_search)"
      to: "Twitter API via self.tweepy_client.search_recent_tweets"
      via: "Now ONLY video-clip search path — quote-tweet search branch deleted"
      pattern: "_search_video_clips"
    - from: "scheduler/agents/content_agent.py (format-selection user_prompt ~line 1240)"
      to: "article-path quote format draft via _draft_quote_post"
      via: "Sonnet chooses 'quote' when article contains a verbatim attributed quote from a credible named figure; new prompt sentence promotes this choice"
      pattern: "Prefer 'quote' format when the article contains a verbatim"
---

<objective>
Two atomic fixes shipped together as one quick task:

**A. Long-form dashboard field-name mismatch.** `LongFormPreview.tsx` reads `d.post_text`, but the drafter writes the field as `post`. Every existing long_form bundle in the DB shows "No draft content available." despite having valid 1500+ char prose. Fix: change the frontend to read `d.post`. Rationale: existing DB rows already have `post` key — fixing the frontend retroactively repairs every stale bundle with zero migration. Changing the backend would only help future rows.

**B. Remove X (Twitter) as a quote-post source.** Quotes should only be sourced from reputable articles, not hardcoded X accounts (PeterSchiff, jimrickards, etc.). Delete `QUOTE_ACCOUNTS`, `_search_quote_tweets()`, and the quote-tweet branch in `_run_twitter_content_search()`. Preserve `_draft_quote_post()` (still used by article-path quote format). Enhance the Claude format-selection prompt to favor "quote" format when an article contains a verbatim attributed quote from a credible named figure.

Purpose: Restore dashboard usefulness for the dominant content type (long_form) and realign quote-format semantics with the editorial policy ("quotes only from reputable articles").

Output: 4 files modified. Zero new files. Zero migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Exact contracts the executor needs. Line numbers verified against current main. -->

From frontend/src/components/content/LongFormPreview.tsx (lines 1-44, current state):
```typescript
import { Button } from '@/components/ui/button'
import { toast } from 'sonner'

interface LongFormDraft {
  format: 'long_form'
  post_text?: string                                              // LINE 6 — rename to `post`
}

export function LongFormPreview({ draft }: { draft: unknown }) {
  const d = (draft ?? {}) as LongFormDraft
  const postText = typeof d.post_text === 'string' ? d.post_text : ''   // LINE 11 — rename both reads to `d.post`
  // ... rest unchanged
}
```

From frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx (lines 153-173, Test 3):
```typescript
it('renders LongFormPreview when bundle.content_type === "long_form"', () => {
  const bundle = makeBundle({
    content_type: 'long_form',
    draft_content: {
      format: 'long_form',
      post_text: 'A detailed long-form post about gold markets.',    // LINE 159 — rename to `post`
    },
  })
  // ... rest unchanged, including the getByText assertion (line 172) which is text-content based, not key-based
})
```

From scheduler/agents/content_agent.py (current line ranges VERIFIED against main):
```
Lines 84-90   : QUOTE_ACCOUNTS = ["PeterSchiff", "jimrickards", "JimRickards", "RealJimRogers", "RobertKiyosaki"]  — DELETE
Lines 944-1022: async def _search_quote_tweets(self, session)  — DELETE (whole method, 79 lines)
Line  1240    : format-selection prompt line for "quote" — ENHANCE with one sentence promoting article-embedded quotes
Line  1261    : long_form output shape `{"format": "long_form", "post": "..."}`  — LEAVE UNCHANGED (confirms backend `post` key is authoritative)
Line  1789    : comment "# 11. Twitter video clip + quote search..."  — EDIT to remove "+ quote"
Line  1795    : docstring "Search Twitter for video clips and quotes..."  — EDIT to "Search Twitter for video clips"
Lines 1863-1921: # --- Quote tweets --- block (call + for-loop)  — DELETE (59 lines)
Lines 1804-1861: # --- Video clips --- block  — LEAVE UNCHANGED
```

Video-clip path (PRESERVE everything here):
- `_search_video_clips()` — separate method for video search
- `_draft_video_caption()` — drafter for video captions
- `VIDEO_ACCOUNTS` — module constant
- URL pattern `https://x.com/i/web/status/...` (twitter_agent.py) — different file, untouched

Article-path quote format (PRESERVE):
- `_draft_quote_post()` method — Sonnet still chooses "quote" as a format from article content (see prompt line 1240); this method is exercised by two tests at test_content_agent.py:939 and :1613
- `QUOTE_KEYWORDS`, `GOLD_KEYWORDS`, other constants — NOT deleted

Grep confirmation before task starts (executor should verify):
- `grep -n "QUOTE_ACCOUNTS\|_search_quote_tweets" scheduler/` should return only the 3 file paths listed above
- `grep "QUOTE_ACCOUNTS\|_search_quote_tweets" scheduler/tests/` returns zero (confirmed — no test mocks the quote-tweet pipeline directly)
- `grep "post_text" frontend/src/` returns only the two files listed above (LongFormPreview.tsx + ContentDetailModal.test.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix long-form dashboard field-name mismatch (frontend only)</name>
  <files>
    - frontend/src/components/content/LongFormPreview.tsx
    - frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx
  </files>
  <action>
**Scope:** Frontend-only. Change the field name `post_text` → `post` in the long-form preview component and its test fixture. DO NOT modify the backend drafter — existing DB rows already have the `post` key and changing the backend would only help future rows while leaving stale rows broken.

**Step 1 — Grep sanity check before editing.** Run:
```bash
grep -rn "post_text" frontend/src/
```
Expected: exactly 3 matches across 2 files — `LongFormPreview.tsx:6`, `LongFormPreview.tsx:11`, and `ContentDetailModal.test.tsx:159`. If you find additional hits, update them the same way (rename `post_text` → `post` in any LongFormPreview/long_form context). Do NOT touch unrelated `post_text` uses in other formats (there shouldn't be any, but verify context before each edit).

**Step 2 — Edit `frontend/src/components/content/LongFormPreview.tsx`:**
- Line 6: change `post_text?: string` → `post?: string` inside the `LongFormDraft` interface.
- Line 11: change `const postText = typeof d.post_text === 'string' ? d.post_text : ''` → `const postText = typeof d.post === 'string' ? d.post : ''`.
- Do NOT rename the local variable `postText` (line 11 onward) — it's used at lines 31 and 40 and renaming it adds noise. The local name is fine; only the object key changes.
- Everything else (lines 1-5, 7-10, 12-44) stays identical.

**Step 3 — Edit `frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx`:**
- Line 159: change `post_text: 'A detailed long-form post about gold markets.'` → `post: 'A detailed long-form post about gold markets.'`.
- Lines 153-158 and 160-173 stay identical — the `getByText('A detailed long-form post about gold markets.')` assertion on line 172 is text-content based and doesn't care about the key name, so it keeps passing.

**Step 4 — Post-edit grep verification:**
```bash
grep -rn "post_text" frontend/src/
```
Expected: zero matches.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run ContentDetailModal && npm run build</automated>
  </verify>
  <done>
    - `grep -rn "post_text" frontend/src/` returns zero matches
    - `npm test -- --run ContentDetailModal` passes (Test 3 "renders LongFormPreview when bundle.content_type === 'long_form'" asserts the rendered post body text and passes)
    - `npm run build` succeeds (tsc 0 errors, vite bundles successfully)
    - `LongFormPreview.tsx` lines 6 and 11 now reference `post` (not `post_text`); the local variable name `postText` at line 11 onward is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove X as a quote-post source (backend content agent)</name>
  <files>
    - scheduler/agents/content_agent.py
    - scheduler/tests/test_content_agent.py
    - scheduler/seed_content_data.py
  </files>
  <action>
**Scope:** Backend content agent. Delete the hardcoded X-account quote-tweet pipeline entirely. Preserve the article-path quote format (Sonnet-chosen "quote" from article content) and the video-clip Twitter pipeline.

**Step 0 — Pre-edit grep to lock baseline.** Run:
```bash
grep -n "QUOTE_ACCOUNTS\|_search_quote_tweets\|twitter.com/status" scheduler/agents/content_agent.py
```
Record the exact line numbers you see — they should be approximately: 84, 85, 86, 87, 88, 89 (QUOTE_ACCOUNTS constant), 944 (def _search_quote_tweets), 967 (comment), 968 (accounts_clause), 997 (twitter.com/status URL build), 1015 (log line), 1864 (call site). If line numbers have shifted, use the Read tool to reconfirm the exact blocks before editing.

**Step 1 — Delete `QUOTE_ACCOUNTS` constant.** At lines 84-90 (inclusive of the closing `]` and the blank line that follows before the CREDIBILITY_TIERS comment at line 92), delete:
```python
QUOTE_ACCOUNTS = [
    "PeterSchiff",
    "jimrickards",
    "JimRickards",
    "RealJimRogers",
    "RobertKiyosaki",
]

```
Keep the blank line separator before `# ---------------------------------------------------------------------------` at line 92. Net removal: 7 lines.

**Step 2 — Delete `_search_quote_tweets()` method.** At lines 944-1022 (inclusive of the trailing `return []` and the blank line before `_set_config_str`), delete the entire method. After deletion, the method immediately preceding (`_search_video_clips` ending with `return []` at line 942) should be followed by a single blank line and then `async def _set_config_str(...)` at line 1024. Net removal: 79 lines.

**Step 3 — Delete the quote-tweets branch in `_run_twitter_content_search`.** At lines 1863-1921 (the whole `# --- Quote tweets ---` block starting at line 1863 with the comment, line 1864 `quote_tweets = await self._search_quote_tweets(session)`, through line 1921 `logger.error("Quote tweet processing error...`), delete everything. After deletion, line 1862 (end of video-clip block: `logger.error("Video clip processing error for %s: %s", ...)`) is followed by blank line then `async def run(self)` at what was line 1923. Net removal: 59 lines.

**Step 4 — Update video-clip comment + docstring (remove "quote" mentions).**
- Line 1789: change `# 11. Twitter video clip + quote search (runs after main RSS/SerpAPI story loop)` → `# 11. Twitter video clip search (runs after main RSS/SerpAPI story loop)`.
- Line 1795 (inside `_run_twitter_content_search` docstring): change `"""Search Twitter for video clips and quotes; draft, comply, and persist each.` → `"""Search Twitter for video clips; draft, comply, and persist each.`

**Step 5 — Enhance the Claude format-selection prompt to promote article-embedded quotes.** At line 1240 (inside the `user_prompt` f-string), the current line reads:
```
   - "quote" — when a strong text quote from a credible named figure is found in the article content. Pull-quote format: quote in quotation marks, attribution, 1-2 lines of analyst context. Produces a tweet post plus three fields (suggested_headline, data_facts, image_prompt) for the operator to render in claude.ai.
```
Append one additional sentence to the end of this line (keep it as a single bullet — do not split into multi-line):
```
   - "quote" — when a strong text quote from a credible named figure is found in the article content. Pull-quote format: quote in quotation marks, attribution, 1-2 lines of analyst context. Produces a tweet post plus three fields (suggested_headline, data_facts, image_prompt) for the operator to render in claude.ai. Prefer "quote" format when the article contains a verbatim, attributed statement from a credible named figure (bank analyst, central bank official, WGC, IMF, Fed speaker, etc.) that stands on its own as the story's headline.
```

**Step 6 — Grep sweep for stragglers.** Run:
```bash
grep -rn "QUOTE_ACCOUNTS\|_search_quote_tweets" scheduler/
grep -n "twitter.com/status" scheduler/agents/content_agent.py
```
Expected:
- Zero `QUOTE_ACCOUNTS` or `_search_quote_tweets` hits anywhere under `scheduler/` (including tests). If any test references these, remove/update them — but pre-analysis confirms tests do NOT reference them, so expect zero hits cleanly.
- Zero `twitter.com/status` hits in `content_agent.py` (the video_clip path uses `x.com` via `twitter_agent.py`, which is a separate file).

**Step 7 — Check `scheduler/seed_content_data.py` for quote-account config rows.** Run:
```bash
grep -n "quote_account\|QUOTE_ACCOUNTS\|PeterSchiff\|jimrickards\|JimRickards\|RealJimRogers\|RobertKiyosaki" scheduler/seed_content_data.py
```
Pre-analysis confirms zero matches expected. If anything surfaces, delete those config rows. (The operator has already verified `seed_twitter_data.py:40` has a separate `PeterSchiff` entry for Twitter watchlist — leave that alone. It's a different seed file and a different purpose.)

**Step 8 — Preserve tests for `_draft_quote_post`.** Do NOT touch `scheduler/tests/test_content_agent.py` tests at lines 939 (`test_quote_draft_returns_three_new_fields`) and 1613 (`test_draft_quote_post_system_prompt_contains_snapshot_block`) — these exercise the article-path quote drafter (`_draft_quote_post()`), which remains intact. Run the full test file to confirm nothing breaks:
```bash
cd scheduler && uv run pytest tests/test_content_agent.py -x
```

**Step 9 — Ruff clean.** Run:
```bash
cd scheduler && uv run ruff check .
```
Fix any new ruff violations introduced by the deletion (most likely candidates: unused imports if QUOTE_ACCOUNTS was the last consumer of something — grep confirms it wasn't, but double-check).
  </action>
  <verify>
    <automated>cd scheduler && uv run ruff check . && uv run pytest -x</automated>
  </verify>
  <done>
    - `grep -rn "QUOTE_ACCOUNTS\|_search_quote_tweets" scheduler/` returns zero matches
    - `grep -n "twitter.com/status" scheduler/agents/content_agent.py` returns zero matches
    - `_draft_quote_post()` method still exists and its two existing tests pass
    - `_search_video_clips()` / `_draft_video_caption()` / `VIDEO_ACCOUNTS` untouched; video-clip pipeline still runs on next content_agent invocation
    - Format-selection prompt at line ~1240 now contains the sentence starting "Prefer \"quote\" format when the article contains a verbatim, attributed statement..."
    - `cd scheduler && uv run ruff check .` clean (zero errors)
    - `cd scheduler && uv run pytest -x` passes all tests (expected: 85+ tests green, zero failures, zero errors — the pre-existing `_draft_quote_post` tests still pass since that method is preserved)
  </done>
</task>

</tasks>

<verification>

**End-to-end verification (run after both tasks complete):**

1. **Frontend clean:**
   ```bash
   cd frontend && npm run build && npm test
   ```
   All tests pass, build succeeds.

2. **Backend clean:**
   ```bash
   cd scheduler && uv run ruff check . && uv run pytest
   ```
   Ruff clean, all tests pass.

3. **Grep guardrails:**
   ```bash
   grep -rn "QUOTE_ACCOUNTS\|_search_quote_tweets" scheduler/        # Expect: zero
   grep -n  "twitter.com/status" scheduler/agents/content_agent.py   # Expect: zero
   grep -rn "post_text" frontend/src/                                # Expect: zero
   ```

4. **Live dashboard verification (operator-performed post-merge):**
   - Pull up the content queue in the dashboard.
   - Find any existing `long_form` bundle row (there should be many — operator has confirmed they were showing the broken placeholder).
   - Open the preview. The actual prose should render (1500+ chars of analyst copy), not "No draft content available."
   - No migration was needed — DB rows already had the `post` key.

5. **Future-run semantic check (observable on next content_agent cron, every 3h):**
   - Scheduler logs do NOT contain `"Quote tweet search returned"` or `"Quote tweet queued"` lines anymore.
   - Scheduler logs DO still contain `"Video clip queued from @..."` lines if any video clips are found (video-clip pipeline preserved).
   - Any new `content_type='quote'` bundles created must have `story_url` pointing to a news article domain (reuters.com, bloomberg.com, etc.), NOT `twitter.com/.../status/...` or `x.com/.../status/...`.
   - No new bundle has `source_name` in `{PeterSchiff, jimrickards, JimRickards, RealJimRogers, RobertKiyosaki}`.

</verification>

<success_criteria>

- [ ] `frontend/src/components/content/LongFormPreview.tsx` line 6 reads `post?: string`; line 11 reads `d.post` (both occurrences)
- [ ] `frontend/src/components/approval/__tests__/ContentDetailModal.test.tsx` line 159 reads `post:` (not `post_text:`)
- [ ] `grep -rn "post_text" frontend/src/` → zero matches
- [ ] `frontend && npm run build` succeeds; `frontend && npm test` all pass
- [ ] `scheduler/agents/content_agent.py` has `QUOTE_ACCOUNTS` constant deleted (lines 84-90 gone)
- [ ] `scheduler/agents/content_agent.py` has `_search_quote_tweets()` method deleted (lines 944-1022 gone)
- [ ] `scheduler/agents/content_agent.py` has quote-tweets for-loop deleted from `_run_twitter_content_search` (lines 1863-1921 gone)
- [ ] `scheduler/agents/content_agent.py` format-selection prompt at ~line 1240 has the new "Prefer \"quote\" format when the article contains a verbatim..." sentence appended
- [ ] `grep -rn "QUOTE_ACCOUNTS\|_search_quote_tweets" scheduler/` → zero matches
- [ ] `grep -n "twitter.com/status" scheduler/agents/content_agent.py` → zero matches
- [ ] `_draft_quote_post()` method still present and its existing tests pass (test_content_agent.py:939, :1613)
- [ ] `_search_video_clips()`, `_draft_video_caption()`, `VIDEO_ACCOUNTS` untouched
- [ ] `scheduler && uv run ruff check .` clean
- [ ] `scheduler && uv run pytest` all pass
- [ ] Operator-verified: next dashboard visit shows long_form cards rendering actual prose for existing DB rows
- [ ] Operator-verified: next content_agent cron (within 3h of deploy) produces zero quote-tweet log lines and zero new bundles with `source_name` in the old QUOTE_ACCOUNTS list

</success_criteria>

<output>
After completion, create `.planning/quick/260420-r18-long-form-dashboard-field-name-fix-remov/260420-r18-SUMMARY.md`
</output>
