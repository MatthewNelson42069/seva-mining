---
phase: quick-260419-ko3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_crud_endpoints.py
  - scheduler/agents/senior_agent.py
  - scheduler/tests/test_senior_agent.py
  - frontend/src/components/approval/ApprovalCard.test.tsx
  - frontend/src/pages/DigestPage.test.tsx
autonomous: true
requirements:
  - QUICK-KO3-01
  - QUICK-KO3-02
  - QUICK-KO3-03

must_haves:
  truths:
    - "`cd backend && uv run pytest` completes with 0 failures"
    - "`cd scheduler && uv run pytest` completes with 0 failures"
    - "`cd frontend && npx vitest run` completes with 0 failures"
    - "No Phase 11 production code is modified (backend/app/routers/content_bundles.py, scheduler/agents/image_render_agent.py, scheduler/agents/content_agent.py _enqueue_render_job_if_eligible, frontend ContentDetailModal/RenderedImagesGallery)"
    - "Each service fix is an atomic commit (backend = 1 commit, scheduler = 1 commit, frontend = 1-2 commits)"
  artifacts:
    - path: "backend/tests/test_crud_endpoints.py"
      provides: "Passing test_today_content_404 (no sqlalchemy driver:// plugin error)"
    - path: "scheduler/tests/test_senior_agent.py"
      provides: "Passing test_morning_digest_assembly (source_account field aligned between agent and WhatsApp digest template)"
    - path: "frontend/src/components/approval/ApprovalCard.test.tsx"
      provides: "Passing 'renders draft tabs' test (queries tab button role, not inactive tab panel content)"
    - path: "frontend/src/pages/DigestPage.test.tsx"
      provides: "All 6 previously-failing DigestPage tests pass"
  key_links:
    - from: "scheduler/agents/senior_agent.py (morning digest assembly)"
      to: "scheduler/services/whatsapp.py (morning digest template consumer)"
      via: "story dict shape — source_account key"
      pattern: "source_account"
    - from: "frontend/src/components/approval/ApprovalCard.test.tsx"
      to: "shadcn Tabs (tailwind-v4 branch) behavior"
      via: "role='tab' query instead of panel text query"
      pattern: "getByRole\\(['\"]tab['\"]"
---

<objective>
Fix the 8 pre-existing test failures that have been on the backlog since before Phase 11 landed. These have been flagged in the Phase 11 Plan 01 SUMMARY and Plan 07 SUMMARY "Known limitations" section and must be cleared so CI is green before v1.0.2 planning starts.

Purpose: Restore clean-green CI across all three services (backend pytest, scheduler pytest, frontend vitest). Phase 11 Plan 07 confirmed these are pre-existing and not Phase 11 regressions — they've been red for multiple phases and block confident future execution.

Output: Three service test suites passing with 0 failures. 3-4 atomic commits on `main`. No Phase 11 production code touched (unless a root cause is genuinely inside Phase 11 production code — unlikely per Plan 07 SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-content-preview-and-rendered-images/01-foundation-and-wave0-SUMMARY.md
@.planning/phases/11-content-preview-and-rendered-images/07-human-verification-SUMMARY.md
@CLAUDE.md

<!-- Phase 11 SUMMARYs confirm: 2 pre-existing Python failures + 7 pre-existing frontend failures (6 DigestPage + 1 ApprovalCard). All 8 are in scope for this quick plan (the 2 Python plus the 7 frontend = 9 total, but orchestrator counted the 6 DigestPage as one cluster → "8 failures"). -->

<constraints>
- DO NOT modify Phase 11 production code unless a root cause is proven to live there:
  - backend/app/routers/content_bundles.py
  - scheduler/agents/image_render_agent.py
  - scheduler/agents/content_agent.py _enqueue_render_job_if_eligible
  - frontend ContentDetailModal.tsx, RenderedImagesGallery.tsx
- Each service fix is an atomic commit (after the fix passes that service's full suite)
- APScheduler 3.11.2, SQLAlchemy 2.0 async, Pydantic v2, TanStack Query v5, Tailwind v4 / shadcn tailwind-v4 branch — do not introduce v3/v4-incompatible patterns
- Follow existing test patterns seen in neighboring tests in the same file
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix backend test_today_content_404 SQLAlchemy driver error</name>
  <files>backend/tests/conftest.py, backend/tests/test_crud_endpoints.py, backend/app/database.py</files>
  <action>
    Fix `backend/tests/test_crud_endpoints.py::test_today_content_404` which fails with:
    `sqlalchemy.exc.NoSuchModuleError: Can't load plugin: sqlalchemy.dialects:driver`

    That error signature (`dialects:driver`) means a literal `driver://...` placeholder URL is being loaded as a DSN — almost certainly because DATABASE_URL is unset in the test environment and some code path is seeing a default/placeholder instead of the conftest's override engine.

    Investigation steps (run before writing any fix):
    1. Read `backend/tests/conftest.py` end to end — identify how the test DB is overridden (in-memory SQLite, fixture scope, dependency_overrides).
    2. Read `backend/tests/test_crud_endpoints.py::test_today_content_404` — see exactly how it sets up its client/session.
    3. Read `backend/app/database.py` and `backend/app/config.py` — check if Settings has a DATABASE_URL default like `"driver://..."` or if the module reads os.environ directly at import time.
    4. Run the single failing test to see the full traceback:
       `cd backend && uv run pytest tests/test_crud_endpoints.py::test_today_content_404 -xvs 2>&1 | tail -80`
    5. Compare to a passing test in the same file — what does the passing test do differently (different fixture? different dependency override?) that keeps SQLAlchemy from seeing the placeholder URL?

    Likely root causes (pick the one that matches the traceback):
    - The test bypasses the `db_session`/`client` fixture and calls something that triggers the real `get_engine()` / `create_async_engine(settings.database_url)` where settings defaults to `"driver://..."`.
    - The conftest's dependency_override is scoped to a fixture the test isn't using.
    - A module-level `engine = create_async_engine(...)` in `app/database.py` fires at import time with the placeholder before any fixture can override it — fix by making engine creation lazy or by setting DATABASE_URL in conftest.py before importing app.

    Fix in the SMALLEST scope that makes the test pass without changing production behavior:
    - Preferred: update conftest so the test uses the same override the passing tests use (add the missing fixture dependency, or set DATABASE_URL env var in a session-scoped autouse fixture BEFORE app import).
    - Acceptable: update the test itself to use the correct fixture.
    - Last resort: update `app/database.py` to create the engine lazily instead of at import time — only if root cause is genuinely eager engine creation.

    Do NOT modify `app/routers/content_bundles.py` (Phase 11 production code).

    After the fix, run `cd backend && uv run pytest` — MUST be 0 failures (was 1 pre-existing: test_today_content_404). Commit as ONE atomic commit: `fix(quick-ko3): backend test_today_content_404 — <one-line root cause>`.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/backend && uv run pytest tests/test_crud_endpoints.py::test_today_content_404 -xvs && uv run pytest</automated>
  </verify>
  <done>
    - `cd backend && uv run pytest tests/test_crud_endpoints.py::test_today_content_404 -xvs` passes (exit 0)
    - `cd backend && uv run pytest` reports 0 failures (Phase 11 Plan 07 baseline was 68 passed + 1 pre-existing failure; this task brings it to 69 passed + 0 failures)
    - No Phase 11 production code modified (git diff shows only test/conftest/database changes)
    - ONE atomic commit created on main with a clear root-cause message
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix scheduler test_morning_digest_assembly source_account key</name>
  <files>scheduler/agents/senior_agent.py, scheduler/tests/test_senior_agent.py, scheduler/services/whatsapp.py</files>
  <action>
    Fix `scheduler/tests/test_senior_agent.py::test_morning_digest_assembly` which fails with:
    `AssertionError: 'source_account' key not in story dict. Got keys: headline, score, source, time, ...`

    The test expects a `source_account` field; the senior agent's digest assembly currently emits `source`. Decide which side is correct by checking the CONSUMER — the WhatsApp morning digest template in `scheduler/services/whatsapp.py` (or wherever `run_morning_digest` / WhatsApp body string is assembled). The consumer's field expectation is authoritative; the other side gets aligned to match.

    Investigation steps (run before writing any fix):
    1. Read the failing test to see the full expected shape:
       `cd scheduler && uv run pytest tests/test_senior_agent.py::test_morning_digest_assembly -xvs 2>&1 | tail -80`
    2. Grep for `source_account` across the scheduler codebase:
       Use Grep tool: pattern=`source_account`, path=`/Users/matthewnelson/seva-mining/scheduler`
    3. Grep for the digest body assembly in `scheduler/services/whatsapp.py` and `scheduler/agents/senior_agent.py` — find the template string or f-string that turns stories into WhatsApp text.
    4. Read `scheduler/agents/senior_agent.py` morning digest assembly function — see what keys it puts on each story dict and where those keys flow to.

    Decision rule:
    - If the WhatsApp template reads `story['source_account']` → senior_agent.py is WRONG, rename its emitted key `source` → `source_account`.
    - If the WhatsApp template reads `story['source']` → the TEST is wrong, update the test to expect `source` (aligning the test with shipped behavior and the consumer).
    - If neither template reads either key → the test encodes design intent; pick the more descriptive name (`source_account` is better; it disambiguates from `source` as in RSS/SerpAPI origin). Update both senior_agent.py AND any other test fixtures/assertions that touch this dict.

    Also verify no other test in the suite breaks from the rename — grep for `['source']` and `.source` on story dicts across `scheduler/tests/`.

    Do NOT modify `scheduler/agents/image_render_agent.py` or `scheduler/agents/content_agent.py _enqueue_render_job_if_eligible` (Phase 11 production code — unrelated).

    After the fix, run `cd scheduler && uv run pytest` — MUST be 0 failures (was 1 pre-existing: test_morning_digest_assembly). Commit as ONE atomic commit: `fix(quick-ko3): scheduler morning digest source_account key alignment`.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/test_senior_agent.py::test_morning_digest_assembly -xvs && uv run pytest</automated>
  </verify>
  <done>
    - `cd scheduler && uv run pytest tests/test_senior_agent.py::test_morning_digest_assembly -xvs` passes (exit 0)
    - `cd scheduler && uv run pytest` reports 0 failures (Phase 11 Plan 07 baseline was 114 passed + 1 pre-existing failure; this task brings it to 115 passed + 0 failures)
    - No Phase 11 production code modified (git diff shows only senior_agent.py / whatsapp.py / test changes)
    - Field name decision documented in the commit message (one-line: "align with WhatsApp consumer" OR "align test with shipped code")
    - ONE atomic commit created on main
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix frontend ApprovalCard + DigestPage test failures (7 total)</name>
  <files>frontend/src/components/approval/ApprovalCard.test.tsx, frontend/src/pages/DigestPage.test.tsx</files>
  <action>
    Fix 7 pre-existing frontend test failures:
    - `frontend/src/components/approval/ApprovalCard.test.tsx::renders draft tabs` (1 failure)
    - `frontend/src/pages/DigestPage.test.tsx::*` (6 failures — NOT yet diagnosed)

    RUN THE SUITE FIRST to see current failure output before patching anything:
    ```
    cd /Users/matthewnelson/seva-mining/frontend && npx vitest run 2>&1 | tail -200
    ```
    Note which 7 tests fail, copy the assertion errors and DOM dumps into your working notes. DO NOT patch blind.

    --- Part A: ApprovalCard "renders draft tabs" (diagnosed) ---

    Hypothesis (from orchestrator): After the shadcn Tabs (tailwind-v4 branch) upgrade, only the active tab's panel content is in the DOM. The test queries `getByText('Draft A')` and `getByText('Draft B')` expecting both panel bodies to be simultaneously present — but inactive tab panels are now hidden/unmounted.

    Fix: query the tab BUTTONS (role='tab') not the panel bodies. Replace:
    ```tsx
    expect(screen.getByText('Draft A')).toBeInTheDocument();
    expect(screen.getByText('Draft B')).toBeInTheDocument();
    ```
    with:
    ```tsx
    expect(screen.getByRole('tab', { name: /draft a/i })).toBeInTheDocument();
    expect(screen.getByRole('tab', { name: /draft b/i })).toBeInTheDocument();
    ```
    (adjust the exact regex to match the real tab label text in `DraftTabBar.tsx` / `ApprovalCard.tsx`).

    If tab labels don't include the literal text "Draft A"/"Draft B" (e.g. they render draft index numbers or alternative titles), query by position: `screen.getAllByRole('tab')` and assert length === 2 (or whatever the expected count is for the test's fixture).

    Do NOT modify `ApprovalCard.tsx` production code — the test is what's out of date after the shadcn upgrade.

    --- Part B: DigestPage 6 failures (undiagnosed — investigate first) ---

    After running the suite, analyze the 6 DigestPage failures as a cluster and look for a SINGLE common root cause. Common suspects for this test file (based on Phase 08 decisions in STATE.md):
    - **MSW handler drift**: the digest endpoint path or response shape changed and `frontend/src/mocks/handlers.ts` wasn't updated. Check DigestPage.tsx's fetch URL vs the MSW handler path.
    - **localStorage mock missing**: Phase 08 decision noted "any component calling apiFetch on render needs vi.stubGlobal localStorage mock in beforeEach". If DigestPage or a child component calls apiFetch, the test's beforeEach may be missing `vi.stubGlobal('localStorage', ...)`.
    - **shadcn Tabs behavior change (same root cause as Part A)**: if DigestPage uses Tabs, inactive panels are hidden and tests may be querying panel content that's not mounted.
    - **TanStack Query v5 refetchInterval callback signature change**: noted in STATE.md Phase 11-05 decision — v5 passes a query object, not `(data, query)`. If DigestPage tests use fake timers + refetchInterval, they may deadlock.
    - **JSDOM URL / MSW relative path**: Phase 08 decision noted "JSDOM URL must be http://localhost:3000 in vitest config so MSW relative path handlers resolve". Check `vitest.config.ts` / `vite.config.ts`.

    Read `frontend/src/pages/DigestPage.tsx` and `frontend/src/pages/DigestPage.test.tsx` side by side. Identify the shared fixture setup that all 6 tests use. The common failure is almost certainly in that shared setup.

    Fix the 6 failures together (one root cause → one diff). If investigation proves the 6 failures have DIFFERENT root causes, fix them all in one commit anyway (they're all in the same test file, so one commit is still atomic).

    Do NOT modify DigestPage.tsx production code unless the test reveals a real bug (e.g. it's calling a wrong URL). Prefer fixing the test/mock setup.

    --- Commit strategy ---

    Commit strategy: preferred ONE combined commit for all 7 frontend failures (both files), message: `fix(quick-ko3): frontend pre-existing test failures (ApprovalCard tabs + DigestPage)`. Acceptable to split into TWO commits (one per file) if the root causes are clearly unrelated. Max 2 frontend commits total.

    After the fix, run `cd frontend && npx vitest run` — MUST be 0 failures (was 67 passed + 7 pre-existing failures per Phase 11 Plan 07 SUMMARY; this task brings it to 74 passed + 0 failures).
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/frontend && npx vitest run src/components/approval/ApprovalCard.test.tsx src/pages/DigestPage.test.tsx && npx vitest run</automated>
  </verify>
  <done>
    - `cd frontend && npx vitest run src/components/approval/ApprovalCard.test.tsx` passes (exit 0, "renders draft tabs" green)
    - `cd frontend && npx vitest run src/pages/DigestPage.test.tsx` passes (exit 0, all 6 previously-failing tests green)
    - `cd frontend && npx vitest run` reports 0 failures across the whole suite (74+ passed)
    - No Phase 11 production code modified (no changes to ContentDetailModal.tsx, RenderedImagesGallery.tsx, or any Phase 11 component)
    - 1-2 atomic commits created on main (preferred 1, max 2)
  </done>
</task>

</tasks>

<verification>
Final gate — ALL THREE suites must report 0 failures on `main`:

```
cd /Users/matthewnelson/seva-mining/backend && uv run pytest
cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest
cd /Users/matthewnelson/seva-mining/frontend && npx vitest run
```

Expected final tallies (based on Phase 11 Plan 07 SUMMARY baseline):
- Backend: 69 passed, 0 failed (was 68 + 1 pre-existing failure)
- Scheduler: 115 passed, 0 failed (was 114 + 1 pre-existing failure)
- Frontend: 74+ passed, 0 failed (was 67 + 7 pre-existing failures)

Also verify:
```
cd /Users/matthewnelson/seva-mining && git log --oneline origin/main..HEAD
```
Should show 3-4 atomic commits (1 backend + 1 scheduler + 1-2 frontend), each with a clear root-cause message prefixed `fix(quick-ko3):`.

And git diff against Phase 11 protected files MUST be empty:
```
cd /Users/matthewnelson/seva-mining && git diff origin/main..HEAD -- backend/app/routers/content_bundles.py scheduler/agents/image_render_agent.py frontend/src/components/approval/ContentDetailModal.tsx frontend/src/components/approval/RenderedImagesGallery.tsx
```
(Empty diff proves Phase 11 production code untouched. scheduler/agents/content_agent.py may have diff ONLY if the change is outside `_enqueue_render_job_if_eligible` — unlikely for this quick fix.)
</verification>

<success_criteria>
- All three test suites report 0 failures
- 3-4 atomic commits on main, each with `fix(quick-ko3):` prefix and a one-line root-cause description
- Zero changes to Phase 11 protected production files (see verification grep)
- STATE.md "Quick Tasks Completed" table updated with a new row for 260419-ko3
- Each task's root cause is captured in its commit message so future readers understand what was wrong
</success_criteria>

<output>
After completion:
1. Update `.planning/STATE.md` "Quick Tasks Completed" table with a new row:
   | 260419-ko3 | Fix 8 pre-existing test failures (backend + scheduler + frontend) | 2026-04-19 | <first-commit-sha> | [260419-ko3](./quick/260419-ko3-fix-8-pre-existing-test-failures-across-/) |
2. Commit STATE.md update: `docs(quick-ko3): log quick task completion in STATE.md`
3. Report final pass counts for all three suites in the completion summary
</output>
