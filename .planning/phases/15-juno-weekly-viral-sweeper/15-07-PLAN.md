---
phase: 15-juno-weekly-viral-sweeper
plan: 07
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
  - 15-03
  - 15-04
  - 15-05
  - 15-06
files_modified:
  - .planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md
autonomous: false
requirements:
  - JSWEEP-01
  - JSWEEP-04

must_haves:
  truths:
    - "Operator runs python -m scheduler.agents.juno_weekly_sweeper against the production DB (or a high-fidelity staging DB) and one Juno weekly_sweeps row is persisted with status in (completed, partial)"
    - "Operator opens the persisted weekly_sweeps row's content_angles_md content and reads exactly 3 angles (NOT 2, NOT 4)"
    - "Operator confirms each angle reads as Janes/CSIS desk (sober, sourced-with-receipts, geopolitical context) and NOT tactical/operational/OOB/targeting"
    - "Operator confirms each angle connects an X signal with a news signal (the sweeper task contract)"
    - "Operator confirms no defence-prime equity/financial signal slipped into any angle (LMT, RTX, NOC, BA, GD, BAESY)"
    - "Operator confirms refusal-detector behavior: if first-attempt refusal fired (visible in agent_runs.notes), the retry-with-framing-nudge produced clean output OR status='partial' was persisted"
    - "Operator confirms voice differentiation from Seva's Bloomberg-gold-desk voice (eye-check side-by-side comparison if a recent Seva sweep is available)"
    - "Operator records the UAT verdict in voice_calibration_uat.md (APPROVED / DEFERRED-needs-iteration / REJECTED) and signs off"
    - "If APPROVED: operator flips JUNO_SWEEPER_CRON_ENABLED=true in Railway env (out-of-band action — happens AFTER this checkpoint signs off)"
  artifacts:
    - path: ".planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md"
      provides: "NEW file capturing the 6-criteria voice UAT verdict + the sample Sonnet output reviewed + operator sign-off (or specific issues if rejected)"
      contains: "APPROVED|DEFERRED|REJECTED"
  key_links:
    - from: ".planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md (APPROVED verdict)"
      to: "Railway env var JUNO_SWEEPER_CRON_ENABLED=true (operator out-of-band action)"
      via: "Operator handoff per D-07 step 4 — env flip happens AFTER voice UAT signs off"
      pattern: "JUNO_SWEEPER_CRON_ENABLED"
---

<objective>
Operator voice UAT checkpoint for the Juno Weekly Viral Sweeper. After all production code lands (Plans 15-01 through 15-06), the operator runs `python -m scheduler.agents.juno_weekly_sweeper` for a manual smoke fire against the production DB, reviews the 3 generated content angles, and signs off on a 6-criteria voice UAT before flipping `JUNO_SWEEPER_CRON_ENABLED=true` in Railway env vars.

Purpose: Mirrors Phase 10's voice UAT precedent verbatim. Defence content's higher refusal-rate risk (per RESEARCH §4 Anthropic-Pentagon dispute Feb-Apr 2026) makes the human voice check load-bearing — automated tests can verify the FORBID-block bytes are present, but only the operator can confirm the Sonnet output ACTUALLY reads as Janes/CSIS desk vs. drifting into tactical/operational territory.

Per D-07 (locked decision) — the 4-step operator rollout:
1. Deploy disabled (already DONE by Plan 15-06 — default-disabled registration)
2. Manual smoke fire (this checkpoint — operator runs `python -m agents.juno_weekly_sweeper`)
3. Voice UAT (this checkpoint — 6 criteria from CONTEXT specifics)
4. Flip env gate (operator out-of-band action AFTER this checkpoint signs off)

Output:
- `.planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md` — NEW file capturing the smoke-fire output + the 6-criteria verdict + operator sign-off
- Operator handoff: after APPROVED verdict, flip `JUNO_SWEEPER_CRON_ENABLED=true` in Railway env vars
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-juno-weekly-viral-sweeper/15-CONTEXT.md
@.planning/phases/15-juno-weekly-viral-sweeper/15-RESEARCH.md

# Wave 2 prerequisites must all be on disk
@scheduler/agents/juno_weekly_sweeper.py
@scheduler/worker.py
@scheduler/companies/juno/prompts.py
@scheduler/companies/juno/x_queries.py
@scheduler/agents/daily_summary.py

# Phase 10 voice UAT precedent — read the analogous artifact format
@.planning/milestones/v3.0-phases/10-juno-defence-news-funnel/10-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator manual smoke fire of run_juno_weekly_sweeper + voice UAT against 6 criteria + sign-off</name>
  <files>.planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md</files>
  <action>
    Operator runs the manual smoke fire via `python -m agents.juno_weekly_sweeper`, reviews the persisted weekly_sweeps row's content_angles_md against the 6 voice UAT criteria (voice/quantity/task-contract/anti-equity/anti-tactical/refusal-detector), records the verdict in voice_calibration_uat.md, and signs off APPROVED / DEFERRED / REJECTED. On APPROVED, the operator flips JUNO_SWEEPER_CRON_ENABLED=true in Railway env vars out-of-band (NOT part of this plan's execution). Full detail in <what-built> + <how-to-verify> below.
  </action>
  <verify>
    <automated>ls -la .planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md &amp;&amp; grep -cE "^## Final Verdict$" .planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md</automated>
  </verify>
  <done>
    voice_calibration_uat.md exists with the smoke fire metadata, sample Sonnet output (content_angles_md), 6-criteria PASS/FAIL verdicts, optional voice-differentiation notes, final verdict (APPROVED / DEFERRED / REJECTED), and operator sign-off. On APPROVED: operator commits to flipping JUNO_SWEEPER_CRON_ENABLED=true in Railway. On DEFERRED: phase ships with v3.2 prompt-iteration backlog item. On REJECTED: orchestrator routes back to /gsd:plan-phase 15 --gaps with the specific failing criteria.
  </done>
  <what-built>
    All Phase 15 production code from Plans 15-01 through 15-06:
    - Plan 15-01: Juno daily_summaries raw_sources_jsonb now persists defence_news + canadian_procurement + world_events story-URL arrays (D-03a substrate fix)
    - Plan 15-02: JUNO_SWEEPER_SYSTEM_PROMPT in companies/juno/prompts.py + JUNO_SWEEPER_X_QUERY in companies/juno/x_queries.py (post-research corrected D-02 final form: 11 handles + 2 hashtags)
    - Plan 15-03: Frontend Juno short-circuit at WeeklyViralSweeperPage.tsx:48-60 deleted + tenant-agnostic component name + cross-tenant isolation RTL test
    - Plan 15-04: Backend cross-tenant 404 test for /api/{company}/weekly-sweeps
    - Plan 15-05: scheduler/agents/juno_weekly_sweeper.py orchestrator (run_juno_weekly_sweeper) + comprehensive unit tests
    - Plan 15-06: worker.py JUNO_SWEEPER_CRON_ENABLED-gated cron registration at lock 1021, Sun 08:00 PT (DEFAULT DISABLED — operator must flip env var AFTER this checkpoint)

    Cron is NOT yet registered in production. The operator now runs a manual smoke fire to validate the Sonnet output BEFORE flipping the env gate.
  </what-built>
  <how-to-verify>
    **STEP 1 — Manual smoke fire (D-07 step 2)**

    From the Railway shell (or a local environment with production DB credentials + JUNO_ANTHROPIC_API_KEY or shared ANTHROPIC_API_KEY + X_API_BEARER_TOKEN env vars set):

    ```bash
    cd scheduler
    python -m agents.juno_weekly_sweeper
    ```

    Expected behavior:
    - Module's `__main__` block calls `asyncio.run(run_juno_weekly_sweeper())`
    - Logs at INFO level show: idempotency check → agent_runs INSERT → fetch_top_x_posts(JUNO_SWEEPER_X_QUERY) → _compute_juno_virality → call_with_refusal_guard → weekly_sweeps INSERT
    - One row written to `weekly_sweeps` with `company_id='juno'`, `status in ('completed', 'partial')` (NOT 'failed')
    - One row written to `agent_runs` with `agent_name='juno_weekly_sweeper'`, `status='completed'`

    **CAVEAT (D-03b backfill window):** If the production DB does NOT yet contain Juno daily_summaries rows written under the Plan 15-01 schema (i.e., the rows pre-date Plan 15-01's substrate fix), the virality compute will return 0 cross-references and the sweeper will trip the insufficient-signal fallback → status='partial' + diagnostic note. THIS IS EXPECTED for the first 0-2 sweeps post-deploy. Voice UAT can still proceed against the Sonnet output (even if X posts alone drive synthesis; the 3-angle structure must still be produced).

    **STEP 2 — Query the persisted row + capture sample output**

    ```sql
    -- Inspect the just-written row
    SELECT id, generated_at, status, content_angles_md, raw_sources_jsonb->'refusal_diagnostic' AS refusal_diag
    FROM weekly_sweeps
    WHERE company_id = 'juno'
    ORDER BY generated_at DESC
    LIMIT 1;
    ```

    Copy the `content_angles_md` column value into `voice_calibration_uat.md` under a "## Sample Sonnet Output (smoke fire YYYY-MM-DD)" section.

    **STEP 3 — Apply the 6 voice UAT criteria (verbatim from CONTEXT specifics)**

    Read the 3 angles in `content_angles_md` and verify EACH criterion below. Record per-criterion verdict (PASS / FAIL / N/A) in voice_calibration_uat.md.

    1. **Voice — Janes/CSIS desk:** Each angle reads as Janes desk brief / CSIS analysis / IISS Military Balance / Defense News editorial — sober, sourced-with-receipts, geopolitical context. NOT tactical/operational.
       - PASS = all 3 angles read as defence-trade-press analyst voice
       - FAIL = any angle reads as op-ed / advocacy / forum chatter / tactical-analyst voice

    2. **Quantity — exactly 3 angles:** The content_angles_md contains exactly 3 `### Angle N:` headers (NOT 2, NOT 4 — Sonnet sometimes drifts off the structure prompt).
       - PASS = exactly 3
       - FAIL = anything else; Sonnet is drifting and the prompt may need tightening in v3.2

    3. **Task contract — X signal + news signal:** Each angle's bullets clearly connect an X (Twitter) signal with a mainstream defence-news signal from the past week.
       - PASS = all 3 angles have both "X signal: ..." and "News signal: ..." bullets with concrete attribution
       - FAIL = any angle conflates the two, or has only one (X-only or news-only), or fabricates an X signal not in the supplied substrate

    4. **Anti-features absent — no defence-prime equity/financial signals:** No angle cites stock tickers, P/E ratios, share-price movements, earnings, dividends, or buy/sell framings on defence primes (LMT, RTX, NOC, BA, GD, BAESY, etc.).
       - PASS = zero equity-flavored content
       - FAIL = even one equity bullet → check the negative-space DO-NOT list in JUNO_SWEEPER_SYSTEM_PROMPT; may need iteration in v3.2

    5. **Anti-features absent — no tactical/operational content:** No angle includes troop-movement / OOB / capability-gap / targeting / force-posture analysis under any framing. If a source story crossed into operational territory, the angle summarizes market/industry implications only and notes the operational details were excluded.
       - PASS = zero tactical content; anti-tactical clause held
       - FAIL = any angle leaks operational analysis → the FORBID clause string-equality contract is intact but Sonnet may have drifted around it; iterate prompt in v3.2

    6. **Refusal-detector behavior — clean OR partial:** Open `agent_runs.notes` for the smoke-fire's agent_run row. If `refusal_detected: true` is in the notes, verify that EITHER (a) the retry-with-framing-nudge produced clean angles_md output (look for `retry_attempted: true` + non-None second-attempt text) OR (b) the row's status is 'partial' with a diagnostic note.
       - PASS = either path is acceptable; the refusal-detector wrap behaved per Phase 10 contract
       - FAIL = refusal-detector did NOT engage when it should have (e.g., visible refusal text in angles_md without status='partial')

    **STEP 4 — Eye-check voice differentiation (BONUS criterion, soft-pass)**

    If a recent Seva weekly_sweeps row exists, side-by-side read its content_angles_md alongside the Juno smoke-fire output. Confirm the voices DIFFER clearly:
    - Seva: Bloomberg commodities-desk energy (gold-bull-thesis bias, "flows tell a different story" framing)
    - Juno: Janes/CSIS desk energy (sober, neutral-on-conflict, contract values + vendor names + policy instruments)

    If voices read as too similar, the JUNO_SWEEPER_SYSTEM_PROMPT may need iteration in v3.2 to push harder on the voice anchor (this is NOT a Phase 15 blocker — soft check).

    **STEP 5 — Record verdict in voice_calibration_uat.md**

    Create `.planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md` with the following sections:
    - `## Smoke Fire Metadata` — date/time, env (production / staging), substrate-window status (D-03b backfill caveat applies?)
    - `## Sample Sonnet Output (content_angles_md)` — verbatim copy of the row's content_angles_md
    - `## 6-Criteria Voice UAT Verdict`:
      - Voice (Janes/CSIS): PASS / FAIL — notes
      - Quantity (exactly 3): PASS / FAIL — observed count
      - Task contract (X + news): PASS / FAIL — notes
      - Anti-feature: equity/financial: PASS / FAIL — any cited tickers?
      - Anti-feature: tactical/operational: PASS / FAIL — any leaked content?
      - Refusal-detector: PASS / FAIL — refusal_detected status from agent_runs.notes
    - `## Voice Differentiation (BONUS)` — side-by-side notes vs recent Seva sweep, if available
    - `## Final Verdict`:
      - **APPROVED** — all 6 criteria PASS; operator flips JUNO_SWEEPER_CRON_ENABLED=true in Railway next
      - **DEFERRED — needs iteration** — one or more criteria FAIL but not blocker; specific fixes documented for v3.2 prompt iteration
      - **REJECTED** — multiple criteria FAIL; Phase 15 needs additional plan(s) to fix; do NOT flip env gate
    - `## Operator Sign-off` — operator initials + date

    **STEP 6 — On APPROVED verdict (out-of-band action — happens AFTER this checkpoint signs off):**

    Per D-07 step 4, the operator flips `JUNO_SWEEPER_CRON_ENABLED=true` in Railway env vars:
    1. Railway dashboard → scheduler service → Variables
    2. Add new variable: `JUNO_SWEEPER_CRON_ENABLED` = `true`
    3. Railway redeploys the scheduler service (~1-2 min)
    4. Verify boot logs show `juno_weekly_sweeper cron ENABLED via JUNO_SWEEPER_CRON_ENABLED=true env var`
    5. Next Sunday 08:00 PT America/Los_Angeles, the real cron fires for the first time

    Rollback path: unset / remove the env var in Railway → next deploy registers no cron → no Sunday fire (no data deletion needed; existing weekly_sweeps rows survive).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — all 6 criteria PASS; voice_calibration_uat.md verdict is APPROVED; operator will flip JUNO_SWEEPER_CRON_ENABLED=true in Railway next (out-of-band)
    - `deferred: <list specific criteria + iteration plan>` — non-blocker FAILs; phase ships, prompt iteration deferred to v3.2
    - `rejected: <list specific criteria + what additional plans Phase 15 needs>` — blocker; phase needs more work before env gate flip
  </resume-signal>
</task>

</tasks>

<verification>
**Overall plan checks** (run AFTER operator signs off):

1. **voice_calibration_uat.md exists:**
   - `ls .planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md` exists
   - File contains the 6-criteria verdict + sample Sonnet output + operator sign-off

2. **Phase-level checks (the orchestrator runs these after this plan signs off):**
   - `bash scripts/verify-tenant-isolation.sh` exits 0
   - `bash scripts/verify-anthropic-resolver.sh` exits 0
   - `cd scheduler && uv run pytest -q` exits 0 (full scheduler suite green; ≥ Phase 14 baseline + Phase 15 delta)
   - `cd backend && uv run pytest -q` exits 0 (full backend suite green)
   - `cd frontend && npm test --silent` exits 0 (full frontend suite green)
   - `git status --porcelain` shows only Phase 15 artifacts modified (no stray Seva touches — D-10 contract held end-to-end)

3. **Operator handoff complete:**
   - On APPROVED verdict: operator commits to flipping JUNO_SWEEPER_CRON_ENABLED=true in Railway (out-of-band)
   - On DEFERRED verdict: phase ships at status='partial-allowed'; v3.2 backlog updated
   - On REJECTED verdict: orchestrator routes back to plan-phase --gaps to address the failing criteria
</verification>

<success_criteria>
**Phase 15 ready for env-gate flip (or fail-closed with diagnosis):**

- Operator manually fired `python -m agents.juno_weekly_sweeper` against production DB; one weekly_sweeps row written with `company_id='juno'`, `status in ('completed', 'partial')`
- 6-criteria voice UAT documented in voice_calibration_uat.md
- Operator sign-off recorded (APPROVED / DEFERRED / REJECTED)
- On APPROVED: operator flips `JUNO_SWEEPER_CRON_ENABLED=true` in Railway → next Sunday 08:00 PT fires real cron

**JSWEEP-01 through JSWEEP-06 all satisfied behaviorally:**

- JSWEEP-01: Cron registration env-gated via JUNO_SWEEPER_CRON_ENABLED (Plan 15-06)
- JSWEEP-02: Sun 08:00 PT cron fires + tweepy search + virality over 3-sub-array union (Plans 15-01 + 15-05)
- JSWEEP-03: weekly_sweeps row persisted with company_id='juno' + idempotency-filter-includes-partial (Plan 15-05)
- JSWEEP-04: Sonnet 4.6 → 3 angles + Janes/CSIS voice + anti-tactical + refusal-detector path (Plans 15-02 + 15-05) — VERIFIED in voice UAT
- JSWEEP-05: /juno/sweeper renders the same card UI Seva uses (Plan 15-03)
- JSWEEP-06: Cross-tenant isolation in frontend (Plan 15-03) + backend (Plan 15-04)

**D-10 zero-regression for Seva preserved end-to-end:**

- Phase 7 Seva weekly_sweeper continues to fire Sunday 08:00 PT identically
- All Seva regression suites (frontend, backend, scheduler) pass byte-identically
- No Seva file edited across all 6 production plans

**Plan boundary respected:**

- This plan touches `.planning/phases/15-juno-weekly-viral-sweeper/voice_calibration_uat.md` ONLY
- NO production code touched in this plan — purely operator validation + handoff to env flip
</success_criteria>

<output>
After operator signs off, create `.planning/phases/15-juno-weekly-viral-sweeper/15-07-SUMMARY.md` following `$HOME/.claude/get-shit-done/templates/summary.md`. The summary should explicitly note:

1. **The smoke fire outcome** — date, env, persisted row id, status (completed / partial)
2. **The 6-criteria verdict** with per-criterion notes
3. **The voice_calibration_uat.md location** + the final verdict (APPROVED / DEFERRED / REJECTED)
4. **The env-gate flip status** — DONE / PENDING / NOT-YET-APPROVED
5. **Next Sunday fire expectation** — first real cron fire YYYY-MM-DD 08:00 PT (or N/A if rejected)
6. **v3.2 backlog items** (if any) — prompt iteration, additional Tier-2 handles, voice tuning per RESEARCH §2

Once this plan signs off APPROVED + env flip is DONE: Phase 15 is COMPLETE. Update `.planning/REQUIREMENTS.md` Traceability table: JSWEEP-01..06 from "Pending" → "Complete".
</output>
</content>
