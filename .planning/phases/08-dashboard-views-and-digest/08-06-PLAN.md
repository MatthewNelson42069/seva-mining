---
phase: 08-dashboard-views-and-digest
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified: []
autonomous: false
requirements:
  - DGST-01
  - DGST-02
  - DGST-03
  - CREV-01
  - CREV-02
  - CREV-03
  - CREV-04
  - CREV-05
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04
  - SETT-05
  - SETT-06
  - SETT-07
  - SETT-08

must_haves:
  truths:
    - "All three pages render correctly with mock data in the browser"
    - "Operator has verified the visual layout matches design expectations"
    - "All interactions (navigation, approve, CRUD, save) work end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all three new dashboard pages — DigestPage, ContentPage, and SettingsPage — running in the browser with mock data.

Purpose: Visual and interaction verification that cannot be automated. The operator confirms layout, readability, and interaction flow before marking Phase 8 complete.

Output: User approval or specific issues for hotfix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-dashboard-views-and-digest/08-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and interaction verification of all 3 dashboard pages</name>
  <files></files>
  <action>
    Start the dev server with `cd frontend && npm run dev`, then run both test suites: `cd frontend && npm run test -- --run` and `cd backend && uv run pytest -x -q`. Present verification checklist to user.
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms all 3 pages render correctly, interactions work, and both test suites pass</done>
  <what-built>
    Three new dashboard pages replacing stubs:
    1. DigestPage (/digest) — daily digest with top stories, queue snapshot, yesterday summary, priority alert, prev/next navigation
    2. ContentPage (/content) — content bundle review with format-specific rendering, source links, approve/reject flow
    3. SettingsPage (/settings) — 6-tab settings: Watchlists (CRUD), Keywords (CRUD), Scoring (config save), Notifications (config save), Agent Runs (filtered log + quota bar), Schedule (intervals + restart note)

    Plus 2 backend endpoints: GET /config and PATCH /config/{key} for config CRUD.
  </what-built>
  <how-to-verify>
    Start the dev server:
    ```
    cd frontend && npm run dev
    ```

    1. **DigestPage** — Navigate to /digest
       - Verify: page shows "Daily Digest" heading with today's date
       - Verify: top stories section shows a list (may be empty without real data)
       - Verify: queue snapshot shows 3 stat cards (Twitter, Instagram, Content)
       - Verify: prev/next arrows are visible; next button should be disabled at latest date
       - Click prev — verify date changes and content updates (or shows "No digest available")

    2. **ContentPage** — Navigate to /content
       - Verify: shows "Content Review" heading
       - Verify: if mock data available, shows content bundle with format badge
       - Verify: approve button is visible; clicking it should show toast
       - Verify: reject button expands to show rejection reason field

    3. **SettingsPage** — Navigate to /settings
       - Verify: 6 tabs visible (Watchlists, Keywords, Scoring, Notifications, Agent Runs, Schedule)
       - Click each tab:
         - Watchlists: platform toggle (Twitter/Instagram), "Add Account" button, table rows
         - Keywords: table with Term column, active checkboxes, "Add Keyword" button
         - Scoring: number input fields for weights/thresholds, "Save Scoring Settings" button
         - Notifications: config fields, save button
         - Agent Runs: filter dropdown, table, quota bar at top showing "X API Monthly Quota"
         - Schedule: number inputs with "hours" labels, restart note text

    4. **Full test suite** — in a separate terminal:
       ```
       cd frontend && npm run test -- --run
       cd backend && uv run pytest -x -q
       ```
       Both must exit 0.
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 pages look correct, or describe any visual/interaction issues</resume-signal>
</task>

</tasks>

<verification>
User confirms all 3 pages render correctly, interactions work, and both test suites pass.
</verification>

<success_criteria>
User approves the visual layout and interaction flow of DigestPage, ContentPage, and SettingsPage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-views-and-digest/08-06-SUMMARY.md`
</output>
