---
id: 11-03
phase: 11-audit-cleanup-bundle
plan: 3
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - scheduler/agents/daily_summary.py
autonomous: true
requirements: [CLEANUP-03]
requirements_addressed: [CLEANUP-03]
task_count: 1
gap_closure: false
nyquist_compliant: true

must_haves:
  truths:
    - "`grep -c 'def run_juno_daily_summary' scheduler/agents/daily_summary.py` returns 1 (precondition AND postcondition — i.e., the count stays 1 after this plan even if the Phase 9 stub had survived earlier; in current shipped state the count is already 1)"
    - "No stale 'Phase 9 stub entry point' section header comment remains in the file"
    - "scheduler/worker.py lazy import of run_juno_daily_summary continues to resolve to the live Phase 10 implementation"
    - "Full scheduler pytest suite stays GREEN after the cleanup"
  artifacts:
    - path: "scheduler/agents/daily_summary.py"
      provides: "Exactly one run_juno_daily_summary definition (Phase 10 live); no stub/stale-comment debris"
      contains: "def run_juno_daily_summary"
  key_links:
    - from: "scheduler/worker.py (lazy import at line 265)"
      to: "scheduler/agents/daily_summary.py::run_juno_daily_summary (line 1150 pre-edit)"
      via: "from agents.daily_summary import run_juno_daily_summary"
      pattern: "from agents\\.daily_summary import run_juno_daily_summary"
---

<objective>
Land the dead-code half of CLEANUP-03 by removing any remaining Phase-9-stub debris in `scheduler/agents/daily_summary.py` while preserving the single live Phase 10 implementation.

**IMPORTANT — actual current state of file (verified at planning time, commit context post-Phase-10 + post-CLEANUP-01):**

- `grep -cn 'def run_juno_daily_summary' scheduler/agents/daily_summary.py` returns `1`. There is ONLY the Phase 10 live implementation at line 1150 (offsets may shift slightly post-CLEANUP-01 edits to lines 993-994 and 1232-1246).
- A stale section-divider comment block survives at lines 813-823 that announces a "v3.0 Phase 9 — Juno daily_summary stub entry point" but the stub body itself was already replaced by the Phase 10 work (`_fetch_7day_avg_for_feed`, `_run_juno_health_check`, `_build_juno_defence_news_section`, `_build_juno_canadian_procurement_section`, `_build_juno_world_events_section`, and the live `run_juno_daily_summary`).
- The ROADMAP's "line 762 stub block (~110 lines)" wording reflects the audit's view of the as-shipped code; the working tree's stub body has already been replaced inline by the Phase 10 helpers — only the misleading comment header remains.

So CLEANUP-03 in current working-tree reality reduces to: **(1) confirm via grep that exactly one definition exists, and (2) delete the stale comment block at lines 813-823** that misdescribes the following helpers as a "stub entry point". If the executor's grep finds 2+ definitions (unexpected — would indicate a post-planning regression), they MUST delete the Phase-9 stub block and keep only the Phase 10 implementation; otherwise the comment-only edit is the entire change.

Purpose: Close CLEANUP-03 (run_juno_daily_summary double-definition / Phase 9 stub debris).
Output: `daily_summary.py` modified — stale "Phase 9 stub entry point" section-divider comment block removed; file shrinks by ~11 lines (or ~110 lines if a stub body is somehow still present).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md
@/Users/matthewnelson/seva-mining/.planning/ROADMAP.md
@/Users/matthewnelson/seva-mining/.planning/milestones/v3.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Verbatim stale comment block at lines 811-823 of scheduler/agents/daily_summary.py: -->

```
811:
812:
813:# ---------------------------------------------------------------------------
814:# v3.0 Phase 9 — Juno daily_summary stub entry point (TENANT-08).
815:#
816:# Registered as an APScheduler job in scheduler/worker.py at
817:# CronTrigger(hour="8,12", minute=5, timezone="America/Los_Angeles") — 5-min
818:# stagger from the Seva run_daily_summary() above (D-01a). Writes a
819:# status='partial' daily_summaries row with all section markdown NULL so the
820:# Juno tab in the v3.0 dashboard renders an empty-state. Phase 10 (DEF-01..10)
821:# replaces this stub with the real Defence-News Funnel pipeline (Canadian
822:# procurement contracts + defence-industry headlines + Sonnet synthesis).
823:# ---------------------------------------------------------------------------
824:
825:
826:async def _fetch_7day_avg_for_feed(
```

<!-- worker.py lazy import context (line 265): -->

```
from agents.daily_summary import run_juno_daily_summary  # lazy
```

<!-- All call sites of run_juno_daily_summary (verified at planning time): -->
- /Users/matthewnelson/seva-mining/scheduler/worker.py:265 (lazy import inside job-registration helper)
- /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_juno_daily_summary.py (multiple test imports, all `from agents.daily_summary import run_juno_daily_summary`)
- /Users/matthewnelson/seva-mining/scheduler/companies/juno/feeds.py:13 (comment-only reference)
- /Users/matthewnelson/seva-mining/scheduler/agents/juno_health_check.py:87 (comment-only reference)

Python's last-wins module semantics: if a stub block existed at line ~762 alongside the live impl at line ~1150, all imports would resolve to the line-1150 definition. The grep at planning time shows only the line-1150 definition, so the import chain is already correctly wired — no behavioral regression risk from this cleanup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-grep, delete the stale Phase-9-stub section-divider comment block, post-grep + pytest</name>
  <files>scheduler/agents/daily_summary.py</files>

  <read_first>
    - /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py (lines 811-823 — the stale comment block; lines 1140-1170 — the live Phase 10 def around what was line 1150 pre-CLEANUP-01)
    - /Users/matthewnelson/seva-mining/scheduler/worker.py (lines 250-275 — the lazy import + APScheduler job registration)
    - /Users/matthewnelson/seva-mining/.planning/ROADMAP.md (Phase 11 Hard Part P2 — dead-code removal safety)
  </read_first>

  <behavior>
    - After this task, exactly one `def run_juno_daily_summary` exists in the file (precondition preserved)
    - No `# v3.0 Phase 9 — Juno daily_summary stub entry point` comment lingers anywhere in the file
    - `scheduler/worker.py` lazy import still resolves to the live Phase 10 implementation (verified by both a Python import smoke and full pytest)
    - File line count drops by ~11 lines (the comment block) — OR ~110 lines if a stub body somehow still exists, in which case both the body AND the comment block are removed
  </behavior>

  <action>
    Per CLEANUP-03 (REQUIREMENTS.md line 18) + ROADMAP Hard Part P2 (dead-code removal safety):

    **Step 1 — Pre-deletion grep (record the current count for the SUMMARY artifact):**

    ```bash
    cd /Users/matthewnelson/seva-mining
    grep -n "def run_juno_daily_summary" scheduler/agents/daily_summary.py
    grep -rn "run_juno_daily_summary" scheduler/ backend/ frontend/ 2>/dev/null | grep -v "__pycache__"
    ```

    Confirm the production-code grep returns exactly ONE `def run_juno_daily_summary` line and that worker.py's lazy import is the only non-test non-comment external reference. If the production-code grep returns 2+ definitions, STOP and report — the assumption documented in `<objective>` above no longer holds and the planner needs to be re-engaged.

    **Step 2a — Comment-only path (expected — current shipped state).** Open `/Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` and DELETE lines 811-824 INCLUSIVE (the two blank lines at 811-812, the 11-line comment block at 813-823, and the trailing blank line at 824). The next non-blank line after the deletion (currently line 826, `async def _fetch_7day_avg_for_feed(...)`) becomes the immediate successor of `run_daily_summary`'s closing `except Exception: logger.exception(...)` block at line 810.

    Specifically, in the working tree, find this exact 14-line slab and delete the WHOLE slab:

    ```
    \n
    \n
    # ---------------------------------------------------------------------------
    # v3.0 Phase 9 — Juno daily_summary stub entry point (TENANT-08).
    #
    # Registered as an APScheduler job in scheduler/worker.py at
    # CronTrigger(hour="8,12", minute=5, timezone="America/Los_Angeles") — 5-min
    # stagger from the Seva run_daily_summary() above (D-01a). Writes a
    # status='partial' daily_summaries row with all section markdown NULL so the
    # Juno tab in the v3.0 dashboard renders an empty-state. Phase 10 (DEF-01..10)
    # replaces this stub with the real Defence-News Funnel pipeline (Canadian
    # procurement contracts + defence-industry headlines + Sonnet synthesis).
    # ---------------------------------------------------------------------------
    \n
    ```

    Replace with the standard two-blank-line separator between top-level helpers:

    ```
    \n
    \n
    ```

    (i.e., the deletion is 12 net lines — the divider+11-line comment block — and we leave the two surrounding blank lines that PEP 8 requires between top-level defs.)

    **Step 2b — Stub-body path (defensive, only if Step 1 grep showed 2+ definitions).** If pre-deletion grep finds a SECOND `async def run_juno_daily_summary` somewhere between line 813 and the live impl, ALSO delete that entire `async def run_juno_daily_summary(...)` block (function header through final `await session.commit()` / final return / class-level dedent). DO NOT touch the live implementation. Verify post-deletion that exactly one definition remains.

    **Step 3 — Post-deletion verification:**

    ```bash
    cd /Users/matthewnelson/seva-mining
    grep -c "def run_juno_daily_summary" scheduler/agents/daily_summary.py
    # MUST return: 1

    grep -c "v3.0 Phase 9 — Juno daily_summary stub" scheduler/agents/daily_summary.py
    # MUST return: 0

    cd scheduler
    uv run python -c "from agents.daily_summary import run_juno_daily_summary; print(run_juno_daily_summary.__module__, run_juno_daily_summary.__qualname__)"
    # MUST print: agents.daily_summary run_juno_daily_summary

    uv run pytest -x -q
    # MUST be GREEN end-to-end (no test count change vs pre-edit)
    ```

    If any verification step fails, STOP and report — do not commit a partial cleanup.
  </action>

  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest -x -q 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'def run_juno_daily_summary' /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns `1` (matches ROADMAP success criterion #3)
    - `grep -c 'v3.0 Phase 9 — Juno daily_summary stub' /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns `0`
    - `grep -c 'Phase 9 stub' /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns `0`
    - `cd scheduler && uv run python -c "from agents.daily_summary import run_juno_daily_summary; print(run_juno_daily_summary.__module__, run_juno_daily_summary.__qualname__)"` prints `agents.daily_summary run_juno_daily_summary`
    - `cd scheduler && uv run pytest -x -q` GREEN end-to-end (same test count as pre-Phase-11 baseline; no regressions)
    - File line count: original `wc -l scheduler/agents/daily_summary.py` was 1404 (pre-Phase-11); post-CLEANUP-01 + CLEANUP-03 expectation is approximately `1404 - 2 (CLEANUP-01 gate removal) - 1 (CLEANUP-01 client-init `if is_morning_fire:` line) - 12 (CLEANUP-03 comment block) ≈ 1389` — verify within ±5 lines as a sanity check
  </acceptance_criteria>

  <done>
    Stale "v3.0 Phase 9 — Juno daily_summary stub entry point" comment block removed; exactly one `def run_juno_daily_summary` exists in the file; lazy import in worker.py still resolves correctly; full scheduler pytest suite GREEN. ROADMAP Phase 11 success criterion #3 satisfied.
  </done>
</task>

</tasks>

<verification>
After Task 1:

1. `grep -c 'def run_juno_daily_summary' /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` → `1`
2. `grep -c 'Phase 9 stub\|Phase 9 — Juno daily_summary stub' /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` → `0`
3. `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -x -q` GREEN
4. `cd /Users/matthewnelson/seva-mining/scheduler && uv run python -c "from agents.daily_summary import run_juno_daily_summary; print(run_juno_daily_summary.__qualname__)"` prints `run_juno_daily_summary`
</verification>

<success_criteria>
- ROADMAP Phase 11 Success Criterion #3 satisfied: `grep -c 'def run_juno_daily_summary'` returns 1; full scheduler pytest GREEN; worker.py lazy import resolves correctly
- The misleading "Phase 9 stub entry point" section header is gone from the file
- No behavioral change (this is a comment-only edit in the expected path)
</success_criteria>

<output>
After completion, create `/Users/matthewnelson/seva-mining/.planning/phases/11-audit-cleanup-bundle/11-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. SUMMARY should explicitly record the pre-edit grep result (1 definition expected, but capture exact count) so the audit trail reflects current reality vs the ROADMAP's "line 762 stub" wording.
</output>
