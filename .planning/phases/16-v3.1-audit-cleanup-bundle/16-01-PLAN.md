---
id: 16-01
phase: 16-v3.1-audit-cleanup-bundle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/SummaryFeedPage.test.tsx
  - frontend/src/pages/PerAgentQueuePage.tsx
  - frontend/src/api/__tests__/summaries.test.tsx
  - frontend/src/components/calendar/DayCell.tsx
  - frontend/src/pages/__tests__/SummaryFeedPage.test.tsx
  - backend/app/main.py
  - backend/app/models/weekly_sweep.py
  - backend/tests/test_models_byte_identical.py
  - backend/tests/test_multitenant_isolation.py
  - scheduler/agents/daily_summary.py
  - scheduler/agents/weekly_sweeper.py
  - scheduler/scripts/uat_voice_calibration.py
  - scheduler/tests/agents/test_juno_health_check.py
  - scheduler/tests/agents/test_daily_summary_prune.py
  - frontend/src/pages/SummaryFeedPage.tsx
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05]
requirements_addressed: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05]
task_count: 5
gap_closure: true
gap_source: .planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over)
nyquist_compliant: true

must_haves:
  truths:
    - "Frontend lints clean: `cd frontend && npm run lint` exits 0 (was: 15 errors across 6 files)"
    - "Backend lints clean: `cd backend && uv run ruff check` exits 0 (was: 17 errors — mix of UP017 + E501 + I001)"
    - "Scheduler lints clean for F401: `cd scheduler && uv run ruff check` exits 0 (was: 6 F401 unused-import errors — 3 documented in REQUIREMENTS + 3 surfaced during audit run)"
    - "Scheduler pytest shows 0 'RuntimeWarning: coroutine was never awaited' (was: 4 from test_daily_summary_prune.py AsyncMock misconfig)"
    - "Stale Phase-9-era empty-state copy at SummaryFeedPage.tsx:62-74 is GONE: `grep -c 'Coming in Phase 10' frontend/src/pages/SummaryFeedPage.tsx` returns 0 (block either deleted entirely OR replaced with tenant-aware copy)"
    - "Regression baselines preserved with zero regressions: frontend >=181 tests pass, backend >=191 tests pass, scheduler >=363 tests pass"
  artifacts:
    - path: "frontend/src/pages/SummaryFeedPage.test.tsx"
      provides: "ESLint-clean Phase-1 SummaryFeedPage tests (no-explicit-any + display-name fixed)"
      must_not_contain: ": any"
    - path: "frontend/src/pages/PerAgentQueuePage.tsx"
      provides: "Fast-refresh-clean per-agent queue page (constants moved to a separate module OR re-exported per react-refresh rule)"
    - path: "frontend/src/components/calendar/DayCell.tsx"
      provides: "react-hooks/set-state-in-effect violation resolved (either suppressed with justification comment OR refactored to avoid synchronous setState in useEffect body)"
      contains: "useEffect"
    - path: "scheduler/agents/daily_summary.py"
      provides: "Imports F401-clean: HAIKU_MODEL no longer imported from agents.juno_relevance"
      must_not_contain: "HAIKU_MODEL,"
    - path: "scheduler/agents/weekly_sweeper.py"
      provides: "Imports F401-clean: `from sqlalchemy import select` removed (unused after Phase 9 scoped_* migration)"
      must_not_contain: "from sqlalchemy import select"
    - path: "scheduler/scripts/uat_voice_calibration.py"
      provides: "Imports F401-clean: _build_juno_world_events_section no longer imported"
      must_not_contain: "_build_juno_world_events_section,"
    - path: "scheduler/tests/agents/test_daily_summary_prune.py"
      provides: "AsyncMock factory + side_effect calls return non-awaited values correctly; 4 RuntimeWarnings resolved"
      contains: "MagicMock"
    - path: "frontend/src/pages/SummaryFeedPage.tsx"
      provides: "Tenant-aware empty-state copy OR deleted Juno-branch; stale 'Coming in Phase 10' string gone"
      must_not_contain: "Coming in Phase 10"
  key_links:
    - from: "scheduler/agents/daily_summary.py top-of-file imports"
      to: "scheduler/agents/juno_relevance.py module exports"
      via: "from agents.juno_relevance import (classify_story, survives_threshold, DefenceRelevance)"
      pattern: "from agents.juno_relevance import"
    - from: "scheduler/scripts/uat_voice_calibration.py top-of-file imports"
      to: "scheduler/agents/daily_summary.py public API"
      via: "from agents.daily_summary import (_build_juno_defence_news_section, JUNO_SONNET_MODEL, JUNO_SONNET_MAX_PROCUREMENT, JUNO_SONNET_TIMEOUT)"
      pattern: "from agents.daily_summary import"
    - from: "frontend/src/pages/SummaryFeedPage.tsx empty-state branch"
      to: "shared empty-state path (nextCronFireLabelPT helper already in file at lines 12-27) OR tenant-aware copy registry"
      via: "delete companyId === 'juno' branch → shared 'Waiting for first summary' renders for both tenants; OR replace string with tenant-specific copy referencing Juno cron schedule"
      pattern: "companyId === 'juno'"
---

<objective>
Close 5 pre-existing tech-debt items surfaced by the v3.1 milestone audit (`.planning/v3.1-MILESTONE-AUDIT.md` `tech_debt.pre_existing_carried_over` block + integration-checker Flow A step 4 PARTIAL finding) before v3.1 milestone archive. Mirrors Phase 11's shape (v3.0 audit cleanup bundle — 5 items, single plan, multi-task).

Items in scope:

1. **CLEAN-01** — Frontend ESLint (15 errors across 6 files; mix of auto-fixable + manual: `no-explicit-any` requires picking real types, `react/display-name` is a rule-config gap, `react-hooks/set-state-in-effect` in DayCell.tsx needs a suppression-with-justification OR refactor, `react-refresh/only-export-components` in PerAgentQueuePage.tsx needs the non-component export moved or suppressed)
2. **CLEAN-02** — Backend ruff (17 errors — mix of UP017 `datetime.UTC` migrations + I001 import-sort + E501 line-too-long; 12 auto-fixable via `uv run ruff check --fix`, remainder needs targeted manual fix)
3. **CLEAN-03** — Scheduler ruff F401 (6 unused imports — the 3 documented in REQUIREMENTS + 3 surfaced during audit run: `MagicMock` + `pytest` in test_juno_health_check.py; all 6 auto-fixable)
4. **CLEAN-04** — Scheduler test RuntimeWarning (4 `coroutine was never awaited` in `test_daily_summary_prune.py` — AsyncMock factory misconfig; fix AsyncMock setup so side_effect callables return MagicMock context managers rather than coroutines that are never awaited)
5. **CLEAN-05** — Stale Phase-9-era empty-state copy at `frontend/src/pages/SummaryFeedPage.tsx:62-74` ("Coming in Phase 10 — Defence-sector ingestion not yet enabled") — Phase 10 shipped 2026-05-19 and Phase 12 production cron flipped 2026-05-20; dead-text. Default approach: rewrite the Juno empty-state copy to be tenant-aware (more thoughtful UX); fallback: delete the Juno-branch entirely so the shared "Waiting for first summary. Next fire at {next_cron_PT}." copy handles both tenants.

Purpose: Close 5 of 5 audit cleanup items so v3.1 milestone can archive at 5/5 phases complete with zero pre-existing debt carried into v3.2. Mirrors Phase 11 shape (5 items single bundle phase) which itself shipped 2026-05-19 as the v3.0.1 capstone.

Output:
- 5 frontend files lint-clean
- 4 backend files lint-clean
- 4 scheduler files lint-clean
- 1 scheduler test file warning-free
- 1 frontend production file (SummaryFeedPage.tsx) with stale copy gone
- Regression baselines monotonically preserved (frontend >=181 / backend >=191 / scheduler >=363; all tests GREEN)
- Single atomic ship — file-disjoint within the plan so tasks are parallel-safe inside one wave
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matthewnelson/seva-mining/CLAUDE.md
@/Users/matthewnelson/seva-mining/.planning/ROADMAP.md
@/Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md
@/Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md

# Phase 11 mirror — same audit-cleanup-bundle shape, same atomic-code-and-test pattern
@/Users/matthewnelson/seva-mining/.planning/phases/11-audit-cleanup-bundle/11-01-PLAN.md

# Production / test files to modify (read for current line numbers + content)
@/Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx
@/Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py
@/Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py
@/Users/matthewnelson/seva-mining/scheduler/scripts/uat_voice_calibration.py
@/Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py

<audit_evidence>
<!-- Ground truth from running the lint tools 2026-05-20 — confirms REQUIREMENTS.md
     counts may UNDER-report because the audit window picked up 3 additional
     scheduler F401 errors + the broader frontend rule surface. Executor should
     trust the TOOL exit code, not the REQUIREMENTS.md numeric quote. -->

**Frontend (`cd frontend && npm run lint`) — 15 errors across 6 files:**

```
frontend/src/api/__tests__/summaries.test.tsx
  13:3   error  Definition for rule 'react/display-name' was not found       react/display-name
frontend/src/components/calendar/DayCell.tsx
  54:5   error  Avoid calling setState() directly within an effect           react-hooks/set-state-in-effect
frontend/src/pages/PerAgentQueuePage.tsx
  52:10, 66:10, 84:10, 96:10, 106:10  error  Unexpected any                  @typescript-eslint/no-explicit-any
  59:17                                error  Fast refresh only works...     react-refresh/only-export-components
frontend/src/pages/__tests__/SummaryFeedPage.test.tsx
  11:3                                  error  react/display-name (rule-not-found)
  27:94, 33:94, 54:10, 61:75, 67:89    error  Unexpected any (5x)
```

Total: 15 errors. Note: the file `frontend/src/pages/SummaryFeedPage.test.tsx` mentioned in REQUIREMENTS line 54 IS the `frontend/src/pages/__tests__/SummaryFeedPage.test.tsx` path; the file lives under `__tests__/`.

**Backend (`cd backend && uv run ruff check`) — 17 errors:**

```
backend/app/main.py + backend/app/models/weekly_sweep.py — UP017 errors (datetime.now(timezone.utc) → datetime.now(UTC))
backend/tests/test_models_byte_identical.py — I001 (Organize imports)
backend/tests/test_multitenant_isolation.py — UP017 at lines 147 + 412
Found 17 errors. [*] 12 fixable with the --fix option.
```

Note: REQUIREMENTS line 56 quotes "17 UP017 + 1 E501"; the actual tool output shows 17 total (mix of UP017 + I001; may include 1 E501). Executor should trust the live `uv run ruff check` output.

**Scheduler (`cd scheduler && uv run ruff check`) — 6 F401 errors confirmed via tool output 2026-05-20:**

```
scheduler/agents/daily_summary.py:57            — HAIKU_MODEL                       (documented; grep confirms 1 occurrence == import only)
scheduler/agents/weekly_sweeper.py:44           — `from sqlalchemy import select`   (documented; line 55-56 references "select" only inside a COMMENT)
scheduler/scripts/uat_voice_calibration.py:56   — _build_juno_world_events_section  (documented; line 296 references it only in docstring/comment)
scheduler/tests/agents/test_juno_health_check.py:23 — MagicMock                     (NEW — surfaced during audit run)
scheduler/tests/agents/test_juno_health_check.py:25 — pytest                        (NEW — surfaced during audit run)
+ 1 more F401 in same test_juno_health_check.py file
Found 6 errors. [*] 6 fixable with the --fix option.
```

**Scheduler RuntimeWarnings (`cd scheduler && uv run pytest tests/agents/test_daily_summary_prune.py 2>&1 | grep -c "RuntimeWarning: coroutine"`):**

```
4   (from AsyncMock factory misconfig — likely in the `_build_session_factory` helper at test_daily_summary_prune.py:62-128
     where `_factory()` returns a MagicMock with __aenter__ / __aexit__ as AsyncMock — but the OUTER `sessions_created.append(s)` path
     also creates an AsyncMock that's never awaited. Inspect the 4 specific warning sources via `pytest -W error::RuntimeWarning` to
     pinpoint the exact lines.)
```

**SummaryFeedPage.tsx Juno empty-state (CURRENT — pre-edit, lines 62-74):**

```typescript
  if (summaries.length === 0) {
    if (companyId === 'juno') {
      // v3.0 Phase 9 — Juno News Funnel empty-state. Phase 10 (DEF-01..10)
      // populates real defence-sector content; until then this copy renders
      // when the cron writes zero rows or hasn't fired yet for Juno.
      return (
        <div className="max-w-[720px] mx-auto py-8 px-4">
          <p className="text-sm text-muted-foreground">
            Coming in Phase 10 — Defence-sector ingestion not yet enabled.
          </p>
        </div>
      )
    }
    const nextFire = nextCronFireLabelPT()
    return (
      <div className="max-w-[720px] mx-auto py-8 px-4">
        <p className="text-sm text-muted-foreground">
          Waiting for first summary. Next fire at {nextFire}.
        </p>
      </div>
    )
  }
```

The `nextCronFireLabelPT()` helper at lines 12-27 ALREADY computes the right copy for the Seva branch (08:00 PT / 12:00 PT). For Juno, the cron schedule is 08:05 PT / 12:05 PT (from Phase 10 + Phase 12 production cron flip 2026-05-20) — so a tenant-aware rewrite needs either a per-tenant helper variant or a parameter.
</audit_evidence>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CLEAN-01 — Frontend ESLint cleanup (15 errors across 5 files)</name>
  <files>
    frontend/src/pages/__tests__/SummaryFeedPage.test.tsx,
    frontend/src/pages/PerAgentQueuePage.tsx,
    frontend/src/api/__tests__/summaries.test.tsx,
    frontend/src/components/calendar/DayCell.tsx
  </files>

  <read_first>
    - /Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over → first bullet — "frontend ESLint: 15 errors")
    - /Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md (CLEAN-01 line 54 — acceptance gate: `cd frontend && npm run lint` exits 0)
    - /Users/matthewnelson/seva-mining/frontend/eslint.config.js (confirm which rule set is active; do NOT loosen rules to make errors disappear)
    - /Users/matthewnelson/seva-mining/frontend/package.json (check whether `eslint-plugin-react` is installed — drives the react/display-name fix path)
    - All 4 affected files end-to-end (read enough to choose the right fix per rule class):
      * frontend/src/pages/__tests__/SummaryFeedPage.test.tsx (5x no-explicit-any at 27:94, 33:94, 54:10, 61:75, 67:89 + 1x react/display-name at 11:3)
      * frontend/src/pages/PerAgentQueuePage.tsx (5x no-explicit-any at lines 52, 66, 84, 96, 106 + 1x react-refresh/only-export-components at 59:17)
      * frontend/src/api/__tests__/summaries.test.tsx (1x react/display-name at 13:3)
      * frontend/src/components/calendar/DayCell.tsx (1x react-hooks/set-state-in-effect at 54:5)
  </read_first>

  <action>
    Per CLEAN-01 acceptance gate (REQUIREMENTS.md line 54): make `cd frontend && npm run lint` exit 0. The 15 errors split into 4 rule classes; each needs a different fix shape. Address them in this order.

    **Step 1 — Attempt auto-fix first (small-scope discovery pass):**

    ```bash
    cd /Users/matthewnelson/seva-mining/frontend && npx eslint --fix src/
    ```

    Expected: 0-1 of the 15 are auto-fixable (these 4 rule classes are all SEMANTIC, not formatting). Run `npm run lint` after to confirm what remains.

    **Step 2 — Fix `no-explicit-any` errors (10 total: 5 in PerAgentQueuePage.tsx at lines 52/66/84/96/106 + 5 in SummaryFeedPage.test.tsx at lines 27/33/54/61/67):**

    For each `: any` annotation, pick a REAL type. Read the surrounding code to choose:
    - In test files (`SummaryFeedPage.test.tsx`): typically narrow to the actual mock shape — `Partial<Summary>` from `@/api/types`, or `ReturnType<typeof useSummaries>` for hook mocks, or `{ summaries: Summary[]; meta?: Meta }` for response shape mocks, or `unknown` if truly generic and only used in a destructure.
    - In `PerAgentQueuePage.tsx` (lines 52, 66, 84, 96, 106): read the actual statement at each line to determine if `any` is on an API response (use the domain type from `@/api/types.ts`), an event handler (use the proper `React.SyntheticEvent<...>` or `React.ChangeEvent<HTMLInputElement>`), or a component prop (define a local interface). The 5 errors are clustered in close proximity — likely they all share the same root cause (e.g., one shared interface that should be typed).

    **Critical**: do NOT use `// eslint-disable-next-line @typescript-eslint/no-explicit-any` as the primary fix — that masks debt. Use `unknown` as a last resort with an inline comment explaining why a narrower type isn't feasible. Reserve eslint-disable for cases where a third-party type genuinely is `any` (e.g., an untyped vi.mock factory).

    **Step 3 — Fix `react/display-name` errors (2 total: summaries.test.tsx:13 + SummaryFeedPage.test.tsx:11):**

    Both errors read "Definition for rule 'react/display-name' was not found" — that is a RULE-CONFIG gap, not a code error. Determine the fix path:

    ```bash
    cat /Users/matthewnelson/seva-mining/frontend/package.json | grep -E "eslint-plugin-react|@vitejs/plugin-react"
    ```

    - IF `eslint-plugin-react` is NOT in `devDependencies`: take option (a) — open `frontend/eslint.config.js` and REMOVE the `'react/display-name'` rule entry (it's referenced but unbacked; modern flat configs treat this as an error). The rule is a no-op in this codebase because there's no plugin wiring it.
    - IF `eslint-plugin-react` IS present: take option (b) — add `import react from 'eslint-plugin-react'` at the top of `eslint.config.js` and include `react.configs.flat.recommended` (or the equivalent flat-config import) in the array so the rule is properly registered.

    Default to option (a) — removing the unbacked rule reference — UNLESS the surrounding config already imports `eslint-plugin-react` (in which case the rule is half-wired and option (b) completes the wiring). Confirm post-fix with `cd frontend && npm run lint 2>&1 | grep -c "react/display-name"` returns 0.

    **Step 4 — Fix `react-hooks/set-state-in-effect` in DayCell.tsx:54:**

    The current code (lines 50-55):
    ```typescript
    // optimistic invalidation refetch, week navigation, or another tab's edit).
    useEffect(() => {
      setCurrent(item?.body ?? '')
    }, [item?.id, item?.body])
    ```

    This is an INTENTIONAL pattern — sync local textarea state when the underlying server-side row changes. Two fix options:

    - **(a) Suppress with justification** (PREFERRED — the pattern IS correct for this use case; refactoring would lose in-flight edits):
    ```typescript
    useEffect(() => {
      // eslint-disable-next-line react-hooks/set-state-in-effect -- v2.1 Phase 6 pattern: sync local
      // textarea state when server-side row changes (optimistic invalidation, week nav, cross-tab edit).
      // Refactoring to derive-from-props would lose the user's in-flight edits during background refetch.
      setCurrent(item?.body ?? '')
    }, [item?.id, item?.body])
    ```

    - **(b) Refactor to key-on-id pattern** — wrap DayCell's editable content in a child component keyed on `item?.id` so React unmounts/remounts on row change. This is a heavier refactor (30-50 line change touching DayCell + WeeklyGrid).

    Default to option (a). If the inline `--` comment style violates ESLint's expected disable-comment grammar, fall back to a per-line `// eslint-disable-line react-hooks/set-state-in-effect` on the `setCurrent(...)` line only.

    **Step 5 — Fix `react-refresh/only-export-components` in PerAgentQueuePage.tsx:59:**

    Read line 59 to determine what non-component export is breaking the rule. Two fix options:

    - **(a) Move the non-component export to a sibling module** (preferred for value/constant exports): create `frontend/src/pages/perAgentQueueConstants.ts`, move the const/function there, update the import sites accordingly. Cleanest fix; preserves fast-refresh.
    - **(b) Suppress with `// eslint-disable-next-line react-refresh/only-export-components`** — acceptable if the non-component export is trivial (a type re-export or a tiny constant) and the HMR overhead is negligible.

    If line 59 is a TYPE export (e.g., `export type AgentName = ...`) — option (b) is fine (types are erased at runtime, no real HMR impact). If it's a real value export, prefer (a).

    **Step 6 — Final verification:**

    ```bash
    cd /Users/matthewnelson/seva-mining/frontend && npm run lint 2>&1 | tail -5
    ```

    Must exit 0 with no error summary block.

    **DO NOT:**
    - Loosen ESLint rules in `eslint.config.js` to silence violations (CLEAN-01 is about FIXING the code; the only config edit allowed is REMOVING the unbacked `react/display-name` rule per Step 3 option (a) — which is a config FIX, not a rule loosening)
    - Add file-level `/* eslint-disable */` blocks — use per-line/per-block disables only with inline justification comments
    - Touch any production file outside the 4 listed in `<files>` (the lint errors are confined to those 4 files; if you discover additional lint errors mid-task, file as a follow-up — do NOT scope-creep)
    - Modify `eslint.config.js` for any rule OTHER than `react/display-name` (the other 3 rule classes are correctly configured; the bug is in the code, not the rules)
    - Run `npx eslint --fix --fix-type problem,suggestion` — use bare `--fix` to avoid unintended side effects on rules outside this scope
  </action>

  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; npm run lint 2>&amp;1 | tail -10; echo "---LINT-EXIT---"; cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; npm test --silent 2>&amp;1 | tail -10; echo "---TEST-EXIT---"; cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `cd /Users/matthewnelson/seva-mining/frontend && npm run lint` exits 0 (no "X problems" error summary; was: "15 problems (15 errors, 0 warnings)")
    - `grep -cE ":\s*any[^a-zA-Z_]" /Users/matthewnelson/seva-mining/frontend/src/pages/PerAgentQueuePage.tsx` returns 0 (no `: any` annotations remain in this file)
    - `grep -cE ":\s*any[^a-zA-Z_]" /Users/matthewnelson/seva-mining/frontend/src/pages/__tests__/SummaryFeedPage.test.tsx` returns 0 (no `: any` annotations remain in this file)
    - `cd /Users/matthewnelson/seva-mining/frontend && npm run lint 2>&1 | grep -c "react/display-name"` returns 0 (rule-config gap resolved)
    - `cd /Users/matthewnelson/seva-mining/frontend && npm test --silent` exits 0 with >=181 tests passing (regression baseline preserved; any test-file edits keep all existing assertions passing)
    - `cd /Users/matthewnelson/seva-mining/frontend && npx tsc --noEmit` exits 0 (TypeScript clean — the type-narrowing fixes for no-explicit-any must not introduce TS errors)
    - `git diff --stat frontend/src/` shows only the 4 listed files modified (+ possibly `eslint.config.js` per Step 3 + possibly NEW `frontend/src/pages/perAgentQueueConstants.ts` per Step 5 option (a))
  </acceptance_criteria>

  <done>
    Frontend lint exits 0. All 15 errors resolved via type-narrowing + rule-config fix + targeted suppressions with inline justifications. Frontend test count >=181 GREEN. TypeScript clean. CLEAN-01 acceptance gate satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CLEAN-02 — Backend ruff cleanup (17 errors)</name>
  <files>
    backend/app/main.py,
    backend/app/models/weekly_sweep.py,
    backend/tests/test_models_byte_identical.py,
    backend/tests/test_multitenant_isolation.py
  </files>

  <read_first>
    - /Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over → second bullet — "backend ruff: 17 UP017 + 1 E501")
    - /Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md (CLEAN-02 line 56 — acceptance gate: `cd backend && uv run ruff check` exits 0)
    - /Users/matthewnelson/seva-mining/backend/pyproject.toml (or ruff.toml — confirm ruff rule set; do NOT loosen rules to silence errors)
    - The FULL output of `cd /Users/matthewnelson/seva-mining/backend && uv run ruff check` BEFORE editing (capture the COMPLETE error list — REQUIREMENTS quotes "17 UP017 + 1 E501" but the actual run shows 17 total errors with breakdown mixing UP017 + I001 + possibly E501; trust the tool, not the quoted breakdown)
  </read_first>

  <action>
    Per CLEAN-02 acceptance gate (REQUIREMENTS.md line 56): make `cd backend && uv run ruff check` exit 0.

    **Step 1 — Capture current error inventory:**

    ```bash
    cd /Users/matthewnelson/seva-mining/backend && uv run ruff check 2>&1 | tail -40
    ```

    Read the full list. The audit observed "17 UP017 + 1 E501" but the live run may show a different breakdown (e.g., 14 UP017 + 1 I001 + 1 E501 + 1 other). Do NOT assume the breakdown — address every error class present.

    **Step 2 — Apply auto-fix:**

    ```bash
    cd /Users/matthewnelson/seva-mining/backend && uv run ruff check --fix
    ```

    Expected: 12 of 17 auto-fixed. UP017 + I001 are both auto-fixable. E501 is NOT auto-fixable because ruff doesn't know where to break a long line without changing semantics.

    Critical: UP017's auto-fix adds `from datetime import UTC` at the top of each modified file. Confirm post-auto-fix that:
    - `from datetime import UTC` (or `from datetime import datetime, UTC`) appears at the top of each file that previously used `timezone.utc`
    - All `datetime.now(timezone.utc)` calls are replaced with `datetime.now(UTC)`
    - The unused `timezone` import is also auto-removed IF no other code in the file uses it (ruff handles this conservatively)

    **Step 3 — Manually fix any remaining errors:**

    Re-run ruff and address the residual:

    ```bash
    cd /Users/matthewnelson/seva-mining/backend && uv run ruff check 2>&1 | tail -20
    ```

    Likely remainders:
    - **E501 line-too-long errors** — break long lines at natural seams (after a comma in argument list, after `:` in dict literal, after `or`/`and` in boolean expressions). Match the project's existing line-wrap style by scanning a nearby file for similar wraps.
    - Any UP017 that auto-fix skipped (rare — usually because the file has a custom `UTC` shadow variable; manual review needed to confirm)

    For each manual fix, READ the file context first, verify the change doesn't break any test that depends on the old import shape, and re-run `ruff check` after each file.

    **Step 4 — Final verification:**

    ```bash
    cd /Users/matthewnelson/seva-mining/backend && uv run ruff check 2>&1 | tail -5
    ```

    Must exit 0 with output "All checks passed!".

    Then run the backend test suite to confirm no regressions:

    ```bash
    cd /Users/matthewnelson/seva-mining/backend && uv run pytest -q 2>&1 | tail -10
    ```

    Must exit 0 with >=191 tests passing (audit baseline per `.planning/v3.1-MILESTONE-AUDIT.md` regression_baseline.backend).

    **DO NOT:**
    - Loosen ruff rules in `pyproject.toml` / `ruff.toml` (CLEAN-02 is about FIXING the code, not silencing the linter)
    - Add `# noqa: UP017` suppressions — UP017 is auto-fixable and the migration to `datetime.UTC` is the right call (the legacy `timezone.utc` alias is deprecated in favor of `datetime.UTC` as of Python 3.11+)
    - Touch any file outside the 4 listed (if `--fix` surfaces errors in other files, file as follow-up — do NOT scope-creep)
    - Change `datetime.now(timezone.utc)` → `datetime.utcnow()` (deprecated since Python 3.12 and silently returns NAIVE datetime — the correct UP017 fix is `datetime.now(UTC)`, NOT `utcnow()`)
    - Skip running the test suite — UP017 changes the import surface (`from datetime import UTC` added); a test elsewhere that does `from datetime import timezone; timezone.utc` must still work, and the test suite is the safety net
  </action>

  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/backend &amp;&amp; uv run ruff check 2>&amp;1 | tail -5; echo "---RUFF-EXIT---"; cd /Users/matthewnelson/seva-mining/backend &amp;&amp; uv run pytest -q 2>&amp;1 | tail -10; echo "---PYTEST-EXIT---"; cd /Users/matthewnelson/seva-mining/backend &amp;&amp; uv run python -c "from app.main import app; print('OK')" 2>&amp;1</automated>
  </verify>

  <acceptance_criteria>
    - `cd /Users/matthewnelson/seva-mining/backend && uv run ruff check` exits 0 with output "All checks passed!" (was: "Found 17 errors. [*] 12 fixable")
    - `grep -cE "datetime\.now\(timezone\.utc\)" /Users/matthewnelson/seva-mining/backend/app/main.py /Users/matthewnelson/seva-mining/backend/app/models/weekly_sweep.py /Users/matthewnelson/seva-mining/backend/tests/test_multitenant_isolation.py` returns 0 (the UP017-source pattern is gone from the 3 named files)
    - `grep -cE "datetime\.utcnow\(\)" /Users/matthewnelson/seva-mining/backend/app/ /Users/matthewnelson/seva-mining/backend/tests/ -r` returns 0 (the deprecated `utcnow()` pattern must NOT be introduced as a UP017 workaround)
    - `grep -c "# noqa: UP017" /Users/matthewnelson/seva-mining/backend/ -r` returns 0 (no suppressions — the migration is the fix)
    - `cd /Users/matthewnelson/seva-mining/backend && uv run pytest -q` exits 0 with >=191 tests passing (regression baseline preserved)
    - `git diff --stat backend/` shows only the 4 listed files modified
    - `cd /Users/matthewnelson/seva-mining/backend && uv run python -c "from app.main import app; print('OK')"` prints `OK` (smoke check — the import-sort/UP017 changes didn't break module-level imports)
  </acceptance_criteria>

  <done>
    Backend ruff exits 0 ("All checks passed!"). All 17 errors resolved via auto-fix + targeted manual edits for the residual. No rule suppressions added. Backend test count >=191 GREEN. Module imports clean. CLEAN-02 acceptance gate satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CLEAN-03 — Scheduler ruff F401 cleanup (6 unused imports across 4 files)</name>
  <files>
    scheduler/agents/daily_summary.py,
    scheduler/agents/weekly_sweeper.py,
    scheduler/scripts/uat_voice_calibration.py,
    scheduler/tests/agents/test_juno_health_check.py
  </files>

  <read_first>
    - /Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over → third bullet — "scheduler ruff: F401 unused imports in daily_summary.py (HAIKU_MODEL), weekly_sweeper.py (select), uat_voice_calibration.py")
    - /Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md (CLEAN-03 line 58 — acceptance gate: `cd scheduler && uv run ruff check` exits 0 for F401; documents 3 known F401s, audit run surfaces 3 additional ones in test_juno_health_check.py)
    - All 4 affected files (import block + grep of each unused symbol to confirm zero non-import references):
      * scheduler/agents/daily_summary.py:57 — `HAIKU_MODEL` (planner-verified: `grep -c "HAIKU_MODEL" scheduler/agents/daily_summary.py` returns 1 == import only)
      * scheduler/agents/weekly_sweeper.py:44 — `from sqlalchemy import select` (planner-verified: line 55-56 references "select" only inside a COMMENT, no callsite)
      * scheduler/scripts/uat_voice_calibration.py:56 — `_build_juno_world_events_section` (planner-verified: line 296 references it only in docstring/comment)
      * scheduler/tests/agents/test_juno_health_check.py:23 + :25 — `MagicMock` + `pytest` (NEW — surfaced during audit; planner did not deep-inspect, executor must verify)
  </read_first>

  <action>
    Per CLEAN-03 acceptance gate (REQUIREMENTS.md line 58): make `cd scheduler && uv run ruff check` exit 0 for F401.

    **Step 1 — Verify each F401 is a TRUE unused import** (not a false positive from re-export or type-only use):

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler
    grep -c "HAIKU_MODEL" agents/daily_summary.py    # expect 1 (just the import line)
    grep -nE "\bselect\(" agents/weekly_sweeper.py   # expect 0 (no select() callsite)
    grep -nE "_build_juno_world_events_section\(" scripts/uat_voice_calibration.py  # expect 0 (no callsite)
    grep -nE "\bMagicMock\b" tests/agents/test_juno_health_check.py  # expect 1 (just the import line)
    grep -nE "\bpytest\." tests/agents/test_juno_health_check.py    # expect 0 (no pytest.X usage); if >0, check for pytest fixture decorators
    ```

    If any grep shows the symbol IS used (e.g., `HAIKU_MODEL` is referenced 2+ times in daily_summary.py), the F401 is a false positive — do NOT delete the import. Instead add `# noqa: F401  # used by ...` with a justification and report the discrepancy in the task summary.

    Based on planner read_first confirmation: the 3 documented F401s (HAIKU_MODEL, select, _build_juno_world_events_section) are TRUE unused. The 3 in test_juno_health_check.py — executor must verify via the greps above.

    **Step 2 — Apply auto-fix:**

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run ruff check --fix
    ```

    Expected: all 6 F401s auto-fixed (ruff is conservative — only removes imports it's certain are unused; it will not break re-exports or type-only imports).

    **Step 3 — Manual confirmation for compound imports:**

    Three F401s are inside multi-symbol `from X import (A, B, C, D)` statements — auto-fix will surgically remove ONLY the unused symbol while preserving the others. Confirm post-auto-fix:

    - `scheduler/agents/daily_summary.py:53-58` — was `from agents.juno_relevance import (classify_story, survives_threshold, DefenceRelevance, HAIKU_MODEL,)`; post-fix expect 3 symbols (HAIKU_MODEL gone)
    - `scheduler/scripts/uat_voice_calibration.py:54-60` — was `from agents.daily_summary import (_build_juno_defence_news_section, _build_juno_world_events_section, JUNO_SONNET_MODEL, JUNO_SONNET_MAX_PROCUREMENT, JUNO_SONNET_TIMEOUT,)`; post-fix expect 4 symbols (_build_juno_world_events_section gone)

    The `from sqlalchemy import select` at weekly_sweeper.py:44 is a single-symbol import — auto-fix deletes the whole line. The `from unittest.mock import ...` line at test_juno_health_check.py:23 may share with `AsyncMock` or others — verify post-fix that only the unused symbols are removed.

    **Step 4 — Final verification:**

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run ruff check 2>&1 | tail -5
    ```

    Must exit 0 with output "All checks passed!".

    Then run the scheduler test suite to confirm no module-level import-failure regressions:

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q 2>&1 | tail -10
    ```

    Must exit 0 with >=363 tests passing (Phase 15 baseline per audit).

    **DO NOT:**
    - Add `# noqa: F401` to any import unless Step 1 grep proves the symbol IS actually used (for the 6 identified here, planner verification shows TRUE-unused, so noqa would mask real debt)
    - Loosen ruff rules in `pyproject.toml` to silence F401 (CLEAN-03 is about REMOVING the unused imports, not silencing the rule)
    - Touch any file outside the 4 listed (if `--fix` surfaces F401s in other files, file as follow-up — do NOT scope-creep)
    - Delete an entire `from X import (...)` block that has remaining used symbols — auto-fix handles surgical removal; manual deletion of compound imports is error-prone
    - Re-add the deleted imports "for future use" — YAGNI; if a future plan needs them, that plan adds them back
  </action>

  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run ruff check 2>&amp;1 | tail -5; echo "---RUFF-EXIT---"; cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest -q 2>&amp;1 | tail -10; echo "---PYTEST-EXIT---"; echo "---GREPS---"; grep -c "HAIKU_MODEL" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py; grep -cE "^from sqlalchemy import select$" /Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py; grep -c "_build_juno_world_events_section," /Users/matthewnelson/seva-mining/scheduler/scripts/uat_voice_calibration.py</automated>
  </verify>

  <acceptance_criteria>
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run ruff check` exits 0 with output "All checks passed!" (was: "Found 6 errors. [*] 6 fixable")
    - `grep -c "HAIKU_MODEL" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns 0 (the import line is gone; no other reference exists in the file)
    - `grep -cE "^from sqlalchemy import select$" /Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py` returns 0 (the single-line import is removed)
    - `grep -c "_build_juno_world_events_section," /Users/matthewnelson/seva-mining/scheduler/scripts/uat_voice_calibration.py` returns 0 (the compound-import entry is removed)
    - `grep -c "MagicMock" /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_juno_health_check.py` returns 0 OR equals 1 IF the file still has another `MagicMock` use that ruff didn't flag (executor reports either case)
    - `grep -c "# noqa: F401" /Users/matthewnelson/seva-mining/scheduler/ -r` returns 0 (no F401 suppressions added — every flagged import was a true-unused)
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q` exits 0 with >=363 tests passing (regression baseline preserved)
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run python -c "from agents.daily_summary import run_juno_daily_summary; from agents.weekly_sweeper import run_weekly_sweeper; print('OK')"` prints `OK` (smoke check — the deleted imports didn't break module-level loading)
    - `git diff --stat scheduler/` shows only the 4 listed files modified
  </acceptance_criteria>

  <done>
    Scheduler ruff exits 0 ("All checks passed!"). All 6 F401 errors resolved via auto-fix (no manual edits needed; ruff handles compound-import surgical removal). No noqa suppressions added. Scheduler test count >=363 GREEN. Module imports clean. CLEAN-03 acceptance gate satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: CLEAN-04 — Scheduler test RuntimeWarning fix (4 unawaited coroutines in test_daily_summary_prune.py)</name>
  <files>scheduler/tests/agents/test_daily_summary_prune.py</files>

  <read_first>
    - /Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over → fourth bullet — "scheduler tests: 4 RuntimeWarnings in test_daily_summary_prune.py AsyncMock (pre-Phase 12)")
    - /Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md (CLEAN-04 line 60 — acceptance gate: scheduler pytest output shows 0 RuntimeWarnings or matches documented pre-existing baseline)
    - /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py (full file — 293 lines; focus on the `_build_session_factory` helper at lines 62-128 and the inline `_factory` in `test_prune_writes_failure_telemetry_on_exception` at lines 247-259)
    - First, RUN the test to capture the exact warning messages and source lines:
      ```bash
      cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning 2>&1 | grep -A 5 "RuntimeWarning"
      ```
      The `-W error::RuntimeWarning` flag converts warnings to test failures, which forces pytest to emit a full traceback pointing to the line where the unawaited coroutine was created. Without this, the warnings render in a summary block at the end with no traceback.
  </read_first>

  <action>
    Per CLEAN-04 acceptance gate (REQUIREMENTS.md line 60): scheduler pytest output shows 0 `RuntimeWarning: coroutine was never awaited` from `test_daily_summary_prune.py`.

    **Step 1 — Diagnose the 4 warnings:**

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -v 2>&1 | head -60
    ```

    The most likely root cause based on planner read of the file: the `_build_session_factory` helper at lines 62-128 creates `AsyncMock()` instances eagerly (line 75-78, 84-86, 95, 100, 105) and assigns `.add = AsyncMock()` / `.commit = AsyncMock()` etc. When a test eventually awaits `sess.add(obj)`, the AsyncMock returns a coroutine. But the helper ALSO does `sessions_created.append(s)` inside `_make_session()` at line 78-80 (a closure that's NEVER called because the actual factory path at lines 108-128 uses a different `_factory()` closure) — the closure-created AsyncMock `s` at line 79 is created but never used, so any `await s.foo()` that gets registered as a side_effect produces an unawaited coroutine when the side_effect is invoked but the result is discarded.

    Alternative root cause: `sess3.get = AsyncMock(return_value=agent_run_obj)` at line 105 — if the production code under test calls `await session.get(AgentRun, agent_run_id)` more than once on the same session (a retry path), the second call returns a NEW coroutine that the production code may discard, triggering the warning.

    Read the actual warning tracebacks from Step 1's command output to confirm the exact source lines.

    **Step 2 — Fix the diagnosed warnings:**

    Apply ONE of these patterns based on diagnosis. The general principles:

    - **Pattern A — Replace orphaned `AsyncMock()` with `MagicMock()`** for any mock that's NEVER actually awaited in the test path. AsyncMock-by-default returns a coroutine on call; if the production code path under test doesn't await it, the coroutine is leaked. If a mock is only used for `.attribute` access (not `await mock()`), `MagicMock()` is the correct choice.

    - **Pattern B — Add `return_value=None` to AsyncMock for fire-and-forget paths** that the production code DOES await but where the return value is discarded. `AsyncMock(return_value=None)` is explicit and reduces ambiguity.

    - **Pattern C — Delete the orphaned `_make_session()` helper at lines 78-80** if it's truly dead code. Read the full helper carefully — the planner's quick scan suggests `_make_session()` is defined but never invoked anywhere in the file (the actual factory closure is `_factory()` at line 110). If confirmed dead code, delete lines 76-80:
      ```python
      sessions_created: list[AsyncMock] = []

      def _make_session():
          s = AsyncMock()
          sessions_created.append(s)
          return s
      ```
      This removes the orphan AsyncMock creation that may be the warning source.

    **Step 3 — Special case for `test_prune_writes_failure_telemetry_on_exception` (line 229-273):**

    This test has its OWN inline factory at lines 247-259 that mirrors the helper. If the helper has bugs, this inline version likely shares them. Apply the same fix pattern (delete dead code, replace AsyncMock with MagicMock where awaitless).

    **Step 4 — Verify no new test failures introduced:**

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -v 2>&1 | tail -20
    ```

    All 5 tests in the file must still pass (test_prune_deletes_rows_older_than_30_days_only, test_prune_idempotent_within_window, test_prune_writes_agent_runs_telemetry, test_prune_writes_failure_telemetry_on_exception, test_prune_function_is_callable_with_no_args).

    **Step 5 — Confirm 0 RuntimeWarnings:**

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py 2>&1 | grep -c "RuntimeWarning: coroutine was never awaited"
    ```

    Must return 0 (was: 4).

    **Step 6 — Confirm full scheduler suite still GREEN:**

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q 2>&1 | tail -10
    ```

    Must exit 0 with >=363 tests passing.

    **DO NOT:**
    - Add a `warnings.filterwarnings("ignore", category=RuntimeWarning)` at the top of the test file — that masks the bug, doesn't fix it (CLEAN-04 is about RESOLVING the warnings)
    - Change `AsyncMock` → `MagicMock` blindly on every mock — only on the mocks where the production code path doesn't await them; production code DOES await `session.commit()`, `session.execute()`, `session.refresh()`, `session.get()`, so those mocks MUST remain `AsyncMock`
    - Touch the production code in `scheduler/agents/daily_summary_prune.py` — the bug is in the test mocks, not the production code (per audit: "AsyncMock misconfig")
    - Change any assertion logic in the 5 tests — only the mock-factory infrastructure
    - Skip Step 1's diagnostic capture — without it, fixes are guesses; with it, fixes are targeted
  </action>

  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest tests/agents/test_daily_summary_prune.py 2>&amp;1 | tee /tmp/clean04_test.log | tail -20; echo "---RUNTIMEWARNING-COUNT---"; grep -c "RuntimeWarning: coroutine was never awaited" /tmp/clean04_test.log; echo "---FULL-SUITE---"; cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest -q 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py 2>&1 | grep -c "RuntimeWarning: coroutine was never awaited"` returns 0 (was: 4)
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -v` reports 5 passed (count unchanged from pre-edit; same 5 tests still GREEN)
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q` exits 0 with >=363 tests passing (full scheduler suite GREEN — no regressions from the test-helper refactor)
    - `grep -c "warnings.filterwarnings" /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py` returns 0 (no warning suppressions added — the warnings are RESOLVED, not silenced)
    - `git diff --stat scheduler/tests/agents/test_daily_summary_prune.py` shows only this 1 file modified
    - `git diff scheduler/agents/daily_summary_prune.py | wc -l` returns 0 (production code untouched — bug was in test mocks per audit)
  </acceptance_criteria>

  <done>
    `test_daily_summary_prune.py` produces 0 RuntimeWarnings (was: 4). All 5 tests still GREEN. Full scheduler suite >=363 tests pass. Fix applied to test-mock infrastructure only; production code byte-identical. CLEAN-04 acceptance gate satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: CLEAN-05 — Replace stale Phase-9-era empty-state copy in SummaryFeedPage.tsx:62-74</name>
  <files>frontend/src/pages/SummaryFeedPage.tsx</files>

  <read_first>
    - /Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_partial_flow + integration-checker Flow A step 4 PARTIAL — the source of this cleanup item)
    - /Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md (CLEAN-05 line 62 — acceptance gate: stale copy gone; default approach = rewrite tenant-aware; fallback = delete block entirely)
    - /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx (full file — 93 lines; focus on lines 62-83 — the empty-state branch with both the Juno-specific block at 62-74 and the shared Seva block at 75-83 that uses `nextCronFireLabelPT()`)
    - /Users/matthewnelson/seva-mining/frontend/src/pages/__tests__/SummaryFeedPage.test.tsx (the existing test file — confirm if any test ASSERTS on the literal string "Coming in Phase 10" or "Defence-sector ingestion not yet enabled"; if so, the test must be updated atomically with the production change to preserve GREEN state)
    - Search the codebase for any other reference to the stale copy:
      ```bash
      grep -rn "Coming in Phase 10" /Users/matthewnelson/seva-mining/frontend/src/ 2>/dev/null
      grep -rn "Defence-sector ingestion not yet enabled" /Users/matthewnelson/seva-mining/frontend/src/ 2>/dev/null
      ```
      If hits beyond `SummaryFeedPage.tsx`, address those atomically in the same task (e.g., a test fixture or snapshot).
  </read_first>

  <action>
    Per CLEAN-05 acceptance gate (REQUIREMENTS.md line 62): replace stale Phase-9-era empty-state copy at `frontend/src/pages/SummaryFeedPage.tsx:62-74`. REQUIREMENTS specifies "rewrite to tenant-aware empty-state copy OR delete the block entirely". Per planning guidance, **default approach: rewrite tenant-aware (more thoughtful UX)**.

    **Step 1 — Capture current state + test dependencies:**

    ```bash
    cd /Users/matthewnelson/seva-mining/frontend
    grep -n "Coming in Phase 10" src/pages/SummaryFeedPage.tsx src/pages/__tests__/SummaryFeedPage.test.tsx 2>&1
    grep -n "Defence-sector ingestion" src/pages/SummaryFeedPage.tsx src/pages/__tests__/SummaryFeedPage.test.tsx 2>&1
    ```

    Note any test reference — those need synchronized updates.

    **Step 2 — Apply the rewrite (DEFAULT approach):**

    Replace lines 62-74 of `frontend/src/pages/SummaryFeedPage.tsx`. CURRENT (delete this block):

    ```typescript
    if (companyId === 'juno') {
      // v3.0 Phase 9 — Juno News Funnel empty-state. Phase 10 (DEF-01..10)
      // populates real defence-sector content; until then this copy renders
      // when the cron writes zero rows or hasn't fired yet for Juno.
      return (
        <div className="max-w-[720px] mx-auto py-8 px-4">
          <p className="text-sm text-muted-foreground">
            Coming in Phase 10 — Defence-sector ingestion not yet enabled.
          </p>
        </div>
      )
    }
    ```

    NEW (replace with):

    ```typescript
    if (companyId === 'juno') {
      // v3.1 Phase 16 (CLEAN-05) — Juno daily cron is live (Phase 10 shipped 2026-05-19,
      // Phase 12 per-tenant key wiring shipped 2026-05-20). Crons fire at 08:05 PT and
      // 12:05 PT America/Los_Angeles. This branch renders when the cron has not yet
      // produced a row for the current 60-day window (e.g., first-deploy backfill gap,
      // or a stretch where every defence-news fire returned empty-after-classifier).
      return (
        <div className="max-w-[720px] mx-auto py-8 px-4">
          <p className="text-sm text-muted-foreground">
            No defence-industry briefs for this window yet — Juno's daily cron fires at 08:05 and 12:05 PT America/Los_Angeles.
          </p>
        </div>
      )
    }
    ```

    Notes on the rewrite:
    - Keeps the Juno-specific branch (preserves tenant-aware UX shape per default approach)
    - New copy is grammatically tenant-aware AND references real cron schedule (08:05/12:05 PT — verified ground truth from v3.1 audit Phase 12 operator action #5)
    - Removes the misleading "Coming in Phase 10" phrase (Phase 10 IS shipped)
    - Removes the "not yet enabled" phrase (the cron IS enabled — JUNO_CRON_ENABLED=true was flipped 2026-05-20 per CLAUDE.md / Phase 11 history)
    - Provenance comment cites Phase 16 CLEAN-05 for future audits

    **Step 3 — Fallback approach (use ONLY if Step 2's rewrite breaks more than 3 existing tests OR if the test file asserts on the EXACT string "Coming in Phase 10" in a way that can't be cleanly updated):**

    Delete lines 62-74 entirely (the whole `if (companyId === 'juno') { ... }` block). Result: the shared `nextFire = nextCronFireLabelPT()` branch at lines 75-83 handles BOTH tenants. Caveat: `nextCronFireLabelPT()` returns "08:00 PT" / "12:00 PT" hard-coded — that's Seva's cron schedule (08:00/12:00 PT), NOT Juno's (08:05/12:05 PT). For Juno the displayed time is 5 minutes early. Acceptable for an empty-state placeholder since the copy reads "Waiting for first summary. Next fire at {next_fire}." — the user understands "next fire" is approximate.

    Default to Step 2 (rewrite). Only use Step 3 (delete) if Step 2 introduces irreconcilable test friction.

    **Step 4 — Update tests if needed:**

    If `src/pages/__tests__/SummaryFeedPage.test.tsx` asserts on the OLD string ("Coming in Phase 10" or "Defence-sector ingestion not yet enabled"), update the assertion to match the NEW string atomically in this task. Do NOT split the test update into a separate task — that violates the atomic-code-and-test pattern from Phase 11.

    Match shape:
    ```typescript
    // BEFORE
    expect(screen.getByText(/Coming in Phase 10/)).toBeInTheDocument()

    // AFTER (rewrite approach)
    expect(screen.getByText(/No defence-industry briefs for this window yet/)).toBeInTheDocument()
    ```

    **Step 5 — Final verification:**

    ```bash
    cd /Users/matthewnelson/seva-mining/frontend
    grep -c "Coming in Phase 10" src/pages/SummaryFeedPage.tsx               # MUST be 0
    grep -c "Defence-sector ingestion not yet enabled" src/pages/SummaryFeedPage.tsx  # MUST be 0
    npm test --silent -- --run src/pages/__tests__/SummaryFeedPage.test.tsx  # MUST exit 0
    npm test --silent                                                          # MUST exit 0 with >=181 tests passing
    npx tsc --noEmit                                                           # MUST exit 0
    ```

    **DO NOT:**
    - Modify the file header docstring at lines 6-11 / 29-38 (those describe the page's v2.0 contract and are still accurate)
    - Modify the `nextCronFireLabelPT()` helper at lines 12-27 (out of scope; that function serves the SEVA branch correctly)
    - Touch the loading/error states at lines 44-58 (out of scope)
    - Touch the rendering block at lines 85-91 (out of scope)
    - Add a new prop to `<SummaryFeedPage />` (this page is rendered by react-router via `useParams`; no parent passes props)
    - Introduce a new dependency (no `companyBrandConfig` import needed — the rewrite uses a literal string, simpler than a registry lookup for a single empty-state line)
    - Comment out the old block instead of replacing it (the requirements file is the live spec; dead-code blocks rot)
    - Update REQUIREMENTS.md or ROADMAP.md from this task (those metadata flips happen at phase-verifier signoff, not per-task)
  </action>

  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; grep -c "Coming in Phase 10" src/pages/SummaryFeedPage.tsx; echo "---DEFENCE-PHRASE---"; grep -c "Defence-sector ingestion not yet enabled" src/pages/SummaryFeedPage.tsx; echo "---SPECIFIC-TEST---"; cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; npm test --silent -- --run src/pages/__tests__/SummaryFeedPage.test.tsx 2>&amp;1 | tail -15; echo "---FULL-TESTS---"; cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; npm test --silent 2>&amp;1 | tail -5; echo "---TYPECHECK---"; cd /Users/matthewnelson/seva-mining/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Coming in Phase 10" /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 0 (the stale phrase is gone)
    - `grep -c "Defence-sector ingestion not yet enabled" /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 0 (the stale phrase is gone)
    - `grep -rn "Coming in Phase 10" /Users/matthewnelson/seva-mining/frontend/src/` returns 0 (no other file references the stale phrase — including the test file post-update)
    - If Step 2 (rewrite) was taken: `grep -c "No defence-industry briefs for this window yet" /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 1 (the new copy is present); AND `grep -c "08:05 and 12:05 PT" /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 1 (cron schedule referenced); AND `grep -c "CLEAN-05" /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 1 (provenance comment present)
    - If Step 3 (delete) was taken: `grep -c "companyId === 'juno'" /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 0 (the Juno branch is fully removed)
    - `cd /Users/matthewnelson/seva-mining/frontend && npm test --silent -- --run src/pages/__tests__/SummaryFeedPage.test.tsx` exits 0 (the SummaryFeedPage test file passes)
    - `cd /Users/matthewnelson/seva-mining/frontend && npm test --silent` exits 0 with >=181 tests passing (full frontend suite GREEN)
    - `cd /Users/matthewnelson/seva-mining/frontend && npx tsc --noEmit` exits 0 (TypeScript clean)
    - `git diff --stat frontend/src/pages/SummaryFeedPage.tsx` shows the file modified; `git diff --stat frontend/src/pages/__tests__/SummaryFeedPage.test.tsx` shows it modified IF tests asserted on the old string (else byte-identical)
    - `wc -l /Users/matthewnelson/seva-mining/frontend/src/pages/SummaryFeedPage.tsx` returns 93 ±5 lines (rewrite approach) OR ~80 lines (delete approach)
  </acceptance_criteria>

  <done>
    `SummaryFeedPage.tsx:62-74` stale Phase-9-era copy replaced with tenant-aware Juno empty-state OR deleted entirely (per executor's discretion based on test-friction signal during Step 4). Stale strings GONE from the entire frontend tree. SummaryFeedPage test file passes; full frontend suite >=181 GREEN; TypeScript clean. CLEAN-05 acceptance gate satisfied; integration-checker Flow A step 4 PARTIAL finding from v3.1 audit is closed.
  </done>
</task>

</tasks>

<verification>
**Overall phase checks** (run AFTER all 5 tasks land — these are the orchestrator's phase-completion gates; mirrors Phase 11 verification block):

1. **Lint exit codes — all 3 tools GREEN:**
   - `cd /Users/matthewnelson/seva-mining/frontend && npm run lint` exits 0 (CLEAN-01 closed)
   - `cd /Users/matthewnelson/seva-mining/backend && uv run ruff check` exits 0 (CLEAN-02 closed)
   - `cd /Users/matthewnelson/seva-mining/scheduler && uv run ruff check` exits 0 (CLEAN-03 closed)

2. **RuntimeWarnings — 0 from test_daily_summary_prune.py:**
   - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py 2>&1 | grep -c "RuntimeWarning: coroutine was never awaited"` returns 0 (CLEAN-04 closed)

3. **Stale copy gone — CLEAN-05 closed:**
   - `grep -rn "Coming in Phase 10" /Users/matthewnelson/seva-mining/frontend/src/` returns 0
   - `grep -rn "Defence-sector ingestion not yet enabled" /Users/matthewnelson/seva-mining/frontend/src/` returns 0

4. **Regression baselines preserved — all 3 test suites GREEN:**
   - `cd /Users/matthewnelson/seva-mining/frontend && npm test --silent` exits 0 with >=181 tests passing
   - `cd /Users/matthewnelson/seva-mining/backend && uv run pytest -q` exits 0 with >=191 tests passing
   - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q` exits 0 with >=363 tests passing

5. **No CI grep gate regressions:**
   - `bash /Users/matthewnelson/seva-mining/scripts/verify-tenant-isolation.sh` exits 0 (Phase 9 scoped-helpers gate; the weekly_sweeper.py `select` import removal must not break this)
   - `bash /Users/matthewnelson/seva-mining/scripts/verify-anthropic-resolver.sh` exits 0 (Phase 12 resolver gate; the daily_summary.py HAIKU_MODEL removal must not break this)

6. **TypeScript clean:**
   - `cd /Users/matthewnelson/seva-mining/frontend && npx tsc --noEmit` exits 0

7. **No production-file scope creep:**
   - `git status --porcelain` shows ONLY the 15 files listed in this plan's `files_modified` frontmatter (no additional production files modified)
   - All edits are confined to the 5 audit-cleanup item scopes (CLEAN-01..05)
</verification>

<success_criteria>
**All 5 CLEAN requirements behaviorally satisfied:**

- **CLEAN-01** — Frontend lint exits 0 (was: 15 errors). 4 rule classes fixed via type-narrowing (no-explicit-any) + rule-config repair (react/display-name) + justified suppression (react-hooks/set-state-in-effect) + module reorganization or suppression (react-refresh/only-export-components).
- **CLEAN-02** — Backend ruff exits 0 (was: 17 errors). Auto-fix resolved 12; remainder addressed via targeted manual line wraps + UP017 migration. `datetime.utcnow()` NOT introduced; `datetime.now(UTC)` is the migration target.
- **CLEAN-03** — Scheduler ruff F401 exits 0 (was: 6 errors). All 6 unused imports auto-removed; no noqa suppressions added. Module imports clean; scheduler suite GREEN.
- **CLEAN-04** — Test_daily_summary_prune.py produces 0 RuntimeWarnings (was: 4). Fix applied to AsyncMock factory infrastructure; production code byte-identical; all 5 tests still GREEN.
- **CLEAN-05** — Stale "Coming in Phase 10" copy gone from `SummaryFeedPage.tsx:62-74`; replaced with tenant-aware Juno empty-state referencing real cron schedule (08:05/12:05 PT) OR deleted entirely if test-friction signal forced the fallback. Integration-checker Flow A step 4 PARTIAL finding closed.

**Net change matches Phase 11 mirror scope:**

- 14 files modified (5 frontend + 4 backend + 4 scheduler + 1 frontend production for CLEAN-05) — well-scoped, mostly auto-fix-driven
- 0 new files created (this is pure cleanup; no new tests or modules needed; the `perAgentQueueConstants.ts` may be created if Step 5(a) of Task 1 is taken — that's the only optional NEW file)
- Total LOC delta: ~50-100 lines net (mostly DELETIONS — unused imports + stale copy; rewrites are small)

**Zero-regression contract preserved (mirrors Phase 11 D-07 byte-identical contract):**

- Frontend test count: 181 (audit baseline) → >=181 (no regression; Task 1's test-file type-narrowing must keep all existing assertions GREEN)
- Backend test count: 191 (audit baseline) → >=191 (no regression; Task 2's UP017 migration must not break any datetime-handling test)
- Scheduler test count: 363 (audit baseline) → >=363 (no regression; Task 3's F401 removal + Task 4's AsyncMock fix must not break any test)
- CI grep gates (verify-tenant-isolation.sh + verify-anthropic-resolver.sh) both still exit 0

**v3.1 milestone archive unblocked:**

After this phase verifier signoff, v3.1 closes at 5/5 phases complete (Phase 12 + 13 + 14 + 15 + 16) with zero pre-existing tech debt carried into v3.2. Mirrors v3.0.1 closing pattern (v3.0 audit cleanup bundle = Phase 11, shipped 2026-05-19 as the v3.0.1 capstone).
</success_criteria>

<output>
After completion, create `/Users/matthewnelson/seva-mining/.planning/phases/16-v3.1-audit-cleanup-bundle/16-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The summary should explicitly note:

1. **All 3 lint-tool exit codes** (frontend npm run lint, backend ruff check, scheduler ruff check) — all 0
2. **RuntimeWarning count** from test_daily_summary_prune.py — 0 (was 4); fix pattern applied (delete dead `_make_session()` helper OR replace orphan AsyncMock with MagicMock OR add `return_value=None` to fire-and-forget AsyncMocks — note which pattern was used per the diagnostic)
3. **CLEAN-05 approach taken** — rewrite tenant-aware (default) OR delete (fallback); paste the new string verbatim
4. **Test pass counts** — frontend 181+ → final; backend 191+ → final; scheduler 363+ → final
5. **Files touched** — list each, with a 1-line summary of the change applied
6. **CI grep gate exit codes** — verify-tenant-isolation.sh and verify-anthropic-resolver.sh both 0
7. **Any unaddressed errors** — if a lint error surfaced in a file outside this plan's scope (e.g., a new F401 introduced in a file not listed), file as a follow-up Phase-16-02 candidate; do NOT silently extend scope
8. **v3.1 milestone archive readiness** — after verifier signoff, run `/gsd:complete-milestone v3.1` to close at 5/5 phases

Phase 16 is then ready for the phase-level verifier; after verifier PASS, update `.planning/REQUIREMENTS.md` Traceability table rows for CLEAN-01..05 from "Pending" → "Complete".
</output>
