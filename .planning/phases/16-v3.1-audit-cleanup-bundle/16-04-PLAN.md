---
id: 16-04
phase: 16-v3.1-audit-cleanup-bundle
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduler/tests/agents/test_daily_summary_prune.py
autonomous: true
requirements: [CLEAN-04]
requirements_addressed: [CLEAN-04]
task_count: 1
gap_closure: true
gap_source: .planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over)
nyquist_compliant: true

must_haves:
  truths:
    - "Test_daily_summary_prune.py produces 0 RuntimeWarnings (was: 4 `coroutine was never awaited`)"
    - "All 5 tests in test_daily_summary_prune.py still GREEN (count unchanged from pre-edit)"
    - "Scheduler regression baseline preserved: >=363 tests pass (was: 363 per .planning/v3.1-MILESTONE-AUDIT.md regression_baseline.scheduler)"
    - "Behavioral assertion: `cd scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -q` exits 0 (RuntimeWarnings now zero — promoted-to-error mode passes cleanly)"
    - "No `warnings.filterwarnings(\"ignore\")` suppressions added — the warnings are RESOLVED, not silenced"
  artifacts:
    - path: "scheduler/tests/agents/test_daily_summary_prune.py"
      provides: "AsyncMock factory + side_effect calls return non-awaited values correctly; 4 RuntimeWarnings resolved"
      contains: "MagicMock"
      must_not_contain: "warnings.filterwarnings"
  key_links:
    - from: "scheduler/tests/agents/test_daily_summary_prune.py::_build_session_factory"
      to: "scheduler/tests/agents/test_daily_summary_prune.py::_make_session (dead helper at lines 78-80)"
      via: "either deletion of dead helper OR replacement of AsyncMock with MagicMock where awaitless"
      pattern: "_make_session|AsyncMock\\(\\)"
---

<objective>
Close CLEAN-04 — scheduler test RuntimeWarning fix. The v3.1 milestone audit (`.planning/v3.1-MILESTONE-AUDIT.md` tech_debt.pre_existing_carried_over → fourth bullet) flags 4 `RuntimeWarning: coroutine was never awaited` in `scheduler/tests/agents/test_daily_summary_prune.py` from AsyncMock factory misconfig. Pre-Phase-12 debt. Acceptance gate (REQUIREMENTS.md line 60): scheduler pytest output shows 0 RuntimeWarnings (or matches documented pre-existing baseline).

Mirrors Phase 11's single-plan-per-cleanup-item shape. Touches a SINGLE test file (`test_daily_summary_prune.py`); production code at `scheduler/agents/daily_summary_prune.py` is NOT touched (the bug is in test mocks, not production per audit "AsyncMock misconfig").

**Coordination notes:**
- Plan 16-03 touches `scheduler/tests/agents/test_juno_health_check.py` (a DIFFERENT test file). File-disjoint from this plan.
- Plan 16-03 also touches `scheduler/agents/daily_summary.py` (removing HAIKU_MODEL import). The prune logic for the `run_daily_summary_prune` cron actually lives in `scheduler/agents/daily_summary_prune.py` (a SEPARATE module), so Plan 16-03's edit to `daily_summary.py` does NOT touch any prune-related production code. The two plans are file-disjoint and parallel-safe.

Purpose: 1 of 5 audit cleanup items closed; contributes to v3.1 milestone archive unblock at 5/5 phases.
Output: 1 scheduler test file with AsyncMock misconfig resolved; production code byte-identical; scheduler regression baseline (>=363 tests) preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matthewnelson/seva-mining/CLAUDE.md
@/Users/matthewnelson/seva-mining/.planning/ROADMAP.md
@/Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md
@/Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md

# Phase 11 mirror — same audit-cleanup-bundle shape (single-task plan per cleanup item)
@/Users/matthewnelson/seva-mining/.planning/phases/11-audit-cleanup-bundle/11-01-PLAN.md

# File to modify (read for full structure)
@/Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py

<audit_evidence>
Ground truth from running `cd scheduler && uv run pytest tests/agents/test_daily_summary_prune.py` 2026-05-20.

Scheduler RuntimeWarnings (`cd scheduler && uv run pytest tests/agents/test_daily_summary_prune.py 2>&1 | grep -c "RuntimeWarning: coroutine"`):

```
4   (from AsyncMock factory misconfig — likely in the `_build_session_factory` helper at test_daily_summary_prune.py:62-128
     where `_factory()` returns a MagicMock with __aenter__ / __aexit__ as AsyncMock — but the OUTER `sessions_created.append(s)` path
     also creates an AsyncMock that's never awaited. Inspect the 4 specific warning sources via `pytest -W error::RuntimeWarning` to
     pinpoint the exact lines.)
```

Production prune module lives at `scheduler/agents/daily_summary_prune.py` (separate from `daily_summary.py`). This plan touches ONLY the test file; production code stays byte-identical.
</audit_evidence>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CLEAN-04 — Fix 4 unawaited-coroutine RuntimeWarnings in test_daily_summary_prune.py</name>
  <files>scheduler/tests/agents/test_daily_summary_prune.py</files>

  <read_first>
    - /Users/matthewnelson/seva-mining/.planning/v3.1-MILESTONE-AUDIT.md (tech_debt.pre_existing_carried_over → fourth bullet — "scheduler tests: 4 RuntimeWarnings in test_daily_summary_prune.py AsyncMock (pre-Phase 12)")
    - /Users/matthewnelson/seva-mining/.planning/REQUIREMENTS.md (CLEAN-04 line 60 — acceptance gate: scheduler pytest output shows 0 RuntimeWarnings or matches documented pre-existing baseline)
    - /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py (full file — 293 lines; focus on the `_build_session_factory` helper at lines 62-128 and the inline `_factory` in `test_prune_writes_failure_telemetry_on_exception` at lines 247-259)
    - First, RUN the test to capture the exact warning messages and source lines:
      ```bash
      cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning 2>&1 | grep -A 5 "RuntimeWarning"
      ```
      The `-W error::RuntimeWarning` flag converts warnings to test failures, which forces pytest to emit a full traceback pointing to the line where the unawaited coroutine was created. Without this, the warnings render in a summary block at the end with no traceback.
  </read_first>

  <action>
    Per CLEAN-04 acceptance gate (REQUIREMENTS.md line 60): scheduler pytest output shows 0 `RuntimeWarning: coroutine was never awaited` from `test_daily_summary_prune.py`.

    Step 1 — Diagnose the 4 warnings:

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -v 2>&1 | head -60
    ```

    The most likely root cause based on planner read of the file: the `_build_session_factory` helper at lines 62-128 creates `AsyncMock()` instances eagerly (line 75-78, 84-86, 95, 100, 105) and assigns `.add = AsyncMock()` / `.commit = AsyncMock()` etc. When a test eventually awaits `sess.add(obj)`, the AsyncMock returns a coroutine. But the helper ALSO does `sessions_created.append(s)` inside `_make_session()` at line 78-80 (a closure that's NEVER called because the actual factory path at lines 108-128 uses a different `_factory()` closure) — the closure-created AsyncMock `s` at line 79 is created but never used, so any `await s.foo()` that gets registered as a side_effect produces an unawaited coroutine when the side_effect is invoked but the result is discarded.

    Alternative root cause: `sess3.get = AsyncMock(return_value=agent_run_obj)` at line 105 — if the production code under test calls `await session.get(AgentRun, agent_run_id)` more than once on the same session (a retry path), the second call returns a NEW coroutine that the production code may discard, triggering the warning.

    Read the actual warning tracebacks from Step 1's command output to confirm the exact source lines.

    Step 2 — Fix the diagnosed warnings:

    Apply ONE of these patterns based on diagnosis. The general principles:

    - Pattern A — Replace orphaned `AsyncMock()` with `MagicMock()` for any mock that's NEVER actually awaited in the test path. AsyncMock-by-default returns a coroutine on call; if the production code path under test doesn't await it, the coroutine is leaked. If a mock is only used for `.attribute` access (not `await mock()`), `MagicMock()` is the correct choice.

    - Pattern B — Add `return_value=None` to AsyncMock for fire-and-forget paths that the production code DOES await but where the return value is discarded. `AsyncMock(return_value=None)` is explicit and reduces ambiguity.

    - Pattern C — Delete the orphaned `_make_session()` helper at lines 78-80 if it's truly dead code. Read the full helper carefully — the planner's quick scan suggests `_make_session()` is defined but never invoked anywhere in the file (the actual factory closure is `_factory()` at line 110). If confirmed dead code, delete lines 76-80:
      ```python
      sessions_created: list[AsyncMock] = []

      def _make_session():
          s = AsyncMock()
          sessions_created.append(s)
          return s
      ```
      This removes the orphan AsyncMock creation that may be the warning source.

    Step 3 — Special case for `test_prune_writes_failure_telemetry_on_exception` (line 229-273):

    This test has its OWN inline factory at lines 247-259 that mirrors the helper. If the helper has bugs, this inline version likely shares them. Apply the same fix pattern (delete dead code, replace AsyncMock with MagicMock where awaitless).

    Step 4 — Verify no new test failures introduced:

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -v 2>&1 | tail -20
    ```

    All 5 tests in the file must still pass (test_prune_deletes_rows_older_than_30_days_only, test_prune_idempotent_within_window, test_prune_writes_agent_runs_telemetry, test_prune_writes_failure_telemetry_on_exception, test_prune_function_is_callable_with_no_args).

    Step 5 — Confirm 0 RuntimeWarnings (behavioral assertion — the AUTHORITATIVE drift-tolerant gate for this plan):

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -q
    ```

    Must exit 0. With `-W error::RuntimeWarning`, any remaining unawaited-coroutine warning is promoted to a test failure. Exit 0 = zero RuntimeWarnings (was: 4).

    Step 6 — Confirm full scheduler suite still GREEN:

    ```bash
    cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q 2>&1 | tail -10
    ```

    Must exit 0 with >=363 tests passing.

    DO NOT:
    - Add a `warnings.filterwarnings("ignore", category=RuntimeWarning)` at the top of the test file — that masks the bug, doesn't fix it (CLEAN-04 is about RESOLVING the warnings)
    - Change `AsyncMock` → `MagicMock` blindly on every mock — only on the mocks where the production code path doesn't await them; production code DOES await `session.commit()`, `session.execute()`, `session.refresh()`, `session.get()`, so those mocks MUST remain `AsyncMock`
    - Touch the production code in `scheduler/agents/daily_summary_prune.py` — the bug is in the test mocks, not the production code (per audit: "AsyncMock misconfig"). Production code stays byte-identical.
    - Touch `scheduler/agents/daily_summary.py` — that file is owned by Plan 16-03 (HAIKU_MODEL removal) and contains no prune logic; the prune cron lives in `daily_summary_prune.py` (separate module)
    - Change any assertion logic in the 5 tests — only the mock-factory infrastructure
    - Skip Step 1's diagnostic capture — without it, fixes are guesses; with it, fixes are targeted
  </action>

  <verify>
    <automated>echo "---PROMOTED-TO-ERROR RUN (authoritative gate)---"; cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -q 2>&amp;1 | tail -10; echo "---NORMAL RUN (count check)---"; cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest tests/agents/test_daily_summary_prune.py 2>&amp;1 | tee /tmp/clean04_test.log | tail -10; echo "---RUNTIMEWARNING-COUNT (informational, expect 0)---"; grep -c "RuntimeWarning: coroutine was never awaited" /tmp/clean04_test.log; echo "---FULL-SUITE---"; cd /Users/matthewnelson/seva-mining/scheduler &amp;&amp; uv run pytest -q 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - **Authoritative behavioral gate (drift-tolerant):** `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -q` exits 0. With `-W error::RuntimeWarning`, any remaining unawaited-coroutine warning fails the run. Exit 0 = zero RuntimeWarnings (was: 4). This is the single source of truth — drift-tolerant because it directly tests the behavior (warning-free) rather than counting strings.
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py 2>&1 | grep -c "RuntimeWarning: coroutine was never awaited"` returns 0 (informational confirmation of the behavioral gate; was: 4)
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -v` reports 5 passed (count unchanged from pre-edit; same 5 tests still GREEN)
    - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q` exits 0 with >=363 tests passing (D-10 regression baseline preserved per v3.1 audit)
    - `grep -c "warnings.filterwarnings" /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py` returns 0 (no warning suppressions added — the warnings are RESOLVED, not silenced)
    - `git diff --stat scheduler/tests/agents/test_daily_summary_prune.py` shows only this 1 file modified
    - `git status --porcelain scheduler/agents/daily_summary_prune.py` shows EMPTY (production prune module byte-identical — bug was in test mocks per audit). Note: `scheduler/agents/daily_summary.py` is touched by Plan 16-03 (HAIKU_MODEL removal); that file contains NO prune logic so the 16-03 edit does not affect this plan's contract.
  </acceptance_criteria>

  <done>
    `test_daily_summary_prune.py` exits 0 under `-W error::RuntimeWarning` (was: 4 unawaited-coroutine warnings). All 5 tests still GREEN. Full scheduler suite >=363 tests pass. Fix applied to test-mock infrastructure only; production prune module (`scheduler/agents/daily_summary_prune.py`) byte-identical. CLEAN-04 acceptance gate satisfied.
  </done>
</task>

</tasks>

<verification>
1. **Behavioral assertion (authoritative drift-tolerant gate):**
   - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -W error::RuntimeWarning -q` exits 0 (CLEAN-04 closed — promoted-to-error mode now passes cleanly; was: 4 failures)

2. **Test count preserved:**
   - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary_prune.py -v` reports 5 passed (same count as pre-edit)

3. **D-10 regression baseline preserved:**
   - `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -q` exits 0 with >=363 tests passing (audit baseline per `.planning/v3.1-MILESTONE-AUDIT.md` regression_baseline.scheduler = 363)

4. **No production-code contamination:**
   - `git status --porcelain scheduler/agents/daily_summary_prune.py` shows EMPTY (production prune module untouched)
   - `git diff --stat scheduler/tests/agents/test_daily_summary_prune.py` shows only this 1 test file modified

5. **No warning suppression added:**
   - `grep -c "warnings.filterwarnings" /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_daily_summary_prune.py` returns 0
</verification>

<success_criteria>
- **CLEAN-04** — `test_daily_summary_prune.py` produces 0 RuntimeWarnings (was: 4). Behavioral assertion: pytest with `-W error::RuntimeWarning` exits 0. Fix applied to AsyncMock factory infrastructure; production prune code (`scheduler/agents/daily_summary_prune.py`) byte-identical; all 5 tests still GREEN.
- **D-10 regression baseline preserved:** Scheduler test count: 363 (audit baseline) → >=363 (no regression; AsyncMock fix must not break any test).
- File-disjoint from plans 16-01/02/03/05 → safe to land in parallel within Wave 1.
</success_criteria>

<output>
After completion, create `/Users/matthewnelson/seva-mining/.planning/phases/16-v3.1-audit-cleanup-bundle/16-04-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The summary should explicitly note:

1. **Behavioral gate result** — `pytest -W error::RuntimeWarning` exits 0 (was: 4 warnings = 4 failures under promoted-to-error mode)
2. **RuntimeWarning count** — 0 (was: 4); informational confirmation
3. **Fix pattern applied** — which of Pattern A (MagicMock substitution) / Pattern B (return_value=None) / Pattern C (dead `_make_session()` helper deletion) — note which one and at which lines
4. **Test pass count for the file** — 5 passed (unchanged from pre-edit)
5. **Scheduler suite total** — 363+ → final
6. **Production code byte-identical** — confirm `scheduler/agents/daily_summary_prune.py` not touched

After verifier signoff, update `.planning/REQUIREMENTS.md` Traceability table row for CLEAN-04 from "Pending" → "Complete".
</output>
</content>
</invoke>