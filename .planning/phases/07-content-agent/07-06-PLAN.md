---
phase: 07-content-agent
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0005_rename_format_type_to_content_type.py
  - backend/app/models/content_bundle.py
  - backend/app/schemas/content_bundle.py
  - scheduler/models/content_bundle.py
  - scheduler/agents/content_agent.py
  - backend/tests/test_crud_endpoints.py
  - frontend/src/api/types.ts
  - frontend/src/pages/ContentPage.tsx
  - frontend/src/pages/ContentPage.test.tsx
  - frontend/src/mocks/handlers.ts
autonomous: true
requirements: [CONT-09]
must_haves:
  truths:
    - "The content_bundles table column is named content_type, not format_type"
    - "All Python models, Pydantic schemas, TypeScript types, and agent code reference content_type"
    - "Alembic migration 0005 renames the column cleanly with upgrade and downgrade"
  artifacts:
    - path: "backend/alembic/versions/0005_rename_format_type_to_content_type.py"
      provides: "Alembic migration renaming format_type to content_type"
      contains: "alter_column"
    - path: "backend/app/models/content_bundle.py"
      provides: "Backend model with content_type column"
      contains: "content_type"
    - path: "scheduler/models/content_bundle.py"
      provides: "Scheduler mirror model with content_type column"
      contains: "content_type"
  key_links:
    - from: "backend/alembic/versions/0005_rename_format_type_to_content_type.py"
      to: "content_bundles table"
      via: "op.alter_column rename"
      pattern: "new_column_name.*content_type"
    - from: "scheduler/agents/content_agent.py"
      to: "scheduler/models/content_bundle.py"
      via: "ContentBundle.content_type assignment"
      pattern: "content_type="
---

<objective>
Rename the `format_type` column to `content_type` on the content_bundles table via Alembic migration, and update every reference across the entire codebase (backend model, scheduler mirror, Pydantic schema, TypeScript types, frontend components, test files, mock handlers).

Purpose: The expanded 7-format content type system uses `content_type` as the canonical field name per CONTEXT.md decisions. This rename must complete before any new format code is added, as all expansion plans reference `content_type`.

Output: Alembic migration 0005, updated models/schemas/types/components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-agent/07-CONTEXT.md
@.planning/phases/07-content-agent/07-RESEARCH.md

<interfaces>
From backend/app/models/content_bundle.py:
```python
class ContentBundle(Base):
    __tablename__ = "content_bundles"
    format_type = Column(String(50))  # THIS IS THE COLUMN TO RENAME
```

From backend/app/schemas/content_bundle.py:
```python
class ContentBundleResponse(BaseModel):
    format_type: Optional[str] = None  # RENAME TO content_type
```

From scheduler/models/content_bundle.py:
```python
class ContentBundle(Base):
    format_type = Column(String(50))  # RENAME TO content_type
```

From scheduler/agents/content_agent.py line 732:
```python
format_type=draft_content.get("format"),  # RENAME TO content_type=
```

From frontend/src/api/types.ts line 42:
```typescript
format_type?: string  // RENAME TO content_type
```

From frontend/src/pages/ContentPage.tsx lines 14-221:
```typescript
// Multiple references to bundle.format_type — all need renaming
switch (bundle.format_type) { ... }
if (bundle.format_type === 'thread') { ... }
```

From frontend/src/pages/ContentPage.test.tsx lines 54-81:
```typescript
format_type: 'thread',  // all test fixtures need renaming
format_type: 'long_form',
format_type: 'infographic',
```

From frontend/src/mocks/handlers.ts line 199:
```typescript
format_type: 'thread',  // mock data needs renaming
```

From backend/tests/test_crud_endpoints.py line 99:
```python
format_type = Col(String(50))  # test model mirror needs renaming
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration + backend model + schema rename</name>
  <files>
    backend/alembic/versions/0005_rename_format_type_to_content_type.py
    backend/app/models/content_bundle.py
    backend/app/schemas/content_bundle.py
    backend/tests/test_crud_endpoints.py
  </files>
  <read_first>
    backend/alembic/versions/0004_add_engagement_alert_columns.py
    backend/app/models/content_bundle.py
    backend/app/schemas/content_bundle.py
    backend/tests/test_crud_endpoints.py
  </read_first>
  <action>
1. Create Alembic migration `backend/alembic/versions/0005_rename_format_type_to_content_type.py`:
   - `revision = "0005"`, `down_revision = "0004"`
   - `upgrade()`: `op.alter_column("content_bundles", "format_type", new_column_name="content_type")`
   - `downgrade()`: `op.alter_column("content_bundles", "content_type", new_column_name="format_type")`
   - Follow the exact pattern from `0004_add_engagement_alert_columns.py` for imports and structure

2. Update `backend/app/models/content_bundle.py`:
   - Rename `format_type = Column(String(50))` to `content_type = Column(String(50))`
   - Update comment to: `# thread, long_form, infographic, breaking_news, video_clip, quote, gold_history`

3. Update `backend/app/schemas/content_bundle.py`:
   - Rename `format_type: Optional[str] = None` to `content_type: Optional[str] = None`

4. Update `backend/tests/test_crud_endpoints.py`:
   - Find the test model mirror class and rename `format_type` to `content_type`
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining && grep -r "content_type" backend/app/models/content_bundle.py backend/app/schemas/content_bundle.py backend/alembic/versions/0005_rename_format_type_to_content_type.py && ! grep "format_type" backend/app/models/content_bundle.py && ! grep "format_type" backend/app/schemas/content_bundle.py</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/alembic/versions/0005_rename_format_type_to_content_type.py` exists
    - Migration upgrade calls `op.alter_column("content_bundles", "format_type", new_column_name="content_type")`
    - Migration downgrade calls `op.alter_column("content_bundles", "content_type", new_column_name="format_type")`
    - `backend/app/models/content_bundle.py` contains `content_type = Column(String(50))` and does NOT contain `format_type`
    - `backend/app/schemas/content_bundle.py` contains `content_type: Optional[str]` and does NOT contain `format_type`
    - `backend/tests/test_crud_endpoints.py` contains `content_type` and does NOT contain `format_type`
  </acceptance_criteria>
  <done>Alembic migration 0005 created. Backend model, schema, and test mirror all use content_type.</done>
</task>

<task type="auto">
  <name>Task 2: Scheduler model + agent code + frontend rename</name>
  <files>
    scheduler/models/content_bundle.py
    scheduler/agents/content_agent.py
    frontend/src/api/types.ts
    frontend/src/pages/ContentPage.tsx
    frontend/src/pages/ContentPage.test.tsx
    frontend/src/mocks/handlers.ts
  </files>
  <read_first>
    scheduler/models/content_bundle.py
    scheduler/agents/content_agent.py
    frontend/src/api/types.ts
    frontend/src/pages/ContentPage.tsx
    frontend/src/pages/ContentPage.test.tsx
    frontend/src/mocks/handlers.ts
  </read_first>
  <action>
1. Update `scheduler/models/content_bundle.py`:
   - Rename `format_type = Column(String(50))` to `content_type = Column(String(50))`
   - Update comment to: `# thread, long_form, infographic, breaking_news, video_clip, quote, gold_history`

2. Update `scheduler/agents/content_agent.py`:
   - Line ~732: change `format_type=draft_content.get("format")` to `content_type=draft_content.get("format")`
   - Search for any other `format_type` references and rename to `content_type`

3. Update `frontend/src/api/types.ts`:
   - Line 42: rename `format_type?: string` to `content_type?: string`

4. Update `frontend/src/pages/ContentPage.tsx`:
   - All occurrences of `bundle.format_type` become `bundle.content_type`
   - All occurrences of `format_type` in comments become `content_type`
   - Exact locations: lines 14, 18, 75, 89, 94, 220, 221

5. Update `frontend/src/pages/ContentPage.test.tsx`:
   - All test fixture `format_type:` keys become `content_type:`
   - Lines 54, 70, 81

6. Update `frontend/src/mocks/handlers.ts`:
   - Line 199: `format_type: 'thread'` becomes `content_type: 'thread'`
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining && grep -c "content_type" scheduler/models/content_bundle.py scheduler/agents/content_agent.py frontend/src/api/types.ts frontend/src/pages/ContentPage.tsx && ! grep "format_type" scheduler/models/content_bundle.py && ! grep "format_type" scheduler/agents/content_agent.py && ! grep "format_type" frontend/src/api/types.ts && ! grep "format_type" frontend/src/pages/ContentPage.tsx && ! grep "format_type" frontend/src/pages/ContentPage.test.tsx && ! grep "format_type" frontend/src/mocks/handlers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scheduler/models/content_bundle.py` contains `content_type = Column(String(50))` and does NOT contain `format_type`
    - `scheduler/agents/content_agent.py` contains `content_type=draft_content.get("format")` and does NOT contain `format_type`
    - `frontend/src/api/types.ts` contains `content_type?: string` and does NOT contain `format_type`
    - `frontend/src/pages/ContentPage.tsx` contains `bundle.content_type` and does NOT contain `bundle.format_type`
    - `frontend/src/pages/ContentPage.test.tsx` contains `content_type:` and does NOT contain `format_type:`
    - `frontend/src/mocks/handlers.ts` contains `content_type:` and does NOT contain `format_type:`
  </acceptance_criteria>
  <done>Every format_type reference in the codebase is renamed to content_type. Migration + model + schema + agent + frontend are all consistent.</done>
</task>

</tasks>

<verification>
1. `grep -r "format_type" backend/app/ scheduler/ frontend/src/ --include="*.py" --include="*.ts" --include="*.tsx"` returns ZERO results (ignoring alembic 0001 which is historical)
2. `grep -r "content_type" backend/app/models/content_bundle.py scheduler/models/content_bundle.py scheduler/agents/content_agent.py frontend/src/api/types.ts` returns hits in all 4 files
3. Alembic migration 0005 exists and has both upgrade and downgrade functions
</verification>

<success_criteria>
- Zero remaining `format_type` references in active code (models, schemas, agents, frontend)
- Alembic migration 0005 can rename the column in both directions
- All 4 content_type references (backend model, scheduler model, agent, frontend types) are consistent
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-agent/07-06-SUMMARY.md`
</output>
