---
id: 09-05
phase: 09-multi-tenant-foundation
plan: 05
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - backend/tests/test_multitenant_isolation.py
  - .planning/PROJECT.md
autonomous: false
requirements_addressed:
  - TENANT-10
task_count: 4
gap_closure: false
nyquist_compliant: true

must_haves:
  truths:
    - "backend/tests/test_multitenant_isolation.py — fully populated parametrized cross-tenant leak detection: 3 endpoints (summaries, calendar, weekly-sweeps) × 2 tenants (seva, juno) × assert ONLY-own-rows; PLUS test_invalid_company_returns_404; PLUS test_unprefixed_legacy_url_returns_404 (e.g. GET /summaries 404 since the prefix is now /api/{company}/summaries)"
    - "Full test suite GREEN across all 3 layers: backend (`cd backend && uv run pytest -x`), scheduler (`cd scheduler && uv run pytest -x`), frontend (`cd frontend && npm test -- --run`)"
    - "CI grep gate (`bash scripts/verify-tenant-isolation.sh`) exits 0 with no temporary whitelists"
    - "Human-verify checkpoint at 1440×900 in DevTools — operator runs through the 30+ item UI-SPEC QA checklist reproduced verbatim below; ALL items must be checked PASS before the phase is marked complete; any FAIL items become gap-closure plans"
    - "Smoke-test human-confirmation checkpoint (Iteration 2 — Blocker 3 path a): operator runs the REPL heredoc and confirms exactly TWO new daily_summaries rows (one seva-completed, one juno-partial) + TWO new agent_runs rows + idempotency re-check produces ZERO new rows — verified visually from the REPL output before the human-verify QA checkpoint runs"
    - ".planning/PROJECT.md Key Decisions table updated with a v3.0 Phase 9 entry documenting the AppHeader freeze-lift per D-02 (third documentation location per CONTEXT.md D-02 requirements)"
    - "Phase-level 09-SUMMARY.md (created at plan close) MUST contain a Decisions section referencing the AppHeader freeze-lift (first of three D-02 documentation locations — verified by grep gate in plan-level verification)"
  artifacts:
    - path: "backend/tests/test_multitenant_isolation.py"
      provides: "Populated parametrized cross-tenant isolation tests"
      contains: "@pytest.mark.parametrize"
      min_lines: 100
    - path: ".planning/PROJECT.md"
      provides: "AppHeader freeze-lift documented in Key Decisions table"
      contains: "v3.0 freeze-lift"
  key_links:
    - from: "backend/tests/test_multitenant_isolation.py"
      to: "/api/{seva,juno}/{summaries,calendar,weekly-sweeps} endpoints"
      via: "authed_client.get + parametrized assertion that response.json() contains only the queried tenant's rows"
      pattern: "@pytest.mark.parametrize\\\\(.company.\\\\s*,\\\\s*\\\\[.seva.,\\\\s*.juno.\\\\]"
    - from: "Manual scheduler fire (08:05 PT or REPL)"
      to: "daily_summaries.company_id='juno' row + agent_runs row"
      via: "_make_juno_daily_summary_job(engine)().func() under advisory lock 1020"
      pattern: "company_id=.juno."
---

<objective>
Wave 4 closes Phase 9 with four complementary verifications: (1) populate the cross-tenant isolation test suite (TENANT-10 final deliverable — Wave 0 scaffold becomes fully-populated parametrized tests), (2) run the live-DB smoke test + update PROJECT.md, (3) human-confirm the smoke-test REPL output (Iteration 2 — Blocker 3 fix; replaces the prior auto-task path-a checkpoint), (4) human-verify QA checklist at 1440×900 covering the 30+ item UI-SPEC contract. The smoke-test gate (Task 3) is separated from the visual QA gate (Task 4) so the human verifies REPL DB rows BEFORE walking the visual checklist — cleanest signal at the smallest unit.

Purpose: Phase 9's atomic deploy is verified end-to-end before merge. Partial multi-tenancy is worse than none — Wave 4 is the gate that prevents shipping anything less than the full TENANT-01..10 contract.
Output: 1 fully-populated test file + 1 PROJECT.md edit + 1 smoke-test execution task + 1 smoke-test human-confirm checkpoint + 1 visual-QA human-verify checkpoint with the verbatim QA checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenant-foundation/09-CONTEXT.md
@.planning/phases/09-multi-tenant-foundation/09-UI-SPEC.md
@.planning/phases/09-multi-tenant-foundation/09-VALIDATION.md
@.planning/phases/09-multi-tenant-foundation/09-02-SUMMARY.md
@.planning/phases/09-multi-tenant-foundation/09-03-SUMMARY.md
@.planning/phases/09-multi-tenant-foundation/09-04-SUMMARY.md
@.planning/PROJECT.md
@CLAUDE.md

<interfaces>
<!-- The complete UI-SPEC QA Checklist (verbatim from 09-UI-SPEC.md §Manual Visual QA Checklist lines 354-425). Reproduced here so the checkpoint task has it inline — operator works through this list at 1440×900. -->

The QA checklist is reproduced verbatim in Task 4's `<how-to-verify>` block below. Do not paraphrase — copy exactly so visual verification is unambiguous.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate test_multitenant_isolation.py + run full 3-layer test suite</name>
  <files>backend/tests/test_multitenant_isolation.py</files>
  <read_first>
    - .planning/phases/09-multi-tenant-foundation/09-RESEARCH.md §Code Example 10 (full template starting at line 1201) — this becomes the populated production-quality test
    - .planning/phases/09-multi-tenant-foundation/09-VALIDATION.md §Per-Task Verification Map TENANT-10 (line 82) — defines the parametrized matrix
    - backend/tests/test_multitenant_isolation.py (Wave 0 scaffold — read entirely; it should currently have the parametrized skeleton with `pytest.skip` at top OR with individual test `pytest.skip` decorators — Task 1 removes those + adds full assertions)
    - backend/tests/conftest.py (existing — confirm `authed_client` fixture and `async_db_session` fixture names + signatures; the populated test uses them)
    - backend/app/models/{daily_summary,calendar_item,weekly_sweep}.py (post-Wave 1 — confirm column names so seed-rows are correct)
    - .planning/research/PITFALLS.md MEDIUM-6 — Test fixtures don't reset company_id context between tests (motivation for explicit per-test seeding rather than module-scoped ContextVars)
  </read_first>
  <behavior>
    - **test_multitenant_isolation.py** becomes a fully-populated parametrized test module:
      * `both_tenant_rows` async fixture — function-scoped — seeds exactly 2 rows per multi-tenant table (one Seva, one Juno) so the 6 endpoint×tenant combinations can each assert "only my tenant's rows came back". See RESEARCH §Code Example 10 fixture body.
      * `@pytest.mark.parametrize("company", ["seva", "juno"])` × `@pytest.mark.parametrize("endpoint", [...])` produces 6 (company, endpoint) tuples. Each tuple's test asserts:
        1. GET `endpoint.format(company=company)` returns 200.
        2. Response body's row list contains rows ONLY where `row["company_id"] == company` (the Pydantic response models gained `company_id: str | None` in Wave 2 per the 09-03 task; verify the field is in the response).
        3. The row count from the API matches the seeded fixture count for that tenant (1 row per tenant per table in the fixture).
      * `test_invalid_company_returns_404` — `GET /api/bogus/summaries` returns 404 with `{"detail": "Unknown company: bogus"}`.
      * `test_invalid_company_uppercase_returns_422` — `GET /api/SEVA/summaries` returns 422 (regex pattern enforced by FastAPI Path validator BEFORE the dep body runs).
      * `test_unprefixed_legacy_url_returns_404` — `GET /summaries` returns 404 (post Wave 2, the unprefixed route does NOT exist; this confirms there's no accidental dual-registration).
      * `test_create_calendar_item_persists_company_id` — POST `/api/seva/calendar` body `{"date":"2026-01-01","body":"hello"}` returns 201; the new row in DB has `company_id='seva'` (not from the request body, but injected by the get_current_company dep + scoped_calendar).
      * `test_patch_calendar_item_cannot_cross_tenant` — seed a Juno calendar_item with known UUID; PATCH `/api/seva/calendar/{juno-uuid}` returns 404 (scoped_calendar('seva') WHERE id=juno-uuid returns no rows → handler returns 404 before write).
    - Module-level `pytest.skip(...)` line (added in Wave 0) MUST be removed.
    - Use `@pytest_asyncio.fixture` (asyncio_mode=auto from existing conftest handles the test functions; fixtures need the decorator).
    - **PITFALL guard**: each test creates fresh seed rows via `both_tenant_rows` fixture (function-scoped); no module-level state; no ContextVar pollution between tests.
  </behavior>
  <action>
    1. Read `backend/tests/test_multitenant_isolation.py` (Wave 0 scaffold).

    2. Replace with a fully-populated version following RESEARCH §Code Example 10 verbatim for the `both_tenant_rows` fixture + 6-tuple parametrized matrix. Add the 4 additional standalone tests per the behavior block above:
       - `test_invalid_company_returns_404`
       - `test_invalid_company_uppercase_returns_422`
       - `test_unprefixed_legacy_url_returns_404`
       - `test_create_calendar_item_persists_company_id`
       - `test_patch_calendar_item_cannot_cross_tenant`

    3. Remove any module-level or per-test `pytest.skip(...)` from Wave 0.

    4. Run the full 3-layer test suite to confirm no regressions:
       ```bash
       cd /Users/matthewnelson/seva-mining/backend && uv run pytest -x
       cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest -x
       cd /Users/matthewnelson/seva-mining/frontend && npm test -- --run
       bash /Users/matthewnelson/seva-mining/scripts/verify-tenant-isolation.sh
       ```
       Any FAILED test or non-zero exit halts the plan; investigate + fix before proceeding.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/backend && uv run pytest tests/test_multitenant_isolation.py -v 2>&1 | tail -20 && cd /Users/matthewnelson/seva-mining && bash scripts/verify-tenant-isolation.sh && cd backend && uv run pytest -x 2>&1 | tail -3 && cd ../scheduler && uv run pytest -x 2>&1 | tail -3 && cd ../frontend && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `test_multitenant_isolation.py` shows the full parametrized matrix passing: 6 (company × endpoint) combinations + 5 standalone tests = 11 PASSED, 0 FAILED, 0 SKIPPED.
    - `bash scripts/verify-tenant-isolation.sh` exits 0.
    - Backend full suite (156+ tests) PASSES.
    - Scheduler full suite (264+ tests) PASSES.
    - Frontend full suite (141+ tests) PASSES.
    - No regressions: pre-existing v2.x tests all GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run smoke-test heredoc + update PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (existing — read the Key Decisions table section; the new entry goes alphabetically by phase OR at the bottom matching existing append convention)
    - .planning/phases/09-multi-tenant-foundation/09-CONTEXT.md §D-02 (third documentation location requirement for AppHeader freeze-lift)
    - .planning/phases/09-multi-tenant-foundation/09-03-SUMMARY.md (Wave 2 output — confirm the smoke-test pattern worked there; it should already have verified one cron fire produces both rows. Wave 4 re-confirms.)
    - scheduler/worker.py (post-Wave 2 — confirm `_make_juno_daily_summary_job(engine)` factory + `JOB_LOCK_IDS["juno_daily_summary"]=1020` are in place)
    - scheduler/agents/daily_summary.py (post-Wave 2 — confirm `run_juno_daily_summary` is exported)
  </read_first>
  <behavior>
    - **Smoke verification** runs as a REPL heredoc against the live dev DB. This task EXECUTES the heredoc and saves the stdout to a temp file (`/tmp/seva-09-05-smoke.log`) so the human-verify checkpoint in Task 3 has unambiguous output to reference. **Confirmation that the smoke test produced the expected rows is handed to Task 3 (`checkpoint:smoke-test`) — this Task 2 does NOT assert on row counts directly; it merely runs the heredoc and persists the log.**
    - **PROJECT.md update**: add an entry to the Key Decisions table (or equivalent log section — read PROJECT.md first to confirm exact section name) documenting:
      * Date: 2026-05-19
      * Decision: "AppHeader.tsx Phase 5 byte-freeze formally lifted in v3.0 (Phase 9). 5-line surgical insert of `<CompanySwitcher />` between brand-mark `<div>` and Logout `<button>`. Brand mark + wordmark stay 'Seva Mining' on both tenants per D-02a; per-company branding deferred to v3.1+ per TENANT-BRAND-v31."
      * Rationale: per 09-CONTEXT.md D-02 (3 documentation locations required: a=SUMMARY.md done in 09-05-SUMMARY.md, b=inline code comment done in 09-04 Task 2, c=THIS PROJECT.md update).
      * Status: Locked
  </behavior>
  <action>
    1. **Smoke-test execution** — open a scheduler REPL and tee stdout to a log file for the next checkpoint:
       ```bash
       cd /Users/matthewnelson/seva-mining/scheduler && uv run python << 'EOF' 2>&1 | tee /tmp/seva-09-05-smoke.log
       import asyncio
       from database import engine, AsyncSessionLocal
       from agents.daily_summary import run_daily_summary, run_juno_daily_summary
       from sqlalchemy import text

       async def smoke():
           # Fire both entry points back-to-back (the real APScheduler fires
           # them 5 min apart — this is a smoke test, not a race condition test).
           await run_daily_summary()  # Seva
           await run_juno_daily_summary()  # Juno
           # Idempotency re-check — should be a no-op
           await run_juno_daily_summary()

           # Query results
           async with AsyncSessionLocal() as session:
               result = await session.execute(text("""
                   SELECT company_id, status, agent_run_id
                   FROM daily_summaries
                   WHERE generated_at > NOW() - INTERVAL '5 minutes'
                   ORDER BY company_id
               """))
               print("=== daily_summaries (last 5 min) ===")
               for row in result:
                   print(f"daily_summaries: company={row.company_id} status={row.status} agent_run_id={row.agent_run_id}")

               result = await session.execute(text("""
                   SELECT agent_name, status, notes
                   FROM agent_runs
                   WHERE started_at > NOW() - INTERVAL '5 minutes'
                   ORDER BY agent_name
               """))
               print("=== agent_runs (last 5 min) ===")
               for row in result:
                   print(f"agent_runs: agent={row.agent_name} status={row.status} notes={row.notes[:80] if row.notes else None}")

       asyncio.run(smoke())
       EOF
       ```
       The expected output (will be inspected in Task 3) is exactly 2 `daily_summaries` rows (seva + juno, juno=partial), 2 `agent_runs` rows (`daily_summary` + `juno_daily_summary`, both completed), AND no idempotency duplicate (the third call returns early).

    2. **Cleanup smoke-test rows** is DEFERRED to AFTER Task 3 human-verify (so the operator can see the rows for themselves):
       ```sql
       -- Run only AFTER Task 3 approval:
       DELETE FROM daily_summaries WHERE generated_at > NOW() - INTERVAL '10 minutes';
       DELETE FROM agent_runs WHERE started_at > NOW() - INTERVAL '10 minutes' AND agent_name IN ('daily_summary', 'juno_daily_summary');
       ```

    3. **Edit .planning/PROJECT.md** — first Read the file to locate the Key Decisions section. Append a new row to the table (or follow the existing decision-log style — match what's there):
       ```markdown
       | 2026-05-19 | v3.0 Phase 9 — AppHeader freeze formally lifted | 09-CONTEXT.md D-02 | Locked | Surgical 5-line `<CompanySwitcher />` insert between brand-mark and Logout. Brand mark + wordmark stay "Seva Mining" both tenants per D-02a; per-tenant branding → v3.1+ (TENANT-BRAND-v31). Third documentation location per D-02 (a: 09-SUMMARY.md; b: inline comment in AppHeader.tsx; c: this entry). |
       ```
       Adjust column count/order to match the existing PROJECT.md table — read first.
  </action>
  <verify>
    <automated>test -f /tmp/seva-09-05-smoke.log && wc -l /tmp/seva-09-05-smoke.log && cd /Users/matthewnelson/seva-mining/scheduler && uv run python -c "from worker import JOB_LOCK_IDS; assert JOB_LOCK_IDS.get('juno_daily_summary') == 1020; print('JOB_LOCK_IDS OK')" && grep -c "v3.0 freeze-lift\\|AppHeader freeze\\|freeze formally lifted" /Users/matthewnelson/seva-mining/.planning/PROJECT.md</automated>
  </verify>
  <done>
    - `/tmp/seva-09-05-smoke.log` exists and has > 5 lines (the smoke test ran and produced inspectable output for the next checkpoint).
    - `.planning/PROJECT.md` contains the new Key Decisions entry mentioning "v3.0 freeze-lift" or "AppHeader freeze formally lifted" (grep returns >=1 match).
    - **(Iteration 2 — Blocker 3 path a) Row-count assertion is intentionally NOT in this task's verify gate** — that confirmation is the dedicated Task 3 checkpoint where the human reads `/tmp/seva-09-05-smoke.log` and confirms the row counts visually.
  </done>
</task>

<task type="checkpoint:smoke-test" gate="blocking">
  <name>Task 3: Human-confirm smoke-test produced the expected DB rows (Iteration 2 — Blocker 3 fix)</name>
  <what-built>
    Task 2 fired `run_daily_summary()` + `run_juno_daily_summary()` + a second `run_juno_daily_summary()` (idempotency re-check) against the live dev DB, then queried the last 5 minutes of `daily_summaries` and `agent_runs`. The stdout is captured at `/tmp/seva-09-05-smoke.log`. The operator now confirms the row counts match contract before the visual-QA gate in Task 4 runs.
  </what-built>
  <how-to-verify>
    **Step 1 — Read the smoke-test log:**
    ```bash
    cat /tmp/seva-09-05-smoke.log
    ```

    **Step 2 — Confirm each of the 4 row-count contracts:**

    - [ ] **daily_summaries section** shows exactly TWO rows in the last 5 minutes:
      - One row with `company=seva` and `status` in `{completed, partial}` (Seva may degrade to partial if Sonnet had a flaky moment; both are acceptable)
      - One row with `company=juno` and `status=partial` (Juno is ALWAYS partial in Phase 9 — Phase 10 content not yet shipped)
      - Each row has a distinct `agent_run_id` UUID

    - [ ] **agent_runs section** shows exactly TWO rows in the last 5 minutes:
      - One row with `agent=daily_summary` and `status=completed`
      - One row with `agent=juno_daily_summary` and `status=completed` (the agent_run completes successfully even though the daily_summary row is `partial` — the agent_run tracks orchestration success, not content completeness; per RESEARCH §Code Example 9 design)
      - Both rows show notes excerpts (first 80 chars)

    - [ ] **Idempotency proof — only 2 daily_summaries rows total, NOT 3:**
      - Task 2 called `run_juno_daily_summary()` TWICE. If the second call had created a duplicate row, the daily_summaries section would show 3 rows (1 seva + 2 juno).
      - Confirm there is exactly 1 juno row, not 2.

    - [ ] **Juno notes contain `company_id=juno` marker:**
      - The `juno_daily_summary` agent_runs row's notes excerpt should contain `"company_id"` and `"juno"` substrings (verify visually — JSON encoding may show as `\"company_id\": \"juno\"` or similar)

    **Step 3 — If any of the 4 contracts FAIL:**
    - Document which contract failed and what was seen instead in 09-05-SUMMARY.md.
    - Investigate the failure (likely candidates: idempotency window math, lazy-import error, OPS-02 lock contention, missing scheduler.queries.scoped import).
    - Do NOT proceed to Task 4. Either fix and re-run Task 2, OR spawn a gap-closure plan via `/gsd:plan-phase 09 --gaps`.

    **Step 4 — Cleanup (only after PASS):**
    ```sql
    psql $DATABASE_URL -c "
      DELETE FROM daily_summaries WHERE generated_at > NOW() - INTERVAL '10 minutes';
      DELETE FROM agent_runs WHERE started_at > NOW() - INTERVAL '10 minutes' AND agent_name IN ('daily_summary', 'juno_daily_summary');
    "
    ```
    (Optional — leave the rows in place if you want to inspect them during Task 4's visual QA at `/juno/`.)
  </how-to-verify>
  <resume-signal>Type "smoke-approved" if all 4 row-count contracts PASS. Type "smoke-fail: {contract}" for each failed contract. Type "abandon" to halt Phase 9.</resume-signal>
  <files>(no file edits — REPL output verification only; failures spawn gap-closure plans)</files>
  <action>The operator (human, NOT Claude) opens `/tmp/seva-09-05-smoke.log`, walks through the 4 row-count contracts above, marks each PASS or FAIL. On any FAIL, the operator does NOT proceed to Task 4; instead documents the failure and either re-runs Task 2 or spawns gap-closure via `/gsd:plan-phase 09 --gaps`.</action>
  <verify>Operator types "smoke-approved" to confirm all 4 smoke-test row-count contracts PASS; OR types "smoke-fail: {contract}" per failed contract.</verify>
  <done>Operator approval received (resume-signal answered with "smoke-approved"); 2 daily_summaries rows + 2 agent_runs rows confirmed in the smoke log; idempotency invariant (no 3rd juno row) confirmed; Juno agent_runs notes contain company_id marker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual QA at 1440×900 — UI-SPEC QA Checklist (30+ items)</name>
  <what-built>
    Phase 9 is functionally complete: backend serves `/api/{company}/...`, scheduler writes per-company rows (confirmed by Task 3 smoke-test), frontend routes `/seva/*` byte-equivalent + `/juno/*` empty-states, AppHeader has CompanySwitcher between brand and Logout. The operator now confirms visual + interaction quality at the target resolution (1440×900) before the phase merges.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Frontend running locally: `cd frontend && npm run dev` → open `http://localhost:5173` in Chrome.
    2. Backend + scheduler running locally OR pointed at Railway preview.
    3. Open Chrome DevTools → Device Toolbar → set viewport to **1440 × 900** (Responsive, exact dimensions).
    4. Disable cache in DevTools Network tab to ensure clean state.
    5. Log in as the single operator (auth gate behavior unchanged from v2.1).
    6. Have these URLs ready for testing: `/`, `/calendar`, `/viral`, `/queue`, `/agents/breaking_news`, `/seva/`, `/seva/calendar`, `/seva/viral`, `/juno/`, `/juno/calendar`, `/juno/viral`, `/digest`, `/settings`.

    **Run the verbatim UI-SPEC QA Checklist (from 09-UI-SPEC.md §Manual Visual QA Checklist). Check each box PASS or FAIL.**

    ### AppHeader (verify on every authenticated page — visit /seva/, /seva/calendar, /seva/viral, /juno/, /juno/calendar, /juno/viral, /digest, /settings)

    - [ ] Brand-mark square renders `bg-amber-500` 28×28 with "S" centered, weight 700, color `text-zinc-900` (frozen baseline preserved)
    - [ ] Wordmark renders `Seva Mining` at `text-sm font-semibold text-white` (frozen baseline)
    - [ ] CompanySwitcher renders BETWEEN brand-mark group and Logout button (NOT before brand, NOT after Logout)
    - [ ] CompanySwitcher renders TWO buttons side-by-side, labels `Seva` and `Juno`, in that order
    - [ ] AppHeader bottom border is `border-zinc-800` (frozen baseline)
    - [ ] AppHeader stays sticky at `top-0` on scroll (frozen baseline preserved)
    - [ ] No horizontal overflow at 1440px — switcher + wordmark + logout all fit within `max-w-[720px]` inner container
    - [ ] Wordmark stays `Seva Mining` even when viewing `/juno/*` (D-02a)

    ### CompanySwitcher visual states

    - [ ] On `/seva/*`: "Seva" button has `border-brand-accent` (amber-500 visual) + `text-brand-accent` text + `bg-brand-accent-subtle` (5% amber tint); "Juno" button has `border-zinc-800 text-zinc-400` no background
    - [ ] On `/juno/*`: "Juno" button has the active styling; "Seva" button has the inactive styling
    - [ ] Hover over inactive button: border transitions smoothly to `zinc-700`, text to `zinc-100`. No layout jitter. Transition feels ~150ms (Tailwind default).
    - [ ] Hover over active button: NO change. Button stays static.
    - [ ] Click inactive button: URL changes to `/${otherCompany}${currentSubPath}` (e.g. on `/seva/calendar`, clicking "Juno" lands on `/juno/calendar`)
    - [ ] After click: active-state visual SWAPS without any flash of stale data (TanStack cache cleared per D-08)
    - [ ] Tab-focus reaches the switcher buttons (keyboard navigation): focus ring visible in amber-500 with 1px zinc-900 offset
    - [ ] Pixel-stable: switching active/inactive does NOT cause neighbouring elements (wordmark, Logout) to shift horizontally

    ### Tab preservation on switch

    - [ ] Switch `/seva/` → "Juno": lands on `/juno/` (News Funnel empty-state)
    - [ ] Switch `/seva/calendar` → "Juno": lands on `/juno/calendar` (Calendar empty-state)
    - [ ] Switch `/seva/viral` → "Juno": lands on `/juno/viral` (Sweeper empty-state)
    - [ ] Switch `/juno/calendar` → "Seva": lands on `/seva/calendar` (Seva's real calendar)
    - [ ] TabNav active tab remains correct after switch — switching while on Calendar keeps Calendar tab highlighted

    ### Juno empty-states

    - [ ] `/juno/` (News Funnel): renders single `<p>` with copy `Coming in Phase 10 — Defence-sector ingestion not yet enabled.` in `text-sm text-muted-foreground`, wrapped in `max-w-[720px] mx-auto py-8 px-4` (assumes Wave 2 cron has NOT yet fired or its rows have been cleaned; if a status='partial' Juno row exists, you'll see that card instead — both are correct outcomes for this checklist item, but pick which is being verified)
    - [ ] `/juno/calendar`: renders single `<p>` with copy `Coming in v3.1 — Juno Calendar not yet enabled.` — same visual treatment. WeekNav + WeeklyGrid do NOT render (short-circuit before mount)
    - [ ] `/juno/viral`: renders single `<p>` with copy `Coming in v3.1 — Juno Sweeper not yet enabled.` — same visual treatment. SweeperCard + week-picker do NOT render
    - [ ] No flash of stale Seva data when switching Seva → Juno (cache cleared per D-08)
    - [ ] No console errors when rendering Juno empty-states (no failed API call attempts on `/juno/calendar` or `/juno/viral` — they short-circuit before the hook fires)
    - [ ] Empty-state text aligns LEFT (inherits from wrapper); matches the Seva empty-state precedent

    ### Cross-tenant Seva preservation (TENANT-01 byte-equivalence)

    - [ ] `/seva/` News Funnel: renders byte-equivalent to v2.1 — same SummaryCard layout, same Gold News / Ontario Law / Ontario Stats sections, same hover transitions, same Phase 8 X-handle pills if any handles present
    - [ ] `/seva/calendar`: weekly grid renders byte-equivalent — today-cell ring, prev/Today/next nav, click-to-focus textarea, blur-to-save behavior all preserved
    - [ ] `/seva/viral`: sweep card + week-picker render byte-equivalent — Phase 8 X-handle pills + monospace numerics + partial/failed banners all intact
    - [ ] TabNav active-tab indicator: amber-500 border-b-2 on the active tab; matches Phase 8 baseline exactly
    - [ ] No stray blue / green / purple accents anywhere — only zinc + amber (semantic + frozen literals) + red-for-destructive

    ### Cross-tab + browser navigation

    - [ ] Browser Back from `/juno/calendar` to `/seva/viral` (recent history): switcher active state updates correctly to "Seva"; TabNav active tab updates to "Weekly Viral"
    - [ ] Browser Forward back to `/juno/calendar`: switcher updates to "Juno" active; TabNav updates to Calendar active
    - [ ] Hard reload on `/juno/`: page renders correctly (no client-only state required for switcher to know it's on Juno)
    - [ ] Direct URL paste of `/juno/viral` (deep link): renders empty-state correctly; switcher reflects Juno active

    ### Bookmark grace redirects (TENANT-06)

    - [ ] Navigate to `/` (bare root) → URL changes to `/seva/`
    - [ ] Navigate to `/calendar` → URL changes to `/seva/calendar`
    - [ ] Navigate to `/viral` → URL changes to `/seva/viral`
    - [ ] Navigate to `/queue` (legacy v2.0) → URL changes to `/seva/`
    - [ ] Navigate to `/agents/breaking_news` (legacy v2.0) → URL changes to `/seva/`
    - [ ] Navigate to `/digest` → URL stays at `/digest` (NOT tenant-scoped per D-06)
    - [ ] Navigate to `/settings` → URL stays at `/settings` (NOT tenant-scoped)

    ### Accessibility spot-check

    - [ ] Tab order through AppHeader is: Seva button → Juno button → Logout button (brand-mark not focusable)
    - [ ] Both switcher buttons receive visible amber focus ring on keyboard tab-focus
    - [ ] DevTools color contrast inspector on active switcher button text: ≥ 4.5:1 (AA Normal ✓)
    - [ ] DevTools color contrast inspector on inactive switcher button text: ≥ 4.5:1 (AA Normal ✓)
    - [ ] No console warnings about missing keys or ARIA violations on switcher

    ### Performance / cache behavior

    - [ ] Switching tenants: visible cache clear happens BEFORE the new page renders (no flash of Seva content on a Juno page)
    - [ ] DevTools Network tab on switch: refetch fires for the new tenant's endpoints (`/api/juno/summaries` etc.) — confirms cache was cleared
    - [ ] No infinite refetch loop after switch

    ### Persistence (Zustand)

    - [ ] After visiting `/juno/`, DevTools Application → Local Storage → http://localhost:5173 shows key `seva-mining-app-state-v3` with value `{"state":{"lastVisitedCompany":"juno"},"version":0}` (or similar — exact JSON shape may vary, but `lastVisitedCompany` field MUST be present and equal `"juno"`)
    - [ ] After visiting `/seva/`, the same localStorage value updates to `"seva"`
    - [ ] Bare `/` redirect still goes to `/seva/` (NOT last-visited per D-05 — this is intentional v3.0 behavior; v3.1+ will switch to last-visited)

    ### Production-equivalence smoke (already confirmed by Task 3 — re-check optional)

    - [ ] Task 3 smoke-approval already confirmed: one manual scheduler fire produces both a Seva and Juno daily_summaries row. Confirm here by visiting `/juno/` and seeing either the status='partial' card (if rows preserved) OR the Phase 10 empty-state (if Task 3 cleanup ran).
    - [ ] Querying `SELECT company_id, count(*) FROM daily_summaries GROUP BY company_id` (optional) shows 2 distinct company_id values: 'seva' (>=N existing rows) and 'juno' (>=0 rows depending on Task 3 cleanup choice).

    **TOTAL: 50+ checklist items. Every box must show PASS before the operator approves.**

    **If ANY item is FAIL:**
    - Document the FAIL items in 09-05-SUMMARY.md.
    - Plan a Wave 5 gap-closure plan via `/gsd:plan-phase 09 --gaps` if 3+ items fail OR if any item is critical.
    - Single non-critical failures may be tracked as v3.0.1 follow-up tasks.
  </how-to-verify>
  <resume-signal>Type "approved" if all items PASS. Type "fail: {item description}" for each FAIL item. Type "abandon" to halt Phase 9 and revisit.</resume-signal>
  <files>(no file edits — visual verification only; failures spawn gap-closure plans)</files>
  <action>The operator (human, NOT Claude) opens Chrome at http://localhost:5173, sets DevTools viewport to 1440x900, and walks through every checklist item in the &lt;how-to-verify&gt; block above. For each item, the operator marks PASS or FAIL. If any item is FAIL, the operator reports the specific item back; this plan then spawns a Wave 5 gap-closure plan via `/gsd:plan-phase 09 --gaps`.</action>
  <verify>Operator types "approved" to confirm all 50+ checklist items PASS; OR types "fail: {item}" per failed item.</verify>
  <done>Operator approval received (resume-signal answered with "approved"); all 50+ QA items confirmed PASS at 1440x900; any FAIL items captured for gap-closure planning.</done>
</task>

</tasks>

<verification>
  Plan-level acceptance gate:
  - Task 1 verification: full 3-layer test suite PASSES + CI grep gate exits 0.
  - Task 2 verification: smoke-test heredoc ran and persisted output to `/tmp/seva-09-05-smoke.log` (>5 lines) + PROJECT.md updated with the freeze-lift decision.
  - Task 3 verification: operator types "smoke-approved" — confirming the 4 row-count contracts (2 daily_summaries rows + 2 agent_runs rows + idempotency invariant + Juno notes marker) all PASS at the DB level.
  - Task 4 verification: operator types "approved" — confirming all 50+ visual QA checklist items PASS at 1440×900.
  - **(Iteration 2 — Warning 3 fix) Phase-level 09-SUMMARY.md (written at plan close per the &lt;output&gt; block) MUST contain the AppHeader freeze-lift in its Decisions section.** Enforce via grep gate:
    ```bash
    grep -c "freeze-lift\\|AppHeader freeze" /Users/matthewnelson/seva-mining/.planning/phases/09-multi-tenant-foundation/09-SUMMARY.md
    ```
    Must return ≥ 1. If 0, the third D-02 documentation location (a = phase-level SUMMARY) is missing and the plan FAILS.
  - Phase exit criteria from ROADMAP §Phase 9 success criteria 1-5 all satisfied:
    1. `/seva/*` byte-equivalent to v2.1 ✓ (Task 4 visual QA)
    2. `/juno/*` empty-states on all 3 tabs ✓ (Task 4 visual QA)
    3. One cron fire produces both Seva and Juno daily_summaries rows ✓ (Task 3 smoke-approved)
    4. CI grep gate passes ✓ (Task 1)
    5. Cross-tenant isolation test suite GREEN ✓ (Task 1)
</verification>

<success_criteria>
  - All 10 TENANT-* requirements verified in code AND tests AND human eyeball:
    * TENANT-01..03: backend test_migration_0014 + test_queries_scoped + CI grep gate
    * TENANT-04: test_multitenant_isolation parametrized matrix
    * TENANT-05: App.test.tsx :company route test + visual QA browser back/forward items
    * TENANT-06: App.test.tsx grace-redirect tests + visual QA bookmark grace items
    * TENANT-07: CompanySwitcher.test.tsx tests + visual QA all CompanySwitcher visual-state items
    * TENANT-08: scheduler test_worker.py + test_juno_daily_summary.py + Task 3 smoke-test approval
    * TENANT-09: queryKeys.test.ts + visual QA cache-behavior items
    * TENANT-10: test_multitenant_isolation.py populated + GREEN
  - The atomic-deploy contract is met: nothing partial ships.
  - PROJECT.md Key Decisions table has the v3.0 freeze-lift entry (third of three documentation locations per D-02).
  - Phase-level 09-SUMMARY.md Decisions section has the AppHeader freeze-lift entry (first of three documentation locations per D-02; gated by the new grep verification in the plan-level acceptance block).
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenant-foundation/09-05-SUMMARY.md` AND the phase-level `.planning/phases/09-multi-tenant-foundation/09-SUMMARY.md`. The phase-level SUMMARY.md MUST include a "Decisions" section documenting the AppHeader freeze-lift (first of three documentation locations per D-02) — this is enforced by the plan-level grep verification gate. Both summaries should contain:

- 09-05-SUMMARY.md (plan-level):
  * Files touched (2 production-source + the /tmp smoke log)
  * Smoke-test output excerpt (companies × statuses produced)
  * Task 3 checkpoint result: "smoke-approved" with all 4 row-count contracts PASS, OR list of FAIL contracts + remediation plan
  * Task 4 human-verify checkpoint result: "approved" with all 50+ items PASS, OR list of FAIL items + remediation plan
  * Final test counts across all 3 layers (backend N → N+M passing; scheduler N → N+M; frontend N → N+M)

- 09-SUMMARY.md (phase-level):
  * Phase outcome: GREEN/RED/ABANDONED
  * All 10 TENANT-* requirements with their evidence (test file paths, line numbers)
  * **Decisions section** documenting D-01..D-08 outcomes (especially D-02 freeze-lift with rationale and the 3 documentation locations cited — this section is what the plan-level grep gate checks for "freeze-lift" / "AppHeader freeze" substrings)
  * Wave-by-wave plan summaries (one paragraph each)
  * Lessons learned / pitfalls hit
  * Handoff to Phase 10: "Phase 10 fills scheduler/companies/juno/{feeds,prompts,serpapi}.py with real defence-industry content. The infrastructure shipped in Phase 9 (per-company cron, /api/juno/* routes, queryKeys factory, CompanySwitcher) is byte-stable; Phase 10 is config-only."
</output>
</output>
