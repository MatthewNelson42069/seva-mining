---
id: 09-01
phase: 09-multi-tenant-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/verify-tenant-isolation.sh
  - backend/tests/test_migration_0014.py
  - backend/tests/test_queries_scoped.py
  - backend/tests/test_multitenant_isolation.py
  - backend/tests/test_model_parity.py
  - scheduler/tests/test_worker.py
  - scheduler/tests/agents/test_juno_daily_summary.py
  - frontend/src/api/queryKeys.test.ts
  - frontend/src/stores/__tests__/companySlice.test.ts
  - frontend/src/components/layout/__tests__/CompanyScopedRoute.test.tsx
  - frontend/src/components/layout/__tests__/CompanySwitcher.test.tsx
  - frontend/src/__tests__/App.test.tsx
autonomous: true
requirements_addressed:
  - TENANT-01
  - TENANT-02
  - TENANT-03
  - TENANT-04
  - TENANT-05
  - TENANT-06
  - TENANT-07
  - TENANT-08
  - TENANT-09
  - TENANT-10
task_count: 3
gap_closure: false
nyquist_compliant: true

must_haves:
  truths:
    - "All 12 Wave 0 test files exist and FAIL (RED phase of TDD) before any Wave 1 production code is written"
    - "scripts/verify-tenant-isolation.sh exits 0 today (no raw selects exist yet) and is wired to fail when raw select(DailySummary|CalendarItem|WeeklySweep) appears outside backend/app/queries/scoped.py"
    - "Every TENANT-* requirement (01..10) has at least one Wave 0 test that asserts the behavior; tests reference the expected import path / function shape so executors know what to build"
    - "(Iteration 2 — Warning 1 fix) Skip idiom is consistent: all NEW backend pytest Wave 0 RED files use module-level `pytest.skip(..., allow_module_level=True)`; the SINGLE exception is the extension to existing test_model_parity.py which uses per-function `pytest.skip` since module-level would skip the existing CalendarItem + WeeklySweep parity tests that are already GREEN"
  artifacts:
    - path: "scripts/verify-tenant-isolation.sh"
      provides: "CI grep gate enforcing scoped helper contract"
      contains: "select\\\\((DailySummary|CalendarItem|WeeklySweep)"
      min_lines: 30
    - path: "backend/tests/test_migration_0014.py"
      provides: "Migration column/constraint/index assertions"
      min_lines: 60
    - path: "backend/tests/test_queries_scoped.py"
      provides: "Compiled-SQL assertions for scoped_summaries/calendar/weekly_sweeps"
      min_lines: 40
    - path: "backend/tests/test_multitenant_isolation.py"
      provides: "Parametrized cross-tenant leak detection (3 endpoints x 2 tenants)"
      min_lines: 80
    - path: "scheduler/tests/agents/test_juno_daily_summary.py"
      provides: "Asserts run_juno_daily_summary writes company_id=juno status=partial row"
      min_lines: 30
    - path: "frontend/src/api/queryKeys.test.ts"
      provides: "Asserts queryKeys.summaries('seva', 60) returns ['summaries','seva',60] as const"
      min_lines: 25
    - path: "frontend/src/components/layout/__tests__/CompanySwitcher.test.tsx"
      provides: "Asserts click triggers queryClient.clear() + navigate(/${next}${subPath})"
      min_lines: 50
  key_links:
    - from: "scripts/verify-tenant-isolation.sh"
      to: "backend/app + scheduler/agents"
      via: "grep -rnE 'select\\\\((DailySummary|CalendarItem|WeeklySweep)[\\\\)\\\\.]'"
      pattern: "select\\\\((DailySummary|CalendarItem|WeeklySweep)"
    - from: "backend/tests/test_multitenant_isolation.py"
      to: "/api/{company}/{summaries,calendar,weekly-sweeps}"
      via: "authed_client GET requests parametrized over ('seva','juno')"
      pattern: "@pytest.mark.parametrize\\\\(\"company\""
    - from: "frontend/src/components/layout/__tests__/CompanySwitcher.test.tsx"
      to: "@tanstack/react-query queryClient.clear + react-router-dom navigate"
      via: "vi.fn() spies on useQueryClient + useNavigate"
      pattern: "queryClient\\\\.clear|navigate\\\\("
---

<objective>
Create all 12 Wave 0 test scaffolding files (RED phase of TDD) plus the CI grep gate (scripts/verify-tenant-isolation.sh) before any production code is written for Phase 9. These tests document the contract every later wave must satisfy. Mirror Phase 8 Wave 0 pattern (`pytest.skip()` BEFORE lazy imports so tests collect as `skipped`, not `error`, when target modules don't exist yet).

**(Iteration 2 — Warning 1 fix) Skip idiom STANDARDIZED:** All NEW backend pytest Wave 0 RED files use **module-level skip** with `pytest.skip("...", allow_module_level=True)` at the top of the module (after the `import pytest` line). This makes Wave 1/2 removal a single-line delete per file. The SINGLE exception is the extension to the existing `test_model_parity.py` (which has pre-existing GREEN tests for CalendarItem + WeeklySweep parity — a module-level skip would falsely skip those). For that file ONLY, the new `test_daily_summary_parity` function starts with a per-function `pytest.skip(..., allow_module_level=False)`.

Purpose: Lock the TENANT-01..10 contract in code, not prose. The plan-level acceptance is "every test file exists, imports work, tests run (skipped or red), CI grep gate exits 0 cleanly."
Output: 12 NEW test files + 1 NEW shell script. Zero production code edits in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenant-foundation/09-CONTEXT.md
@.planning/phases/09-multi-tenant-foundation/09-VALIDATION.md
@.planning/phases/09-multi-tenant-foundation/09-RESEARCH.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Key contracts these tests assert. Executors should COPY THESE SHAPES VERBATIM into the test bodies. -->

CompanyId Literal (assert this shape):
```python
# backend/app/companies/__init__.py — Wave 1 will create
from typing import Literal
CompanyId = Literal["seva", "juno"]
ACTIVE_COMPANIES: tuple[CompanyId, ...] = ("seva", "juno")
```

Scoped helper signatures (assert these names + return types):
```python
# backend/app/queries/scoped.py — Wave 1 will create
def scoped_summaries(company_id: CompanyId) -> Select  # returns SELECT pre-filtered
def scoped_calendar(company_id: CompanyId) -> Select
def scoped_weekly_sweeps(company_id: CompanyId) -> Select
```

TanStack key factory shape (assert this exact tuple structure):
```ts
// frontend/src/api/queryKeys.ts — Wave 3 will create
export const queryKeys = {
  summaries: (companyId, limit) => ['summaries', companyId, limit] as const,
  calendar: (companyId, start, end) => ['calendar', companyId, start, end] as const,
  weeklySweeps: (companyId, limit) => ['weekly-sweeps', companyId, limit] as const,
}
```

JOB_LOCK_IDS expected post-Wave 2:
```python
JOB_LOCK_IDS = {
  "midday_digest": 1005, "daily_summary": 1017, "daily_summary_prune": 1018,
  "weekly_sweeper": 1019,
  "juno_daily_summary": 1020,  # NEW
  "juno_weekly_sweeper": 1021, # NEW (slot-only)
}
```

Skip-idiom canonical pattern for NEW Wave 0 backend pytest files (Iteration 2 — Warning 1):
```python
# At top of NEW Wave 0 backend test files (test_migration_0014.py, test_queries_scoped.py, test_multitenant_isolation.py):
import pytest

pytest.skip(
    "Wave 0 RED — production code lands in Wave {N} ({09-XX-PLAN.md}). "
    "Remove this line in that wave's task to turn tests GREEN.",
    allow_module_level=True,
)

# All test functions below are guaranteed to be skipped at collection time;
# no per-function pytest.skip needed.
```

EXCEPTION: backend/tests/test_model_parity.py extension (existing file with GREEN tests):
```python
# Add a NEW function in test_model_parity.py:
def test_daily_summary_parity():
    pytest.skip(
        "DailySummary.company_id lands in Wave 1 (09-02-PLAN.md)",
        allow_module_level=False,
    )
    # ... rest of the new test (currently unreachable; Wave 1 removes the skip line)
```

Existing Phase 8 Wave 0 pattern reference (cite this):
- Wave 0 test stubs use module-level `pytest.skip(..., allow_module_level=True)` BEFORE lazy imports so the whole module shows 'skipped' not 'error' when target modules don't exist yet (see STATE.md "Phase 05-senior-agent-core" + "Phase 06-instagram-agent" + "Phase 07-content-agent" — all use this idiom)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CI grep gate + backend RED tests</name>
  <files>scripts/verify-tenant-isolation.sh, backend/tests/test_migration_0014.py, backend/tests/test_queries_scoped.py, backend/tests/test_multitenant_isolation.py, backend/tests/test_model_parity.py</files>
  <read_first>
    - .planning/phases/09-multi-tenant-foundation/09-VALIDATION.md (per-task verification map, lines 60-105 — gives exact pytest commands and expected file paths)
    - .planning/phases/09-multi-tenant-foundation/09-RESEARCH.md §Code Example 7 (verify-tenant-isolation.sh script — copy verbatim) and §Code Example 10 (test_multitenant_isolation.py — copy verbatim)
    - .planning/phases/09-multi-tenant-foundation/09-CONTEXT.md §D-03 (CompanyId Literal source-of-truth — backend/app/companies/__init__.py with `ACTIVE_COMPANIES: Literal["seva","juno"]`)
    - backend/tests/test_model_parity.py (existing file — read entirely, mirror its pattern; per pitfall #5 in RESEARCH this file ONLY covers CalendarItem + WeeklySweep currently, NOT DailySummary)
    - backend/tests/test_calendar_migration_0013.py (existing migration test — mirror its shape for the new 0014 test)
    - backend/tests/conftest.py (existing fixtures — confirm `authed_client` and `async_db_session` fixture names so test_multitenant_isolation.py uses real names)
    - scripts/ directory (verify other scripts/*.sh files exist as patterns — e.g. Phase 8 grep verification scripts)
  </read_first>
  <behavior>
    - **scripts/verify-tenant-isolation.sh** must:
      * Use `set -euo pipefail` + `cd /Users/matthewnelson/seva-mining`
      * Define `TARGETS=("backend/app" "scheduler/agents")` and `ALLOWED=("backend/app/queries/scoped.py" "backend/app/queries/__init__.py")`
      * Use exact regex `PATTERN='select\((DailySummary|CalendarItem|WeeklySweep)[\)\.]'` (per RESEARCH note: tightened to also catch `select(DailySummary.col)` column-selects — the `[\)\.]` character class)
      * `grep -rnE "$PATTERN" "${TARGETS[@]}" 2>/dev/null || true` then strip allowed paths with `grep -v "^$path:"`
      * Exit 1 with descriptive message on any violation; exit 0 with `PASS — all tenant-scoped selects routed through queries/scoped.py` on success
      * Be chmod +x (executable). Confirm with `ls -la scripts/verify-tenant-isolation.sh` showing `-rwxr-xr-x`.
      * As of Wave 0 (this plan), the script MUST exit 0 (no raw selects yet — those will land in Wave 1 before Wave 2 refactors them).
    - **backend/tests/test_migration_0014.py** asserts (one test per assertion, no per-test skip — instead the whole module skips at collection time):
      * `test_company_id_column_exists` — after `alembic upgrade head`, `information_schema.columns` shows `company_id` in `daily_summaries`, `calendar_items`, `weekly_sweeps` with `data_type='character varying'`, `is_nullable='NO'`, `column_default='seva'::character varying`
      * `test_check_constraint` — `pg_catalog.pg_constraint` shows `ck_daily_summaries_company_id` with `pg_get_constraintdef LIKE '%IN%seva%juno%'` on all 3 tables
      * `test_composite_indexes` — `pg_catalog.pg_indexes` shows `ix_daily_summaries_company_generated`, `ix_calendar_items_company_date`, `ix_weekly_sweeps_company_generated`
      * `test_backfill_uses_server_default` — pre-migration row inserts get `company_id='seva'` automatically after migration
      * **(Iteration 2 — Warning 1) Use MODULE-LEVEL skip `pytest.skip("Migration 0014 lands in Wave 1 (09-02-PLAN.md)", allow_module_level=True)` at the top of the module (right after `import pytest`). Wave 1 removes that single line to turn the whole module GREEN.**
    - **backend/tests/test_queries_scoped.py** asserts:
      * `test_scoped_summaries_compiles_with_filter` — `scoped_summaries('seva')` is a `Select`; `str(stmt.compile(compile_kwargs={"literal_binds": True}))` contains `daily_summaries.company_id = 'seva'`
      * `test_scoped_calendar_compiles_with_filter` — same shape for `calendar_items.company_id = 'seva'`
      * `test_scoped_weekly_sweeps_compiles_with_filter` — same for `weekly_sweeps.company_id = 'seva'`
      * **(Iteration 2 — Warning 1) Use MODULE-LEVEL skip `pytest.skip("backend/app/queries/scoped.py lands in Wave 1 (09-02-PLAN.md)", allow_module_level=True)` at the top of the module.** Lazy imports (e.g. `from app.queries.scoped import scoped_summaries`) live INSIDE each test function, so they're unreachable at collection time; Wave 1 removes the single skip line to make the module collect + execute.
    - **backend/tests/test_multitenant_isolation.py** — COPY VERBATIM from RESEARCH §Code Example 10 (`test_tenant_isolation` parametrized over `company in ['seva','juno']` × `endpoint in ['/api/{company}/summaries', '/api/{company}/calendar?...', '/api/{company}/weekly-sweeps']`); add `test_invalid_company_returns_404` asserting GET `/api/bogus/summaries` returns 404; use **MODULE-LEVEL skip** `pytest.skip("/api/{company}/* router prefix lands in Wave 2 (09-03-PLAN.md)", allow_module_level=True)` until Wave 2 lands router prefix. (This was already module-level pre-iteration-2 — no change here; called out for consistency.)
    - **backend/tests/test_model_parity.py** — EXTEND existing file. Add `test_daily_summary_parity()` mirroring the existing `test_calendar_item_parity()` and `test_weekly_sweep_parity()` (whatever shape they use — read first). The new test imports `backend.app.models.daily_summary.DailySummary` AND `scheduler.models.daily_summary.DailySummary` and asserts column names, types, and constraints match. **(Iteration 2 — Warning 1) EXCEPTION to module-level idiom: this file has pre-existing GREEN tests. Use PER-FUNCTION `pytest.skip("requires DailySummary parity — Wave 1 (09-02-PLAN.md)", allow_module_level=False)` at the top of the NEW function body ONLY.** Wave 1 removes that line to make the new function GREEN without disturbing the existing tests.
  </behavior>
  <action>
    Use the Write tool (not heredoc) to create these 4 NEW backend test files and 1 NEW shell script. ONE Edit to extend existing test_model_parity.py.

    1. **scripts/verify-tenant-isolation.sh** — copy CODE EXAMPLE 7 from 09-RESEARCH.md verbatim, save with executable permissions via `chmod +x scripts/verify-tenant-isolation.sh` as the final step.

    2. **backend/tests/test_migration_0014.py** — write 4 test functions per the behavior block above. **(Iteration 2 — Warning 1)** Add MODULE-LEVEL skip at the top:
       ```python
       import pytest

       pytest.skip(
           "Migration 0014 lands in Wave 1 (09-02-PLAN.md). "
           "Remove this line in Wave 1 Task 1 step 4 to turn tests GREEN.",
           allow_module_level=True,
       )

       # ... rest of imports and test functions (all unreachable at collection)
       ```
       Use SQL-introspection assertions via `async_db_session.execute(text("SELECT ..."))` against `information_schema.columns` / `pg_catalog.pg_constraint` / `pg_catalog.pg_indexes`. Use `asyncio_mode=auto` from existing conftest.

    3. **backend/tests/test_queries_scoped.py** — three tests. **(Iteration 2 — Warning 1)** Add MODULE-LEVEL skip at the top:
       ```python
       import pytest

       pytest.skip(
           "backend/app/queries/scoped.py lands in Wave 1 (09-02-PLAN.md). "
           "Remove this line in Wave 1 Task 3 step 4 to turn tests GREEN.",
           allow_module_level=True,
       )

       # ... rest of imports and test functions
       ```
       Within each test function: `from app.queries.scoped import scoped_summaries` (lazy import, unreachable until skip is removed). Compile statement via `stmt.compile(compile_kwargs={"literal_binds": True})` and `assert "company_id" in str(compiled)`.

    4. **backend/tests/test_multitenant_isolation.py** — COPY CODE EXAMPLE 10 from 09-RESEARCH.md verbatim. Lines 1201-1299 of that file are the template. Top-of-module:
       ```python
       import pytest

       pytest.skip(
           "/api/{company}/* router prefix lands in Wave 2 (09-03-PLAN.md). "
           "Remove this line in Wave 2 Task 1 step 6 to turn tests GREEN.",
           allow_module_level=True,
       )

       # ... full parametrized test body verbatim from RESEARCH §Code Example 10
       ```

    5. **backend/tests/test_model_parity.py** — first Read the existing file. Then add a new function `test_daily_summary_parity()` modeled exactly on the existing parity tests. **(Iteration 2 — Warning 1 EXCEPTION)** Use per-function skip (NOT module-level) so the existing CalendarItem + WeeklySweep parity tests keep running GREEN. Shape:
       ```python
       # Append to existing test_model_parity.py
       def test_daily_summary_parity():
           pytest.skip(
               "DailySummary.company_id lands in Wave 1 (09-02-PLAN.md). "
               "Remove this line in Wave 1 Task 2 step 7 to turn this test GREEN.",
               allow_module_level=False,
           )
           # ... new parity assertion body
       ```

    Use Edit (not Write) for test_model_parity.py since it already exists.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining && bash scripts/verify-tenant-isolation.sh && cd backend && uv run pytest tests/test_migration_0014.py tests/test_queries_scoped.py tests/test_multitenant_isolation.py tests/test_model_parity.py -v 2>&1 | grep -E "PASSED|SKIPPED|FAILED|ERROR" | head -30 && grep -c "allow_module_level=True" tests/test_migration_0014.py tests/test_queries_scoped.py tests/test_multitenant_isolation.py</automated>
  </verify>
  <done>
    - `scripts/verify-tenant-isolation.sh` is executable (`ls -la` shows `-rwxr-xr-x`) and exits 0 when run from repo root (no violations yet; legacy code uses `select(DailySummary)` but Wave 1 will refactor those — for now, grep is wired but no enforcement-breaking violations exist if the script is configured per RESEARCH §Code Example 7's "Note on `select(DailySummary.id)`" — if Wave 0 finds existing violations in scheduler/agents/daily_summary.py, document them in the plan summary and tighten the script to whitelist those existing call sites with a TODO comment).
    - All 4 backend test files import without syntax errors (`pytest --collect-only` succeeds).
    - All 3 NEW backend test files (test_migration_0014.py + test_queries_scoped.py + test_multitenant_isolation.py) show as MODULE-LEVEL SKIPPED in pytest output (each entire module reported as skipped, not per-function — proving the consistent idiom).
    - **(Iteration 2 — Warning 1)** `grep -c "allow_module_level=True"` returns ≥1 for each of the three NEW backend test files (consistent idiom enforced).
    - test_model_parity.py extension adds exactly 1 new test function `test_daily_summary_parity` with per-function `pytest.skip(..., allow_module_level=False)` — the existing CalendarItem + WeeklySweep parity tests still PASS (not skipped).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scheduler RED tests + Juno agent test scaffold</name>
  <files>scheduler/tests/test_worker.py, scheduler/tests/agents/test_juno_daily_summary.py</files>
  <read_first>
    - .planning/phases/09-multi-tenant-foundation/09-VALIDATION.md lines 75-82 (TENANT-08 verification map — exact pytest commands)
    - .planning/phases/09-multi-tenant-foundation/09-RESEARCH.md §Code Example 8 (juno_daily_summary factory + CronTrigger shape) and §Code Example 9 (run_juno_daily_summary stub)
    - .planning/phases/09-multi-tenant-foundation/09-CONTEXT.md §D-01 (per-company jobs: juno_daily_summary=1020, juno_weekly_sweeper=1021 SLOT-ONLY) and §D-01a (5-min stagger: hour="8,12", minute=5 — NOT 07:05)
    - scheduler/tests/test_worker.py (existing file — read entirely; per STATE.md Phase 07 note: "any new job registration in build_scheduler MUST update test_retired_crons_absent_from_job_lock_ids + test_scheduler_registers_N_jobs + test_build_scheduler_omits_v1_sub_agent_crons")
    - scheduler/worker.py lines 91-102 (existing JOB_LOCK_IDS dict) and lines 214-235 (existing _make_daily_summary_job factory pattern)
    - scheduler/agents/daily_summary.py (existing entry point — confirm the imports & DailySummary / AgentRun model usage for the new stub test)
    - scheduler/tests/agents/ directory (list contents to confirm pattern — likely empty or has placeholder __init__.py)
  </read_first>
  <behavior>
    - **scheduler/tests/test_worker.py extensions** — add 4 new tests + bump 1 existing assertion. The existing file has GREEN tests so we CANNOT module-level skip. Each new test uses per-function `pytest.skip(..., allow_module_level=False)` (EXCEPTION to the Iteration-2 Warning-1 idiom — same precedent as test_model_parity.py):
      * `test_juno_lock_ids_present` — `assert JOB_LOCK_IDS.get("juno_daily_summary") == 1020` AND `assert JOB_LOCK_IDS.get("juno_weekly_sweeper") == 1021`. Marked `pytest.skip("juno_* lock IDs land in Wave 2 (09-03-PLAN.md)", allow_module_level=False)` until Wave 2.
      * `test_juno_daily_summary_registered` — after `build_scheduler(engine)`, `scheduler.get_job("juno_daily_summary")` is not None; its `trigger` is a `CronTrigger`; `trigger.fields[<minute-field-index>]` evaluates to `5` (5-min stagger) AND hour field evaluates to `[8, 12]`. Per-function skip until Wave 2.
      * `test_scheduler_registers_N_jobs` — BUMP existing job-count assertion by +1 (Phase 9 adds exactly juno_daily_summary; juno_weekly_sweeper is slot-only, NOT registered). Read existing test first to find the current N value (likely 5 or 6 after Phase 8 cleanup). New value = existing + 1. Add comment `# v3.0 Phase 9 — juno_daily_summary added (juno_weekly_sweeper slot reserved but not registered)`. Per-function skip until Wave 2 (the assertion is BUMPED in this Wave 0 plan but stays gated by skip until Wave 2 actually registers the new job).
      * `test_juno_weekly_sweeper_NOT_registered` — defensive — `scheduler.get_job("juno_weekly_sweeper") is None` (slot-only per D-01). Per-function skip until Wave 2.
    - **scheduler/tests/agents/test_juno_daily_summary.py** asserts:
      * `test_run_juno_daily_summary_writes_partial_row` — after `await run_juno_daily_summary()`, query `daily_summaries WHERE company_id='juno'`: exactly 1 row exists, `status='partial'`, `gold_news_md IS NULL`, `error_text` contains the phrase 'Juno content pipeline pending' or 'Phase 10', and an `agent_runs` row with `agent_name='juno_daily_summary'` exists with status='completed' (not 'running'/'failed') and `notes` JSON contains `"company_id": "juno"`.
      * `test_run_juno_daily_summary_idempotency` — calling `run_juno_daily_summary()` twice in the same 30-minute window does NOT create a second row (returns early with idempotency_skip log).
      * **(Iteration 2 — Warning 1)** Use MODULE-LEVEL skip `pytest.skip("scheduler/agents/daily_summary.py:run_juno_daily_summary lands in Wave 2 (09-03-PLAN.md)", allow_module_level=True)` at the top of this NEW file (it's a brand-new file with no pre-existing tests, so module-level is safe and removal is a single-line delete in Wave 2).
  </behavior>
  <action>
    Use Edit to extend `scheduler/tests/test_worker.py` and Write to create `scheduler/tests/agents/test_juno_daily_summary.py`.

    1. **scheduler/tests/test_worker.py** — first Read the file to locate:
       - The existing `JOB_LOCK_IDS` assertion test (likely named `test_job_lock_ids_unique` or similar)
       - The existing scheduler-registers-N-jobs assertion (find the integer literal N and add 1)
       - Existing import section so the new tests can reuse `JOB_LOCK_IDS`, `build_scheduler`, `engine` fixture, `CronTrigger` imports

       Then APPEND 4 new test functions per the behavior block. **(Iteration 2 — Warning 1 EXCEPTION)** Each NEW test gets its OWN per-function `pytest.skip("...Wave 2 (09-03-PLAN.md)...", allow_module_level=False)` (we can't use module-level because existing GREEN tests in test_worker.py must keep running). Bump the existing job-count assertion's integer literal by exactly +1 (preserving the comment "v3.0 Phase 9 — juno_daily_summary added"). The bumped integer is gated by the per-function skip until Wave 2.

    2. **scheduler/tests/agents/test_juno_daily_summary.py** — Write a new file with **(Iteration 2 — Warning 1) MODULE-LEVEL skip** at the top:
       ```python
       import pytest

       pytest.skip(
           "scheduler/agents/daily_summary.py:run_juno_daily_summary lands in Wave 2 (09-03-PLAN.md). "
           "Remove this line in Wave 2 Task 2 step 6 to turn tests GREEN.",
           allow_module_level=True,
       )

       # ... full test body with the two test functions
       ```
       After the skip, include the two test function bodies (they will collect but skip until Wave 2 implements `run_juno_daily_summary`). Use lazy imports: `from agents.daily_summary import run_juno_daily_summary` inside each test function body (unreachable at collection until skip is removed).
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/test_worker.py tests/agents/test_juno_daily_summary.py -v 2>&1 | grep -E "PASSED|SKIPPED|FAILED|ERROR" | head -20 && grep -c "allow_module_level=True" tests/agents/test_juno_daily_summary.py</automated>
  </verify>
  <done>
    - Both files import cleanly (`pytest --collect-only` succeeds).
    - 4 new test_worker.py tests show as per-function SKIPPED (existing GREEN tests in the file remain PASSED — Warning-1 exception for existing-file extensions documented).
    - 2 new test_juno_daily_summary.py tests show as MODULE-LEVEL SKIPPED (the whole NEW file skips cleanly, single-line Wave-2 removal).
    - **(Iteration 2 — Warning 1)** `grep -c "allow_module_level=True" tests/agents/test_juno_daily_summary.py` returns ≥1.
    - Existing test_worker.py tests still PASS (job-count bump applied to the right integer literal but gated by per-function skip until Wave 2).
    - No `ERROR` lines from import failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create frontend RED test scaffolds (Vitest)</name>
  <files>frontend/src/api/queryKeys.test.ts, frontend/src/stores/__tests__/companySlice.test.ts, frontend/src/components/layout/__tests__/CompanyScopedRoute.test.tsx, frontend/src/components/layout/__tests__/CompanySwitcher.test.tsx, frontend/src/__tests__/App.test.tsx</files>
  <read_first>
    - .planning/phases/09-multi-tenant-foundation/09-VALIDATION.md lines 72-81 (TENANT-05/06/07/09 verification map)
    - .planning/phases/09-multi-tenant-foundation/09-UI-SPEC.md §Component Inventory (CompanySwitcher canonical contract) and §CompanySwitcher button — full state table
    - .planning/phases/09-multi-tenant-foundation/09-RESEARCH.md §Code Example 4 (CompanyScopedRoute), §Code Example 5 (companySlice + persist), §Code Example 6 (queryKeys factory + CompanySwitcher)
    - .planning/phases/09-multi-tenant-foundation/09-CONTEXT.md §D-07 (segmented control) and §D-08 (queryClient.clear() + Zustand persist) and §D-06 (bookmark grace redirects)
    - frontend/src/components/layout/__tests__/ (existing — list pattern; confirm naming convention)
    - frontend/src/stores/index.ts and frontend/src/stores/slices/ (existing slices — companySlice will be new)
    - frontend/src/pages/__tests__/PerAgentQueuePage.test.tsx OR DigestPage.test.tsx (read one existing vitest test to confirm imports: `@testing-library/react`, `vitest`, `vi`)
  </read_first>
  <behavior>
    - Frontend Vitest does NOT have an `allow_module_level=True` equivalent — Iteration-2 Warning 1 only applies to backend pytest files. The frontend skip idiom stays as per-test `it.skip(...)` (this is the canonical Vitest pattern; modifying it would break the established frontend test conventions established in Phase 8). Wave 3 removes each `.skip` per-test (already enforced by plan 09-04 Task 1 step 8 + Task 2 step 5 with grep verification).
    - **frontend/src/api/queryKeys.test.ts** asserts:
      * `it('summaries factory returns tuple with companyId and limit')` — `expect(queryKeys.summaries('seva', 60)).toEqual(['summaries', 'seva', 60])`; for 'juno' too
      * `it('calendar factory returns tuple with companyId, start, end')` — `expect(queryKeys.calendar('juno', '2026-01-01', '2026-12-31')).toEqual(['calendar', 'juno', '2026-01-01', '2026-12-31'])`
      * `it('weeklySweeps factory returns tuple')` — `expect(queryKeys.weeklySweeps('seva', 12)).toEqual(['weekly-sweeps', 'seva', 12])`
      * Use `it.skip()` blocks (Vitest equivalent of pytest.skip) until Wave 3 lands. Skip reason inline comment: `// queryKeys.ts lands in Wave 3 (09-04-PLAN.md)`.
    - **frontend/src/stores/__tests__/companySlice.test.ts** asserts:
      * `it.skip('setLastVisitedCompany updates state to juno')` — `useAppStore.getState().setLastVisitedCompany('juno')` then `expect(useAppStore.getState().lastVisitedCompany).toBe('juno')`
      * `it.skip('persists lastVisitedCompany to localStorage under seva-mining-app-state-v3 key')` — after setLastVisitedCompany('juno'), read `localStorage.getItem('seva-mining-app-state-v3')` and `expect(JSON.parse(...).state.lastVisitedCompany).toBe('juno')`
      * `it.skip('partialize excludes queueUi and auth state from persistence')` — assert only `lastVisitedCompany` is in the persisted state object
    - **frontend/src/components/layout/__tests__/CompanyScopedRoute.test.tsx** asserts:
      * `it.skip('redirects invalid slug to /seva')` — render CompanyScopedRoute inside MemoryRouter with `initialEntries={['/bogus']}` and route shape `<Route path=":company" element={<CompanyScopedRoute />}>...`. Assert window location ends up at `/seva` (use `useLocation` spy or `screen.getByTestId('current-path')`).
      * `it.skip('renders Outlet for valid slug')` — initialEntries=['/seva'], assert child component renders.
      * `it.skip('calls setLastVisitedCompany on mount with valid slug')` — spy on Zustand store action.
    - **frontend/src/components/layout/__tests__/CompanySwitcher.test.tsx** asserts:
      * `it.skip('renders Seva and Juno buttons with active class on current tenant')` — render inside MemoryRouter `initialEntries={['/seva/calendar']}`; assert "Seva" button has `border-brand-accent` class (or `aria-selected="true"`) and "Juno" button does not.
      * `it.skip('clicking Juno calls queryClient.clear() then navigate to /juno/calendar')` — wrap with QueryClientProvider; spy on `useQueryClient` and `useNavigate`. Click button. Assert `queryClient.clear` was called BEFORE `navigate('/juno/calendar')`.
      * `it.skip('preserves sub-path when switching')` — start at `/seva/viral`, click Juno, expect navigate to `/juno/viral`.
      * `it.skip('switching while at /seva/ goes to /juno/')` — sub-path is empty when on tenant index.
      * `it.skip('clicking the already-active tenant is a no-op')` — no navigate, no clear.
    - **frontend/src/__tests__/App.test.tsx** asserts (or extend existing if present):
      * `it.skip('bare / redirects to /seva')` — initialEntries=['/'], assert window location becomes `/seva`.
      * `it.skip('/calendar redirects to /seva/calendar')` — bookmark grace per D-06.
      * `it.skip('/viral redirects to /seva/viral')` — bookmark grace.
      * `it.skip('/queue redirects to /seva')` — legacy v2.0 grace.
      * `it.skip('/agents/breaking_news redirects to /seva')` — legacy v2.0 grace.
      * `it.skip('/digest stays at /digest (not tenant-scoped)')` — D-06.
      * `it.skip(':company route mounts TabbedDashboard with useParams')` — initialEntries=['/seva/calendar'], assert TabbedDashboard rendered.
  </behavior>
  <action>
    Use Write to create 5 new test files. If `frontend/src/__tests__/App.test.tsx` already exists, use Edit to extend instead — first Read to confirm.

    1. **frontend/src/api/queryKeys.test.ts** — Write a Vitest file. Top-level `describe('queryKeys factory (TENANT-09)', () => {...})`. All `it()` blocks wrapped as `it.skip('...', () => {})` with inline `// Wave 3 (09-04-PLAN.md) — queryKeys.ts not yet created`. Lazy dynamic import `await import('@/api/queryKeys')` inside each test so the module doesn't need to exist for the file to load.

    2. **frontend/src/stores/__tests__/companySlice.test.ts** — Write a Vitest file. Use `beforeEach(() => localStorage.clear())`. All `it.skip()` until Wave 3. Lazy `await import('@/stores')`.

    3. **frontend/src/components/layout/__tests__/CompanyScopedRoute.test.tsx** — Write with `MemoryRouter` + `Routes` + `Route` from react-router-dom. All `it.skip()` until Wave 3.

    4. **frontend/src/components/layout/__tests__/CompanySwitcher.test.tsx** — Write with `MemoryRouter` + `QueryClient` + `QueryClientProvider` wrappers. Spy on `useQueryClient.clear` and `useNavigate` from `react-router-dom` via `vi.mock('react-router-dom', async () => ({...await vi.importActual('react-router-dom'), useNavigate: () => mockNavigate}))`. All `it.skip()` until Wave 3.

    5. **frontend/src/__tests__/App.test.tsx** — first check if file exists with `Read`. If exists, Edit to APPEND the 7 new test cases. If not, Write a new file. Wrap App in MemoryRouter (NOT BrowserRouter — research notes App.tsx mounts its own BrowserRouter; the test must mock or substitute). All `it.skip()` until Wave 3.

    For each file, after the `it.skip()` block, leave a comment block `// Once Wave 3 ships {component}, REMOVE the .skip and the test should pass GREEN.`
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/frontend && npm test -- --run src/api/queryKeys.test.ts src/stores/__tests__/companySlice.test.ts src/components/layout/__tests__/CompanyScopedRoute.test.tsx src/components/layout/__tests__/CompanySwitcher.test.tsx src/__tests__/App.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 5 frontend test files parse and collect without syntax errors.
    - All new `it.skip(...)` tests show as "skipped" in Vitest output (NOT "failed", NOT "passed", NOT "error").
    - No TypeScript compilation errors (`npm test --run` would fail loudly if so).
    - Pre-existing Vitest tests (PerAgentQueuePage, DigestPage, etc.) still pass — Phase 9 Wave 0 does not break Phase 8.
    - `cd frontend && npx tsc --noEmit` passes (no type errors introduced).
  </done>
</task>

</tasks>

<verification>
  Plan-level acceptance gate (all must pass before this plan is marked complete):
  - `bash scripts/verify-tenant-isolation.sh` exits 0.
  - `cd backend && uv run pytest -x 2>&1 | tail -10` shows existing 156 tests PASS plus the new tests SKIPPED (no FAILED, no ERROR).
  - `cd scheduler && uv run pytest -x 2>&1 | tail -10` shows existing 264+ tests PASS plus the new tests SKIPPED.
  - `cd frontend && npm test --run 2>&1 | tail -10` shows existing 141+ tests PASS plus the new tests skipped.
  - **(Iteration 2 — Warning 1)** Module-level skip idiom enforced on 3 NEW backend pytest files + 1 NEW scheduler agent test file:
    ```bash
    grep -l "allow_module_level=True" \
      /Users/matthewnelson/seva-mining/backend/tests/test_migration_0014.py \
      /Users/matthewnelson/seva-mining/backend/tests/test_queries_scoped.py \
      /Users/matthewnelson/seva-mining/backend/tests/test_multitenant_isolation.py \
      /Users/matthewnelson/seva-mining/scheduler/tests/agents/test_juno_daily_summary.py
    ```
    Should list all 4 files. (Existing-file extensions — test_model_parity.py + test_worker.py — use per-function skip; this is the documented exception.)
  - `git diff --stat` shows ~12 new files + 2 extended files (test_model_parity.py, test_worker.py) — zero production code edits.
</verification>

<success_criteria>
  - 12 Wave 0 deliverable files exist on disk per VALIDATION.md §Wave 0 Requirements (count: 1 shell script + 4 backend test files + 2 scheduler test files + 5 frontend test files = 12).
  - Every TENANT-* requirement (01..10) has at least one test that — once Wave 1/2/3 lands — will turn GREEN. This is the Nyquist Dim 8 contract.
  - Zero production code touched in this plan; the contracts are documented in tests, not prose.
  - CI grep gate is in place and ready to enforce as Wave 1/2 ship; Wave 2 SHOULD see the gate fail mid-refactor (when scheduler/agents/daily_summary.py still has raw `select(DailySummary...)`) and pass again at end-of-Wave-2.
  - **(Iteration 2 — Warning 1)** Skip-idiom standardization: every NEW Wave 0 backend pytest file (4 of them) uses module-level `allow_module_level=True`; the only per-function skips are in the two existing-file extensions (test_model_parity.py + test_worker.py) where module-level would falsely skip pre-existing GREEN tests. Removal in Waves 1/2 is a single-line delete per NEW file + per-function-line delete per existing-file extension.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenant-foundation/09-01-SUMMARY.md` summarizing:
- 12 files created/extended (full paths)
- Pre-existing test count baseline (e.g. "Backend: 156→160 collected, +4 skipped")
- Any deviations from RESEARCH §Code Examples 7+10 (verbatim copy expected — note if anything had to be adapted)
- Confirmation that `bash scripts/verify-tenant-isolation.sh` exits 0
- **(Iteration 2 — Warning 1)** Confirmation of consistent skip idiom: 4 NEW files (test_migration_0014, test_queries_scoped, test_multitenant_isolation, test_juno_daily_summary) use module-level `allow_module_level=True`; 2 existing-file extensions (test_model_parity, test_worker) use per-function skip; cite the grep verification output.
- Plan handoff note to Wave 1: "Wave 1 must implement these contracts in this exact order: (1) backend/app/companies/__init__.py, (2) alembic 0014, (3) backend models + dual-model parity, (4) backend/app/queries/scoped.py. Each Wave 1/2 task that needs to flip a Wave 0 test GREEN should DELETE the single `pytest.skip(..., allow_module_level=True)` line at the top of the module — single-line edit per file."
</output>
