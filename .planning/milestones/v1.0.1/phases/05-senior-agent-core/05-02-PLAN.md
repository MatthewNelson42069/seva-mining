---
phase: 05-senior-agent-core
plan: 02
type: tdd
wave: 1
depends_on: ["05-01"]
files_modified:
  - scheduler/agents/senior_agent.py
  - scheduler/tests/test_senior_agent.py
autonomous: true
requirements:
  - SENR-02

must_haves:
  truths:
    - "Jaccard similarity correctly computes token overlap between two text strings"
    - "Fingerprint extraction preserves cashtags, numbers, and company names while removing stopwords"
    - "Deduplication sets related_id on the newer item pointing to the older item when overlap >= 0.40"
    - "Deduplication leaves related_id as NULL when overlap < 0.40"
  artifacts:
    - path: "scheduler/agents/senior_agent.py"
      provides: "extract_fingerprint_tokens, jaccard_similarity, _run_deduplication"
      exports: ["extract_fingerprint_tokens", "jaccard_similarity"]
    - path: "scheduler/tests/test_senior_agent.py"
      provides: "4 passing tests for dedup logic"
  key_links:
    - from: "scheduler/agents/senior_agent.py"
      to: "scheduler/models/draft_item.py"
      via: "from models.draft_item import DraftItem"
      pattern: "from models.draft_item import DraftItem"
---

<objective>
Implement story fingerprint deduplication with Jaccard text similarity (SENR-02).

Purpose: When two agents surface the same story, both cards must remain in the queue but the newer one gets `related_id` pointing to the older one for visual linking. This is a TDD plan: write tests first (RED), implement (GREEN), refactor.

Output: `extract_fingerprint_tokens()` and `jaccard_similarity()` pure functions, plus `_run_deduplication()` method on SeniorAgent. 4 tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-senior-agent-core/05-CONTEXT.md
@.planning/phases/05-senior-agent-core/05-RESEARCH.md
@.planning/phases/05-senior-agent-core/05-01-SUMMARY.md

<interfaces>
From scheduler/models/draft_item.py (after Plan 01):
```python
class DraftItem(Base):
    __tablename__ = "draft_items"
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    status = Column(draft_status_enum, nullable=False, server_default="pending")
    source_text = Column(Text)
    rationale = Column(Text)
    related_id = Column(UUID(as_uuid=True), ForeignKey("draft_items.id"), nullable=True)
    score = Column(Numeric(5, 2))
    created_at = Column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)
    engagement_alert_level = Column(String(20), nullable=True)
    alerted_expiry_at = Column(DateTime(timezone=True), nullable=True)
```

From scheduler/models/config.py:
```python
class Config(Base):
    __tablename__ = "config"
    key = Column(String(100), primary_key=True)
    value = Column(Text, nullable=False)
```

From scheduler/database.py:
```python
AsyncSessionLocal = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```
</interfaces>
</context>

<feature>
  <name>Story Fingerprint Deduplication (SENR-02)</name>
  <files>scheduler/agents/senior_agent.py, scheduler/tests/test_senior_agent.py</files>
  <behavior>
    - `extract_fingerprint_tokens("Gold price surges to $2400 per troy ounce amid central bank buying")` returns a frozenset containing at minimum: `{"gold", "price", "surges", "$2400", "troy", "ounce", "amid", "central", "bank", "buying"}` — excludes "to", "per"
    - `extract_fingerprint_tokens("")` returns `frozenset()`
    - `extract_fingerprint_tokens(None)` returns `frozenset()` (handle None gracefully)
    - `jaccard_similarity(frozenset({"gold", "price", "surges"}), frozenset({"gold", "price", "drops"}))` returns 0.5 (2 common / 4 union)
    - `jaccard_similarity(frozenset(), frozenset())` returns 0.0
    - When two DraftItems share >= 40% token overlap, `_run_deduplication` sets `related_id` on the newer item to the older item's ID
    - When overlap < 40%, `related_id` stays NULL
    - Only `pending` items created within the last 24 hours are checked for dedup
    - Config key `senior_dedup_threshold` (default "0.40") controls the threshold
  </behavior>
  <implementation>
    TDD cycle:
    1. RED: Replace the 4 dedup test stubs with real test implementations. Tests for `jaccard_similarity` and `extract_fingerprint_tokens` are pure function tests (no DB needed). Tests for `_run_deduplication` need mock DB sessions.
    2. GREEN: Create `scheduler/agents/senior_agent.py` with the SeniorAgent class skeleton, module-level `STOPWORDS` frozenset, `extract_fingerprint_tokens()`, `jaccard_similarity()`, and `SeniorAgent._run_deduplication()`.
    3. REFACTOR if needed.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write real dedup tests, then GREEN — Implement dedup logic</name>
  <files>scheduler/tests/test_senior_agent.py, scheduler/agents/senior_agent.py</files>
  <read_first>
    scheduler/tests/test_senior_agent.py
    scheduler/agents/twitter_agent.py
    scheduler/models/draft_item.py
    scheduler/models/config.py
    scheduler/database.py
    .planning/phases/05-senior-agent-core/05-RESEARCH.md
  </read_first>
  <behavior>
    - test_jaccard_similarity: `jaccard_similarity(frozenset({"gold","price","surges"}), frozenset({"gold","price","drops"}))` == 0.5; empty sets return 0.0; identical sets return 1.0
    - test_extract_fingerprint_tokens: extracts cashtags like `$gld`, strips stopwords ("the", "a", "in", "to", "for", "of", "is"), keeps numbers/company names; returns frozenset; empty/None input returns frozenset()
    - test_dedup_sets_related_id: create two DraftItem dicts with overlapping source_text (>= 40% Jaccard), call dedup, assert newer item's related_id equals older item's id
    - test_dedup_no_match_below_threshold: two items with completely different text, assert related_id stays None
  </behavior>
  <action>
**RED phase:**
Replace the 4 dedup test stubs (test_jaccard_similarity, test_extract_fingerprint_tokens, test_dedup_sets_related_id, test_dedup_no_match_below_threshold) with real implementations.

For the pure function tests (`test_jaccard_similarity`, `test_extract_fingerprint_tokens`):
- Import `from agents.senior_agent import jaccard_similarity, extract_fingerprint_tokens`
- Test concrete input/output pairs as specified in the behavior block
- These tests need NO database — they test pure functions

For the DB integration tests (`test_dedup_sets_related_id`, `test_dedup_no_match_below_threshold`):
- Use unittest.mock to patch `AsyncSessionLocal` and create mock sessions
- Create DraftItem-like objects with known `id`, `source_text`, `rationale`, `status="pending"`, `created_at` within 24h, `related_id=None`
- For `test_dedup_sets_related_id`: Two items with overlapping text like "Gold price hits $2400 amid bank buying" and "Gold price reaches $2400 as central banks buy" — overlap should be >= 0.40
- For `test_dedup_no_match_below_threshold`: Two items with unrelated text like "Gold price surges" and "Tesla announces new factory in Berlin" — overlap should be < 0.40
- Mock `session.execute()` to return the existing items for the SELECT query
- Mock `session.get()` to return the new item
- Assert `new_item.related_id` is set (or not) after calling dedup

Run tests to confirm RED (they will fail because `agents/senior_agent.py` doesn't exist yet or lacks implementations).

**GREEN phase:**
Create `scheduler/agents/senior_agent.py` with:

1. Module-level `STOPWORDS` frozenset containing: "the", "a", "an", "and", "or", "but", "in", "on", "at", "to", "for", "of", "with", "by", "from", "as", "is", "are", "was", "were", "be", "been", "being", "have", "has", "had", "do", "does", "did", "will", "would", "could", "should", "may", "might", "shall", "can", "not", "this", "that", "these", "those", "it", "its", "they", "their", "he", "she", "we", "you", "i", "me", "my", "our", "your", "his", "her", "said", "says", "also", "via", "per", "just", "new", "today", "following", "after", "before", "over", "under", "about", "into"

2. `extract_fingerprint_tokens(text: str | None) -> frozenset[str]`:
   - Return frozenset() if text is None or empty
   - Lowercase, strip
   - Use `re.findall(r'\$[a-z]+|\b\w+\b', text)` to extract tokens (preserving cashtags)
   - Filter out stopwords and tokens with len <= 1
   - Return as frozenset

3. `jaccard_similarity(set_a: frozenset, set_b: frozenset) -> float`:
   - Return 0.0 if both empty
   - Return `len(set_a & set_b) / len(set_a | set_b)`

4. `SeniorAgent` class with `__init__(self)` setting `self.settings = get_settings()`

5. `SeniorAgent._get_config(self, session, key, default)` async helper — `select(Config.value).where(Config.key == key)`, return `scalar_one_or_none() or default`

6. `SeniorAgent._run_deduplication(self, session, new_item_id)` async method:
   - Read threshold from config key `senior_dedup_threshold` (default "0.40")
   - Get new item via `session.get(DraftItem, new_item_id)`, return if None
   - Extract tokens from `(new_item.source_text or "") + " " + (new_item.rationale or "")`
   - Return if no tokens
   - Query pending items created in last 24h, excluding the new item, ordered by created_at ASC
   - Compare each against new item tokens, find best match above threshold
   - If match found, set `new_item.related_id = best_match.id`

Imports needed: `import re`, `import uuid`, `import logging`, `from datetime import datetime, timezone, timedelta`, `from sqlalchemy import select, func, update`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from config import get_settings`, `from database import AsyncSessionLocal`, `from models.draft_item import DraftItem`, `from models.config import Config`, `from models.agent_run import AgentRun`

Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/test_senior_agent.py::test_jaccard_similarity tests/test_senior_agent.py::test_extract_fingerprint_tokens tests/test_senior_agent.py::test_dedup_sets_related_id tests/test_senior_agent.py::test_dedup_no_match_below_threshold -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scheduler/agents/senior_agent.py contains `def extract_fingerprint_tokens(text:` and `def jaccard_similarity(set_a:`
    - scheduler/agents/senior_agent.py contains `class SeniorAgent:` and `async def _run_deduplication(self, session`
    - scheduler/agents/senior_agent.py contains `STOPWORDS = frozenset(`
    - scheduler/agents/senior_agent.py contains `senior_dedup_threshold` config key lookup with default `"0.40"`
    - All 4 dedup tests pass (not skipped)
    - Remaining 15 test stubs still skip (not error)
  </acceptance_criteria>
  <done>Story deduplication is implemented with Jaccard similarity. Pure functions (extract_fingerprint_tokens, jaccard_similarity) tested directly. SeniorAgent._run_deduplication sets related_id correctly. 4 tests pass, 15 stubs still skip.</done>
</task>

</tasks>

<verification>
- `cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/test_senior_agent.py -v` shows 4 passed, 15 skipped
- `grep "related_id" /Users/matthewnelson/seva-mining/scheduler/agents/senior_agent.py` shows related_id assignment logic
</verification>

<success_criteria>
Jaccard deduplication works: two items about the same gold story get linked via related_id; unrelated items stay independent. All 4 dedup tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-senior-agent-core/05-02-SUMMARY.md`
</output>
