---
phase: 12-per-tenant-anthropic-api-key
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - scheduler/agents/daily_summary.py
  - scheduler/agents/weekly_sweeper.py
  - scheduler/agents/content_agent.py
  - scheduler/scripts/uat_voice_calibration.py
  - scheduler/tests/test_content_agent.py
autonomous: true
requirements: [KEY-01, KEY-03]

must_haves:
  truths:
    - "scheduler/agents/daily_summary.py line ~583 calls get_anthropic_client('seva', timeout=60.0) (Seva daily summary Sonnet)"
    - "scheduler/agents/daily_summary.py line ~1226 calls get_anthropic_client('juno', timeout=JUNO_SONNET_TIMEOUT) (Juno daily summary Sonnet)"
    - "scheduler/agents/weekly_sweeper.py line ~399 calls get_anthropic_client('seva', timeout=SONNET_TIMEOUT_S) (Seva sweeper Sonnet)"
    - "scheduler/scripts/uat_voice_calibration.py line ~377 calls get_anthropic_client('juno', timeout=JUNO_SONNET_TIMEOUT) (Juno UAT one-shot script)"
    - "scheduler/agents/content_agent.py _do_fetch (formerly line 1108) calls get_anthropic_client('seva', timeout=30.0) for the Seva fetch_stories Haiku scoring pipeline"
    - "Dead functions check_compliance + is_gold_relevant_or_systemic_shock + review removed from scheduler/agents/content_agent.py"
    - "Their corresponding tests removed from scheduler/tests/test_content_agent.py"
    - "grep -c 'AsyncAnthropic(' on the 3 refactored agent files returns 0"
    - "grep -c 'AsyncAnthropic(' scheduler/agents/content_agent.py returns 0 (dead sites excised + live _do_fetch site routed through resolver)"
    - "scheduler/tests pytest suite remains GREEN (zero regressions beyond the deliberately deleted dead-code tests)"
  artifacts:
    - path: "scheduler/agents/daily_summary.py"
      provides: "Seva + Juno daily summary Sonnet calls now resolve via get_anthropic_client"
      contains: "get_anthropic_client"
    - path: "scheduler/agents/weekly_sweeper.py"
      provides: "Seva weekly sweeper Sonnet call now resolves via get_anthropic_client"
      contains: "get_anthropic_client('seva'"
    - path: "scheduler/agents/content_agent.py"
      provides: "fetch_stories scoring pipeline routes through resolver; dead review/check_compliance/is_gold_relevant_or_systemic_shock functions deleted"
      contains: "get_anthropic_client('seva'"
    - path: "scheduler/scripts/uat_voice_calibration.py"
      provides: "Juno UAT script routes through resolver (preserves attribution if operator re-fires it post-deploy)"
      contains: "get_anthropic_client('juno'"
    - path: "scheduler/tests/test_content_agent.py"
      provides: "Tests for deleted dead functions surgically removed; tests for live fetch_stories + deduplicate_stories preserved"
      contains: "fetch_stories"
  key_links:
    - from: "scheduler/agents/daily_summary.py::run_daily_summary"
      to: "scheduler/anthropic_client.py::get_anthropic_client"
      via: "from anthropic_client import get_anthropic_client; client = get_anthropic_client('seva', timeout=60.0)"
      pattern: "get_anthropic_client\\(\"?'?seva'?\"?"
    - from: "scheduler/agents/daily_summary.py::run_juno_daily_summary"
      to: "scheduler/anthropic_client.py::get_anthropic_client"
      via: "client = get_anthropic_client('juno', timeout=JUNO_SONNET_TIMEOUT)"
      pattern: "get_anthropic_client\\(\"?'?juno'?\"?"
    - from: "scheduler/agents/weekly_sweeper.py::run_weekly_sweeper"
      to: "scheduler/anthropic_client.py::get_anthropic_client"
      via: "client = get_anthropic_client('seva', timeout=SONNET_TIMEOUT_S)"
      pattern: "get_anthropic_client\\(\"?'?seva'?\"?"
    - from: "scheduler/agents/content_agent.py::_do_fetch"
      to: "scheduler/anthropic_client.py::get_anthropic_client"
      via: "anthropic_client = get_anthropic_client('seva', timeout=30.0)"
      pattern: "get_anthropic_client\\(\"?'?seva'?\"?.*30"
---

<objective>
Refactor every production Anthropic instantiation site in the scheduler to route through the Plan-01 resolver, AND surgically delete the dead Anthropic-using functions inside `content_agent.py` (per D-06). After this plan:

1. Every `AsyncAnthropic(api_key=...)` call site in `scheduler/agents/` is gone — replaced by `get_anthropic_client(<tenant>, timeout=<existing-timeout>)`.
2. The Juno UAT script (`scheduler/scripts/uat_voice_calibration.py`) routes through the resolver too (preserves attribution if operator re-fires for sample synthesis).
3. The dead functions `check_compliance`, `is_gold_relevant_or_systemic_shock`, and `review` are deleted from `content_agent.py` — their only callers were the test file and each other. The LIVE exports `fetch_stories` and `deduplicate_stories` are PRESERVED verbatim.
4. The corresponding test functions in `test_content_agent.py` are deleted (lines covering review, check_compliance, is_gold_relevant_or_systemic_shock).

Purpose: This is the Phase 12 substance. Plan 01 created the contract; Plan 02 consumes it. After this plan, Seva's Sonnet+Haiku calls bill to Seva's key and Juno's Sonnet+Haiku calls bill to Juno's key (once operator sets the per-tenant Railway env vars). The fallback path (per-tenant unset) preserves local-dev and gives zero-deploy-gap rollout.

Output: 5 files modified, ~6 instantiation lines changed, 3 dead functions deleted, ~30 dead-code tests removed. Net: scheduler test count goes DOWN by ~30 (the deleted tests) and the scheduler line count goes DOWN by ~220 (the deleted functions); the surviving full-suite pytest stays GREEN.

Honors locked decisions: D-06 (dead-code surgical excision, preserve live exports), D-07 (hardcoded company_id literal at instantiation sites), D-07a (downstream agents like juno_relevance.py / juno_refusal_detector.py / ontario_law.py keep their caller-injected `client: AsyncAnthropic` parameter — NOT modified in this task), D-09 (exact 3 production sites + their exact timeout passthrough).

**Deviation flagged for the executor:** CONTEXT.md D-06 enumerates the dead sites as lines 428, 483, 1108 inside "review() and related draft-validation functions." On verification grep, line 1108 lives inside `_do_fetch` which is the engine of the LIVE `fetch_stories()` export (called by `daily_summary.py:36` import + Juno daily summary). Line 1108 is therefore a REFACTOR target (Seva, timeout=30.0) — NOT a deletion target. Lines 428 (inside `check_compliance`) and 483 (inside `is_gold_relevant_or_systemic_shock`) are the genuinely dead sites. This deviation is documented in the SUMMARY at plan close — the substantive change is that Plan 02 refactors 4 production sites (not 3) AND deletes 2 dead functions (not 3). The grep gate result is identical: zero `AsyncAnthropic(` outside the resolver after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-tenant-anthropic-api-key/12-CONTEXT.md
@.planning/phases/12-per-tenant-anthropic-api-key/12-01-PLAN.md
@.planning/phases/12-per-tenant-anthropic-api-key/12-01-SUMMARY.md

# Source-of-truth files this plan modifies
@scheduler/anthropic_client.py
@scheduler/agents/daily_summary.py
@scheduler/agents/weekly_sweeper.py
@scheduler/agents/content_agent.py
@scheduler/scripts/uat_voice_calibration.py
@scheduler/tests/test_content_agent.py

<interfaces>
<!-- The Plan-01 resolver contract — referenced at every refactor site -->

From `scheduler/anthropic_client.py` (created in Plan 01):
```python
def get_anthropic_client(
    company_id: Literal["seva", "juno"],
    *,
    timeout: float = 60.0,
) -> AsyncAnthropic:
    """Resolve the per-tenant Anthropic client for `company_id`.
    Returns cached instance for same (company_id, timeout).
    Falls back to shared ANTHROPIC_API_KEY with WARN-once when per-tenant unset.
    Raises RuntimeError if anthropic_resolver_strict=True and per-tenant unset.
    """
```

Current call sites being refactored (exact line numbers from grep):

`scheduler/agents/daily_summary.py:583` — Seva daily summary:
```python
anthropic_client = AsyncAnthropic(
    api_key=settings.anthropic_api_key, timeout=60.0,
)
```

`scheduler/agents/daily_summary.py:1226` — Juno daily summary:
```python
anthropic_client = AsyncAnthropic(
    api_key=settings.anthropic_api_key, timeout=JUNO_SONNET_TIMEOUT,
)
# JUNO_SONNET_TIMEOUT = 60.0 is a module-level constant at line 93
```

`scheduler/agents/weekly_sweeper.py:399` — Seva weekly sweeper:
```python
anthropic_client = AsyncAnthropic(
    api_key=settings.anthropic_api_key,
    timeout=SONNET_TIMEOUT_S,  # P6 — 60s
)
# SONNET_TIMEOUT_S = 60.0 is a module-level constant at line 66
```

`scheduler/agents/content_agent.py:1108` — Seva fetch_stories engine (LIVE; was misclassified as dead in CONTEXT.md):
```python
anthropic_client = AsyncAnthropic(
    api_key=settings.anthropic_api_key, timeout=30.0
)
```

`scheduler/scripts/uat_voice_calibration.py:377` — Juno UAT one-shot script (extra site not in CONTEXT.md; surfaced by grep):
```python
client = AsyncAnthropic(
    api_key=settings.anthropic_api_key, timeout=JUNO_SONNET_TIMEOUT,
)
# JUNO_SONNET_TIMEOUT imported at top of file
```

Dead functions to delete (lines from grep):
- `content_agent.py` lines 417-441 (~25 lines): `async def check_compliance(text: str, anthropic_client=None) -> bool` — only caller is `review()` (also dead) and tests
- `content_agent.py` lines 444-621 (~178 lines): `async def is_gold_relevant_or_systemic_shock(story, config, client=None) -> dict` — only callers are tests
- `content_agent.py` lines 1277-1304 (~28 lines): `async def review(draft) -> dict` — only callers are tests
- Total deleted: ~231 lines of agent code + ~30 test functions

Functions to PRESERVE verbatim (D-06):
- `content_agent.py::fetch_stories` (line 1175) — imported by `daily_summary.py:36`
- `content_agent.py::deduplicate_stories` (line 214) — imported by `weekly_sweeper.py:47`
- `content_agent.py::_do_fetch` (line 1097) — internal engine of fetch_stories; refactored line 1108 but function preserved
- `content_agent.py::classify_format_lightweight` (line 268), `_score_relevance` (line 1034), all other helpers — preserved
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor 3 daily_summary + weekly_sweeper Anthropic call sites</name>
  <files>scheduler/agents/daily_summary.py, scheduler/agents/weekly_sweeper.py</files>
  <read_first>
    - scheduler/anthropic_client.py (the resolver — verify the exact import statement and function signature)
    - scheduler/agents/daily_summary.py lines 1-70 (top imports — to find the correct insertion point for the new import; existing imports section ends around line 69-70)
    - scheduler/agents/daily_summary.py lines 575-595 (Seva instantiation site at line 583)
    - scheduler/agents/daily_summary.py lines 1218-1240 (Juno instantiation site at line 1226)
    - scheduler/agents/daily_summary.py lines 85-100 (the JUNO_SONNET_TIMEOUT = 60.0 module constant at line 93 — confirm it stays unchanged)
    - scheduler/agents/weekly_sweeper.py lines 1-70 (top imports — line 43 has `from anthropic import AsyncAnthropic`)
    - scheduler/agents/weekly_sweeper.py lines 390-410 (instantiation site at line 399)
    - scheduler/agents/weekly_sweeper.py lines 60-70 (SONNET_TIMEOUT_S = 60.0 at line 66)
  </read_first>
  <behavior>
    - Test (regression): all existing `scheduler/tests/agents/test_daily_summary.py` tests pass unchanged. The tests use `patch("agents.daily_summary.fetch_stories", AsyncMock(...))` etc. and do NOT inspect the Anthropic client construction directly, so swapping the constructor for a resolver call is invisible to them. Verified via the grep `patch.*AsyncAnthropic\|patch.*get_anthropic_client` in test_daily_summary.py — neither pattern appears.
    - Test (regression): `scheduler/tests/test_weekly_sweeper.py` passes unchanged. Same reasoning.
    - Behaviour preserved: the `AsyncAnthropic` instance the agents receive is functionally identical to today's — same api_key (when per-tenant unset → falls back to shared per Plan 01 D-01), same timeout passed through. The ONLY change is the construction path goes via `get_anthropic_client(...)`.
  </behavior>
  <action>
    **Change A: scheduler/agents/daily_summary.py**

    A1. Remove the direct AsyncAnthropic import. Find line 30 `from anthropic import AsyncAnthropic` and DELETE it. The resolver returns an `AsyncAnthropic`, so any downstream typing reference (e.g. function signatures using `AsyncAnthropic` as a type hint) needs the import — but check first: grep `AsyncAnthropic` inside `daily_summary.py` AFTER you delete the line-583 + line-1226 instantiations; if no remaining references exist, the import deletion stands. If `AsyncAnthropic` is still referenced (e.g. as a type hint for a helper parameter), keep the import.

    A2. Add the resolver import. After the existing `from agents.content_agent import fetch_stories` line (around line 36), add a new line:
    ```python
    from anthropic_client import get_anthropic_client
    ```
    Place it alphabetically among the `from ...` import block — between `agents.` imports and `companies.` imports if both exist; readability over strict ordering.

    A3. Replace the Seva instantiation at line ~583. Find this block:
    ```python
    anthropic_client = AsyncAnthropic(
        api_key=settings.anthropic_api_key, timeout=60.0,
    )
    ```
    Replace with EXACTLY:
    ```python
    # v3.1 Phase 12 — Seva Sonnet routes through per-tenant resolver (D-07, D-09).
    anthropic_client = get_anthropic_client("seva", timeout=60.0)
    ```
    Preserve the surrounding `quick-260514-ii6` comment about the 60s timeout (lines ~576-582) — those lines explain WHY timeout is 60.0; the comment stays.

    A4. Replace the Juno instantiation at line ~1226. Find this block:
    ```python
    anthropic_client = AsyncAnthropic(
        api_key=settings.anthropic_api_key, timeout=JUNO_SONNET_TIMEOUT,
    )
    ```
    Replace with EXACTLY:
    ```python
    # v3.1 Phase 12 — Juno Sonnet routes through per-tenant resolver (D-07, D-09).
    anthropic_client = get_anthropic_client("juno", timeout=JUNO_SONNET_TIMEOUT)
    ```
    Do NOT modify the `JUNO_SONNET_TIMEOUT = 60.0` constant at line 93 — it remains the module-level timeout symbol for the Juno fire.

    A5. Verify `settings = get_settings()` lines immediately preceding the instantiations are NO LONGER NEEDED for the Anthropic construction. The `get_settings()` call is likely still needed because the surrounding code uses `settings.serpapi_api_key` etc. — DO NOT remove `settings = get_settings()` unless it has zero other references in the function. Check via grep around the function: if `settings.` appears in only the Anthropic line you just replaced, the `settings = get_settings()` line can be removed; otherwise preserve it.

    **Change B: scheduler/agents/weekly_sweeper.py**

    B1. Remove `from anthropic import AsyncAnthropic` at line 43, UNLESS other usages remain (grep `AsyncAnthropic` in the file after the swap). The file has only one instantiation site (line 399), so the import is removable if no type hints reference it.

    B2. Add `from anthropic_client import get_anthropic_client` in the imports block — alphabetically among the `from agents.` imports (around line 47-48).

    B3. Replace the instantiation at line ~399. Find:
    ```python
    anthropic_client = AsyncAnthropic(
        api_key=settings.anthropic_api_key,
        timeout=SONNET_TIMEOUT_S,  # P6 — 60s
    )
    ```
    Replace with EXACTLY:
    ```python
    # v3.1 Phase 12 — Seva sweeper Sonnet routes through per-tenant resolver (D-07, D-09).
    anthropic_client = get_anthropic_client("seva", timeout=SONNET_TIMEOUT_S)
    ```
    Do NOT change `SONNET_TIMEOUT_S = 60.0` at line 66.

    Hardcoded literal rule (D-07): use the string literal `"seva"` (or `"juno"` for Juno sites). DO NOT extract a variable `company_id = "seva"` and pass that — D-07 explicitly mandates hardcoded literals at instantiation sites so a typo'd variable from another scope cannot accidentally route to the wrong key. The Literal type in the resolver enforces this statically as well.

    Note for the implementer: `daily_summary.py` line 24 banner comment says `weekly_sweeper.py` mentions `AsyncAnthropic(timeout=60.0)` in a docstring/comment (line 24 of weekly_sweeper.py is `P6  (Sonnet timeout missing)   — AsyncAnthropic(timeout=60.0)` — that's a P6 historical note inside the module docstring). DO NOT modify that historical note; it documents the past pitfall, not a current call site. The grep gate (Plan 03) targets ACTUAL `AsyncAnthropic(` calls, not historical comments — but to be safe Plan 03 uses a pattern that excludes commented lines.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/agents/test_daily_summary.py tests/test_weekly_sweeper.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AsyncAnthropic(" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns 0
    - `grep -c "AsyncAnthropic(" /Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py` returns 0
    - `grep -c "get_anthropic_client(\"seva\"" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns 1
    - `grep -c "get_anthropic_client(\"juno\"" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns 1
    - `grep -c "get_anthropic_client(\"seva\"" /Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py` returns 1
    - `grep -c "from anthropic_client import get_anthropic_client" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns 1
    - `grep -c "from anthropic_client import get_anthropic_client" /Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py` returns 1
    - `grep -c "JUNO_SONNET_TIMEOUT = 60.0" /Users/matthewnelson/seva-mining/scheduler/agents/daily_summary.py` returns 1 (module constant preserved)
    - `grep -c "SONNET_TIMEOUT_S = 60.0" /Users/matthewnelson/seva-mining/scheduler/agents/weekly_sweeper.py` returns 1 (module constant preserved)
    - `cd scheduler && uv run pytest tests/agents/test_daily_summary.py -q` exits 0
    - `cd scheduler && uv run pytest tests/test_weekly_sweeper.py -q` exits 0
    - `cd scheduler && uv run ruff check agents/daily_summary.py agents/weekly_sweeper.py` exits 0
    - `cd scheduler && uv run python -c "from agents.daily_summary import run_daily_summary, run_juno_daily_summary; from agents.weekly_sweeper import run_weekly_sweeper; print('imports OK')"` exits 0 (module-load smoke test)
  </acceptance_criteria>
  <done>3 production sites (daily_summary Seva, daily_summary Juno, weekly_sweeper Seva) now route through resolver; existing test suites for these modules pass unchanged; no orphan `AsyncAnthropic(` left in either file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor content_agent.py — refactor LIVE _do_fetch site + delete 2 dead functions + remove their tests</name>
  <files>scheduler/agents/content_agent.py, scheduler/tests/test_content_agent.py</files>
  <read_first>
    - scheduler/anthropic_client.py (resolver contract)
    - scheduler/agents/content_agent.py FULL FILE (1304 lines — but specifically lines 1-30 for imports, lines 417-441 for `check_compliance` to delete, lines 444-621 for `is_gold_relevant_or_systemic_shock` to delete, lines 1097-1175 for `_do_fetch` to REFACTOR and the boundary with `fetch_stories`, lines 1277-1304 for `review` to delete)
    - scheduler/tests/test_content_agent.py FULL FILE (specifically lines 414-460 for review tests to delete, lines 515-545 for check_compliance tests to delete, lines 709-895 for is_gold_relevant_or_systemic_shock tests to delete — exact line ranges may shift; the executor must grep `def test_review_\|def test_check_compliance\|def test_check_compliance_passes_when_no_seva\|test_is_gold_relevant_or_systemic_shock` to find every test function tied to the deleted production functions)
    - .planning/phases/12-per-tenant-anthropic-api-key/12-CONTEXT.md D-06 (dead-code surgical excision; preserve fetch_stories + deduplicate_stories)
    - CLAUDE.md historical note 2026-04-21 quick-260421-k9z (the dead-code-strip precedent that orphaned the now-targeted functions)
  </read_first>
  <behavior>
    - Behavior preserved for LIVE pipeline: `fetch_stories()` and `deduplicate_stories()` behave identically before/after this task. The only internal change is `_do_fetch` constructs its `anthropic_client` via `get_anthropic_client('seva', timeout=30.0)` instead of `AsyncAnthropic(api_key=..., timeout=30.0)`. Existing `test_content_agent.py::test_fetch_stories_*` tests pass unchanged.
    - Behavior deleted (intentional): `review()`, `check_compliance()`, and `is_gold_relevant_or_systemic_shock()` no longer exist. Calling `from agents.content_agent import review` raises ImportError. Their corresponding tests are also deleted.
    - Test count delta: `test_content_agent.py` test count goes DOWN by exactly the number of `test_review_*` + `test_check_compliance*` + `test_is_gold_relevant_or_systemic_shock*` test functions. Surviving tests (fetch_stories, deduplicate_stories, classify_format_lightweight, scoring helpers) pass unchanged.
  </behavior>
  <action>
    **Change A: scheduler/agents/content_agent.py — refactor LIVE _do_fetch site**

    A1. Find the imports block at the top of the file. Add `from anthropic_client import get_anthropic_client` to the imports (alphabetically among the existing `from ...` lines or grouped with the other internal scheduler imports — match the file's existing style).

    A2. Check whether `from anthropic import AsyncAnthropic` (likely near the top of the file with other third-party imports) still has references AFTER you delete the dead functions in step B below. The dead functions use AsyncAnthropic (lines 428, 483). After deletion, the only AsyncAnthropic references are: line 1108 (this task refactors it away), function signature type hints like `_score_relevance(..., client: AsyncAnthropic)` at line 1034, and `classify_format_lightweight(..., *, client)` at line 268 (no type hint there — verify). If `AsyncAnthropic` is still referenced AS A TYPE HINT after refactor + dead-code-delete, KEEP the import. Otherwise DELETE the import line.

    A3. Refactor `_do_fetch` line 1108. Find this block (currently at line 1097-1110):
    ```python
    async def _do_fetch(bucket: int) -> list[dict]:
        """..."""
        settings = get_settings()
        # Per-request timeout keeps individual Anthropic calls bounded at 30 s.
        # With scoring now parallelised via asyncio.gather the worst-case wall
        # time for the whole fetch drops from ~118 × 30 s = 59 min to ~30 s.
        anthropic_client = AsyncAnthropic(
            api_key=settings.anthropic_api_key, timeout=30.0
        )
    ```
    Replace the `anthropic_client = AsyncAnthropic(...)` block (just those 3 lines) with EXACTLY:
    ```python
        # v3.1 Phase 12 — fetch_stories powers Seva-only daily summary scoring;
        # routes through per-tenant resolver (D-07, D-09 + planner-flagged
        # LIVE-site reclassification of line 1108 from CONTEXT.md's dead list).
        anthropic_client = get_anthropic_client("seva", timeout=30.0)
    ```
    Preserve the existing two-line `# Per-request timeout...` comment above it (explains the 30s timeout rationale).

    A4. Check if `settings = get_settings()` at line 1104 has any other references inside `_do_fetch`. Inspection shows line 1112 uses `settings.serpapi_api_key`. So `settings = get_settings()` STAYS. Only the `AsyncAnthropic(...)` line changes.

    **Change B: scheduler/agents/content_agent.py — delete 2 dead functions**

    B1. Delete `check_compliance` (currently lines 417-441; verify line range with grep `^async def check_compliance` and the next `^async def \|^def ` boundary). Delete the entire function including its docstring and trailing blank line. Net deletion: ~25 lines.

    B2. Delete `is_gold_relevant_or_systemic_shock` (currently lines 444-621; verify range with grep `^async def is_gold_relevant_or_systemic_shock` and next `^async def \|^def ` boundary). Net deletion: ~178 lines.

    B3. Delete `review` (currently lines 1277-1304; the LAST async function in the file). Verify with grep `^async def review\(draft: dict\)` and EOF. Net deletion: ~28 lines.

    B4. After deletions, verify there are NO remaining references inside content_agent.py to the deleted symbols: `grep -n "check_compliance\|is_gold_relevant_or_systemic_shock\|\\breview\\b" scheduler/agents/content_agent.py` should return only the docstring-mention contexts (if any) — NO live function calls. The `# review-service-only` comment in the module docstring (if present) should be UPDATED or REMOVED since the review service is being deleted.

    B5. Check the module docstring at the top of `content_agent.py`. If it claims content_agent provides "fetch_stories, deduplicate_stories, AND review" as the public surface, update the docstring to reflect the new public surface (drop the `review` mention). If it doesn't list a surface, no update needed.

    **Change C: scheduler/tests/test_content_agent.py — delete tests for deleted functions**

    C1. The current test_content_agent.py module docstring (lines 1-17) says "This file now covers only the public surface the 7 content sub-agents depend on: fetch_stories, review(draft), classify_format_lightweight." Update line 9 to remove the `review(draft)` bullet. The new docstring should list only `fetch_stories` and `classify_format_lightweight` as the per-file surface (scoring/dedup helpers remain covered inline).

    C2. Delete every test function that targets the deleted production functions. Grep for the relevant patterns to find them all:
    - `grep -n "^async def test_review_\|^def test_review_" scheduler/tests/test_content_agent.py` — delete all matches and their function bodies. From the file scan: at least `test_review_pass_returns_compliance_passed_true` (line 419), `test_review_fail_returns_compliance_passed_false` (line 429), `test_review_empty_draft_treated_as_pass` (line 439). Delete each function in its entirety (decorator + signature + body + blank line after).
    - `grep -n "^async def test_check_compliance\|^def test_check_compliance" scheduler/tests/test_content_agent.py` — delete all matches. From the scan: `test_check_compliance_short_circuits_on_seva_mining` (line 515 area), and any other `test_check_compliance_*` function. Delete each entirely.
    - `grep -n "^async def test_is_gold_relevant\|^def test_is_gold_relevant" scheduler/tests/test_content_agent.py` — delete all matches. From the scan: many `test_is_gold_relevant_or_systemic_shock_*` test functions in the lines 709-895 region. Delete each entirely.

    C3. Also delete any test-file-level helpers (fixtures, helper functions, helper constants) that are EXCLUSIVELY used by the now-deleted tests. After deleting test bodies, grep each helper symbol in the file; if zero references remain post-deletion, delete the helper too. Examples to inspect: any `_mock_haiku_response` helper used only by review/compliance tests, any fixture decorated with `@pytest.fixture` that only the deleted tests consume.

    C4. Section-header comment cleanup: the file has section banner comments like `# review(draft) — Haiku compliance gate` (line 414 area). Delete the section banners for the deleted-section blocks. The file's surviving structure should have section banners only for: fetch_stories tests, deduplicate_stories tests, classify_format_lightweight tests, scoring helpers tests.

    Notes for the implementer:
    - This is a SURGICAL excision — when in doubt, preserve over delete. The only intentional deletions are the 3 dead functions + the test functions that EXCLUSIVELY exercise them. Any helper that has at least one surviving non-deleted reference STAYS.
    - DO NOT touch `fetch_stories`, `deduplicate_stories`, `classify_format_lightweight`, `_score_relevance`, `_keyword_relevance`, `recency_score`, `credibility_score`, `calculate_story_score`, `_fetch_all_rss`, `_fetch_all_serpapi`, `parse_rss_entries`, `parse_serpapi_results`, `fetch_article`, `extract_article_text`, `search_corroborating`, `build_no_story_bundle`, `build_draft_item`, `_extract_check_text`, or `fetch_analytical_historical_stories`. These are LIVE or LIVE-adjacent surface.
    - `_extract_check_text` (line 772) was previously called by `review()`. After deleting `review()`, check if `_extract_check_text` has any other callers via `grep -rn "_extract_check_text" scheduler/`. If zero non-test callers remain, ALSO delete `_extract_check_text` and its corresponding test (if any). If any caller exists (e.g. an inline call in a sub-agent for compliance text extraction), preserve it.
    - The scheduler's `pytest` collection step is the integration-level safety net — if any test outside `test_content_agent.py` imports `review` or `check_compliance` or `is_gold_relevant_or_systemic_shock`, `pytest -q` will fail at collection time with ImportError. Grep verification before commit: `grep -rn "from agents.content_agent import.*\\(review\\|check_compliance\\|is_gold_relevant\\)\\|content_agent\\.\\(review\\|check_compliance\\|is_gold_relevant\\)" /Users/matthewnelson/seva-mining/scheduler/ /Users/matthewnelson/seva-mining/backend/` should return ZERO hits after this task.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/test_content_agent.py tests/agents/test_daily_summary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AsyncAnthropic(" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 0
    - `grep -c "get_anthropic_client(\"seva\".*timeout=30.0" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 1
    - `grep -c "from anthropic_client import get_anthropic_client" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 1
    - `grep -cE "^async def (check_compliance|is_gold_relevant_or_systemic_shock|review)\b" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 0
    - `grep -c "^async def fetch_stories" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 1 (PRESERVED)
    - `grep -c "^def deduplicate_stories" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 1 (PRESERVED)
    - `grep -c "^async def _do_fetch" /Users/matthewnelson/seva-mining/scheduler/agents/content_agent.py` returns 1 (PRESERVED — internal engine of fetch_stories)
    - `grep -cE "^(async )?def test_(review|check_compliance|is_gold_relevant)" /Users/matthewnelson/seva-mining/scheduler/tests/test_content_agent.py` returns 0
    - `grep -c "fetch_stories" /Users/matthewnelson/seva-mining/scheduler/tests/test_content_agent.py` returns at least 3 (PRESERVED — fetch_stories tests remain)
    - Zero leftover imports of deleted symbols: `grep -rn "from agents.content_agent import.*\\(review\\|check_compliance\\|is_gold_relevant\\)\\|content_agent\\.\\(review\\|check_compliance\\|is_gold_relevant\\)" /Users/matthewnelson/seva-mining/scheduler/ /Users/matthewnelson/seva-mining/backend/` returns 0 hits
    - `cd scheduler && uv run pytest tests/test_content_agent.py -q` exits 0
    - `cd scheduler && uv run pytest -q` exits 0 (full suite; the only test count delta is the deliberately deleted dead-code tests)
    - `cd scheduler && uv run ruff check agents/content_agent.py tests/test_content_agent.py` exits 0
    - Module-load smoke: `cd scheduler && uv run python -c "from agents.content_agent import fetch_stories, deduplicate_stories; print('live exports OK')"` exits 0
  </acceptance_criteria>
  <done>LIVE `_do_fetch` site routes through resolver; 3 dead functions + their tests excised; live exports + their tests preserved; full scheduler suite GREEN (with expected reduction in test count from dead-code-test removal).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor scheduler/scripts/uat_voice_calibration.py to use resolver</name>
  <files>scheduler/scripts/uat_voice_calibration.py</files>
  <read_first>
    - scheduler/anthropic_client.py (resolver contract)
    - scheduler/scripts/uat_voice_calibration.py FULL FILE (~400 lines; particularly the top of file for imports/sys.path setup, and the line-377 instantiation site)
    - .planning/phases/12-per-tenant-anthropic-api-key/12-CONTEXT.md D-08 (grep gate exemption list — scripts/ is NOT exempted, so this site must refactor)
  </read_first>
  <behavior>
    - Behavior preserved: running `cd scheduler && uv run python scripts/uat_voice_calibration.py` still produces the markdown UAT report on stdout. The only change is that the Anthropic client is now resolved via `get_anthropic_client('juno', timeout=JUNO_SONNET_TIMEOUT)` — which (a) requires the resolver module to be importable from the script's working directory, and (b) defaults to fallback (shared key) when `JUNO_ANTHROPIC_API_KEY` is unset (operator-friendly local-dev behavior unchanged).
    - No tests cover this script (it's a one-shot UAT operator tool); the verification is that the script still imports + executes without error against a mocked Anthropic.
  </behavior>
  <action>
    A1. Read the script's top sys.path setup. It already does (lines ~26-30):
    ```python
    SCHEDULER_DIR = Path(__file__).resolve().parent.parent
    sys.path.insert(0, str(SCHEDULER_DIR))
    ```
    So `from anthropic_client import get_anthropic_client` will work after the sys.path insert. Confirm by checking the existing `from config import get_settings` and `from agents.juno_relevance import ...` imports below the sys.path line — same import pattern.

    A2. Add `from anthropic_client import get_anthropic_client` to the import block AFTER the `sys.path.insert(...)` line and AFTER the dotenv-load block (if present). Place it alphabetically among the `from ...` imports.

    A3. Decide on the `from anthropic import AsyncAnthropic` import (currently somewhere near the top). If after the refactor it's still referenced as a type hint anywhere in the script, KEEP. Otherwise DELETE.

    A4. Replace the instantiation at line ~377. Find:
    ```python
    client = AsyncAnthropic(
        api_key=settings.anthropic_api_key, timeout=JUNO_SONNET_TIMEOUT,
    )
    ```
    Replace with EXACTLY:
    ```python
    # v3.1 Phase 12 — Juno UAT script routes through per-tenant resolver so
    # operator re-fires post-deploy attribute to JUNO_ANTHROPIC_API_KEY when set
    # (graceful fallback to ANTHROPIC_API_KEY when unset).
    client = get_anthropic_client("juno", timeout=JUNO_SONNET_TIMEOUT)
    ```

    A5. Verify `JUNO_SONNET_TIMEOUT` is already imported at the top of the file (from `agents.daily_summary` likely). If not, add the import.

    A6. Verify the `settings = get_settings()` line at ~371 is still needed by lines BELOW the refactor. If the only `settings.` usage was on the deleted Anthropic line (unlikely — the script reads other settings), preserve it. If yes, delete the now-orphan line.

    Notes for the implementer:
    - This is the ONE site CONTEXT.md missed. The 3-line CONTEXT enumeration of refactor sites (D-09) was based on grep `scheduler/agents/`; the planner's broader grep `scheduler/` surfaced this 4th site in `scheduler/scripts/`. Documenting in the plan summary: "Plan 02 added uat_voice_calibration.py as a 4th refactor target — CONTEXT.md's 3-site enumeration was scoped to `scheduler/agents/` and missed the `scheduler/scripts/` one-shot. Decision: refactor (not exempt) because attribution accuracy matters even for ad-hoc UAT fires, and Plan 03's grep gate scans `scheduler/` not just `scheduler/agents/`."
    - The script is invoked manually (operator runs it during voice calibration). It's NOT scheduled. Refactor preserves the operator-friendly fallback-to-shared-key behavior.
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run python -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path('.').resolve())); import scripts.uat_voice_calibration; print('uat script imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AsyncAnthropic(" /Users/matthewnelson/seva-mining/scheduler/scripts/uat_voice_calibration.py` returns 0
    - `grep -c "get_anthropic_client(\"juno\".*JUNO_SONNET_TIMEOUT" /Users/matthewnelson/seva-mining/scheduler/scripts/uat_voice_calibration.py` returns 1
    - `grep -c "from anthropic_client import get_anthropic_client" /Users/matthewnelson/seva-mining/scheduler/scripts/uat_voice_calibration.py` returns 1
    - `cd scheduler && uv run ruff check scripts/uat_voice_calibration.py` exits 0
    - Module-load smoke: `cd scheduler && uv run python -c "import sys; sys.path.insert(0, '.'); from scripts import uat_voice_calibration; print(hasattr(uat_voice_calibration, 'main'))"` returns `True` (script's main() symbol still defined)
  </acceptance_criteria>
  <done>UAT script's Anthropic client is resolved via the resolver; script still imports cleanly; ruff-clean. Operator's manual UAT fire workflow unchanged.</done>
</task>

</tasks>

<verification>
After all 3 tasks land in this plan:

1. `cd scheduler && uv run pytest -q` — full scheduler regression suite GREEN. Expected test count: Phase 11 baseline (331+) MINUS the deleted dead-code tests (likely ~25-30 tests across review/check_compliance/is_gold_relevant_or_systemic_shock).
2. `grep -rnE "AsyncAnthropic\\(|Anthropic\\(api_key=" /Users/matthewnelson/seva-mining/scheduler/` — returns hits ONLY in `scheduler/anthropic_client.py` (the resolver). Zero hits in `scheduler/agents/`, `scheduler/scripts/`. (Tests dir may have mock-related references — those are exempted by Plan 03's grep gate whitelist.)
3. `cd scheduler && uv run ruff check .` exits 0
4. Smoke imports succeed:
   - `cd scheduler && uv run python -c "from agents.daily_summary import run_daily_summary, run_juno_daily_summary; from agents.weekly_sweeper import run_weekly_sweeper; from agents.content_agent import fetch_stories, deduplicate_stories; print('all live entry points import OK')"`
5. Negative smoke (confirms dead-code excision): `cd scheduler && uv run python -c "from agents.content_agent import review"` exits with ImportError (review function deleted).
6. Manual operator dry-run (optional, document in SUMMARY): with both `SEVA_ANTHROPIC_API_KEY` and `JUNO_ANTHROPIC_API_KEY` set in `.env`, run `cd scheduler && uv run python -c "import asyncio; from agents.daily_summary import run_daily_summary; asyncio.run(run_daily_summary())"` — should succeed; check logs for `anthropic_call company=seva key_fingerprint=...` line.

**Plan 03 cannot start until all 6 above pass.** Plan 03's grep gate script will fail-loudly the moment ANY `AsyncAnthropic(` survives outside the resolver — Plan 02's job is to leave the codebase in a state where the gate exits 0.
</verification>

<success_criteria>
1. 4 production instantiation sites refactored: daily_summary.py (Seva), daily_summary.py (Juno), weekly_sweeper.py (Seva), content_agent.py::_do_fetch (Seva)
2. 1 UAT script refactored: scripts/uat_voice_calibration.py (Juno)
3. 2 dead functions deleted from content_agent.py: check_compliance, is_gold_relevant_or_systemic_shock, review (3 functions total — CONTEXT.md's "3 dead sites" is preserved by count even though one of them was actually a refactor not a delete; another two were standalone dead functions whose sites were 428 and 483)
4. All test functions exclusively covering the deleted functions are removed from test_content_agent.py
5. Zero `AsyncAnthropic(` calls outside the resolver in `scheduler/agents/` or `scheduler/scripts/`
6. Live exports `fetch_stories()` + `deduplicate_stories()` preserved verbatim; their tests preserved
7. Full scheduler regression suite GREEN; ruff-clean across all 5 modified files
8. Smoke import test: every live agent entry point (run_daily_summary, run_juno_daily_summary, run_weekly_sweeper, fetch_stories, deduplicate_stories) imports cleanly post-refactor
</success_criteria>

<output>
After completion, create `.planning/phases/12-per-tenant-anthropic-api-key/12-02-SUMMARY.md` documenting:
- Files touched (5): daily_summary.py, weekly_sweeper.py, content_agent.py, uat_voice_calibration.py, test_content_agent.py
- Refactor count: 5 sites (3 in CONTEXT.md D-09 + 1 LIVE site in content_agent.py at line 1108 + 1 surfaced UAT script in scheduler/scripts/)
- Dead-code excision: 3 functions deleted from content_agent.py (~231 lines); ~25-30 test functions deleted from test_content_agent.py
- Tenant routing summary:
    - Seva sites: 3 (daily_summary.py line ~583, weekly_sweeper.py line ~399, content_agent.py _do_fetch line ~1108)
    - Juno sites: 2 (daily_summary.py line ~1226, uat_voice_calibration.py line ~377)
- Test count delta: scheduler suite was 331+ pre-Phase 12; after Plan 01 it's 336+ (5 new resolver tests); after Plan 02 it's ~305-310 (minus deleted dead-code tests). Document the exact final count in this SUMMARY.
- Decisions honored: D-06 (dead-code surgical excision; preserved live exports), D-07 (hardcoded company_id literals at every instantiation site), D-07a (downstream agents like juno_relevance.py / juno_refusal_detector.py / ontario_law.py NOT modified — they keep the caller-injected client pattern), D-09 (3-site enumeration honored, with documented +1 deviation each direction: +1 from CONTEXT-missed UAT script, +1 from CONTEXT-misclassified content_agent.py line 1108)
- Deviations from CONTEXT.md (planner-flagged in objective, executor-confirmed by grep):
    1. Line 1108 of content_agent.py is LIVE (inside `_do_fetch` → `fetch_stories()` LIVE export), not dead as CONTEXT.md stated. Refactored as Seva site instead of deleted.
    2. `scheduler/scripts/uat_voice_calibration.py:377` not in CONTEXT.md's enumeration; surfaced by the broader scheduler-wide grep. Refactored as Juno site (preserves attribution for operator UAT fires).
- Next plan: 12-03 (CI grep gate + worker.py env-var status logging extension).
</output>
