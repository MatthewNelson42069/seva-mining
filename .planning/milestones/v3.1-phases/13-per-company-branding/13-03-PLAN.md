---
phase: 13-per-company-branding
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - frontend/src/components/layout/__tests__/AppHeader.test.tsx
  - frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - BRAND-05
must_haves:
  truths:
    - "Existing 4 Seva AppHeader tests pass byte-identically (D-09 contract held)"
    - "New AppHeader.test.tsx Juno tests render 'Juno Industries' wordmark + 'J' brand-mark on /juno route AND assert 'Seva Mining' is NOT present"
    - "New AppHeader.brand.test.tsx FOWB test asserts dataset.company + wordmark + mark letter + document.title all flip synchronously inside a single act() — no waitFor, no async wait"
    - "AppHeader.brand.test.tsx cleanup-on-unmount test asserts dataset.company reverts to 'seva' and document.title reverts to 'Seva Mining' (the /login revert safety net)"
    - "Grep gate: 0 hits for `company === 'juno'` in non-test component files (D-10 branchlessness enforced)"
    - "Full vitest suite green at Phase 12 baseline test count or higher (168+ tests)"
    - "Operator visual QA at 1440x900 confirms 10/10 items from UI-SPEC Operator Visual-QA Checklist (items 1, 2, 4, 5, 6, 7 MUST-PASS)"
  artifacts:
    - path: "frontend/src/components/layout/__tests__/AppHeader.test.tsx"
      provides: "Extended test file with 4 unchanged Seva tests + 2 new Juno tests"
      contains: "Juno Industries"
    - path: "frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx"
      provides: "New test file with 5 FOWB / cleanup / dataset / title / favicon tests"
      contains: "dataset.company"
  key_links:
    - from: "frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx"
      to: "frontend/src/components/layout/CompanyBrandEffect.tsx"
      via: "Component-under-test mount inside MemoryRouter"
      pattern: "CompanyBrandEffect"
    - from: "frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx"
      to: "document.documentElement.dataset"
      via: "JSDom global side-effect assertion"
      pattern: "document\\.documentElement\\.dataset\\.company"
---

<objective>
Lock down Phase 13's brand-resolution contract with three layers of verification:
1. Unit tests — Extend `AppHeader.test.tsx` with 2 Juno-default tests; create new `AppHeader.brand.test.tsx` with 5 FOWB / cleanup / dataset / title / favicon tests.
2. Grep gate — Enforce D-10 branchlessness (`company === 'juno'` returns 0 hits in non-test component files) + full vitest regression suite green at Phase 12 baseline.
3. Operator visual QA checkpoint — Human-verify the 10-item visual checklist from UI-SPEC Operator Visual-QA Checklist at 1440x900.

Purpose: This plan is the formal proof that Phase 13 ships without regression. The Wave 2 plan made the user-facing changes; this plan verifies them with sufficient specificity that future refactors will be caught by the regression net. The operator checkpoint is the final human gate — visual aesthetics (navy reads as institutional defence not tech-startup blue, FOWB-free transition) cannot be JSDom-verified.

Output: 1 modified test file + 1 new test file + a verified-green vitest run + operator sign-off on the visual QA checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-per-company-branding/13-CONTEXT.md
@.planning/phases/13-per-company-branding/13-UI-SPEC.md
@.planning/phases/13-per-company-branding/13-01-SUMMARY.md
@.planning/phases/13-per-company-branding/13-02-SUMMARY.md

# Code under test (Waves 1+2 outputs)
@frontend/src/components/layout/AppHeader.tsx
@frontend/src/components/layout/CompanyBrandEffect.tsx
@frontend/src/components/layout/__tests__/AppHeader.test.tsx
@frontend/src/config/companyBrandConfig.ts
@frontend/src/hooks/useCompanyBrand.ts

<interfaces>
<!-- Wave 2 outputs that the tests assert against -->

From frontend/src/components/layout/CompanyBrandEffect.tsx:
```
export function CompanyBrandEffect(): null
// Side effects on mount/update (when useParams<{company}>() resolves to 'seva' | 'juno'):
//   - document.documentElement.dataset.company = company
//   - document.title = companyBrandConfig[company].pageTitle
//   - <link rel='icon'>.href = companyBrandConfig[company].faviconHref
// Side effects on unmount:
//   - document.documentElement.dataset.company = 'seva'
//   - document.title = 'Seva Mining'
//   - favicon NOT restored (browser cache concern)
```

From frontend/src/components/layout/AppHeader.tsx (Wave 2 refactor):
```
// Reads useCompanyBrand() and renders brand.wordmark, brand.markLetter, brand.markBgClassName
// No `company === 'juno'` branches anywhere
```

Existing test mount pattern (frontend/src/components/layout/__tests__/AppHeader.test.tsx — to be EXTENDED):
```
function renderHeader() {
  const queryClient = new QueryClient()
  return render(
    <QueryClientProvider client={queryClient}>
      <MemoryRouter initialEntries={['/seva']}>
        <Routes>
          <Route path=":company/*" element={<AppHeader />} />
        </Routes>
      </MemoryRouter>
    </QueryClientProvider>,
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend AppHeader.test.tsx with 2 Juno-default tests (preserve 4 existing Seva tests byte-identically)</name>
  <files>
    frontend/src/components/layout/__tests__/AppHeader.test.tsx (MODIFIED — add 2 tests; preserve 4 existing)
  </files>
  <read_first>
    - .planning/phases/13-per-company-branding/13-UI-SPEC.md (Test File 1 matrix — 4 existing + 2 new)
    - .planning/phases/13-per-company-branding/13-CONTEXT.md (D-09 byte-identical regression contract)
    - frontend/src/components/layout/__tests__/AppHeader.test.tsx (current state — 4 existing tests; DO NOT alter them)
    - frontend/src/components/layout/AppHeader.tsx (Wave 2 refactor — reads useCompanyBrand())
    - frontend/src/hooks/useCompanyBrand.ts (resolution chain reads useParams; will resolve to 'juno' when MemoryRouter at `/juno`)
  </read_first>
  <action>
Extend `frontend/src/components/layout/__tests__/AppHeader.test.tsx` by ADDING two new tests (per UI-SPEC Test File 1 rows 5+6). The 4 existing tests at lines 46-67 MUST remain byte-identical — same test names, same assertions, same render setup. D-09 zero-regression contract.

Add a new helper function for the Juno-route render (parallel to the existing `renderHeader()` helper). Place it immediately after the existing `renderHeader()` definition. The body mirrors `renderHeader()` exactly except `initialEntries={['/juno']}` replaces `initialEntries={['/seva']}`.

Then add 2 new `it(...)` test cases inside the existing `describe('AppHeader', () => { ... })` block, AFTER the existing `clicking "Log out"` test. Use these exact test names (acceptance criteria greps for them verbatim):

Test 5: `renders the "Juno Industries" wordmark and "J" mark when route is /juno`
  - Call `renderHeaderJuno()`
  - Assert `screen.getByText('Juno Industries')` is in document
  - Assert `screen.getByText('J')` is in document

Test 6: `does NOT render the "Seva Mining" wordmark or "S" mark when route is /juno`
  - Call `renderHeaderJuno()`
  - Assert `screen.queryByText('Seva Mining')` is `not.toBeInTheDocument()`
  - Assert `screen.queryByText('S')` is `not.toBeInTheDocument()`

Precede the 2 new tests with a comment line:
```
// Phase 13 (BRAND-01, BRAND-03) — Juno-default scenario.
// 4 tests above are the byte-identical Seva-default contract per D-09.
// 2 tests below assert registry-driven rendering at /juno.
```

The existing 4 tests MUST be byte-identical:
- `renders the "Seva Mining" wordmark text` (line ~46)
- `renders the amber "S" logo mark` (line ~51)
- `renders a "Log out" button` (line ~56)
- `clicking "Log out" calls clearToken and navigates to /login` (line ~61)

DO NOT alter their test names, their bodies, the `renderHeader()` helper, the `mockNavigate` setup, the `mockClearToken` setup, the `vi.mock('react-router-dom', ...)` block, the `vi.mock('@/stores', ...)` block, the `beforeEach(() => { vi.clearAllMocks() })`. The only acceptable change is APPENDING the new helper + new tests.

Note on Zustand mock: The existing mock at lines 20-23 stubs `useAppStore` as a function that returns the selector output. Critically, `useCompanyBrand()` calls `useAppStore((s) => s.lastVisitedCompany)` — under the existing mock, the selector reads `s.lastVisitedCompany` from a state stub that only has `{ clearToken }`, meaning the selector returns `undefined`. That's fine: the hook's resolution chain falls through to URL `useParams` (which is `'juno'` for the Juno tests, `'seva'` for the Seva tests). The mock does NOT need extension.

Verify: `cd frontend && npm test -- --run AppHeader.test.tsx` should show 6 tests pass (4 existing + 2 new).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run AppHeader.test.tsx 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'renders the "Seva Mining" wordmark text' frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (existing test preserved)
    - `grep -c 'renders the amber "S" logo mark' frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (existing test preserved)
    - `grep -c 'renders a "Log out" button' frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (existing test preserved)
    - `grep -c 'clicking "Log out" calls clearToken and navigates to /login' frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (existing test preserved)
    - `grep -c 'renders the "Juno Industries" wordmark and "J" mark when route is /juno' frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (NEW test added)
    - `grep -c 'does NOT render the "Seva Mining" wordmark or "S" mark when route is /juno' frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (NEW test added)
    - `grep -c "renderHeaderJuno" frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns at least 3 (function definition + 2 callsites)
    - `grep -c "initialEntries={\['/juno'\]}" frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1
    - `grep -c "initialEntries={\['/seva'\]}" frontend/src/components/layout/__tests__/AppHeader.test.tsx` returns 1 (existing helper unchanged)
    - `cd frontend && npm test -- --run AppHeader.test.tsx` exits 0 with 6 tests passing (4 existing + 2 new)
  </acceptance_criteria>
  <done>
    AppHeader.test.tsx contains 6 tests (4 byte-identical Seva tests + 2 new Juno tests). All pass. The 4 existing tests verify D-09 zero regression; the 2 new tests verify BRAND-01 (wordmark per route) + BRAND-03 (mark letter per route) + the no-FOWB-on-initial-mount property.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create AppHeader.brand.test.tsx with 5 FOWB / dataset / title / favicon / cleanup tests</name>
  <files>
    frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx (NEW)
  </files>
  <read_first>
    - .planning/phases/13-per-company-branding/13-UI-SPEC.md (Test File 2 matrix — 5 tests with assertions)
    - .planning/phases/13-per-company-branding/13-CONTEXT.md (D-07 FOWB test approach + D-08a + D-08b)
    - frontend/src/components/layout/CompanyBrandEffect.tsx (Wave 2 — component under test)
    - frontend/src/components/layout/AppHeader.tsx (Wave 2 — rendered alongside effect)
    - frontend/src/components/layout/__tests__/AppHeader.test.tsx (existing pattern: QueryClient + MemoryRouter + Routes wrapper)
  </read_first>
  <action>
Create `frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` with 5 tests covering the global side-effect surface: `document.documentElement.dataset.company`, `document.title`, and `<link rel='icon'>` href mutations. Per UI-SPEC Test File 2.

This file is SEPARATE from `AppHeader.test.tsx` (per UI-SPEC §"Test File 2 ... NEW") because it pollutes global state (`document.documentElement.dataset`, `document.title`, `<link rel='icon'>`) and isolating the pollution in its own file simplifies the cleanup story for the simpler unit tests in File 1.

Header comment — cite D-07 explicitly and document the 5 test purposes (FOWB, dataset×2, favicon, cleanup).

File structure (TypeScript JSX, vitest, RTL):

1. Imports: `describe, expect, it, vi, beforeEach, afterEach` from vitest; `render, screen, act` from `@testing-library/react`; `MemoryRouter, Routes, Route` from `react-router-dom`; `QueryClient, QueryClientProvider` from `@tanstack/react-query`; the components under test: `AppHeader` and `CompanyBrandEffect`.

2. Mock `react-router-dom`: same partial mock as AppHeader.test.tsx (`useNavigate` returns a `vi.fn()`).

3. Mock `@/stores`: must support BOTH the reactive `useAppStore((s) => ...)` call form AND the non-reactive `useAppStore.getState()` call form (since `useCompanyBrand` uses the reactive form and `BareRootRedirect` uses the non-reactive form). Stub state as `{ clearToken: vi.fn(), lastVisitedCompany: null }`. Use `Object.assign(fn, { getState: () => stub })` pattern.

4. Helper `renderAt(path: '/seva' | '/juno')` — renders both `<CompanyBrandEffect />` AND `<AppHeader />` as siblings inside a `<Route path=":company/*" element={...}>`, wrapped in `<MemoryRouter initialEntries={[path]}>` + `<QueryClientProvider>`.

5. `beforeEach`:
   - `document.documentElement.removeAttribute('data-company')`
   - `document.title = 'Seva Mining'`
   - Remove any existing `link[rel='icon']` from `document.head`, then inject a fresh one with `href='/favicon.svg'` (mirrors `frontend/index.html:5`)
   - `vi.clearAllMocks()`

6. `afterEach`: clean up the same global state.

7. Five `it(...)` test cases with these EXACT names (greppable):
   - `sets data-company="seva" on documentElement when route is /seva`
   - `sets data-company="juno" on documentElement when route is /juno`
   - `FOWB: route flip /seva -> /juno updates wordmark + dataset + title atomically in single act()`
   - `updates favicon link href on route change to /juno`
   - `cleanup on unmount reverts data-company to "seva" and title to "Seva Mining"`

Test 1 — `/seva` mount: assert `document.documentElement.dataset.company === 'seva'` AND `document.title === 'Seva Mining'`.

Test 2 — `/juno` mount: assert `document.documentElement.dataset.company === 'juno'` AND `document.title === 'Juno Industries'`.

Test 3 — FOWB route flip (CRITICAL — UI-SPEC Test File 2 row 3):
   - Render at `/seva`, assert initial `dataset.company === 'seva'` + `getByText('Seva Mining')`
   - Use `rerender()` inside a SYNCHRONOUS `act(() => { ... })` block to re-mount the tree with `initialEntries={['/juno']}`
   - INSIDE the same test body, after the `act()`, assert ALL of: `dataset.company === 'juno'`, `getByText('Juno Industries')`, `getByText('J')`, `document.title === 'Juno Industries'`
   - NO `await waitFor`. NO `act(async () => ...)`. The synchronous pass IS the FOWB proof.
   - If `rerender` proves brittle in practice (mount/unmount timing on a fresh MemoryRouter discards the prior cache), the alternative is a test-only `useNavigate` wrapper exposed via a custom test component. Start with `rerender`; if test 3 fails for non-FOWB reasons, switch and document the deviation in SUMMARY.

Test 4 — favicon: render at `/juno`, query `document.head.querySelector<HTMLLinkElement>("link[rel='icon']")`, assert `.href` matches regex `/\/brand\/juno\.svg$/`.

Test 5 — cleanup on unmount: render at `/juno`, capture `unmount`, assert pre-unmount state (`dataset.company === 'juno'`, `document.title === 'Juno Industries'`), then call `unmount()`, then assert `dataset.company === 'seva'` AND `document.title === 'Seva Mining'`. This is the /login revert safety net (operator logs out from Juno, lands on /login; the dataset must revert so a re-login to /seva doesn't briefly show navy chrome).

Critical FOWB test invariant (UI-SPEC Test File 2 row 3): The route-flip test MUST NOT use `waitFor` or `act(async () => { ... })` for the assertion phase. The whole point is that the dataset + wordmark + mark + title flip synchronously inside the same render commit — wrapping in `waitFor` would mask a real FOWB bug. Acceptance criterion below greps for this: `grep -E "await waitFor|act\(async" AppHeader.brand.test.tsx` MUST return 0 hits.

All assertions are JSDom-feasible (no Playwright dependency).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run AppHeader.brand.test.tsx 2&gt;&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns 0
    - `grep -c "AppHeader brand effect" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 1
    - `grep -cE "^\s*it\(" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 5 (5 test functions)
    - `grep -c 'sets data-company="seva"' frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns 1
    - `grep -c 'sets data-company="juno"' frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns 1
    - `grep -c "FOWB" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 1
    - `grep -c "updates favicon link href" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns 1
    - `grep -c "cleanup on unmount" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns 1
    - FOWB test shape invariant (UI-SPEC Test File 2 row 3): `grep -E "await waitFor|act\(async" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns 0 hits (proves no async waits in FOWB assertion path)
    - `grep -c "document.documentElement.dataset.company" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 4
    - `grep -c "document.title" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 4
    - `grep -c "link\[rel='icon'\]" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 2
    - `grep -c "CompanyBrandEffect" frontend/src/components/layout/__tests__/AppHeader.brand.test.tsx` returns at least 2 (import + JSX usage)
    - `cd frontend && npm test -- --run AppHeader.brand.test.tsx` exits 0 with all 5 tests passing
  </acceptance_criteria>
  <done>
    AppHeader.brand.test.tsx exists with 5 tests, all passing. The FOWB assertion path contains no `waitFor` or `act(async)` calls (proves synchronous flip). Cleanup test verifies the /login revert safety net.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: D-10 branchlessness grep gate + full vitest regression suite + Vite production build</name>
  <files>
    (no file modifications — verification-only task; results documented in SUMMARY)
  </files>
  <read_first>
    - .planning/phases/13-per-company-branding/13-CONTEXT.md (D-10 branchlessness contract)
    - .planning/phases/13-per-company-branding/13-UI-SPEC.md (Test Matrix + Operator Visual-QA Checklist preview)
    - frontend/src/components/layout/CompanyScopedRoute.tsx (existing route guard that uses `!==` inverse check — allowed pattern)
  </read_first>
  <action>
Run the D-10 branchlessness grep gate and the full vitest regression suite. This task does NOT modify any files; it RUNS the verification commands and confirms results, then documents them in the SUMMARY.

GREP GATE A (D-10 — registry IS the only allowed branch). Run from the repo root:

```
grep -rEn "company === '(juno|seva)'" frontend/src/components/ --include="*.tsx" --include="*.ts" | grep -vE "(__tests__|\.test\.|\.spec\.)" | wc -l
```

EXPECTED OUTPUT: 0

GREP GATE B (broader sweep across `frontend/src`, excluding registry files):

```
grep -rEn "company === '(juno|seva)'" frontend/src --include="*.tsx" --include="*.ts" | grep -vE "(__tests__|\.test\.|\.spec\.|companyBrandConfig|companySectionConfig)" | wc -l
```

EXPECTED OUTPUT: 0

Document the exact grep commands + outputs in the SUMMARY. If either command returns > 0, STOP and surface the violation — the offender must be refactored to consume the registry via `useCompanyBrand()` before this plan can pass.

Existing CompanyScopedRoute.tsx escape hatch: Phase 9 uses tenant-existence checks at the route guard (`CompanyScopedRoute.tsx`). Those use a DIFFERENT shape — `if (company !== 'seva' && company !== 'juno') { ... return <Navigate> }` for the 404 redirect — the INVERSE check (early-out guard, not registry consumption). The grep above is for `company === '...'` specifically; the inverse `!==` form in route guards is allowed. Same for `CompanyBrandEffect.tsx`'s early-out (`if (company !== 'seva' && company !== 'juno') return`).

If a future maintainer wants to formalize this distinction, they can add `scripts/verify-no-brand-branching.sh` as a CI gate per ROADMAP Phase 13 output line. This plan does NOT create that script (out of v3.1 scope per CONTEXT D-10 — manual grep at code-review is the v3.1 contract).

FULL VITEST SUITE:

```
cd frontend && npm test -- --run 2>&1 | tee /tmp/13-03-vitest.log | tail -30
```

Confirm:
- Exit code 0
- Test count is at least 168 (Phase 12 baseline). Expected new count: 168 + 2 (Juno extension to AppHeader.test.tsx) + 5 (new AppHeader.brand.test.tsx) = approximately 175. Allow drift for incidental test additions made between Phase 12 close and this run.
- The 4 existing AppHeader.test.tsx tests appear in the pass list with their original names:
  - `renders the "Seva Mining" wordmark text`
  - `renders the amber "S" logo mark`
  - `renders a "Log out" button`
  - `clicking "Log out" calls clearToken and navigates to /login`
- The 2 new AppHeader.test.tsx tests appear:
  - `renders the "Juno Industries" wordmark and "J" mark when route is /juno`
  - `does NOT render the "Seva Mining" wordmark or "S" mark when route is /juno`
- The 5 new AppHeader.brand.test.tsx tests appear (FOWB / dataset x2 / favicon / cleanup)

TYPESCRIPT BUILD VERIFICATION:

```
cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/13-03-tsc.log | tail -30
```

Confirm exit code 0 (or no NEW errors relative to pre-Phase-13 baseline if any preexisting unrelated TS issues exist).

VITE PRODUCTION BUILD (smoke check that the SVG assets are picked up by the build):

```
cd frontend && npm run build 2>&1 | tee /tmp/13-03-build.log | tail -30
```

Confirm:
- Exit code 0
- `dist/brand/seva.svg` exists after build (verify with `ls frontend/dist/brand/`)
- `dist/brand/juno.svg` exists after build
- `dist/index.html` contains `<title>Seva Mining</title>` (build inherits source `index.html`)

Document all verification command outputs (or a summary line each) in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -rEn "company === '(juno|seva)'" src/ --include="*.tsx" --include="*.ts" | grep -vE "(__tests__|\.test\.|\.spec\.|companyBrandConfig|companySectionConfig)" | wc -l &amp;&amp; cd frontend &amp;&amp; npm test -- --run 2&gt;&amp;1 | tail -10 &amp;&amp; cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; cd frontend &amp;&amp; npm run build &amp;&amp; ls frontend/dist/brand/</automated>
  </verify>
  <acceptance_criteria>
    - D-10 grep gate A: `cd frontend && grep -rEn "company === '(juno|seva)'" src/components/ --include="*.tsx" --include="*.ts" | grep -vE "(__tests__|\.test\.|\.spec\.)" | wc -l` returns 0
    - D-10 grep gate B (broader sweep): `cd frontend && grep -rEn "company === '(juno|seva)'" src --include="*.tsx" --include="*.ts" | grep -vE "(__tests__|\.test\.|\.spec\.|companyBrandConfig|companySectionConfig)" | wc -l` returns 0
    - Full vitest suite: `cd frontend && npm test -- --run` exits 0 with at least 168 tests passing
    - All 4 original Seva AppHeader tests pass with their original names (confirms D-09 byte-identical contract)
    - All 2 new Juno AppHeader tests pass
    - All 5 new AppHeader.brand.test.tsx tests pass
    - TypeScript: `cd frontend && npx tsc --noEmit` exits 0 (or produces no NEW errors relative to pre-Phase-13 baseline)
    - Vite production build: `cd frontend && npm run build` exits 0
    - Build artifacts: `test -f frontend/dist/brand/seva.svg && test -f frontend/dist/brand/juno.svg` both return 0
    - Build HTML title: `grep -c '<title>Seva Mining</title>' frontend/dist/index.html` returns 1
  </acceptance_criteria>
  <done>
    D-10 grep gate returns 0 hits (registry is the only allowed branch). Full vitest suite green with at least 168 tests, including all 4 original Seva tests + 2 new Juno tests + 5 new brand-effect tests. TypeScript clean. Vite build succeeds with SVG assets in `dist/brand/`. All automated verification passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator visual QA at 1440x900 — 10-item checklist (BRAND-04 visual aesthetic + FOWB final proof)</name>
  <files>
    (no file modifications — human verification only)
  </files>
  <read_first>
    - .planning/phases/13-per-company-branding/13-UI-SPEC.md (§"Operator Visual-QA Checklist" — the 10 items below)
  </read_first>
  <what-built>
    Phase 13 delivered:
    - Per-tenant brand registry (`companyBrandConfig.ts`) with seva + juno entries
    - URL/Zustand-driven brand hook (`useCompanyBrand`) consumed by `AppHeader`
    - Router-layer `<CompanyBrandEffect />` setting `dataset.company`, `document.title`, and favicon `<link href>` per route
    - 2 SVG favicons (`/brand/seva.svg` amber-S, `/brand/juno.svg` navy-J)
    - CSS override block `:root.dark[data-company='juno']` with Juno navy OKLCH triple
    - Bare-`/` redirect honoring Zustand `lastVisitedCompany` (TENANT-VISITED-v31-redux closed)
    - `index.html` `<title>` corrected from Vite scaffold default to "Seva Mining"
    - 6 AppHeader tests (4 unchanged + 2 new) + 5 brand-effect tests, all passing
    - D-10 grep gate green (0 hits for `company === 'juno'` in non-test components)

    Wave 3 task 3 (above) ran the full automated verification. This checkpoint is the FINAL human gate for visual aesthetic (navy reads as institutional defence, not tech-startup blue) + FOWB observation (operator literally watches for flash during transitions).
  </what-built>
  <action>
    This is a `checkpoint:human-verify` task — Claude does NOT perform automated actions here. The action is for the OPERATOR to run the 10-item visual QA checklist below. See `<how-to-verify>` for the full steps.

    Claude's role: (a) ensure the dev server is running (`cd frontend && npm run dev` if not already up), or run `cd frontend && npm run preview` after `npm run build` to verify the production bundle; (b) wait for the operator's resume-signal.
  </action>
  <how-to-verify>
    Open the app at the dev server (or production build preview: `cd frontend && npm run preview`) at 1440x900 viewport. Walk the 10-item checklist from UI-SPEC §"Operator Visual-QA Checklist" in order:

    MUST-PASS items (block release if any fail):

    1. Visit `/seva/` — AppHeader shows amber square + "S" + "Seva Mining". Identical to v3.0 visual (D-09 zero regression).
    2. Visit `/juno/` — AppHeader shows navy square + "J" + "Juno Industries". Navy reads as institutional (NOT bright royal, NOT cyan, NOT slate-grey). The OKLCH value is `oklch(0.58 0.14 245)`.
    4. Navigate from `/seva/calendar` → click "Juno" in `CompanySwitcher`. NO FLASH OF AMBER under the Juno route during transition. Wordmark + brand-mark + switcher accent flip simultaneously. This is the FOWB acceptance criterion (BRAND-04).
    5. Browser tab title updates: at `/seva/` reads "Seva Mining"; at `/juno/` reads "Juno Industries". Switch between tabs in the browser; the title in the OS tab strip updates live.
    6. Browser tab favicon updates: at `/seva/` favicon is amber-S; at `/juno/` it's navy-J. Both distinguishable at 14-16px in the OS tab strip. (Note: hard-refresh may be required on first deploy due to browser cache; subsequent route changes update live in-session.)
    7. Navigate to bare `/` after having visited Juno last. Browser redirects to `/juno`, not `/seva`. After clearing localStorage (DevTools → Application → Local Storage → clear) and revisiting `/`, the redirect goes to `/seva` (the hardcoded fallback in `BareRootRedirect`).

    SHOULD-PASS items (cosmetic regressions; investigate but don't block release):

    3. At `/juno/` — `CompanySwitcher`'s active "Juno" button has navy outline + navy text + faint navy fill. Inactive "Seva" button has zinc-800 border + zinc-400 text.
    8. At `/juno/calendar` (or `/juno/viral`) — any v2.1 Phase 8 surface that uses `--color-brand-accent*` (`SectionBlock` accent rule, `SweeperCard` accents, etc.) renders in navy AUTOMATICALLY. No file changes were made to those components; the cascade through the CSS-var token does the work. Spot-check at least one such surface.
    9. At `/juno/` — keyboard-tab into `CompanySwitcher` buttons. Focus ring is navy (not amber). Inverse check at `/seva/` — focus ring is amber.
    10. Log out from `/juno/` to `/login`. The login page title is "Seva Mining" (default cleanup), not "Juno Industries". Re-login as Seva — no flash of navy on `/seva/` first paint.

    Per item, record PASS / FAIL. If any MUST-PASS item fails, do NOT approve — provide details and surface for repair (likely another revision pass). If any SHOULD-PASS item fails, note it but approval can proceed with a follow-up issue.

    Expected operator reaction (sanity check): "I can see at a glance which tenant I'm in. Amber screams gold-sector. Navy screams defence-industry. Neither feels generic." If navy reads as "Microsoft Teams blue" or "tech-startup blue," planner should re-tune the OKLCH triple (try lifting L by 0.02 to `oklch(0.60 0.14 245)` — contrast still passes; UI-SPEC §"Contrast Audit" notes this allowance).
  </how-to-verify>
  <verify>
    Manual operator inspection only. No automated command.
  </verify>
  <acceptance_criteria>
    - All 6 MUST-PASS items (1, 2, 4, 5, 6, 7) confirmed PASS by operator
    - SHOULD-PASS items (3, 8, 9, 10) either PASS or logged as follow-ups (not blocking)
    - Operator types resume-signal per the format below
  </acceptance_criteria>
  <done>
    Operator has confirmed PASS for all 6 MUST-PASS items and signed off via resume-signal. SHOULD-PASS items either passed or are documented as follow-up issues in the SUMMARY.
  </done>
  <resume-signal>
    Type "approved" if all 6 MUST-PASS items pass.
    Type a list of failed items (e.g., "fail 2, fail 4") with description of what you observed to trigger a fix cycle.
    Type "approved with notes: ..." if MUST-PASS items pass but SHOULD-PASS items have issues you want logged for follow-up.
  </resume-signal>
</task>

</tasks>

<verification>
After all four tasks complete, the phase is fully verified when:

1. Vitest suite green: at least 168 tests passing, including 4 byte-identical Seva tests + 2 new Juno tests in `AppHeader.test.tsx` + 5 new tests in `AppHeader.brand.test.tsx`.

2. D-10 branchlessness: both grep gates (A and B) return 0 hits.

3. TypeScript clean: `npx tsc --noEmit` produces no new errors.

4. Vite build green: `npm run build` succeeds, `dist/brand/seva.svg` + `dist/brand/juno.svg` exist, `dist/index.html` has the correct `<title>`.

5. Operator visual QA: 6/6 MUST-PASS items confirmed at 1440x900 by the operator. SHOULD-PASS items either pass or are logged as follow-ups.
</verification>

<success_criteria>
- 4 existing AppHeader Seva tests pass byte-identically (D-09 contract held without modifying the test file's pre-existing test bodies).
- 2 new Juno tests added to AppHeader.test.tsx pass.
- 5 new tests in AppHeader.brand.test.tsx pass (FOWB / dataset x2 / favicon / cleanup); FOWB test contains zero `waitFor` or `act(async)` calls.
- D-10 grep gates A and B both return 0 hits (BRAND-05 branchlessness contract enforced).
- Vitest suite green at >= 168 tests (Phase 12 baseline preserved + 7 new tests).
- TypeScript and Vite build clean.
- Operator visual QA: 6/6 MUST-PASS items confirmed (BRAND-01 wordmark, BRAND-02 color, BRAND-03 mark + favicon, BRAND-04 FOWB-free transition).
</success_criteria>

<output>
After completion, create `.planning/phases/13-per-company-branding/13-03-SUMMARY.md` documenting:
- Test additions: 2 new tests in AppHeader.test.tsx + 5 new tests in AppHeader.brand.test.tsx (exact test names + pass results)
- D-09 byte-identical verification: confirm the 4 original Seva tests pass with their original names + that no edits were made to the original test bodies (only additions appended)
- D-10 grep gate results: exact commands run + exact wc -l outputs (must be 0 for both gates)
- Full vitest suite: total test count before/after (Phase 12 baseline → current count), exit code, any flaky/skipped tests
- TypeScript: exit code of `npx tsc --noEmit`
- Vite build: exit code + confirmation that `dist/brand/seva.svg`, `dist/brand/juno.svg`, and `dist/index.html` <title>Seva Mining</title> all exist
- Operator visual QA results: per-item PASS/FAIL for all 10 checklist items + operator's verbal sign-off
- Note that Phase 13 is COMPLETE — all 5 BRAND requirements (BRAND-01..05) satisfied + TENANT-VISITED-v31-redux closed as opportunistic polish (D-08c)
</output>
