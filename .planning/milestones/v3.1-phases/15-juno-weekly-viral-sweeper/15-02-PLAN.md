---
phase: 15-juno-weekly-viral-sweeper
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduler/companies/juno/prompts.py
  - scheduler/companies/juno/x_queries.py
  - scheduler/tests/companies/test_juno_prompts.py
  - scheduler/tests/companies/test_juno_x_queries.py
autonomous: true
requirements:
  - JSWEEP-02
  - JSWEEP-04

must_haves:
  truths:
    - "scheduler/companies/juno/prompts.py exports a NEW JUNO_SWEEPER_SYSTEM_PROMPT constant alongside the existing DEFENCE_NEWS_SYSTEM_PROMPT (Phase 10 prompt UNTOUCHED)"
    - "JUNO_SWEEPER_SYSTEM_PROMPT contains the EXACT bytes of DEFENCE_NEWS_SYSTEM_PROMPT's FORBID anti-tactical clause (string-equality contract per D-04)"
    - "JUNO_SWEEPER_SYSTEM_PROMPT carries Janes/CSIS desk voice anchor, 'exactly 3 content angles' task framing, neutral-on-conflict bias INVERTING Seva's bull-thesis bias"
    - "scheduler/companies/juno/x_queries.py exports JUNO_SWEEPER_X_QUERY constant — verbatim 11-handle + 2-hashtag combined query from CONTEXT D-02 final corrected form"
    - "JUNO_SWEEPER_X_QUERY length is less than 512 chars (X API Basic-tier hard cap per RESEARCH §3) and contains all 11 corrected handles + both hashtags + -is:retweet lang:en"
    - "No Seva-side files touched (D-10 zero-regression)"
  artifacts:
    - path: "scheduler/companies/juno/prompts.py"
      provides: "Adds JUNO_SWEEPER_SYSTEM_PROMPT alongside existing DEFENCE_NEWS_SYSTEM_PROMPT — no rename, no Phase 10 edit"
      contains: "JUNO_SWEEPER_SYSTEM_PROMPT"
      must_not_contain: "STUB"
    - path: "scheduler/companies/juno/x_queries.py"
      provides: "NEW file — JUNO_SWEEPER_X_QUERY constant with rationale comment + cashtag-exclusion note"
      contains: "JUNO_SWEEPER_X_QUERY"
    - path: "scheduler/tests/companies/test_juno_prompts.py"
      provides: "Extended with anti-tactical clause string-equality test + voice/structure/anti-bias tests for the new sweeper prompt"
      contains: "JUNO_SWEEPER_SYSTEM_PROMPT"
    - path: "scheduler/tests/companies/test_juno_x_queries.py"
      provides: "NEW test file — length, 11-handle presence, 2-hashtag presence, -is:retweet lang:en, no-cashtag invariants"
      contains: "JUNO_SWEEPER_X_QUERY"
  key_links:
    - from: "scheduler/companies/juno/prompts.py::JUNO_SWEEPER_SYSTEM_PROMPT"
      to: "scheduler/agents/juno_weekly_sweeper.py (Plan 15-05's Sonnet call)"
      via: "from companies.juno.prompts import JUNO_SWEEPER_SYSTEM_PROMPT"
      pattern: "JUNO_SWEEPER_SYSTEM_PROMPT"
    - from: "scheduler/companies/juno/x_queries.py::JUNO_SWEEPER_X_QUERY"
      to: "scheduler/agents/juno_weekly_sweeper.py (Plan 15-05's fetch_top_x_posts call)"
      via: "from companies.juno.x_queries import JUNO_SWEEPER_X_QUERY"
      pattern: "JUNO_SWEEPER_X_QUERY"
    - from: "scheduler/companies/juno/prompts.py::DEFENCE_NEWS_SYSTEM_PROMPT (existing FORBID block)"
      to: "scheduler/companies/juno/prompts.py::JUNO_SWEEPER_SYSTEM_PROMPT (NEW, same FORBID bytes verbatim)"
      via: "Verbatim FORBID block copy — defence content-policy compliance per D-04"
      pattern: "market/industry commentary on the defence sector"
---

<objective>
Create the two Juno-specific constants Plan 15-05 (sweeper orchestrator) will import: (1) `JUNO_SWEEPER_SYSTEM_PROMPT` in `scheduler/companies/juno/prompts.py` alongside the existing `DEFENCE_NEWS_SYSTEM_PROMPT`, and (2) `JUNO_SWEEPER_X_QUERY` in a new file `scheduler/companies/juno/x_queries.py`. Both are tested via RED tests in this plan; Wave 2 Plan 15-05 imports them.

Purpose:
- The prompt encodes Phase 15's Janes/CSIS desk voice + verbatim anti-tactical clause (string-equality contract per D-04 — load-bearing for Anthropic content-policy compliance per RESEARCH §4 Anthropic-Pentagon dispute) + "exactly 3 content angles" task framing
- The X query encodes the post-research corrected handle set: 11 handles (3 think-tanks + 4 defence press + 4 Canadian incl. 2 Tier-2 analysts) + 2 hashtags, sized at ~250 chars (well inside X API Basic tier's 512-char limit per RESEARCH §3)

Output:
- `scheduler/companies/juno/prompts.py` — extended with `JUNO_SWEEPER_SYSTEM_PROMPT` (no edits to existing constant)
- `scheduler/companies/juno/x_queries.py` — NEW file
- `scheduler/tests/companies/test_juno_prompts.py` — extended with anti-tactical string-equality + structure tests
- `scheduler/tests/companies/test_juno_x_queries.py` — NEW test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-juno-weekly-viral-sweeper/15-CONTEXT.md
@.planning/phases/15-juno-weekly-viral-sweeper/15-RESEARCH.md

# Files to modify / create
@scheduler/companies/juno/prompts.py
@scheduler/tests/companies/test_juno_prompts.py

# Reference shape for Sonnet "3 content angles" prompt structure (Seva — READ ONLY, D-10 contract)
@scheduler/agents/weekly_sweeper.py

<interfaces>
<!-- The verbatim anti-tactical FORBID block from scheduler/companies/juno/prompts.py:21-22.
     Bytes confirmed by direct read 2026-05-20. Must appear verbatim as substring of JUNO_SWEEPER_SYSTEM_PROMPT. -->

The verbatim FORBID block (lines 21-22 of prompts.py):
```
FORBID — anti-tactical framing clause:
You produce market/industry commentary on the defence sector. You do NOT produce operational, tactical, targeting, force posture, order of battle (OOB), capability gap, or troop movement analysis. If a source story crosses into operational territory, summarize the market/industry implications only and explicitly note the operational details were excluded.
```

The Seva reference SONNET_SYSTEM_PROMPT in scheduler/agents/weekly_sweeper.py:203-226 carries the "3 content angles" + "Grounding rule" + "### Angle N:" output structure that the Juno sweeper prompt MIRRORS — but with defence audience reframing, neutral-on-conflict bias INVERSION (no gold-bull-thesis equivalent), and the verbatim anti-tactical FORBID block ADDED.

CONTEXT D-02 final corrected query (~250 chars; inside 512-char Basic-tier limit per RESEARCH §3):
```
(from:RUSI_org OR from:CSIS OR from:IISS_org OR
 from:defense_news OR from:BreakingDefense OR from:DefenseScoop OR
 from:JanesINTEL OR from:CDAInstitute OR from:CanadianForces OR
 from:DavePerryCGAI OR from:Murray_Brewster OR #defence OR #NATO) -is:retweet lang:en
```

Corrections vs CONTEXT.md ORIGINAL D-02 (per RESEARCH §1):
- @DefenseNews -> defense_news (snake_case official handle)
- @CDA_CDAI -> CDAInstitute (correct CDA Institute handle)
- @canadaforces -> CanadianForces (correct CAF handle)
- @JanesIntel -> JanesINTEL (case-variant — official form)
- Added Tier-2 Canadian: @DavePerryCGAI + @Murray_Brewster (RESEARCH §2)

EXCLUDED (anti-feature per PROJECT.md): defence-prime cashtags ($LMT, $RTX, $LDOS, $BAESY, $GD, etc.).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add JUNO_SWEEPER_SYSTEM_PROMPT to prompts.py + extend test_juno_prompts.py with 7 string-equality + structure tests</name>
  <files>scheduler/companies/juno/prompts.py, scheduler/tests/companies/test_juno_prompts.py</files>
  <read_first>
    - .planning/phases/15-juno-weekly-viral-sweeper/15-CONTEXT.md decision D-04 in full (NEW dedicated JUNO_SWEEPER_SYSTEM_PROMPT alongside existing DEFENCE_NEWS_SYSTEM_PROMPT; Janes/CSIS voice; verbatim anti-tactical clause from Phase 10 [string-equality contract]; "3 content angles" task framing; refusal-prone-framing avoidance)
    - .planning/phases/15-juno-weekly-viral-sweeper/15-RESEARCH.md §4 "Sonnet '3 Content Angles' Prompting Best Practices" in full (concrete prompt structure: voice anchor, anti-tactical verbatim, exactly-3-angles framing, neutral-on-conflict bias INVERSION of Seva's bull bias, negative-space DO NOT list)
    - scheduler/companies/juno/prompts.py FULL contents (43 lines) — confirm FORBID block bytes; understand the existing constant's docstring style so JUNO_SWEEPER_SYSTEM_PROMPT is APPENDED (not replacing)
    - scheduler/agents/weekly_sweeper.py:203-226 (Seva SONNET_SYSTEM_PROMPT — reference for "3 content angles" structure ONLY; voice + bias + audience DIFFER)
    - scheduler/tests/companies/test_juno_prompts.py FULL contents (121 lines) — confirm existing Phase 10 test shape; new tests are APPENDED at end of file (existing tests untouched per D-10)
  </read_first>
  <behavior>
    Per D-04: After this task lands, the following invariants hold:

    1. `from companies.juno.prompts import JUNO_SWEEPER_SYSTEM_PROMPT` succeeds and the import returns a string of length more than 100 chars.
    2. The verbatim FORBID block from `DEFENCE_NEWS_SYSTEM_PROMPT` lines 21-22 (the exact bytes: "FORBID — anti-tactical framing clause:\nYou produce market/industry commentary on the defence sector. ... operational details were excluded.") is a substring of `JUNO_SWEEPER_SYSTEM_PROMPT`.
    3. `JUNO_SWEEPER_SYSTEM_PROMPT` contains "Janes" + at least one of "CSIS" / "IISS" / "Defense News editorial".
    4. `JUNO_SWEEPER_SYSTEM_PROMPT` contains the substring "exactly 3 content angles" + the three markers "### Angle 1:", "### Angle 2:", "### Angle 3:".
    5. `JUNO_SWEEPER_SYSTEM_PROMPT` mentions both "X signal" (or "X (Twitter) signal") + "news signal" — the task connects X with news.
    6. `JUNO_SWEEPER_SYSTEM_PROMPT` contains "Use ONLY facts" + "hallucinate" (grounding + anti-hallucination, mirror Seva sweeper Phase 7 D-04).
    7. `JUNO_SWEEPER_SYSTEM_PROMPT` cites both "LMT" and "RTX" tickers in the negative-space DO-NOT list (PROJECT.md anti-feature on defence-prime equity signals).
    8. `JUNO_SWEEPER_SYSTEM_PROMPT` does NOT contain "bull thesis" + does NOT contain the standalone token "gold" (D-04 / RESEARCH §4 element 4 — defence is neutral on conflict, must not clone Seva's gold-bull-thesis bias).

    The existing `DEFENCE_NEWS_SYSTEM_PROMPT` constant and the existing Phase 10 tests in `test_juno_prompts.py` are byte-identical-protected (D-10).
  </behavior>
  <action>
    Per D-04: Append `JUNO_SWEEPER_SYSTEM_PROMPT` to `scheduler/companies/juno/prompts.py` BELOW the existing `DEFENCE_NEWS_SYSTEM_PROMPT` constant. The new constant must be a multi-line triple-quoted string with the structure described below. CRITICAL: the FORBID anti-tactical block bytes must appear verbatim — copy by reading the existing constant, not by retyping from memory.

    **Prompt structure** (mirrors Seva's SONNET_SYSTEM_PROMPT at weekly_sweeper.py:203-226 + Phase 10's DEFENCE_NEWS_SYSTEM_PROMPT FORBID block):

    1. Voice paragraph: "senior defence-industry analyst writing for a Canadian defence-tech operator. Tone: authoritative, sober, sourced-with-receipts. Match the energy of a Janes desk brief, a CSIS analysis piece, an IISS Military Balance update, or a Defense News editorial column."
    2. Verbatim FORBID block from DEFENCE_NEWS_SYSTEM_PROMPT lines 21-22 (string-equality contract — BYTE-IDENTICAL copy)
    3. Neutral-on-conflict paragraph: "Take no analyst position on whether conflicts escalate or de-escalate. Neutral-on-conflict per Janes/CSIS desk convention."
    4. Task framing: "Generate exactly 3 content angles. Each angle MUST connect an X (Twitter) signal from the defence-sector conversation with a mainstream defence-news signal from the past week."
    5. Grounding rule (mirror Seva weekly_sweeper.py:208): "Use ONLY facts, figures, claims, vendor names, contract values, and source attributions present in the supplied inputs (X posts + cross-referenced news stories). Do NOT invent quotes, statistics, source attributions, or events. If you cannot ground an angle in the supplied inputs, do not generate it — return fewer than 3 angles rather than hallucinate."
    6. Refusal-prone-framing avoidance (mirror juno_refusal_detector.FRAMING_NUDGE): "This task is defence-industry market commentary, not tactical or operational intelligence."
    7. Output structure block:
       ```
       Output MUST be markdown in this exact structure (no preamble, no postamble):

       ### Angle 1: {short headline tying X signal + news signal}

       **Hook (1-2 sentences):** {opening that connects the X chatter to the news event}

       **Bullets:**
       * X signal: {what the X chatter is showing} (@{author_username})
       * News signal: {what the news story is showing} ({source_name})
       * Defence-industry connection: {policy / procurement / capability-narrative / geopolitical signal — NOT equity, NOT operational}

       ### Angle 2: {headline}
       {same structure}

       ### Angle 3: {headline}
       {same structure}
       ```
    8. Negative-space DO NOT list (mirror DEFENCE_NEWS_SYSTEM_PROMPT lines 37-41 + PROJECT.md anti-feature):
       - "Cite stock tickers, P/E ratios, share-price movement, earnings, dividends, or investment-advice framings on defence primes (LMT, RTX, NOC, BA, GD, BAESY, etc.) — explicit anti-feature per PROJECT.md"
       - "Produce force posture, order of battle, capability gap, troop movement, or targeting analysis under any framing"
       - "Speculate on classified material or operational intent"
       - "Advocate for or against specific weapons programs"
       - "Take a position on whether conflicts escalate or de-escalate"

    Header docstring above the constant should reference: "v3.1 Phase 15 D-04 — Sonnet 4.6 system prompt for the Juno Weekly Viral Sweeper. Verbatim FORBID block from DEFENCE_NEWS_SYSTEM_PROMPT (string-equality contract per D-04 — Anthropic content-policy compliance per RESEARCH §4)."

    **CRITICAL VERBATIM RULE:** The FORBID block bytes — from "FORBID — anti-tactical framing clause:" through "operational details were excluded." — MUST appear identically in both constants. Test asserts the SAME string variable is `in` both. To do this correctly: read `scheduler/companies/juno/prompts.py` first, extract the FORBID block bytes (lines 21-22) into the test as a string literal, then copy those SAME bytes into the new prompt. The new prompt being a multi-line string literal must reproduce the exact whitespace + punctuation.

    **Test file extension** — APPEND to `scheduler/tests/companies/test_juno_prompts.py` (do NOT modify existing tests):

    Add a section divider comment then 7 new test functions:
    - `test_sweeper_prompt_constant_exists` — import succeeds, len more than 100
    - `test_sweeper_prompt_contains_anti_tactical_clause_verbatim` — STRING-EQUALITY contract; assert the literal FORBID-block string (defined as a multi-line Python string in the test) is a substring of BOTH DEFENCE_NEWS_SYSTEM_PROMPT AND JUNO_SWEEPER_SYSTEM_PROMPT
    - `test_sweeper_prompt_has_voice_anchor` — assert "Janes" in prompt + at least one of "CSIS"/"IISS"/"Defense News editorial"
    - `test_sweeper_prompt_demands_exactly_3_angles` — assert "exactly 3 content angles" + "### Angle 1:" + "### Angle 2:" + "### Angle 3:"
    - `test_sweeper_prompt_connects_x_signal_with_news_signal` — assert ("X signal" or "X (Twitter) signal") + "news signal" (case-insensitive for news)
    - `test_sweeper_prompt_grounding_rule_anti_hallucination` — assert "Use ONLY facts" + "hallucinate" (case-insensitive)
    - `test_sweeper_prompt_excludes_equity_signals_on_defence_primes` — assert "LMT" + "RTX" in negative-space list
    - `test_sweeper_prompt_neutral_on_conflict_inverts_seva_bull_bias` — assert "neutral-on-conflict" or "neutral on conflict" in prompt.lower(), AND "bull thesis" NOT in prompt.lower(), AND no standalone "gold" token via re.search(r"\bgold\b", prompt.lower())

    Use the same `sys.path.insert` scaffold at the top of the existing file (already in place at line ~29); the new helper `_sweeper_prompt()` imports `JUNO_SWEEPER_SYSTEM_PROMPT` lazily mirroring the existing `_prompt()` helper.

    DO NOT modify:
    - The existing `DEFENCE_NEWS_SYSTEM_PROMPT` constant (Phase 10 prompt frozen per D-10)
    - The existing 7 tests in `test_juno_prompts.py` (Phase 10 tests are byte-identical-protected)
    - Any other file under `scheduler/companies/` or `scheduler/agents/weekly_sweeper.py`
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/companies/test_juno_prompts.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JUNO_SWEEPER_SYSTEM_PROMPT" scheduler/companies/juno/prompts.py` returns at least 2 (constant definition + the docstring/comment reference)
    - `grep -c "DEFENCE_NEWS_SYSTEM_PROMPT" scheduler/companies/juno/prompts.py` returns at least 1 (existing constant still present — Phase 10 unchanged)
    - `grep -c "FORBID — anti-tactical framing clause" scheduler/companies/juno/prompts.py` returns 2 (one occurrence in each constant — string-equality contract honored)
    - `grep -c "exactly 3 content angles" scheduler/companies/juno/prompts.py` returns at least 1
    - `grep -c "### Angle 1:" scheduler/companies/juno/prompts.py` returns at least 1
    - `grep -c "### Angle 2:" scheduler/companies/juno/prompts.py` returns at least 1
    - `grep -c "### Angle 3:" scheduler/companies/juno/prompts.py` returns at least 1
    - `grep -c "neutral-on-conflict" scheduler/companies/juno/prompts.py` returns at least 1 (D-04 RESEARCH §4 element 4 bias inversion)
    - `grep -c "bull thesis" scheduler/companies/juno/prompts.py` returns 0 (must not clone Seva's gold-bull-thesis framing)
    - `grep -nE "\bgold\b" scheduler/companies/juno/prompts.py` returns no matches (defence prompt must not contain standalone "gold" token)
    - `cd scheduler && uv run pytest tests/companies/test_juno_prompts.py -v` exits 0 with all existing 7 tests + 8 new tests PASSING (15 tests total)
    - `grep -c "test_sweeper_prompt" scheduler/tests/companies/test_juno_prompts.py` returns at least 8 (8 new test functions)
    - `cd scheduler && uv run pytest tests/companies/ -x` exits 0
    - `git status --porcelain scheduler/companies/juno/feeds.py scheduler/companies/juno/serpapi.py scheduler/companies/juno/__init__.py scheduler/agents/weekly_sweeper.py` shows empty output (D-10 — adjacent files untouched)
    - `cd scheduler && uv run ruff check companies/juno/prompts.py tests/companies/test_juno_prompts.py` exits 0
  </acceptance_criteria>
  <done>
    `JUNO_SWEEPER_SYSTEM_PROMPT` exists in `scheduler/companies/juno/prompts.py` alongside the unchanged `DEFENCE_NEWS_SYSTEM_PROMPT`. The verbatim FORBID anti-tactical block appears as a substring in both constants (D-04 string-equality contract honored). The new prompt carries Janes/CSIS voice, "exactly 3 content angles" framing, the X-signal/news-signal task contract, grounding + anti-hallucination rules, neutral-on-conflict bias inverting Seva's bull bias, and the negative-space DO NOT list including LMT + RTX tickers. 8 new tests in `test_juno_prompts.py` cover all these invariants. Existing 7 tests still pass byte-identically.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scheduler/companies/juno/x_queries.py with JUNO_SWEEPER_X_QUERY constant + new test_juno_x_queries.py test file</name>
  <files>scheduler/companies/juno/x_queries.py, scheduler/tests/companies/test_juno_x_queries.py</files>
  <read_first>
    - .planning/phases/15-juno-weekly-viral-sweeper/15-CONTEXT.md decision D-01 (single combined query per Sunday cron) + D-02 final corrected handle set (11 handles + 2 hashtags; cashtags EXCLUDED per anti-feature)
    - .planning/phases/15-juno-weekly-viral-sweeper/15-RESEARCH.md §1 X handle verification (the 4 corrections vs CONTEXT.md original) + §2 Tier-2 Canadian (DavePerryCGAI + Murray_Brewster) + §3 query syntax / 512-char Basic-tier limit (NOT 1024)
    - scheduler/agents/weekly_sweeper.py:74-79 — Seva X_SEARCH_QUERY shape (reference for the constant layout, comment style; D-10 contract — DO NOT touch this file)
    - scheduler/companies/juno/feeds.py, scheduler/companies/juno/serpapi.py — confirm the existing constant-file pattern under companies/juno/ (single module-level constant + docstring; no functions)
  </read_first>
  <behavior>
    Per D-01 + D-02: After this task lands:

    1. `from companies.juno.x_queries import JUNO_SWEEPER_X_QUERY` succeeds.
    2. `JUNO_SWEEPER_X_QUERY` is a non-empty `str`.
    3. `len(JUNO_SWEEPER_X_QUERY)` is less than 512 (X API Basic-tier hard cap per RESEARCH §3; CONTEXT.md original cited 1024 which was the Academic tier limit).
    4. The query contains ALL 11 corrected handles: `RUSI_org`, `CSIS`, `IISS_org`, `defense_news`, `BreakingDefense`, `DefenseScoop`, `JanesINTEL`, `CDAInstitute`, `CanadianForces`, `DavePerryCGAI`, `Murray_Brewster`.
    5. The query contains both hashtags: `#defence`, `#NATO`.
    6. The query contains `-is:retweet` and `lang:en` modifiers.
    7. The query does NOT contain any of the excluded cashtags: `$LMT`, `$RTX`, `$LDOS`, `$BAESY`, `$GD`, `$NOC`, `$BA`.
    8. The query does NOT contain the wrong-spelling handles from RESEARCH §1: `DefenseNews` (CamelCase without underscore — the wrong form per §1), `CDA_CDAI`, `canadaforces`, `JanesIntel`.
  </behavior>
  <action>
    Create `scheduler/companies/juno/x_queries.py` with this exact content (the constant is the post-research corrected D-02 form):

    ```python
    """v3.1 Phase 15 — Juno Weekly Viral Sweeper X recent-search query (D-01, D-02).

    Single combined query per Sunday cron (D-01). Defence-sector handles +
    hashtags only — defence-prime cashtags ($LMT, $RTX, $LDOS, $BAESY, $GD,
    etc.) are EXCLUDED per PROJECT.md anti-feature on equity/financial signals
    on defence primes.

    Final corrected handle set (D-02 + RESEARCH §1 corrections):

      Think-tanks (3):  @RUSI_org, @CSIS, @IISS_org
      Defence press (4): @defense_news, @BreakingDefense, @DefenseScoop, @JanesINTEL
      Canadian (4):      @CDAInstitute, @CanadianForces, @DavePerryCGAI, @Murray_Brewster
      Hashtags (2):      #defence, #NATO

    Corrections vs CONTEXT.md original D-02 (per RESEARCH §1 — 4 of 9 starter
    handles would have returned 0 hits as originally spelled):
      - @DefenseNews     -> defense_news (snake_case official handle)
      - @CDA_CDAI        -> CDAInstitute (correct CDA Institute handle)
      - @canadaforces    -> CanadianForces (correct CAF handle)
      - @JanesIntel      -> JanesINTEL (case-variant — official form)
      - ADDED Tier-2 Canadian: @DavePerryCGAI, @Murray_Brewster (RESEARCH §2)

    Query length: ~250 chars (well inside X API Basic-tier 512-char limit per
    RESEARCH §3 — NOT the 1024-char Academic tier limit that CONTEXT.md
    originally cited).

    Tunability path (D-02): @CadsiCanada (industry association) + @NationalDefence
    (DND official) are RESERVED candidates if v1.0 signal proves thin. Adding
    both stays well inside 512 chars (~308 chars after expansion).
    """
    from __future__ import annotations

    JUNO_SWEEPER_X_QUERY: str = (
        "(from:RUSI_org OR from:CSIS OR from:IISS_org OR "
        "from:defense_news OR from:BreakingDefense OR from:DefenseScoop OR "
        "from:JanesINTEL OR from:CDAInstitute OR from:CanadianForces OR "
        "from:DavePerryCGAI OR from:Murray_Brewster OR "
        "#defence OR #NATO) -is:retweet lang:en"
    )
    ```

    Then create `scheduler/tests/companies/test_juno_x_queries.py`:

    ```python
    """v3.1 Phase 15 — JUNO_SWEEPER_X_QUERY constant invariants (D-01, D-02 + RESEARCH §1, §3).

    Tests the post-research corrected D-02 handle set is encoded correctly + that
    the query stays inside X API Basic-tier 512-char limit + that the explicit
    anti-feature (defence-prime cashtags) does NOT slip in.

    These tests catch regressions where a future contributor reverts to the
    CONTEXT.md original (uncorrected) handle spellings OR accidentally adds a
    cashtag against PROJECT.md anti-feature.
    """
    from __future__ import annotations

    import os
    import sys

    sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", ".."))


    def _query() -> str:
        from companies.juno.x_queries import JUNO_SWEEPER_X_QUERY
        return JUNO_SWEEPER_X_QUERY


    def test_query_constant_exists_and_nonempty():
        """D-01 — JUNO_SWEEPER_X_QUERY importable + non-empty."""
        q = _query()
        assert isinstance(q, str)
        assert len(q) > 0


    def test_query_length_within_basic_tier_cap():
        """RESEARCH §3 — X API Basic tier query length cap is 512 chars (NOT 1024,
        which is the Academic Research tier). Query must be strictly less than 512."""
        q = _query()
        assert len(q) < 512, (
            f"JUNO_SWEEPER_X_QUERY length {len(q)} exceeds X API Basic-tier "
            f"512-char limit per RESEARCH §3"
        )


    def test_query_contains_all_corrected_handles():
        """D-02 + RESEARCH §1 — all 11 corrected handles must appear in the query.

        Verifies the post-research corrections + Tier-2 Canadian additions are
        encoded; a regression to CONTEXT.md original spelling would fail here.
        """
        q = _query()
        # Think-tanks (3)
        assert "from:RUSI_org" in q
        assert "from:CSIS" in q
        assert "from:IISS_org" in q
        # Defence press (4) — RESEARCH §1 corrections applied
        assert "from:defense_news" in q
        assert "from:BreakingDefense" in q
        assert "from:DefenseScoop" in q
        assert "from:JanesINTEL" in q
        # Canadian (4) — RESEARCH §1 + §2 corrections + additions applied
        assert "from:CDAInstitute" in q
        assert "from:CanadianForces" in q
        assert "from:DavePerryCGAI" in q
        assert "from:Murray_Brewster" in q


    def test_query_contains_both_hashtags():
        """D-02 — #defence + #NATO hashtags present."""
        q = _query()
        assert "#defence" in q
        assert "#NATO" in q


    def test_query_contains_modifiers():
        """D-01 + RESEARCH §3 — -is:retweet (exclude retweets) + lang:en filters."""
        q = _query()
        assert "-is:retweet" in q
        assert "lang:en" in q


    def test_query_excludes_defence_prime_cashtags():
        """PROJECT.md anti-feature — equity/financial signals on defence primes
        are explicitly EXCLUDED. Verify no defence-prime cashtag slipped in."""
        q = _query()
        for cashtag in ("$LMT", "$RTX", "$LDOS", "$BAESY", "$GD", "$NOC", "$BA"):
            assert cashtag not in q, (
                f"PROJECT.md anti-feature violated: defence-prime cashtag "
                f"{cashtag!r} must NOT appear in JUNO_SWEEPER_X_QUERY"
            )


    def test_query_excludes_wrong_spelling_handles():
        """RESEARCH §1 — these handle spellings would return 0 hits if used.

        Catches regression to CONTEXT.md original D-02 spellings.
        """
        q = _query()
        # RESEARCH §1 wrong spellings:
        # - @DefenseNews -> use defense_news (snake_case)
        # - @CDA_CDAI    -> use CDAInstitute
        # - @canadaforces -> use CanadianForces
        # We assert the LITERAL substring "from:DefenseNews" does not appear; the
        # corrected "from:defense_news" IS present (see test above).
        assert "from:DefenseNews" not in q
        assert "from:CDA_CDAI" not in q
        assert "from:canadaforces" not in q


    def test_query_is_balanced_parentheses():
        """X API v2 Boolean grouping with parentheses — outer group must be balanced
        per RESEARCH §3 'Boolean grouping with parentheses'."""
        q = _query()
        assert q.count("(") == q.count(")"), (
            f"JUNO_SWEEPER_X_QUERY has unbalanced parentheses: "
            f"{q.count('(')} '(' vs {q.count(')')} ')'"
        )
    ```

    DO NOT modify:
    - Any other file under `scheduler/companies/juno/`
    - `scheduler/agents/x_ingest.py` (D-10 — the Seva sweeper's ingest reuses this verbatim)
    - `scheduler/agents/weekly_sweeper.py` (D-10 byte-identical Seva contract)
  </action>
  <verify>
    <automated>cd /Users/matthewnelson/seva-mining/scheduler && uv run pytest tests/companies/test_juno_x_queries.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `scheduler/companies/juno/x_queries.py` exists
    - File `scheduler/tests/companies/test_juno_x_queries.py` exists
    - `grep -c "JUNO_SWEEPER_X_QUERY" scheduler/companies/juno/x_queries.py` returns at least 2 (docstring reference + assignment)
    - `grep -c "from:RUSI_org" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "from:defense_news" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "from:CDAInstitute" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "from:CanadianForces" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "from:JanesINTEL" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "from:DavePerryCGAI" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "from:Murray_Brewster" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "#defence" scheduler/companies/juno/x_queries.py` returns at least 1
    - `grep -c "#NATO" scheduler/companies/juno/x_queries.py` returns at least 1
    - `grep -c "-is:retweet" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -c "lang:en" scheduler/companies/juno/x_queries.py` returns 1
    - `grep -cE "\\\$LMT|\\\$RTX|\\\$LDOS|\\\$BAESY|\\\$GD" scheduler/companies/juno/x_queries.py` returns 0 (no defence-prime cashtags — anti-feature)
    - `grep -c "from:DefenseNews\|from:CDA_CDAI\|from:canadaforces" scheduler/companies/juno/x_queries.py` returns 0 (wrong-spelling handles absent)
    - `cd scheduler && uv run pytest tests/companies/test_juno_x_queries.py -v` exits 0 with 8 tests passing
    - `grep -cE "^def test_" scheduler/tests/companies/test_juno_x_queries.py` returns 8
    - `cd scheduler && uv run ruff check companies/juno/x_queries.py tests/companies/test_juno_x_queries.py` exits 0
    - `cd scheduler && uv run python -c "from companies.juno.x_queries import JUNO_SWEEPER_X_QUERY; assert len(JUNO_SWEEPER_X_QUERY) < 512, f'query too long: {len(JUNO_SWEEPER_X_QUERY)}'"` exits 0 (smoke check for Basic-tier 512-char cap)
  </acceptance_criteria>
  <done>
    `scheduler/companies/juno/x_queries.py` exists with `JUNO_SWEEPER_X_QUERY` set to the post-research corrected D-02 final form (11 handles + 2 hashtags + -is:retweet lang:en, length less than 512 chars, no cashtags). The new test file covers all 8 invariants (length, 11 handles, 2 hashtags, modifiers, cashtag exclusion, wrong-spelling exclusion, balanced parens). All tests pass. Adjacent files in `companies/juno/` untouched.
  </done>
</task>

</tasks>

<verification>
**Overall plan checks** (run AFTER both tasks land):

1. **Constants present + importable:**
   - `cd scheduler && uv run python -c "from companies.juno.prompts import JUNO_SWEEPER_SYSTEM_PROMPT, DEFENCE_NEWS_SYSTEM_PROMPT; from companies.juno.x_queries import JUNO_SWEEPER_X_QUERY; print('OK')"` outputs `OK`
   - `grep -c "FORBID — anti-tactical framing clause" scheduler/companies/juno/prompts.py` returns 2 (substring contract honored across both prompts)

2. **All test files green:**
   - `cd scheduler && uv run pytest tests/companies/ -v` exits 0 (existing 7 prompt tests + 8 new sweeper-prompt tests + 8 x-query tests = 23 tests)

3. **D-10 zero-regression for Seva:**
   - `git status --porcelain scheduler/agents/weekly_sweeper.py` is empty (untouched)
   - `git status --porcelain scheduler/agents/x_ingest.py` is empty (untouched)
   - `git status --porcelain scheduler/companies/juno/feeds.py scheduler/companies/juno/serpapi.py scheduler/companies/juno/__init__.py` is empty

4. **Full scheduler suite green:**
   - `cd scheduler && uv run pytest -q` exits 0 with at least baseline + ≥16 new tests

5. **CI grep gates honored:**
   - `bash scripts/verify-anthropic-resolver.sh` exits 0 (no `Anthropic(api_key=` calls introduced)
   - `bash scripts/verify-tenant-isolation.sh` exits 0 (no `select(WeeklySweep)` calls introduced — constants are pure strings)
</verification>

<success_criteria>
**JSWEEP-02 prompt + query constant prerequisites satisfied:**

- `JUNO_SWEEPER_SYSTEM_PROMPT` exists alongside `DEFENCE_NEWS_SYSTEM_PROMPT` — Plan 15-05 can `from companies.juno.prompts import JUNO_SWEEPER_SYSTEM_PROMPT`
- `JUNO_SWEEPER_X_QUERY` exists — Plan 15-05 can `from companies.juno.x_queries import JUNO_SWEEPER_X_QUERY`
- String-equality contract (D-04) honored — the FORBID block bytes are identical in both prompts (no Anthropic content-policy refusal-rate regression)
- X query is the post-research corrected form (11 handles + 2 hashtags + -is:retweet lang:en, less than 512 chars, no cashtags)

**D-10 zero-regression preserved:**

- `scheduler/agents/weekly_sweeper.py` UNTOUCHED (no helper extraction yet — happens in Plan 15-05 if needed)
- `scheduler/agents/x_ingest.py` UNTOUCHED
- All other Seva-side files in `scheduler/` directory are byte-identical
- `DEFENCE_NEWS_SYSTEM_PROMPT` (Phase 10 prompt) unchanged
- Existing 7 tests in `test_juno_prompts.py` pass byte-identically

**Plan boundary respected:**

- This plan touches `scheduler/companies/juno/prompts.py`, `scheduler/companies/juno/x_queries.py` (new), `scheduler/tests/companies/test_juno_prompts.py`, `scheduler/tests/companies/test_juno_x_queries.py` (new) ONLY
- NO sweeper module created (Plan 15-05)
- NO worker.py changes (Plan 15-06)
- NO frontend changes (Plan 15-03)
- NO daily_summary.py changes (Plan 15-01)
</success_criteria>

<output>
After completion, create `.planning/phases/15-juno-weekly-viral-sweeper/15-02-SUMMARY.md` following `$HOME/.claude/get-shit-done/templates/summary.md`. The summary should explicitly note:

1. **The new `JUNO_SWEEPER_SYSTEM_PROMPT`** appended to prompts.py (D-04 satisfied) — character count + brief content outline (voice, FORBID block, exactly-3-angles, grounding, output structure, negative-space)
2. **The verbatim FORBID block bytes** confirmed identical across both prompts (string-equality contract)
3. **The new `JUNO_SWEEPER_X_QUERY`** in x_queries.py (D-01 + D-02 satisfied) — character count + handle list confirmation (11 handles + 2 hashtags + modifiers)
4. **The 8 new tests in test_juno_prompts.py + 8 new tests in test_juno_x_queries.py** — final test count delta
5. **D-10 zero-regression evidence** — `git status --porcelain scheduler/agents/weekly_sweeper.py scheduler/agents/x_ingest.py scheduler/companies/juno/feeds.py scheduler/companies/juno/serpapi.py` empty
6. **Outstanding concerns** (none expected — Plan 15-05 imports these constants)
</output>
</content>
