{
  "name": "seva-chart-renderer",
  "version": "1.0.0",
  "description": "Long-running Node.js chart renderer for Seva Mining infographic pipeline. Reads ChartSpec JSON from stdin, returns PNG base64 on stdout.",
  "main": "render-chart.js",
  "scripts": {
    "start": "node render-chart.js"
  },
  "_comment_recharts": "DO NOT UPGRADE recharts to 3.x — SSR is broken (GitHub issue #5997). Pin to 2.15.4.",
  "dependencies": {
    "recharts": "2.15.4",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "@resvg/resvg-js": "2.6.2"
  }
}
